# ADR-134: Path-based workspace file micro-descriptions (semantic index restoration)

## Status

**Closed locally 2026-07-04** — S1–S7 landed in the dirty working tree. Verification: repo lint + `@persai/api` / `@persai/web` / `@persai/runtime` typecheck green; focused ADR-134 tests green (`search-workspace-files-from-manifest.service.test.ts` 1/1, `workspace-file-micro-description.service.test.ts` 4/4); `format:check` red on 11 touched files pending prettier write. Deploy, migration approval, and live acceptance (criteria below) remain pending — ADR stays open only for that gate.

## Date

2026-07-04

## Baseline SHA

`3978f3db0ac4f4b299087d08e14d79eefd3b6960` on `main`. Implementation starts only from a **clean git tree**. Uncommitted work from other programs (e.g. document inspect) must not be mixed into this ADR's slices.

## Orchestration model

This ADR is intended for orchestrated execution.

- The **parent agent** owns this ADR, dispatches bounded slices S1–S7, reviews every diff, verifies invariants, reconciles docs, and decides closure.
- **Implementation subagents** use GPT-5.4 or Sonnet unless the orchestrator documents a concrete reason otherwise.
- Subagents must not broaden scope, preserve legacy compatibility shims, or introduce a second semantic-index source of truth.
- If docs and code disagree at slice start, the orchestrator pauses and reconciles before code changes.
- Every slice must land production-grade behavior. No TODO scaffolding, no parallel old/new paths, no `AssistantFile` revival.

## Founder directive

Every chat file must have a short semantic description so the model understands file **content**, not only filename and path, and can resolve natural-language references such as *"find my photo in a cap"* or *"the excel with competitors"* to an exact workspace path for `files.attach`, `image_edit`, and `document.*`.

This is the **semantic index** program. Working Files is one read surface; the durable product truth is `workspace_file_metadata.shortDescription`.

Requirements:

- one pipeline for all file kinds (image, pdf, xlsx, py, cpp, audio, …) — no product-level type split;
- one source of truth — no mirror in `attachment.metadata`;
- path identity only — no `AssistantFile` registry or `assistantFileId` jobs;
- clean PROD cutover — no legacy upload-micro-description services, no dual write paths.

## Relationship to prior ADRs

- **Builds on ADR-128 / ADR-133.** File identity is `(workspaceId, path)` with hierarchical visible paths under `/workspace/assistants/<assistantId>/sessions/<sessionId>/...`.
- **Corrects ADR-133.** Acceptance item "micro-descriptions preserved" was false at ADR-133 closure; restoration is owned here, not retroactively true in ADR-133.
- **Re-implements the intent of ADR-100 Slice 6A/6F** under path-keyed manifest truth. Historical `AssistantFile.metadata.semanticSummary` and `assistant_upload_micro_description_jobs` are archive only — do not restore them.
- **Does not reopen** ADR-126, ADR-127, ADR-128, ADR-131, ADR-132, or ADR-133 filesystem/path decisions.
- **Does not conflate** `routerPolicy.analyzeUploadsOnB2cUpload` (upload micro-description, Admin → Runtime → Router Policy) with Completion vision (Admin → Plans → image generate/edit framing after async jobs).

## Current state (post S1–S7)

### Landed locally

- `workspace_file_metadata.shortDescription` is the sole durable semantic index; no `attachment.metadata` mirror, no revived `AssistantFile` registry.
- `workspace_file_micro_description_jobs` + leased scheduler + `WorkspaceFileMicroDescriptionService` helper (migration `20260704140000_adr134_workspace_file_micro_description_jobs`).
- Three-layer write pipeline: deterministic STT/text_extract → sync upsert; `buildGeneratedFileSemanticSummary` → sync upsert; background cheap-LLM job when still empty.
- Enqueue at all D5 ingress points with D3 policy (`analyzeUploadsOnB2cUpload` for ordinary/B2C uploads; project + model-generated bypass).
- Working Files batch-joins manifest descriptions into `semanticSummaryHint`.
- `files.search` restored as seventh `files` action; API `POST /api/v1/internal/runtime/files/search`; runtime contract, projection, catalog, and recovery text updated.

### Still pending deploy/live validation

- End-to-end background job lag (~30–60s) on real uploads in cluster.
- B2C toggle ON/OFF live behavior.
- NL discovery acceptance (*"find my photo in a cap"*) against real manifest rows.
- Prettier pass on touched files before commit.

### Historical (pre-ADR-134)

ADR-126 v3 removed the pre-v3 pipeline; ADR-133 incorrectly claimed micro-descriptions were preserved until this ADR restored them.

## Decision

### D1 — Single source of truth

The only durable semantic index field is:

```text
workspace_file_metadata (workspaceId, path).shortDescription
```

Provenance may be recorded in job/upsert internals, but **no** duplicate in `assistant_chat_message_attachment.metadata`, no revived `AssistantFile` row, no second cache the model or UI must reconcile.

### D2 — One write pipeline, three layers

When a file appears in workspace object storage and has a manifest row:

1. **Deterministic (sync, no LLM)** — if STT transcription or text_extract yields a usable one-liner → upsert `shortDescription` immediately; do not enqueue LLM.
2. **Generation request (sync, runtime/API)** — for model-produced files (`files.write`, image/document delivery, etc.): `buildGeneratedFileSemanticSummary({ requestText, requestedName, ... })` → if informative → upsert with source `generation_request`; if weak/empty → fall through to layer 3.
3. **Background cheap-LLM job (async)** — enqueue by `(workspaceId, path)` when summary is still empty and policy allows (see D3). Worker downloads bytes from object storage, runs `WorkspaceFileMicroDescriptionService`, upsert manifest. User turns must not block on job completion.

All file kinds use this same pipeline. Implementation may choose different byte-to-prompt strategies internally (text preview, image block, pdf block, lightweight office extract); that is not a product-facing type split.

### D3 — Enqueue policy

| Source | Enqueue when summary empty |
|--------|---------------------------|
| Project-mode upload / inbound | **Always** |
| Ordinary / B2C upload / inbound | Only if `routerPolicy.analyzeUploadsOnB2cUpload === true` |
| Model-generated (`files.write`, delivery, attach binary) | **Always** (no B2C gate) |

Dedup rules:

- Non-empty `shortDescription` → never enqueue.
- Completed job with null summary (unsupported type, timeout, max attempts) → do not retry forever.

### D4 — Background job model (path-keyed, new table)

Do **not** restore `assistant_upload_micro_description_jobs` or `assistantFileId`.

New leased-job table `workspace_file_micro_description_jobs` keyed by `@@unique([workspaceId, path])` with standard scheduler claim fields (`status`, `attempt`, `schedulerClaim*`, `usageJson`, `usageOccurredAt`, …). Pattern matches existing API schedulers (leased tick → claim → process → complete/fail).

Helper service `WorkspaceFileMicroDescriptionService`:

- system-tool slot from runtime bundle (same class of helper as pre-v3 upload analysis);
- bounded: ~20s timeout, ~60 output tokens, ~4000 char text preview, ~4MB binary cap;
- strict JSON output `{ summary: string }`; empty string when cannot describe;
- cost ledger: `purpose=tool_helper`, `source=upload_micro_description`, non-blocking.

### D5 — Enqueue points (mandatory)

After object bytes exist and manifest row is upserted, wire enqueue (or sync layer 1/2) at:

- web upload / stage — `manage-chat-media.service.ts`
- inbound telegram / channel media — `inbound-media.service.ts`, `handle-internal-telegram-turn.service.ts`
- runtime metadata upsert — `upsert-workspace-file-metadata-from-runtime.service.ts` (`files.write`)
- outbound delivery — `media-delivery.service.ts`
- explicit attach registration — `register-chat-attachment.service.ts`

No shadow enqueue path. No attachment-metadata side channel.

### D6 — Read surfaces (one field, three windows)

`shortDescription` is joined at read time into:

1. **`files.list`** — already wired; verify end-to-end after write pipeline lands.
2. **Working Files developer block** — batch-join manifest descriptions for **all** current-session file paths (attachments + discovered paths), set `semanticSummaryHint` and honest `createdAt` / author on each line. Fix lives in `turn-context-hydration.service.ts`.
3. **`files.search`** — restore as seventh `files` action. Search `workspace_file_metadata` by tokenized match on `path`, display name (basename), and `shortDescription`; rank in memory; return items with `shortDescription` and discovered aliases (`found file #N`). API: `POST /api/v1/internal/runtime/files/search`. Update runtime tool, contract enum, native projection, tool catalog, and Working Files recovery text in `turn-execution.service.ts` (*If user refers to a file not in Working Files → `files.list` → `files.search` → only then unavailable*).

### D7 — Instruction ownership

Active model-facing surfaces must agree:

- semantic hints come from manifest `shortDescription` only;
- `files.search` is a real action again;
- no guidance referencing `fileRef`, `AssistantFile`, or pre-v3 registry search.

## Non-goals

- Full document parse-on-upload for every PDF/DOCX.
- Synchronous vision captioning in the user turn path.
- Reviving `AssistantFile` or `assistantFileId`-keyed jobs.
- GCS handle→UUID migration (ADR-133 separate topic).
- Completion vision / Plans framing (different toggle, different pipeline).
- Reopening closed ADRs 126/128/132/133 for filesystem scope.

## Work plan

### Standard gate (every slice)

1. `corepack pnpm -r --if-present run lint`
2. `corepack pnpm run format:check`
3. `corepack pnpm --filter @persai/api run typecheck`
4. `corepack pnpm --filter @persai/web run typecheck`
5. `corepack pnpm --filter @persai/runtime run typecheck` when runtime touched
6. Focused tests for touched packages
7. Stale-string audit: no `AssistantFile` semantic paths, no attachment.metadata mirror, no parallel enqueue

### S1 — ADR wiring, schema, scheduler skeleton

- [x] Land this ADR; add `workspace_file_micro_description_jobs` migration.
- [x] Job service + leased scheduler skeleton.
- [x] Module registration.

### S2 — Describe helper

- [x] `WorkspaceFileMicroDescriptionService`: download bytes via workspace object storage + path resolution; internal strategies for text/code, image, pdf, office (best-effort xlsx/xml extract).
- [x] Unit tests: text/py, xlsx representative cases.

### S3 — Enqueue + policy

- [x] Wire enqueue at all D5 ingress points.
- [x] Read `analyzeUploadsOnB2cUpload` and `chatMode` snapshot for upload/inbound only.
- [x] Model-generated paths bypass B2C gate.

### S4 — Deterministic + generation_request sync writes

- [x] STT / text_extract → immediate manifest upsert at register/upload paths.
- [x] `buildGeneratedFileSemanticSummary` → manifest upsert on `files.write` / delivery when informative; else enqueue.

### S5 — Working Files read fix

- [x] Batch join `shortDescription` for all session file handles.
- [x] Populate `semanticSummaryHint`, `createdAt`, author on Working Files lines.

### S6 — Restore `files.search`

- [x] API search service over manifest.
- [x] Runtime `files` action + contract enum + projection + catalog.
- [x] Recovery text in Working Files section.
- [x] Tests: tokenized NL query hits `shortDescription` (English fixtures).

### S7 — Docs closure

- [x] Update `DATA-MODEL.md`, `API-BOUNDARY.md`, `TEST-PLAN.md`, `CHANGELOG.md`, `SESSION-HANDOFF.md`.
- [ ] Correct ADR-133 false "preserved" claim in ADR-133 doc (separate follow-up if not already done).
- [x] Full verification gate except `format:check` (11 files pending prettier).

## Acceptance criteria

Closure requires all of the following on a clean tree after S1–S7:

1. **Upload + policy:** B2C chat, toggle ON, PNG upload → `shortDescription` populated in manifest within ~30–60s.
2. **Upload + policy:** B2C chat, toggle OFF → no LLM job enqueued (deterministic-only if applicable).
3. **Upload + policy:** Project-mode upload → LLM job always enqueued when summary empty.
4. **Discovery:** Query *"find my photo in a cap"* via `files.search` returns the correct path when manifest summary matches.
5. **List:** `files.list` items include `shortDescription` when manifest has it.
6. **Working Files:** Session attachment lines show non-empty microDescription and honest createdAt (not `unknown | - | -`).
7. **Generated:** `files.write` xlsx with strong request text → `shortDescription` via generation_request and/or background job.
8. **Generated:** Image delivery output gets summary (generation_request or job).
9. **Code/text:** Upload `.py` / `.cpp` uses same pipeline; summary describes content.
10. **Non-regression:** `analyzeUploadsOnB2cUpload` ≠ Completion vision; no attachment.metadata mirror; paths follow ADR-133 hierarchy.
11. **Cleanliness:** Zero active code reviving `AssistantFile` micro-description jobs or dual semantic-index writes.

## Risks (accepted)

- Office/xlsx extract is best-effort; null summary after failed job is honest.
- Job lag 30–60s — WF/list refresh on next turn is acceptable.
- Search with empty summaries returns honest empty results; model falls back to list/read per D6 recovery text.

## Next recommended step

Run prettier on touched files, commit the ADR-134 slice, deploy with migration approval on `persai-dev-migrations`, then execute live acceptance criteria 1–11 on `persai.dev`.

## Addendum (2026-07-04) — WF cleanup + summary invalidation + version markers

**Status:** implemented locally on top of S1–S7.

### D8 — Invalidate stale summaries on content change

When manifest content changes at the same path, clear `shortDescription` and re-enqueue the background job with `forceRefresh`:

- runtime manifest upsert: `replace: true` or `contentHash` delta;
- register/upload attach path: `sizeBytes` or `mimeType` delta when no new deterministic summary is supplied.

`enqueueIfNeeded({ forceRefresh: true })` bypasses the `summary_exists` short-circuit.

### D9 — Working Files presentation cleanup

Remove legacy developer blocks (`DOC_CURRENT_SOURCE`, `DOC_LAST_DELIVERED_PDF`, `LAST_DELIVERED_FILE = ...`). Express roles only as row markers:

- `last delivered` — newest user/model file in chat;
- `current source` — newest user document-source upload (pdf/docx/xlsx/text/*);
- `last delivered result` — newest model pdf/docx/xlsx output (`document` / `shell` tools included);
- `v{N}` — current registered document version for pdf/docx/xlsx paths.

Pinned rows with these markers survive Working Files trim.

### D10 — Version hint join

`POST /api/v1/internal/runtime/files/short-descriptions` also returns `documentVersionNumber` for registered pdf/docx/xlsx paths. Working Files and discovered-path hydration consume it; `files.list` / `files.search` remain path+summary only.
