# CHANGELOG

> Archive: detailed historical entries from 2026-06-05 and earlier moved to `docs/CHANGELOG.archive-2026-06-05-details-and-earlier.md`; entries from 2026-05-19 and earlier remain in `docs/CHANGELOG.archive-2026-05-19-and-earlier.md`.
> Keep this file short: current entries plus concise recent summaries only.

## 2026-06-20

- **ADR-123 Slice 4 landed (exec image — Python + Node + doc/data stack + Chromium + ripgrep/fd):** Replaced the placeholder `busybox:1.36` exec image with the real in-sandbox toolchain (ADR-123 D5). New standalone image registered as a distinct CI deploy service (`sandbox-exec`) — separate from the control-plane `sandbox` service. **Exec image** (`apps/sandbox/exec-image/Dockerfile` + `requirements.txt`): base `debian:bookworm-slim`; installs bash, coreutils, tar, ca-certificates, curl; Python 3 (Debian 3.11) in an isolated venv at `/opt/venv` with `pandas 3.0.3`, `numpy 2.4.6`, `matplotlib 3.11.0`, `openpyxl 3.1.5`, `python-docx 1.2.0`, `weasyprint 69.0`, `pdfplumber 0.11.10`, `Pillow 12.2.0`; Node.js 22 LTS (NodeSource); `ripgrep` (`rg`) + `fd-find` (symlinked to `fd`); headless `chromium`; fonts `fonts-dejavu`, `fonts-liberation`, `fonts-noto`, `fonts-noto-cjk` (Cyrillic/CJK); fontconfig; `fc-cache` baked in. Non-root user `sandbox` uid=1000/gid=1000. `ENV HOME=/workspace` (writable emptyDir at runtime) so the model's files and runtime `pip install --user` packages persist across the session; regenerable tool caches are redirected to the ephemeral `/tmp` emptyDir via `XDG_CACHE_HOME=/tmp/.cache` and `MPLCONFIGDIR=/tmp/.matplotlib` so they never pollute the persisted `/workspace` snapshot. Build-time self-checks verify every tool and Python import — a broken build fails loudly. **readOnlyRootFilesystem compliance**: all tools installed in immutable image layers; only `/workspace` (emptyDir, session-scoped) and `/tmp` (emptyDir, raised to 256Mi in `exec-pod-bridge.service.ts` to host the toolchain's `/tmp` caches + Chromium profile) writable at runtime. **Chromium note for Slice 5**: must run with `--no-sandbox --headless=new --user-data-dir=/tmp/chromium-profile` (gVisor is the isolation boundary; `--user-data-dir=/tmp` keeps profile in writable /tmp). **CI integration** (`scripts/ci/detect-affected.mjs`): `sandbox-exec` added to `APP_METADATA` (standalone; no pnpm workspace); `classifyFile` routes `apps/sandbox/exec-image/**` changes to `sandbox-exec` deploy (not the control-plane `sandbox`); `NON_WORKSPACE_DEPLOY_SERVICES` includes `sandbox-exec` for `workflow_dispatch` coverage. 3 new tests in `detect-affected.test.mjs` (exec-image → sandbox-exec only; requirements.txt → sandbox-exec only; src change → sandbox not sandbox-exec). **SHA pinning** (`scripts/ci/pin-dev-image-tags.mjs`): `"sandbox-exec": "sandboxExec"` added to `serviceToSection`; pin script validated dry-run. **Helm** (`infra/helm/values-dev.yaml` + `sandbox-deployment.yaml`): new `sandboxExec.image` block (`name: sandbox-exec`, `tag: dev-main`); `SANDBOX_EXEC_IMAGE` removed from `sandbox.env` and instead computed explicitly in the deployment template from `sandboxExec.image.{registryHost,projectId,repository,name,tag}` (falls back to `global.images.tag` if `sandboxExec.image.tag` is unset); rendered value: `europe-west1-docker.pkg.dev/…/persai/sandbox-exec:dev-main`. **sandbox-config.ts**: default `busybox:1.36` retained for local dev (no cluster; value satisfies Zod non-empty validation but is not used without a live k8s API). `apps/runtime` untouched. Local docker build not attempted (Docker Desktop not running); image build is CI/GAR-validated. **Tests**: sandbox 18/18 PASS (no change — config untouched); detect-affected 7/7 PASS (4 pre-existing + 3 new). Gate: lint PASS · format:check PASS · `@persai/api` typecheck PASS · `@persai/web` typecheck PASS · `@persai/runtime` typecheck PASS · `@persai/sandbox` typecheck PASS · sandbox tests 18/18 PASS · helm lint PASS · helm template PASS · detect-affected 7/7 PASS · pin script dry-run PASS.

- **ADR-123 Slice 3 landed (per-session pod reuse, idle-TTL reaper, GCS workspace snapshot):** Session-lived sandbox execution: pods are now reused across jobs within a session instead of created/destroyed per job. Pod name for session jobs is derived deterministically from `runtimeSessionId` via SHA-256 — format `ses-<sha256[0..31]>` (stable, k8s-safe, 36 chars ≤ 63 limit). Sessionless jobs (null `runtimeSessionId`) retain the Slice 1 ephemeral create/delete behavior unchanged. **Session lifecycle** (`apps/sandbox/src/exec-pod-bridge.service.ts`): `runInPod` splits into `runInSessionPod` (reuse path) and `runInEphemeralPod` (ephemeral path). `ensureSessionPodRunning` checks k8s state directly — reuses if Running, creates if absent, recreates if terminal. `lastActivityAt` is updated at the start and end of each session job to protect against reaper eviction during execution. **Idle-TTL reaper**: `OnModuleInit` starts a `setInterval` reaper; `runReaperTick()` iterates the in-memory `sessionPods` map and calls `deletePod` for entries idle longer than `SANDBOX_EXEC_SESSION_IDLE_TTL_MS` (default 30 min). Reaper sweep interval: `SANDBOX_EXEC_REAPER_INTERVAL_MS` (default 2 min). Both configurable via `sandbox-config.ts` and `values-dev.yaml`. **Serialization**: the existing `assistantId+workspaceId` Postgres lease mutex already serializes all jobs for a session — no additional locking needed. **GCS workspace snapshot** (`apps/sandbox/src/sandbox.service.ts`): after each successful session job, the entire workspace directory is tar'd and saved to GCS as `{prefix}/assistants/{assistantId}/sandbox-sessions/{runtimeSessionId}/workspace.tar` (new `buildSessionSnapshotKey` in `SandboxObjectStorageService`). On pod recreate (state token mismatch during `ensureWorkspaceSessionHydrated`): after declared `AssistantFile` records are restored, `restoreSessionSnapshotOverlay` extracts the snapshot into a staging dir and copies only files absent from the workspace (copy-if-absent via `fs.cp({ force: false })`) to add back ephemeral files without overwriting declared content. This avoids the GNU-vs-BSD tar divergence (`--keep-old-files` errors on pre-existing files under GNU tar; `--skip-old-files` is missing on BSD tar). Missing snapshot (first session) is handled gracefully with a `warn` log. All tar operations are soft-fail (log warn + continue) for resilience. **Invariants preserved**: exec pods still have ZERO secrets, `automountServiceAccountToken: false`, `runtimeClassName: gvisor`, hardened securityContext, proxy-only egress. GCS creds remain control-plane-only. **Warm pool DEFERRED**: cold-start (~2–4s gVisor) is acceptable at current scale; ADR-123 Slice 3 notes it as a future optimization. **Config**: `SANDBOX_EXEC_SESSION_IDLE_TTL_MS` (default 1800000 ms), `SANDBOX_EXEC_REAPER_INTERVAL_MS` (default 120000 ms). `apps/runtime` untouched. **Tests** (18/18 pass): session pod name stable from session ID; sessionless falls back to ephemeral with delete; second session job reuses pod (no recreate, no delete); reaper evicts idle pods and updates map; session snapshot save uses correct assistant+session GCS key; no-op restore on missing snapshot; round-trip save→restore adds ephemeral files without overwriting declared files. Gate: lint PASS · format:check PASS · `@persai/api` typecheck PASS · `@persai/web` typecheck PASS · `@persai/runtime` typecheck PASS · `@persai/sandbox` typecheck PASS · sandbox tests 18/18 PASS · helm lint PASS · helm template PASS.

- **ADR-123 Slice 2 landed (egress proxy + deny-all exec pod network boundary):** Kernel-level network isolation for gVisor exec pods: deny-all egress NetworkPolicy + trusted Squid proxy with domain allowlist. **NetworkPolicy** (`infra/helm/templates/networkpolicies.yaml`): `sandbox-exec-deny-egress` selects exec pods (`app.kubernetes.io/component: sandbox-exec`), `policyTypes: [Egress]`, default-deny, allowing only kube-dns (UDP+TCP port 53, namespaceSelector `kube-system`) and the proxy pod (TCP 3128) when `egressProxy.enabled`. `sandbox-egress-proxy-isolation` for the proxy pod: ingress only from exec pods on 3128; egress to DNS + internet on TCP 80/443 (L7 enforcement delegated to Squid). **Egress proxy** (`infra/helm/templates/sandbox-egress-proxy.yaml`): Squid Deployment + Service + ConfigMap; port 3128; allowlist enforced via `acl allowed_domains dstdomain` + `http_access deny all`; hardened securityContext (`allowPrivilegeEscalation: false`, `drop ALL caps`, `seccomp RuntimeDefault`); cache disabled; writable emptyDirs for `/var/spool/squid`, `/var/log/squid`, `/run`; no on-disk state. **Allowed domains** (`values-dev.yaml`): `pypi.org` + `.pypi.org` + `files.pythonhosted.org` + `.files.pythonhosted.org` (pip install), `registry.npmjs.org` + `.registry.npmjs.org` (npm install) — nothing else. **Config** (`packages/config/src/sandbox-config.ts`): `SANDBOX_EXEC_EGRESS_PROXY_URL` (default `""`) and `SANDBOX_EXEC_NO_PROXY` (default `""`). **Bridge** (`apps/sandbox/src/exec-pod-bridge.service.ts`): `createExecPod` now calls `buildProxyEnv()` — when proxy URL is non-empty, injects `HTTP_PROXY`, `HTTPS_PROXY`, `http_proxy`, `https_proxy` (all set to the proxy URL), plus `NO_PROXY`/`no_proxy` when configured; when proxy URL is empty, `env: []` (no secrets ever injected, secret boundary intact). **values-dev.yaml**: `SANDBOX_EXEC_EGRESS_PROXY_URL: "http://sandbox-egress-proxy.persai-dev.svc.cluster.local:3128"`, `SANDBOX_EXEC_NO_PROXY` set with loopback + RFC1918 + in-cluster bypass list; `sandbox.egressProxy` block with image, port, allowedDomains, resources. **Tests**: 2 new tests in `exec-pod-bridge.service.test.ts` — proxy env vars injected when URL set (6 vars asserted), none when URL empty; `sandbox-metrics.service.test.ts` config fixture updated. `apps/runtime` untouched. Gate: lint PASS · format:check PASS · `@persai/api` typecheck PASS · `@persai/web` typecheck PASS · `@persai/runtime` typecheck PASS · `@persai/sandbox` typecheck PASS · sandbox tests 8/8 PASS · helm lint PASS · helm template PASS.

- **Follow-up — multi-step working notes (`workingNotes: string[]` replaces single `workingPreamble`/`preambleText`; no new ADR):** The single pre-first-tool "working preamble" string was replaced end to end by an ordered array of per-step notes — the text the model writes BEFORE each tool call across every tool-loop iteration (empty array = no notes). **Runtime** (`turn-execution.service.ts`): notes are now captured on EVERY `tool_calls` event (was: once, only `iteration === 0`) from that iteration's own `event.result.text` (per-iteration, NOT the cumulative `assembledText`, which would re-include earlier steps). New pure exported `assembleWorkingNotesAndAnswer({ toolStepTexts, finalAnswerText, fullAssistantText })` returns `{ workingNotes, answerText, assistantText }`; the prior `splitPreambleAndAnswer` was removed. **Duplication trap (the bug from the prior cut):** `providerResult.text` on the final `completed` is the CUMULATIVE corrected text (all notes + answer) — so `answerText` is taken from the final iteration's own text (`correctedFinalAnswerText`), never the cumulative text, and `assistantText` stays the verbatim cumulative corrected text. Invariants (locked in tests): `workingNotes = [note0, note1, …]` each exactly once (only its iteration's pre-tool text); `answerText` = final answer only, no notes; `assistantText` = each note exactly once then the answer (no dupes); no-tools turn → `workingNotes=[]`, `answerText===assistantText`; empty final answer after tools → `answerText=""`; whitespace-only note dropped. **Contract:** `RuntimeTurnResult.preambleText` → `workingNotes: string[]` (`runtime-contract`); `AssistantWebChatMessageState.workingPreamble` → `workingNotes` (array of strings) in `openapi.yaml`, contracts regenerated. **API:** done chunk carries `workingNotes` (`web-runtime-stream-client.service.ts`, `assistant-runtime.facade.ts`); `persist-assistant-message.ts` takes `workingNotes?: string[]` and writes `metadata.workingNotes` only when non-empty; `web-chat-message-state.mapper.ts` `extractWorkingPreambleFromMetadata` → `extractWorkingNotesFromMetadata` (`string[]`); `web-chat.types.ts` updated. **Symptom-1 fix:** the live `completed` transport in `stream-web-chat-turn.service.ts` (variable renamed `runtimeWorkingPreamble` → `runtimeWorkingNotes`) now includes `workingNotes` on the hand-built `assistantMessage`, so the "Выполнено" block appears without reopening the chat. **UI:** `assistant-api-client.ts`, `use-chat.ts` (`ChatMessage`, `toCommittedChatMessage`, both `onCompleted` handlers), and `chat-message.tsx` (`WorkingTextBlocks` now reads `message.workingNotes` directly — no `\n\n` split). **Backfill:** `backfill-working-preamble.ts` → idempotent `backfill-working-notes.ts` (skip rows already in `workingNotes` form; `metadata.workingPreamble` string → `[workingPreamble]`; legacy `:::working` content markers → parsed `workingNotes` + cleaned content). **Tests:** new `assemble-working-notes-and-answer.test.ts` (registered in both run-suites) + new `backfill-working-notes.test.ts`; multi-step capture asserted in `turn-execution.service.test.ts` (two `tool_calls` → `workingNotes=["First plan.","Second plan."]`, `answerText="Final answer."`, `assistantText` each note once then answer — the assertion fails on the naive cumulative-as-answer impl, verified by temporary revert, and passes after the fix); web/api suites migrated off the old field name. Gate: lint PASS · format:check PASS · `@persai/api`/`@persai/web`/`@persai/runtime`/`@persai/runtime-contract` typecheck PASS · `stream-web-chat-turn.service.test.ts` 15/15 · `backfill-working-notes.test.ts` 9/9 · runtime assemble + turn-execution tests PASS · web `chat-message`/`use-chat`/`chat-message-streaming` 132/132. No `workingPreamble`/`preambleText` remain outside the backfill's legacy-read paths and historical CHANGELOG/handoff entries.

- **ADR-122 Slice 3 landed (truncation guard — model no longer continues a cut-off answer):** Orthogonal `truncated?: boolean` signal propagated end-to-end. Provider clients: Anthropic sets `truncated: response.stop_reason === "max_tokens"` (non-stream) / `latestStopReason === "max_tokens"` (stream); OpenAI adds `isMaxOutputTokensTruncation()` checking `response.status === "incomplete" && incomplete_details.reason === "max_output_tokens"` and promotes the `response.incomplete` event from a failed event to a completed+truncated event when the reason is `max_output_tokens`. `RuntimeTurnResult.truncated` propagates from the final provider result in `buildTurnResult`. API: `AssistantRuntimeWebChatTurnStreamChunk.truncated?: boolean` added; `done` chunk forwards `truncated: true` when present; `persist-assistant-message.ts` gains `truncatedStatus?: "truncated"` (orthogonal to existing `partialStatus?: "partial"`); `stream-web-chat-turn.service.ts` persists `metadata.status = "truncated"` when the turn completed normally but was cut short. Hydration guard: `withTruncationMarker()` in `TurnContextHydrationService` appends `"\n\n[Note: the previous answer was interrupted before completion.]"` to any prior assistant message whose `metadata.status` is `"partial"` (abort) or `"truncated"` (max_tokens); applied in both the summarized-messages loop and the canonical web hydration loop; idempotent (marker checked before appending, stored content never contains it). Tests: Anthropic client max_tokens non-stream + stream cases; hydration marker (partial, truncated, clean, idempotency); `buildTurnResult` propagation. Gate: format:check PASS · `@persai/api` typecheck PASS · `@persai/runtime` typecheck PASS · `@persai/provider-gateway` typecheck PASS · `@persai/web` typecheck PASS · all test suites PASS.

- **ADR-122 Slice 2 corrective pass (PROD-correct for BOTH providers; turn-0 thinking gap fixed):** Resolver formula corrected to reserve thinking inside the output ceiling: constants are now `OUTPUT_BUDGET_MAX = 128_000` (cap on FINAL answer+thinking), `OUTPUT_BUDGET_FALLBACK = 8_192` (base for null `maxOutputTokens`, safe on every mainstream model), `OUTPUT_BUDGET_FLOOR = 1_024`, `CONTEXT_SAFETY_RESERVE = 4_096` (`OUTPUT_BUDGET_SANITY_CAP` removed). Formula: `totalCeiling = min(maxOutputTokens ?? FALLBACK, MAX)`; `totalRoom = min(totalCeiling, contextWindow - input - reserve)`; `answer = totalRoom - thinkingBudget`; clamp `[FLOOR, MAX]` — so gateway `max_tokens = answer + thinkingBudget = totalRoom ≤ model ceiling`, eliminating the opus 128k+thinking 32768 overflow 400. OpenAI families seeded in `MODEL_CAPABILITY_DEFAULTS` (gpt-5.x = 400k/128k; gpt-4o(-mini) = 128k/16_384) — fixes the OpenAI 400 (it sends `max_output_tokens` verbatim, no clamp); unlisted OpenAI keys → fallback. Family defaults now fold in at READ (`parseRuntimeProviderModelProfiles`) AND WRITE (`normalizeModelProfiles`) normalization, not just synthesis, so PROD rows persisted with the brand-new null fields resolve to the published ceiling without a manual save (explicit admin value always wins; unknown model stays null; supersedes the earlier known-model "null strictly round-trips" note). Turn-0 extended thinking enabled — `prepareTurnExecution` passes the route thinking budget to both `buildProviderRequest` and the resolver (fixes ADR-121 turn-0 gap; safe because the gateway stream timeout is idle-based and watchdogs disabled). `PROVIDER_GATEWAY_STREAM_TIMEOUT_MS = 90_000` confirmed idle (resets on every stream event incl. thinking deltas). Gate: format:check PASS · resolver unit test PASS · runTurnExecutionServiceTest PASS · runAnthropicProviderClientTest PASS · api normalization + read-fold-in tests PASS · `@persai/api`/`@persai/web`/`@persai/runtime`/`@persai/provider-gateway` typecheck PASS.

- **ADR-122 Slice 2 landed (unified output-budget resolver, main-chat-turn truncation fix):** `resolveModelOutputBudget(capability, ctx)` introduced as the single pure resolver in `apps/runtime/src/modules/turns/model-output-budget.ts`. (The initial cut used an `OUTPUT_BUDGET_SANITY_CAP = 200_000` base/clamp and a context-only thinking subtraction; both were **superseded by the corrective-pass entry above** — final constants are `OUTPUT_BUDGET_MAX = 128_000` / `OUTPUT_BUDGET_FALLBACK = 8_192` with thinking reserved inside the ceiling. See that entry for the authoritative formula.) Wired into `prepareTurnExecution` (main turn — was the only path NOT setting `maxOutputTokens`) and `refreshProviderRequestMessages` via the new `resolveSlotCapability` helper and char-based `estimateProviderRequestInputTokens`. Tool-loop continuation inherits via spread. `runtime-document-provider-adapter.service.ts` `resolveMaxOutputTokens` refactored to delegate to the resolver (dead `DEFENSIVE_OUTPUT_TOKEN_CAP` retired). `anthropic-provider.client.ts` four `?? 1_024` literals replaced with `PROVIDER_FALLBACK_MAX_OUTPUT_TOKENS = 4_096`; thinking math (`max_tokens = answer + thinking`) preserved exactly. Bounded callers (auto-extract, router, compaction, media-job, background-task) confirmed to set their own explicit budgets — untouched. Tests: 9-case resolver unit test, `runTurnExecutionServiceTest` (with new maxOutputTokens regression guard), `runAnthropicProviderClientTest` (thinking semantics confirmed). Gate: `@persai/api` typecheck PASS · `@persai/runtime` typecheck PASS · `@persai/provider-gateway` typecheck PASS · all tests PASS.

- **ADR-122 Slice 1 landed (maxOutputTokens + contextWindow as first-class model capability fields):** `maxOutputTokens: number | null` and `contextWindow: number | null` added as first-class admin-managed fields on `RuntimeProviderModelProfileBase`. `normalizeModelProfiles()` validates both via a new `normalizeOptionalPositiveInteger()` helper (null allowed; bounds: max 1_000_000 / 2_000_000). Routing slot type gains both fields; `resolve-runtime-provider-routing.service.ts` enriches every slot via `lookupModelCapabilities()` so Slice 2 can read the values from the slot directly. `MODEL_CAPABILITY_DEFAULTS` defined with authoritative Anthropic values (7 models, all 200k context window at non-premium tier; OpenAI left null). Seeding applied only at synthesis/legacy-row normalization — admin-set explicit values and explicit nulls on real catalog rows are untouched (null round-trips as null). Admin runtime UI gains a `NullableIntegerField` component and two new inputs per model row ("Max output tokens", "Context window"). `RuntimeProviderModelProfileCommonState` in `openapi.yaml` gains both nullable integer fields; contracts regenerated. Gate: format:check PASS · `@persai/api` typecheck PASS · `@persai/web` typecheck PASS · normalization tests PASS · slot-enrichment tests PASS · web UI tests 19/19 PASS.

- **Docs-only — formal closure of ADR-120 and ADR-121 (no code/deploy):** Reconciled ADR status with shipped reality. ADR-120 header was stale (it still listed the two tracked residuals — HNSW index + legacy JSONB column drop — as "pending" while its own Closure II section recorded them resolved); flipped status `closure-mode` → **Closed** and rewrote the blockquote to state both residuals are resolved. ADR-121 was still framed "Open program ADR / Accepted" despite all five slices being landed, deployed, and live-validated; flipped to **Closed** and appended a `## Closure (2026-06-20)` section (per-slice summary + the live-found `slow_avg` watchdog fix). `AGENTS.md` startup-reading closed-archive list now includes ADR-120 and ADR-121 so neither is treated as active backlog. No source, schema, or runtime change.

- **Follow-up fix — runtime preamble/answer split regression (same slice as the tool-loop final-answer fix below):** The first cut of the preamble split in `apps/runtime/src/modules/turns/turn-execution.service.ts` (`buildTurnResult`) was wrong: it set `answerText = providerResult.text` (the FULL corrected `preamble + answer` text) and `assistantText = preamble + "\n\n" + answerText`, so for a tool-loop turn with preamble `P` and answer `A` it produced `answerText = "P A"` (preamble duplicated into the answer → web) and `assistantText = "P\n\nP A"` (doubled preamble → Telegram). It went undetected because the API golden tests mock the runtime stream and inject `finalAnswer`/`workingPreamble` directly into the `done` chunk, never exercising the runtime split. **Fix:** extracted a pure, exported, unit-tested `splitPreambleAndAnswer(fullText, preambleText)` — `assistantText` is now `providerResult.text` verbatim (the canonical full corrected text, single preamble, never reconstructed) and `answerText` is the full text with the captured preamble stripped (verbatim-prefix → left-trimmed retry → safe full-text fallback if the preamble is not a prefix, so content is never silently dropped). `buildTurnResult` now delegates to it. Invariants: tool-loop → `preambleText=P`, `answerText=A`, `assistantText=full`; no-tools → `preambleText=null`, `answerText=assistantText=full`; empty answer after tools → `answerText=""`, `assistantText=P`; whitespace-only preamble → `null`. **Tests:** new runtime unit suite `apps/runtime/test/split-preamble-and-answer.test.ts` (registered in both `run-suite.ts` and `run-suite-isolated.ts`), plus `preambleText`/`answerText` assertions added to the streaming tool-loop + no-tools cases in `turn-execution.service.test.ts` (the integration assertions fail on the buggy logic and pass after the fix — verified by temporarily reverting). Contract (`preambleText`/`answerText`/`finalAnswer`/`workingPreamble`), backfill, and UI are unchanged. Gate: lint PASS · format:check PASS · `@persai/api` typecheck PASS · `@persai/web` typecheck PASS · `@persai/runtime` typecheck PASS · `stream-web-chat-turn.service.test.ts` 21/21 PASS · `backfill-working-preamble.test.ts` PASS · new runtime split test PASS · full `runTurnExecutionServiceTest` PASS.

- **Production bug fix — tool-loop final answer loss (ADR-120 era; no new ADR needed):** Fixed a two-part defect that caused tool-loop chat turns to persist only the pre-tool "working" preamble while discarding the final answer. Root cause: (1) the API `completed` event consumer gated acceptance of `event.result.assistantText` on `assistantText.startsWith(accumulated)` — runtime sanitization and merge separators broke that prefix check so the final text was silently dropped; (2) the API persistence layer wrapped pre-tool text in synthetic `:::working\n...\n:::` markers and stored that as `content` rather than the real final answer. Fix: (A) `RuntimeTurnResult` extended with `preambleText` + `answerText` split; (B) `startsWith` gate removed from `case "completed"` in the API SSE consumer; `finalAnswer` / `workingPreamble` forwarded on the `done` chunk; (C) entire `:::working` pipeline removed from `stream-web-chat-turn.service.ts`; content is now `answerText`, preamble is `metadata.workingPreamble`; aborted/stalled turns persist `metadata.status = "partial"`; (D) one-shot idempotent backfill script `apps/api/prisma/backfill-working-preamble.ts` converts legacy rows; (E) frontend `WorkingTextBlocks` now sources blocks from `message.workingPreamble` instead of parsing `:::working` markers from `content`; all legacy working-block parsing functions removed from `chat-message-streaming.ts` and `use-chat.ts`; (F) `AssistantWebChatMessageState` gains `workingPreamble?: string | null` in openapi.yaml + regenerated contracts. Gate: lint PASS · format:check PASS · `@persai/api` typecheck PASS · `@persai/web` typecheck PASS · `stream-web-chat-turn.service.test.ts` 15/15 PASS · `backfill-working-preamble.test.ts` 6/6 PASS · web tests 69 files / all PASS.

- **ADR-120 landed (RAG/Knowledge unification + memory JIT — pull-first retrieval, single vector store, honest relevance):** program `docs/ADR/120-rag-knowledge-unification-and-memory-jit.md` implemented across seven bounded slices (`d007025b` ADR open · `0e36d959` S1 · `7fd6eeb1` S2 · `1ae3c201` S3 · `fce1e698` S4 · `89046014` S5 · `951580bd` S6 · this closure commit S7). Both adjacent "push large, low-relevance context every turn" subsystems (durable memory + RAG) were reworked onto one principle: **the model pulls what it needs; the server stops pushing "just in case."**
  - **Memory JIT (S1–S2):** the always-on pushed contextual short-memory block was retired end to end — `<persai_memory>` / `durable_memory_contextual` volatile entries are no longer spliced into the recency zone, eliminating cross-chat memory bleeding by construction; `HydrateMemoryForTurnService` now returns the durable `core` block only (stable AOT prefix, global by design). Cross-chat / old-fact recall became **pull-only** via the `knowledge_search` `memory` source. Open loops are scoped to the **current chat + open-only** (`chatId = current` AND `resolvedAt IS NULL`) for both the open-loop refs developer block and the close-by-similarity path; additive composite index migration `20260619230000_adr120_memory_chat_scope_index`.
  - **RAG engine (S3):** user-knowledge + product/global document reads switched from the legacy "first-N rows by table order + in-process cosine" candidate selection to the unified `KnowledgeVectorChunk` pgvector **ANN** query (`ORDER BY embedding_vector <=> query::vector`, the path skills already used) — true nearest-neighbour candidate selection fixes relevance at the root. The dual-persist **legacy JSONB chunk write is retained this release** as rollback safety (see follow-ups). An idempotent parity backfill (`backfill-knowledge-vector-store`) reconciles the vector store from existing source chunks.
  - **RAG precision (S4):** the relevance floor (`passesRelevanceFloor`, score + top-K margin) is now applied to **all** sources including documents/files (previously only memory/chat); the LLM rerank no longer fail-opens into widened lexical order — when the helper is unavailable the engine falls back to ANN-score order **without widening, floor still applied**; the `MIN_CONTEXT_ITEMS = 4` floor was removed so retrieval may return **empty** (no-answer beats wrong-answer).
  - **Pull-first unified (S5):** the entire always-on server push subsystem was removed — the orchestrate retrieval service + controller + tests, the `POST /api/v1/internal/runtime/knowledge/orchestrate` internal endpoint, the runtime client method, the flat `# Retrieved Knowledge Context` developer block, and the `RuntimeRetrievedKnowledgeContext*` contract types. The ADR-119 `<persai_retrieved_knowledge>` push contract is **superseded by pull** (ADR-120 D6): retrieved knowledge flows back exclusively as `knowledge_search` / `knowledge_fetch` tool results. The skill KB became a gated `knowledge_search`/`knowledge_fetch` source; project mode no longer force-sets `useUserKnowledge` and instead guarantees the retrieval tools are projected (pull-dispatch contract — "locate with search, read with fetch; one excerpt is not sufficiency").
  - **Config (S6):** snippet-first is the new default — admin `smartSearchEnabled` ceiling default flipped `true → false` (default-only, no migration); the atomic `skill_knowledge_card` exception returns its FULL card text inline even when smart search is snippet-only. The admin Plans UI gained a `lean` / `balanced` / `rich` retrieval preset dropdown (UI fill-helper only — fills all 16 raw draft fields; no persisted `retrievalPolicy.preset`, no contract change) with the 16 raw knobs moved under an "Advanced retrieval knobs" disclosure.
  - **Closure (S7):** docs finalized (this entry + `SESSION-HANDOFF`, ADR status → Accepted/closure-mode, API-BOUNDARY/DATA-MODEL pull-first corrections) and the ADR-120 golden invariant locked in both prompt-snapshot tests (`apps/runtime/test/adr119-golden-prompt-snapshot.test.ts` + `apps/api/test/adr119-golden-prompt-snapshot.test.ts`): explicit negative assertions that no `<persai_memory>` push, no `# Retrieved Knowledge Context` block, and no pushed `<persai_retrieved_knowledge>` block appear in the assembled context, plus a positive check that `knowledge_search` / `knowledge_fetch` are the pull retrieval path. Golden tests green (api + runtime).
  - **Tracked follow-ups (residuals, NOT new scope, do NOT reopen the program):** (1) **HNSW ANN index** on `knowledge_vector_chunks.embedding_vector` is **deferred** — the `embedding_vector` column is dimensionless; pin `vector(N)` and build the `hnsw` (`vector_cosine_ops`) index once the live embedding model/dimension is confirmed. Correct ANN already ships via sequential scan. (2) **Legacy JSONB chunk columns** are **kept this release** as rollback safety; drop them in a follow-up migration after PROD confirms vector reads healthy and the post-deploy backfill has run.
  - **Post-deploy step:** run the parity backfill (`corepack pnpm --filter @persai/api run backfill:knowledge-vector-store`) in PROD after deploy, then confirm vector-store reads are healthy before scheduling the two follow-ups.
- **ADR-120 Closure II — vector store hardening + legacy embedding column retirement:** resolved the two ADR-120 tracked follow-ups now that the parity backfill is complete and PROD `knowledge_vector_chunks` is uniform `vector(3072)` (982 rows, all text-embedding-3-large; 2 stale sources reindexed to 3072). **(1) HNSW ANN index added** — migration `20260620120000_adr120_drop_legacy_chunk_jsonb_and_hnsw` creates `knowledge_vector_chunks_embedding_hnsw_idx` on a `halfvec(3072)` expression (`halfvec_cosine_ops`), since pgvector 0.8.1 caps the bare `vector` type at 2000 dims for HNSW; `KnowledgeVectorIndex.searchNearest` now orders by `embedding_vector::halfvec(3072) <=> query::halfvec(3072)` so the planner uses the index, and re-scores the returned top-K at full `vector` precision (the `1 - (… <=> …::vector)` score line is unchanged). The stored column type stays `vector(3072)`. **(2) Legacy JSONB embedding columns dropped** — the same migration drops `embedding_vector` + `embedding_generated_at` from `assistant_knowledge_source_chunks`, `global_knowledge_source_chunks`, and `product_knowledge_text_entry_chunks` (NON-reversible data loss; the unified store is now authoritative). These three tables remain the canonical TEXT store (`content`/`locator`/`embedding_model_key` retained). The indexing worker (`persistLegacyChunkRows`) stops dual-writing the JSONB embeddings; vectors continue to flow to `KnowledgeVectorChunk` via `replaceSourceChunks`. The completed one-time backfill runner/service (`BackfillKnowledgeVectorStoreService`, its script, test, module registration, and the `backfill:knowledge-vector-store` npm script) were removed. Full retirement of the legacy `*_chunks` tables themselves remains a separate future program.

## 2026-06-19

- **Disabled stale slow-mo cadence watchdog (`slow_avg`) for web chat streams (live-found post-ADR-121 fix):** `resolveWebStreamCadenceWatchdogOptions` in `stream-web-chat-turn.service.ts` now returns `slowAvgEnabled: false` for normal/smart chat modes (it was already `false` for `project`, and `silentEnabled` is `false` everywhere) — so the cadence watchdog never reports a stall. Root cause found during live validation on `persai.dev`: with ADR-121 routing now reliably selecting the reasoning-tier model (`claude-opus-4-8`) for deep-level turns, that model streams visible text below the per-token `slow_avg` threshold (avg inter-delta gap > 200ms over 20 samples), which tripped `slow_avg`, aborted the runtime fetch mid-answer, and produced a truncated reply with no clean `stream-end`. Real mid-stream hangs remain guarded by the provider/runtime stream timeout (`PERSAI_RUNTIME_STREAM_TIMEOUT_MS`); the watchdog scaffolding, telemetry (`web_stream_stall_*`), and stall-retry path are retained and remain config-reenableable. Test `stream-web-chat-turn.service.test.ts` updated to assert both signals disabled in all chat modes. Gate: `@persai/api` lint PASS · format:check PASS · `@persai/api` typecheck PASS · focused `stream-web-chat-turn.service.test.ts` PASS (14/14).

- **ADR-121 Slice 5 landed (regression-coverage tests + one production bug fix):** `execution-profile-resolver.test.ts` extended with a table-driven `describe("ADR-121 Slice 5 — golden grid")` asserting all 4 levels × 3 override configs (no-override / partial `heavy=4096` / full all-levels) → `{ executionMode, modelRole, thinkingBudget }` plus an override-invariance assertion (13 tests). `run-suite-isolated.ts` updated so the resolver suite is included in the test runner. `turn-routing.service.test.ts` extended with `runSlice5SignalCombinationTests` covering: KB-availability axis (product_knowledge_intent fires with KB, falls to simple_turn without), deepMode × retrieval_intent nudge (light→medium), deepMode saturation (deep+deepMode→deep, capped), Russian deep cue ("проанализируй" standalone → level=deep without requiring a co-occurring reasoning term), chatMode="normal"/"smart" non-interference, snapshot-shape test (project-mode heavy turn → `level=heavy`, `executionMode=premium`, `thinkingBudget=8192`), partial medium-only plan override via router, full four-level plan override via router. **Production bug fixed (found via Slice 5 tests):** `turn-routing.service.ts` `reasoning_request` branch entry guard now includes `|| this.matchesAny(lowerText, DEFAULT_DEEP_CUE_TERMS)` — previously, pure deep-cue-term messages (e.g. "проанализируй этот баг") reached `simple_turn` because `DEFAULT_DEEP_CUE_TERMS` was only checked inside the branch (not as an entry condition), violating ADR-121 §D6 ("Keyword cues: 'think hard', 'проанализируй' are heavy-level contributors"). Gate: lint PASS · format:check PASS · `@persai/runtime` typecheck PASS · runtime test suite PASS (all suites, exit 0).

- **ADR-121 Slice 4 landed (per-plan `thinkingBudgetByLevel` — admin config, materialization, routing override):** New `AdminPlanThinkingBudgetByLevel` OpenAPI schema (`{ light, medium, heavy, deep }`, each `integer | null`) added to `packages/contracts/openapi.yaml`; contracts regenerated. Policy module `thinking-budgets-policy.ts` (new): schema const `"persai.thinkingBudgetByLevel.v1"`, lenient storage resolver, strict input parser (0 valid, negatives rejected), serializer, override guard; mirrors `tool-budgets-policy.ts` exactly. `manage-admin-plans.service.ts` parses on input, writes into `billingProviderHints` only when any leaf is non-null, reads back via lenient resolver. `RuntimeThinkingBudgetByLevelConfig` added to `runtime-contract`; `AssistantRuntimeBundleRuntimeConfig` gains `thinkingBudgetByLevel?`. `materialize-assistant-published-version.service.ts` gains `resolvePlanThinkingBudgetByLevel()` and conditionally spreads the field onto the runtime bundle. `runtime-bundle-registry.service.ts` gains `assertThinkingBudgetByLevelConfig()` (validates each leaf). `turn-routing.service.ts` extracts `ThinkingBudgetOverrides` from the bundle once per `decide()` call and threads them through all `resolveExecutionProfile` call sites via parameters (no singleton state). Admin web page (`apps/web/app/admin/plans/page.tsx`) gains four draft fields + form section for editing per-level thinking budgets. Tests: new `thinking-budgets-policy.test.ts`; extended `manage-admin-plans.service.test.ts`, `runtime-bundle-registry.service.test.ts`, `turn-routing.service.test.ts`, `plans/page.test.tsx`. Gate: contracts:generate PASS · lint PASS · format:check PASS · api/web/runtime/provider-gateway/runtime-contract typecheck all PASS · api tests PASS · runtime tests PASS (exit 0) · web tests PASS (69 files, 797 tests).

- **ADR-121 Slice 3 landed (provider plumbing — `thinkingBudget` threaded from routing decision to Anthropic Extended Thinking and OpenAI `reasoning_effort`):** `buildProviderRequest` gains `thinkingBudget: number = 0`; main-turn caller passes `routeDecision.mode === "active" ? routeDecision.thinkingBudget : 0` (shadow mode suppressed). `provider-text-generation.service.ts` adds `assertValidThinkingBudget` (non-negative integer; called after `assertValidTimeoutMsHint`). `anthropic-provider.client.ts` adds `supportsExtendedThinking` (regex `/claude-(opus-4|sonnet-4|haiku-4|3-7-sonnet)/i`); `generateText` + `streamText` inject `thinking: { type:"enabled", budget_tokens }` and bump `max_tokens = (maxOutputTokens ?? 1024) + thinkingBudget` when `budget >= 1024` and capable model. `openai-provider.client.ts` adds `supportsReasoning` (`/^o[0-9]/i` | `/(^|[^a-z])gpt-5/i`) and `reasoningEffortForBudget` (`<= 10k → low`, `<= 25k → medium`, `> 25k → high`); `generateText` + `streamText` inject `reasoning: { effort }` when `budget > 0` and capable. Default grid (heavy 8192 / deep 32768) now sends thinking on active turns for capable models. Tests: Anthropic (capable/non-capable/budget-0/budget-500-below-min/maxOutputTokens-formula), OpenAI (low/medium/high buckets, gpt-4.1 non-reasoning, budget-0/undefined), validation (negative/non-integer rejected). Gate: lint PASS · format:check PASS · api/web/runtime/provider-gateway/runtime-contract typecheck PASS · provider-gateway tests PASS · runtime tests PASS.

- **ADR-121 Slice 2 landed (router rewrite — `level` replaces direct `executionMode` output; both hardcodes removed):** `TurnRouteDecision` gains `level: RoutingLevel` and `thinkingBudget: number`; `createDecision` becomes the single resolver seat (calls `resolveExecutionProfile`, derives `executionMode` + `thinkingBudget`). Seven `deepMode ? "premium" : "normal"` ternaries + `coerceExecutionMode` deleted; replaced by `applyDeepModeNudge(level, deepMode)`. `reasoning_request` branch: base level `heavy` by default, `deep` only on explicit depth cues (`DEFAULT_DEEP_CUE_TERMS`). `premium_writing` base `medium`; deepMode nudges to `heavy`. `buildProjectModePrecheckDecision` (`project-execution-profile.ts`) returns `CreateDecisionInput` with `level: deepMode ? "deep" : "heavy"` — old `executionMode: "reasoning"` hardcode deleted. `ROUTER_OUTPUT_SCHEMA` switched to `level` enum; classifier parser reads `level` not `executionMode`. `toRuntimeTurnRoutingSnapshot` and `defaultRouteDecision` in `turn-execution.service.ts` populate `level` + `thinkingBudget`. `applyGroundedSkillLevelFloor` updated (level floor, telemetry label preserved). All test fixtures migrated from `executionMode` to `level`; new invariant cases: reasoning_request→heavy (CHANGED from reasoning), project+deepMode-off→heavy/premium (CHANGED from reasoning hardcode), deep cue→deep/reasoning. Gate: lint PASS · format:check PASS · api/web/runtime/provider-gateway/runtime-contract typecheck PASS · runtime tests PASS.

- **ADR-121 opened (two-dimensional execution routing — task `level` separated from `(model, thinking budget)`):** new open program ADR `docs/ADR/121-two-dimensional-execution-routing-model-and-thinking-budget.md` (Accepted — founder go). Post-119 audit found the router output `executionMode: normal|premium|reasoning` conflates "how heavy is the task" with "which model + how much thinking" — the root cause of two production hardcodes: `project-execution-profile.ts:72` forces `reasoning` for every project turn, and `turn-routing.service.ts` hard-caps `deepMode` ("smarter") at `premium` (seven `deepMode ? "premium" : "normal"` ternaries + `coerceExecutionMode`), so thinking budget is never set and "smarter" never reaches a reasoning model. Decision: the router emits a single semantic `level` (`light|medium|heavy|deep`); one pure resolver maps `level → ExecutionProfile { modelRole, thinkingBudget }` (medium and heavy share the premium slot, differ only by ~8k thinking — the reason a second axis is required); `executionMode` is retained as a derived model-slot token (downstream model resolution unchanged, minimal blast radius); new `thinkingBudget` field plumbed contract → provider-gateway (Anthropic `thinking.budget_tokens` / OpenAI `reasoning_effort`); both hardcodes removed (`project` → `heavy` signal, `deepMode` → +1-level nudge); signals enriched (length, attachments, active Skill, chatMode, keyword cues, KB availability); per-plan thinking budgets in admin. Five slices (contract+resolver+plan config → router rewrite → provider plumbing → admin UI → tests). No infra. Out of scope (separate ADRs): memory JIT redesign, scenario step progression, RAG unification (ADR-120, not yet authored), sandbox/shell model. Decided at acceptance: code/reasoning-term turns default to `heavy`, `deep` only with an explicit depth signal. Authored only; no routing code changed yet.

- **ADR-119 program closed (founder acceptance):** ADR-119 status updated to `Closed — 2026-06-19 (founder acceptance; program complete)`. § Founder acceptance closure documents live validation on `persai-dev`: cache/payload assembly **PASS**; scenario gate (c) **PARTIAL** (engage/release + volatile `<persai_active_scenario>` rails OK; step-by-step adherence and `activeStepNumber` deferred). Explicit non-reopen policy — scenario step progression and remaining live matrix gates (a)(b)(d)(e) belong in a **new founder ADR**, not ADR-119 slices. `AGENTS.md` updated: ADR-118 + ADR-119 in closed archive; no open orchestration program ADR. Local evidence: `tmp/adr-119-slice-1-validation/scenario-live-gate-report.md`.

- **ADR-119 polish — Anthropic sliding history cache marker (#2 of 2) fixed + "legacy" wording removed:** Anthropic sliding history cache marker (#2 of 2): fixed formula so the marker actually fires at 3k-token boundaries; previous version required 6k+ before firing and effectively disabled history caching in production. Tool-loop turns intentionally skip the marker — transient `tool_result` data is not cached. Also removed "legacy" wording from the single-block system-prompt path (Slice 2 multi-block rejected on review). Details:
  - **Formula (`applyAnthropicMovingHistoryBreakpoint` in `apps/provider-gateway/src/modules/providers/anthropic/anthropic-provider.client.ts`):** changed `maxCachedPrefixBytes = floor((total - minTail) / minTail) * minTail` (with `< minTail` gate) → `floor(total / minTail) * minTail` (with `<= 0` gate). The old "tail buffer" subtraction required ≈2× the chunk (~6k tokens) of stable history before the marker would fire at all, so ordinary PersAI dialogs never reached it and re-paid full `inputTokens` for the whole growing history every turn. The dynamic tail (`volatile_context` + current question) is already spliced in *after* this marker by `buildAnthropicMessages`, so no extra buffer is needed; the marker now fires as soon as one full chunk of stable history exists, stays put while history grows inside a chunk (cache hot), and advances forward across each chunk boundary (Anthropic READs the cached older prefix instead of re-WRITEing it). Method JSDoc rewritten to document the window and why the tail buffer was wrong. Byte-based measurement (UTF-8 bytes, 3 bytes/token via `APPROX_ANTHROPIC_BYTES_PER_TOKEN`) folded in from the prior session so the threshold fires correctly for Cyrillic/mixed content. The `shouldApplyAnthropicMovingHistoryBreakpoint` gate (`main_turn` only) is unchanged — `tool_loop_followup` turns intentionally never place the marker because transient `tool_result` data will not survive into long-term history. Volatile blocks (`<persai_memory>`, active scenario, `<system-reminder>`) are physically extracted before the marker and spliced back after it, so the cached prefix is the stable history fact only.
  - **"legacy" cleanup + dead multi-block removal (Step 0):** Slice 2 multi-block system path (extra per-zone cache markers #3/#4 via `systemPromptBlocks`) is **rejected on review, not deferred** — the single-block system marker already caches `tools` + the whole `system` zone (Anthropic order `tools → system → messages`), and multi-block only helps under independent per-zone invalidation, which PersAI never does (zones change together on publish). The dormant field/branches were removed (no-dead-stubs): deleted `ProviderGatewayTextGenerateRequest.systemPromptBlocks` from `packages/runtime-contract`, the multi-block branch + `ANTHROPIC_MAX_SYSTEM_CACHE_MARKERS` from `buildAnthropicSystemBlocks`, the never-populated per-block branch from OpenAI `buildOpenAISystemDeveloperItems`, and the related provider-client test cases. Renamed all "legacy" comments/JSDoc in the active single-block system-prompt path and the `turn-execution.service.ts` `buildProviderRequest` comment. `rg "systemPromptBlocks" --type ts` returns only a single explanatory JSDoc reference in the Anthropic client.
  - **Tests:** `apps/provider-gateway/test/anthropic-provider.client.test.ts` — bucketed moving-history suite recomputed under the new formula (stable within a chunk; advances on chunk-boundary crossing; marker lands on the latest eligible assistant message); new "first firing at exactly one chunk" + "just below one chunk does not fire" edge cases; existing `toolHistoryBreakpointRequest`, `shortToolHistoryRequest`, and `tool_loop_followup` coverage preserved. OpenAI test swaps the removed `systemPromptBlocks` cases for single-`systemPrompt` developer-item coverage (`instructions` field stays absent).
  - **Gate green:** `corepack pnpm -r --if-present run lint` PASS · `corepack pnpm run format:check` PASS · `@persai/api` typecheck PASS · `@persai/web` typecheck PASS · `@persai/provider-gateway` test PASS (exit 0, full suite incl. Anthropic). `@persai/runtime` + `@persai/provider-gateway` typecheck also PASS.
  - **Cache prefix impact:** none on system-prefix bytes — change is per-turn marker placement inside `messages[]` plus dead-field removal. Anthropic target config is now **2/4 markers used** (system prefix + sliding history); markers #3/#4 (multi-block) intentionally unused.

## 2026-06-18

- **ADR-119 follow-up — Anthropic moving-history-breakpoint fix for tool-loop conversations:** Founder live-test observation: per-turn `inputTokens` kept climbing past the 3k `anthropicHistoryBreakpointMinTokens` threshold on tool-heavy chats — the moving history `cache_control` marker was never being placed. Root-cause audit of `apps/provider-gateway/src/modules/providers/anthropic/anthropic-provider.client.ts`: (1) `applyAnthropicMovingHistoryBreakpoint` only accepted assistant messages whose content was a **single text block** as breakpoint candidates (via the now-deleted `resolveAnthropicHistoryBreakpointText` returning `null` for any non-`text` block), so every assistant turn emitted from `toolHistory` (pure `tool_use` content) was silently skipped — i.e. in real production conversations where the model has called any tool, the breakpoint never fires; (2) the candidate's content was **destructively replaced** with a single text block, which would have corrupted multi-block messages; (3) `measureAnthropicTextTailChars` counted only `text` blocks, under-estimating the message's true byte length and skewing the chunked-cache math toward "not enough yet" on tool-loop turns; (4) `countAnthropicCacheBreakpoints` only counted `cache_control` markers in `system` and `tools`, so even when the breakpoint did fire there was no log visibility into it. **Fix:** `AnthropicBuiltMessageContent` type extended to allow `cache_control` on `image`, `document`, `tool_use`, and `tool_result` blocks in addition to `text`. `applyAnthropicMovingHistoryBreakpoint` rewritten — accepts **any** assistant message as a candidate, walks chunked-cache math using the new `measureAnthropicMessageContentChars`, and delegates marker placement to a new `attachCacheControlToLastBlock` helper that **non-destructively** clones the message and attaches `cache_control` to the last cache-control-capable block (text/image/document/tool_use/tool_result). `measureAnthropicMessageContentChars` (renamed from `measureAnthropicTextTailChars`) counts `text` text length, `tool_use` `JSON.stringify(input).length`, and `tool_result` string-content length — image/document base64 counted as zero (not part of the textual cache key). `countAnthropicCacheBreakpoints` extended to also walk `payload.messages` so log lines surface message-level markers. **Tests:** existing breakpoint suite passes unchanged; new `toolHistoryBreakpointRequest` case asserts (a) the pure-`tool_use` assistant message emitted from `toolHistory` is byte-preserved and (b) `cache_control` is correctly attached to the last `tool_use` block; new `shortToolHistoryRequest` case asserts chunked math still gates correctly (no marker on tiny histories). **Gate green:** provider-gateway lint PASS, full provider-gateway vitest suite PASS, format:check PASS, api/web/runtime/provider-gateway typecheck PASS. **Cache prefix:** no change to system-prefix bytes — fix purely affects per-turn breakpoint placement inside `messages[]`.

- **ADR-119 cleanup slice (template + tool descriptor normalization + legacy fallback canonicalization):** post-live-test cleanup of structural inconsistencies the founder flagged: (1) markdown headings (`# X`, `## X`) still surfaced inside XML-wrapped templates; (2) four templates were never wrapped (`router_classifier`, `skill_state_classifier`, `preview_bootstrap`, `welcome_bootstrap`); (3) only 8/20 catalog tools and 3/5 synthetic tools followed the WHEN/NOT/EXAMPLES/GOTCHAS ACI shape; (4) `compile-prompt-constructor.service.ts` legacy fallbacks still produced `# Identity` / `# User Context` / `# Character Preview` / `# First Conversation` markdown when a template was null; (5) `renderTraitsBlock` emitted `## Personality Traits` markdown heading inside the `<voice>` block.
  - **Templates (`apps/api/prisma/bootstrap-preset-data.ts`):** all 7 visible XML-wrapped templates (`soul`, `user`, `identity`, `memory_protocol`, `tools`, `heartbeat`, `presence`) had their markdown `# H1`/`## H2` headings replaced with nested canonical XML elements (`<core_persona>`, `<style>`, `<openings>`, `<emotion_response>`, `<silence>`, `<examples>`, `<sense_of_time>`, `<usage>`). Numbered priority list inside `<tool_usage_policy>` (1. 2. 3. …) lifted from Markdown `N.` to canonical XML `<rule order="N">…</rule>`. List labels normalized from `- **Name**: value` to `- Name: value` (bold inline emphasis stripped from list-item labels; bold-emphasis kept where it carries true emphasis like `**${assistantName}**`). 4 previously-unwrapped templates wrapped: `router_classifier` → `<router_classifier>` (with `<modes>`, `<retrieval_plan>`, `<tool_hints>`); `skill_state_classifier` → `<skill_state_classifier>` (with `<rules>`); `preview_bootstrap` → `<character_preview>` (with `<task>`, `<constraints>`); `welcome_bootstrap` → `<first_conversation_greeting>` (with `<task>`, `<opening_requirements>`, `<middle_section>`, `<closing_requirements>`, `<formatting_constraints>`). Provenance documentation added inside `<memory_protocol><read>` enumerating all four enum values (`user_explicit`, `system_inferred`, `auto_extracted`, `legacy`) and how the model should treat each.
  - **Tool descriptors (`apps/api/prisma/tool-catalog-data.ts` + `runtime-tool-policy.ts files-override` + `bootstrap-preset-data.ts hidden synthetics`):** every model-visible tool now carries the canonical 4-section ACI shape. Catalog: 10 new conversions — `video_generate`, `document`, `tts`, `browser`, `scheduled_action`, `background_task`, `persai_tool_quota_status`, `files` (also synced into the runtime override), `exec`, `shell`. Synthetics: 3 new conversions — `summarize_context`, `compact_context`, `quota_status`. Hidden-internal entries (`cron`, `persai_workspace_attach`) keep their one-liner shape deliberately (not model-visible).
  - **Legacy fallback canonicalization (`apps/api/src/modules/workspace-management/application/compile-prompt-constructor.service.ts`):** the legacy markdown-heading fallback paths in `generateSoulPrompt`, `generateUserPrompt`, `generateIdentityPrompt`, `generatePreviewPrompt`, `generateWelcomePrompt` were rewritten to emit canonical XML (`<voice>` + `<character_notes>`, `<user>`, `<identity>`, `<character_preview>`, `<first_conversation_greeting>`) so any code path that runs with `template === null` no longer regresses to markdown headings. `renderTraitsBlock` rewritten from `## Personality Traits` heading + `- **trait**: N/100` bullets to canonical `<personality_traits>` XML with plain `- trait: N/100` bullets. `interpolateTemplate` callers now feed `- Name: value` line builders to `*_line` placeholders, matching the bold-free style of the new template defaults.
  - **Tests:** `bootstrap-preset-data.test.ts` — `SKIPPED_TEMPLATE_KEYS` emptied (all templates now pass XML-balance check); `EXPECTED_OUTER_TAGS` extended with `router_classifier`, `skill_state_classifier`, `preview_bootstrap`, `welcome_bootstrap`; new `runNoMarkdownHeadings` invariant test scans every template (with `stripCodeAndPlaceholders` applied) and asserts no `^#{1,6}\s` markdown headings remain inside any XML tag. `seed-tool-catalog.test.ts` — Slice 7 8-tool ACI check generalized to "every model-visible catalog entry must carry the 4-section ACI shape" (only `cron` and `persai_workspace_attach` excluded); ALLOW_LIST expanded with the new legitimate cross-references (`browser ↔ web_search/web_fetch`; `video_generate → image_generate/image_edit/tts`; `document → files`; `scheduled_action ↔ background_task`; `persai_tool_quota_status → knowledge_search/document`; `files → exec/shell/document`; `exec → shell/files/document`; `shell → files/exec/document`). `runtime-tool-policy.test.ts` `files` override assertions updated for the canonical 4-section shape. Golden snapshot `apps/api/test/fixtures/adr119-golden-prompt-snapshot.expected.txt` deleted and regenerated against the new prompt bytes (11346 bytes vs prior 11007).
  - **Migration:** templates ship as defaults but existing assistants carry their own snapshot copies in DB. Founder will `Reset to default` per template after deploy. Reset checklist provided in handoff: 11 visible+hidden templates (`system`, `soul`, `user`, `identity`, `memory_protocol`, `tools`, `heartbeat`, `presence`, `router_classifier`, `skill_state_classifier`, `preview_bootstrap`, `welcome_bootstrap`) plus seed of `tool-catalog-data.ts` (auto-applied on next API pod startup via existing `SeedToolCatalogService`). Memory provenance audit: write paths (`RuntimeMemoryWriteToolService`=system_inferred, `ManageAssistantWorkspaceMemoryService.add`=user_explicit, `AutoExtractToMemoryService`=auto_extracted) already correct; the live-test subagent's "P1 legacy provenance" finding was an older backfill row from before Slice 9, not a write-path bug. No code fix needed; behavior documented inside `<memory_protocol>` so the model treats each enum correctly.
  - **One-time prompt-cache prefix invalidation on rollout** — template byte changes invalidate the existing Anthropic / OpenAI cache prefix. Per ADR-119 D11 this is acceptable; cache rebuilds on the next turn.
  - **Gate:** lint PASS (all packages), format:check PASS, api typecheck PASS, web typecheck PASS, full api test suite PASS (incl. new ACI invariant, no-markdown-headings invariant, golden snapshot regeneration), full runtime test suite PASS, full provider-gateway test suite PASS, full web test suite PASS.

- **ADR-119 follow-up — Anthropic gateway terminal usage metadata + wired `dumpResponse` (live-test blocker fix):** Live-test runner subagent reported P0 blocker — `provider_payload_response_dump` events were never being emitted despite `PERSAI_DEBUG_PROVIDER_PAYLOAD=true` (Slice 0.5 defined `ProviderDebugPayloadLogger.dumpResponse()` but no call site ever invoked it in any provider client), so cache-hit/miss metrics (`cache_creation_input_tokens`, `cache_read_input_tokens`) could not be read from cluster logs. Fixed in `apps/provider-gateway/src/modules/providers/anthropic/anthropic-provider.client.ts`: (1) new always-on `[anthropic-non-stream-end]` and `[anthropic-stream-end]` info-level metadata lines emit per turn at the same single-line printf format as the existing `[*-start]` lines, surfacing `requestId`, `classification`, `iteration`, `model`, `stopReason`, `toolCalls`, `inputTokens`, `cacheCreationInputTokens`, `cacheReadInputTokens`, `outputTokens`, `totalTokens` (with `null` literal when a field is unset) — readable from `kubectl logs` without enabling the debug toggle; (2) `debugPayloadLogger.dumpResponse(...)` is now actually called from both transports (non-stream after `finalMessage()`, stream at `message_stop` for both `tool_calls` and `completed` paths) carrying the final `stop_reason` + `usage` snapshot + tool call count + text. No user content is ever logged on the always-on line. Tests (`apps/provider-gateway/test/anthropic-provider.client.test.ts`): generate-path asserts a `[anthropic-non-stream-end]` line with the full printf grammar; stream-path asserts a `[anthropic-stream-end]` line and the exact `cacheCreationInputTokens=4`, `cacheReadInputTokens=6`, `inputTokens=10`, `outputTokens=5`, `totalTokens=25` figures from the existing stream-cache fixture. Gate green: provider-gateway typecheck PASS, lint PASS, format:check PASS, full provider-gateway vitest PASS. No cache-prefix bytes touched. OpenAI symmetry follow-up deferred (live test uses Anthropic).
- **ADR-119 follow-up — live-test enablement (Slice 14 partial + comprehensive test plan):** Slice 0.5 toggle hardened in `apps/provider-gateway/src/modules/providers/provider-debug-payload-logger.ts` — `PERSAI_DEBUG_PROVIDER_PAYLOAD` now accepts `"1"`, `"true"`, `"yes"`, `"on"` case-insensitively (with trim), keeping `""`, `"0"`, `"false"`, `"no"`, `"off"`, and unknown spellings off. New `isTruthyEnvFlag` helper centralizes the check. Dump calls migrated from `logger.debug()` to `logger.log()` (info level) so a default `LOG_LEVEL=info` pod surfaces dumps without flipping the entire logger to debug. Gating still lives in `shouldDump()`. Tests (`apps/provider-gateway/test/provider-debug-payload-logger.test.ts`): truthy/falsy spelling matrix expanded (9 truthy spellings + 7 falsy spellings); `withDebugCapture` rewired to intercept `Logger.prototype.log`. `infra/helm/values-dev.yaml` `providerGateway.env` gains `PERSAI_DEBUG_PROVIDER_PAYLOAD: "true"` + `PERSAI_DEBUG_PROVIDER_PAYLOAD_RATE: "1.0"` so ArgoCD reconciles the flag and a `kubectl set env` operator override is no longer needed; production values files MUST keep the flag OFF. New `docs/ADR/119-live-test-plan.md` — comprehensive ADR-119 acceptance-gate plan covering 8 zones (A baseline conversational / B memory protocol / C native tool selection 12 cases / D Skills+scenarios progressive disclosure / E sandbox+deferred-document jobs PDF-hotfix regression / F cache effectiveness across turns / G parallel-tool-call discipline / H error+recovery), pre-conditions, output artifact shape, and acceptance criteria; intended to be run by a browser-use subagent.

- **ADR-119 Slice 11 (closure): six golden tests landed, docs updated, ADR closed.** Golden tests: GT1 full-prompt byte-snapshot (`apps/api/test/adr119-golden-prompt-snapshot.test.ts` + committed fixture `apps/api/test/fixtures/adr119-golden-prompt-snapshot.expected.txt`); GT1b runtime volatile-context three-zone structure (`apps/runtime/test/adr119-golden-prompt-snapshot.test.ts`); GT2 cache-prefix byte-stability across 5 state variants (extended `apps/runtime/test/prompt-cache-stable-blocks.test.ts`); GT3 `<priority_order>` Skills #1 (`runAdr119Invariantstest` in `native-tool-projection.test.ts`); GT4 provider parallel-tool-call flags (labeled in anthropic and openai provider client tests); GT5 persona dedup (`runAdr119GoldenTest5PersonaDedup` in `compile-prompt-constructor.service.test.ts`); GT6 memory provenance XML (labeled in `prompt-cache-stable-blocks.test.ts`). Docs updated: `docs/ARCHITECTURE.md` — new "Prompt architecture (ADR-119)" section covering three-zone structure and provider discipline; `docs/API-BOUNDARY.md` — new "Volatile-context XML kinds" table for all `volatileKind` values; `docs/DATA-MODEL.md` — new "ADR-119 schema additions" section documenting `AssistantMemoryRegistryItem.provenance` (enum, backfill migration), `SkillScenario.firstStepPreview` (VARCHAR, migration), and `SkillScenarioStep` JSON additions; `docs/TEST-PLAN.md` — new "ADR-119 golden tests" section with one-paragraph description of each golden test and the failure mode it catches. ADR-119 `Status` changed from `Accepted` to `Closed — 2026-06-18` with Closure footer (slice SHA table, acceptance gate note, migration status, ADR-118 cross-ref). ADR-118 (`118-skill-scenarios-and-model-owned-activation.md`) `Status` changed from `Accepted` to `Superseded by ADR-119 — 2026-06-18` with Superseded footer (what ADR-119 adds on top, authority reference, reachability pointer).

- **ADR-119 Slice 10 landed (admin UI for new scenario step fields + first_step_preview):** `ScenarioStepDraft` extended with `expectedUserResponse` (≤400 chars), `nextStepTrigger` (≤200 chars), `recoveryGuidance` (≤400 chars), and `firstStepPreview` (step 1 only, ≤200 chars) in `apps/web/app/admin/skills/page.tsx`. `EMPTY_SCENARIO_STEP_DRAFT` defaults all new fields to `""`. `scenarioToDraft` maps `scenario.firstStepPreview` (scenario-level) onto `draft.steps[0].firstStepPreview`; other step fields via `?? ""`. `validateScenarioDraft` enforces length limits; over-length blocks submission. `scenarioDraftToCreatePayload` / `scenarioDraftToUpdatePayload` serialize: step-level fields (expectedUserResponse/nextStepTrigger/recoveryGuidance) null-coerced per step; `firstStepPreview` emitted both at step 0 level and at scenario level (for API dual compatibility). Step editor JSX adds textarea for each new field per step; `firstStepPreview` text input rendered only for step 1. `renderActiveScenarioBlockPreview` rewritten from plain-text to Slice 4 XML format (matching `build-active-scenario-block.service.ts`): `<step number="N">` with conditional `<expected_user_response>`, `<next_step_trigger>`, `<recovery_guidance>`, `<may_be_skipped_if>`, `<negative_guards>`, then `<exit_condition>`. New `renderScenarioCatalogFirstStepPreview` function derives first_step_preview (uses `firstStepPreview` override if non-empty, else truncates `directive` to ≤200 chars). Catalog preview pane shows `<first_step_preview>` value. `SkillScenarioStepState` gains `firstStepPreview: string | null`; `parseStep` validates ≤200 chars; `normalizeStepsState` reads the field from stored JSON. `AdminSkillScenarioState` gains scenario-level `firstStepPreview: string | null` (stored in a new `first_step_preview VARCHAR(200)` Prisma column — migration `20260618160000_adr119_first_step_preview`). `enabled-skills-prompt-materialization.ts` updated: `<first_step_preview>` uses `scenario.firstStepPreview` verbatim when set, falls back to auto-derived from `directive` (Slice 3 behavior preserved). `manage-skill-scenarios.service.ts` persists and loads the new column. `AdminSkillScenario`, `AdminSkillScenarioStep`, `AdminCreateSkillScenarioRequest`, `AdminUpdateSkillScenarioRequest` contract types extended with optional nullable `firstStepPreview?`. `RuntimeBundleSkillScenario` and `RuntimeBundleSkillScenarioStep` gain optional `firstStepPreview?`. Tests: 22 new tests across `page.test.tsx` (Slice 10 test suite), `enabled-skills-prompt-materialization.test.ts` (firstStepPreview override + fallback), `manage-skill-scenarios.service.test.ts` (persistence + backward compat + validation).

- **ADR-119 Slice 9 landed (memory protocol + provenance schema + `<persai_memory>` XML):** `AssistantMemoryProvenance` enum added to Prisma schema (`user_explicit`, `system_inferred`, `auto_extracted`, `legacy`); `provenance` column added to `AssistantMemoryRegistryItem` with `DEFAULT 'legacy'` (additive — existing rows backfill automatically). Migration: `20260618153000_adr119_memory_provenance`. Domain entity, repository interface, and Prisma repository all updated to carry provenance. `WriteAssistantMemoryService.parseInput` defaults provenance to `system_inferred` when absent (callers that cannot distinguish omit the field); `asProvenance` returns `"system_inferred"` for `undefined`. `ManageAssistantWorkspaceMemoryService.add` marks workspace-memory writes as `user_explicit`. `AutoExtractToMemoryService` passes `provenance: "auto_extracted"`; `RuntimeMemoryWriteToolService` passes `provenance: "system_inferred"`. Volatile contextual memory rendering rewritten from markdown-list to canonical XML per ADR D10: `<entry id="..." provenance="..." written_at="YYYY-MM-DD">summary\n</entry>` per item via `formatDurableMemoryContextualBlock(MemoryXmlEntry[])`. `isDurableMemoryContextualMessage` updated to check `role === "assistant" && cacheRole === "volatile_context" && volatileKind === "memory"` (role guard added). Both provider clients wrap `volatileKind: "memory"` messages as `<persai_memory>…</persai_memory>` (replacing `<recent_short_memory>` / `<persai_contextual_memory>`). New `memory_protocol` template added to `VISIBLE_PROMPT_TEMPLATE_DEFAULTS` with `<memory_protocol><read>…</read><write>…</write></memory_protocol>` content; `{{memory_protocol_block}}` inserted in `system` template after `{{reminders_protocol_block}}`; `agents` template emptied (memory_protocol content moved to dedicated template). `compile-prompt-constructor.service.ts` gains `generateMemoryProtocolPrompt`. `extractRenderedShortMemorySummaries` updated to parse both new XML `<entry>` format and legacy markdown-list format (back-compat). `InternalHydratedDurableMemoryItem` and `InternalMemoryWriteInput` carry `provenance`. Batched with Slice 8 (response contract `<must>`/`<prefer>`) for single prefix-rollout.

- **ADR-119 Slice 8 landed (response contract two-tier restructure):** `<response_contract>` block in the `system` template (`apps/api/prisma/bootstrap-preset-data.ts`) restructured from a flat 11-rule list to a two-tier `<must>`/`<prefer>` XML structure per ADR-119 D9. 4 hard invariants in `<must>` (polished product blocks, assistant_gender self-reference, fenced code blocks, delivery honesty). 4 soft preferences in `<prefer>` (opener, calm formatting, Markdown h2/h3 structure, follow-up actions). Tests: `bootstrap-preset-data.test.ts` — new `runResponseContractSlice8` suite (XML balance passes; `<must>`/`<prefer>` nesting inside `<response_contract>`; MUST tier phrase assertions; PREFER tier phrase assertions; first child is `<must>` not a bare list). `compile-prompt-constructor.service.test.ts` — 3 stale assertions updated to match new text; new `runResponseContractSlice8` test confirms compiled default prompt contains `<response_contract>` with `<must>`/`<prefer>` children. Cache-prefix bytes change deliberate; batched with Slice 9 (memory protocol) for single rollout.

- **ADR-119 Slice 7 landed (top-priority tool descriptor rewrite per Anthropic ACI pattern):** `modelDescription` and `modelUsageGuidance` rewritten for 8 tools — `skill`, `image_edit`, `image_generate`, `knowledge_search` (catalog entry `memory_search`), `knowledge_fetch` (catalog entry `memory_get`), `memory_write` (new catalog entry added), `web_search`, `web_fetch` — using the 4-section ACI format (`WHEN TO USE` / `WHEN NOT TO USE` / `EXAMPLES` / `GOTCHAS`) in `apps/api/prisma/tool-catalog-data.ts`. Synthetic tool defaults (`knowledge_search`, `knowledge_fetch`, `memory_write`) also updated to the same format in `HIDDEN_PROMPT_TEMPLATE_DEFAULTS` in `apps/api/prisma/bootstrap-preset-data.ts`. ADR-117 invariant preserved: no cross-tool prose inside per-tool descriptors; allowed chain-link exceptions only (`web_search` ↔ `web_fetch`, `knowledge_search` ↔ `knowledge_fetch`, `web_fetch` ↔ `browser`). Cross-tool prose removed from `image_generate` projection fallback. `native-tool-projection.ts` updated: `resolveToolDefinitionDescription` now uses `\n` separator, adds `TOOL_DESCRIPTION_CAP=1024` guard with graceful truncation preserving `WHEN TO USE:` first line. Tests: `runAdr119Slice7DescriptorTests` export added to `native-tool-projection.test.ts` (per-tool shape test × 8, cross-tool drift test reading catalog source, safe-fallback truncation test); `seed-tool-catalog.test.ts` extended with Slice 7 shape assertions and ADR-117 drift assertions for all 8 tools. Batched with Slice 6 (selection guide XML) for single cache-prefix invalidation.

- **ADR-119 Slice 6 landed (selection guide XML priority order):** `tools` template in `apps/api/prisma/bootstrap-preset-data.ts` rewritten to canonical XML `<tool_usage_policy>` structure with `<priority_order>` (Skills #1 gate via "Skills are the gate" rule), `<parallelism>` (`skill({action:"engage"})` is ALWAYS solo), `<failure_handling>` (`pending_delivery` honesty rule, error/denied/budget exhaustion), and `<category_rules>` with per-domain `<category>` elements (files, documents, tasks, browser, skills). The old Markdown heading `# Native Tool Runtime — Selection Guide` and `##` section headings are removed; the XML structure is self-describing. ADR-118 D7 one-rule Skills contribution migrated into `<priority_order>` #1 and `<category name="skills">`. ADR-117 golden test (`apps/runtime/test/native-tool-projection.test.ts`) invariants preserved — (a) no Tasks Policy, (b) selection-guide seat preserved, (c) tool-catalog-data not cross-tool prose — plus new assertions added: (d) `<priority_order>` enumerates Skills as #1, (e) `<parallelism>` states `skill({engage})` is ALWAYS solo, `<failure_handling>` mentions `pending_delivery`. Web admin presets test mock template updated to new XML form. One-time prompt-cache prefix invalidation deliberate; batched with Slice 7 (tool descriptions) for single rollout.

- **ADR-119 Slice 5 landed (`<system-reminder>` mid-conversation injection protocol):** New `<reminders_protocol>` cache-prefix block declares the protocol; runtime emits reminders for (1) active scenario tick, (2) reference image attached during scenario, (3) tool budget ≥ 80%. Reminders ride the existing volatile-context rails (`cacheRole: volatile_context`, `volatileKind: system_reminder`), wrapped by both providers as `<system-reminder>…</system-reminder>`. ADR D7 mode-switch and date/env reminders deferred to future ADRs.

## 2026-06-17

- **ADR-119 Slice 4 landed (volatile active-scenario XML format + step field extensions):** `BuildActiveScenarioBlockService.renderActiveScenarioBlock` rewritten from Markdown (`## Active Scenario: …`, `Steps:`, `Recommended tool:`, `Guards:`) to canonical XML per ADR-119 D5: inner content starts with `Active: <scenarioDisplayName> (Skill: <skillDisplayName>)` summary line, then `<step number="N">` elements containing `<directive>`, optional `<recommended_tool_call>`, optional `<expected_user_response>`, optional `<next_step_trigger>`, optional `<recovery_guidance>`, optional `<may_be_skipped_if>`, optional `<negative_guards><guard>Do NOT …</guard></negative_guards>`, then `<exit_condition>` at the end. Sub-tags omitted when null/empty for byte stability. Provider wrapper inner tag renamed from `active_scenario` → `persai_active_scenario` in `anthropic-provider.client.ts` (OpenAI already used `persai_active_scenario` since ADR-118 Slice 4). Schema additive — three new optional fields added to `RuntimeBundleSkillScenarioStep` in `packages/runtime-contract`: `expectedUserResponse?`, `nextStepTrigger?`, `recoveryGuidance?` (all `string | null`). Mirrored in `SkillScenarioStepState` (API-side parse type) and `parseStep` (accepts + validates the new fields up to 400 chars each). `normalizeSkillScenarioSteps` in the materializer populates all three with `null` when absent — stable byte order, never `undefined`. `normalizeSkillScenarioSteps` exported for direct unit testing. Tests: `build-active-scenario-block.service.test.ts` fully updated and extended (18 cases — XML tag assertions, new field presence/absence, byte-stability); `anthropic-provider.client.test.ts` updated to assert `<persai_active_scenario>` (NOT `<active_scenario>`); `openai-provider.client.test.ts` negative assertion added; `materialize-assistant-published-version.service.test.ts` extended with 3 normalizer unit tests (field flow-through, missing-fields → null, explicit-null → null). Existing scenarios continue to work — new fields are optional and default to null; Admin UI to expose them deferred to Slice 10.

- **ADR-119 Slice 3 landed (Skills progressive disclosure + `first_step_preview`) — batched with Slices 1+2 in same persai-dev deploy:** Each enabled Skill in the cache prefix is now a compact XML card (`<skill id="..." key="...">`) instead of a Markdown block with full `body`/`guardrails`/`examples`. The long instruction payload (~1,200 chars `body` + guardrails + examples per Skill) is removed from the cache prefix and delivered by the runtime only when the model calls `skill({action:"engage"})` — field name `instruction: {body, guardrails, examples}` in the engage response (Part B). The new `<first_step_preview>` scenario field (≤200-char excerpt of `steps[0].directive`, stripped to a single line) is emitted inside each `<scenario key="...">` entry so the model sees step 1 guidance **before** calling `skill({engage})`, fixing [F3] parallel-tool-call pre-emption. XML card format per ADR-119 D4: `<display_name>`, `<summary>`, `<when_to_use>` (new optional InstructionCard field), `<category>`, `<tags>`, `<available_scenarios>` with per-scenario `<name>`, `<one_line>`, `<first_step_preview>` (omitted when no steps), `<recommended_tools>` (omitted when empty). Expected prefix shrink: ~60-80% per Skill (3 Skills × ~1,500 chars ≈ ~4,500 chars / ~1,100 tokens per request, back-of-envelope from ADR D4). **R8 invariant**: body/guardrails/examples are added to `AssistantRuntimeEnabledSkillSummary` in `packages/runtime-bundle` and materialized from the existing `EnabledSkillPromptCard` fields in the same commit — guaranteed to arrive in the engage tool result the moment they leave the prefix. Tests: `enabled-skills-prompt-materialization.test.ts` rewritten for XML assertions (skill tag, key attr, first_step_preview present/absent, R8 sentinel check, byte-stability); `runtime-skill-tool.service.test.ts` extended with instruction payload assertions (engage no-scenario, engage with-scenario, byte-match sentinel). Web admin presets `skill_cards_block` sample updated to new XML format.

- **ADR-119 Slice 2 landed (provider cache_control markers + parallel-tool-calls discipline):** `packages/runtime-contract` `ProviderGatewayTextGenerateRequest` extended with two optional fields — `skillsEnabled?: boolean` (triggers parallel-tool-calls discipline) and `systemPromptBlocks?: Array<{id, text}>` (per-zone cache marker placement, deferred population to follow-up slice). **OpenAI `developer` role migration (R4):** `payload.instructions` removed from both non-streaming and streaming Responses API paths; system prompt (or each named block) is now emitted as `{role:"developer", content:[{type:"input_text", text}]}` items prepended to `input[]` — makes the stable system prefix the OpenAI Responses API cache prefix. **Anthropic per-block cache markers (R6):** `buildAnthropicSystemBlocks` extended — when `systemPromptBlocks` is provided and validates (concatenation equals `systemPrompt`), emits one `TextBlockParam` with `cache_control:{type:"ephemeral"}` per block, capped at 3 system markers (4th breakpoint reserved for moving-history window); excess blocks emitted as plain text with `anthropic_cache_breakpoints_truncated` warn; concatenation mismatch falls back to single-block with `anthropic_system_blocks_mismatch` warn, no throw. **Parallel-tool-calls discipline (R5):** Anthropic `toAnthropicToolChoice` returns `{type:"auto", disable_parallel_tool_use:true}` when `skillsEnabled===true && tools present`; OpenAI sets `parallel_tool_calls: skillsEnabled===true ? false : true`. **Runtime wiring:** `buildProviderRequest` in `turn-execution.service.ts` passes `skillsEnabled: bundle.skills?.enabled.length > 0`; `systemPromptBlocks` deferred — providers fall back to single-block until a follow-up micro-slice exposes compiler block offsets. **Deviation from spec:** `cacheBreakpoints: number[]` (byte-offset array) replaced by `systemPromptBlocks: Array<{id,text}>` (named-block array) because byte-offsets are fragile under string handling. Tests: `anthropic-provider.client.test.ts` and `openai-provider.client.test.ts` each extended with Slice 2 coverage (disable_parallel_tool_use branches both ways, systemPromptBlocks 3-block / 4-block / mismatch cases, OpenAI `instructions`-absent assertion). **Deploy note:** BATCH Slice 1 + Slice 2 + Slice 3 in the same persai-dev deploy per ADR-119.

- **ADR-119 Slice 1 landed (XML compile output + persona deduplication) — one-time prompt-cache prefix invalidation on rollout:** all eight visible prompt templates in `bootstrap-preset-data.ts` are now wrapped with their canonical ADR-119 outer XML tags (`<voice>` + `<character_notes>` for `soul`; `<user>`, `<identity>`, `<enabled_skills>`, `<tool_usage_policy>`, `<memory_protocol>`, `<background_task_evaluation>`, `<persai_environment>`); the `system` template's "# Response UI Contract" section is wrapped in `<response_contract>`. The `soul` template splits into two adjacent blocks: `<voice>` holds archetype-driven content and `<character_notes>` holds `{{instructions_block}}` (`snapshotInstructions`) — this is the [F1] persona dedup fix; `snapshotInstructions` is now rendered exactly once across the materialized system prompt. `compile-prompt-constructor.service.ts` drops the duplicate `{{persona_instructions_block}}` path (resolves to `null` so legacy custom templates simply drop the line), adds `stripEmptyCharacterNotes` to collapse the empty shell on persona-less assistants, and emits `compileMode: "xml_canonical_v1"` on every new materialization. `packages/runtime-bundle/src/index.ts` gains the `AssistantRuntimePromptCompileMode` type and the optional `compileMode` field on `AssistantRuntimePromptConstructor.ordinary`; the legacy fallback synthesizer emits `"legacy_markdown"`. New tests: `apps/api/test/bootstrap-preset-data.test.ts` — XML balance validator (stack-based, strips fenced code + backticks + placeholders before parsing) + outer-tag presence + `<character_notes>`/`{{instructions_block}}` placement assertions; `apps/api/test/compile-prompt-constructor.service.test.ts` — three fixture snapshots (archetype-only, free-form-only, archetype+instructions) each asserting `compileMode`, `<voice>` count, `<character_notes>` count, single-occurrence of `snapshotInstructions`, and `<voice>`/`<character_notes>` adjacency. ADR-117 golden test updated for `<tool_usage_policy>` and `<memory_protocol>` outer tags (inner heading assertions preserved). **One-time prompt-cache prefix invalidation on rollout** — the XML tag bytes added to every cached template change the stable prefix; `configDirtyAt` cleared implicitly on next materialization.

- **ADR-119 Slice 0.5 landed (Anthropic gateway observability + symmetric debug payload dump):** `apps/provider-gateway` now emits always-on INFO metadata lines before both Anthropic text transports: `[anthropic-non-stream-start]` for caller-facing `generateText()` (even though it uses `messages.stream(...).finalMessage()` internally) and `[anthropic-stream-start]` for `streamText()`, with request id, classification, tool-loop iteration, model, system block count, cache breakpoint count, message count, tool count, and tool-history count derived from the assembled provider payload. A new shared `ProviderDebugPayloadLogger` adds env-flag-gated request body dumps for Anthropic and OpenAI on the dedicated `persai.debug.provider` logger; `PERSAI_DEBUG_PROVIDER_PAYLOAD` must be exactly `true`, `PERSAI_DEBUG_PROVIDER_PAYLOAD_RATE` defaults to `0.05` when enabled and sanitizes invalid/out-of-range values back to `0.05`, and all flags are OFF by default in production. Dumps include bounded system/message/tool previews, 500-char truncation, and base64 redaction markers such as `<redacted:image/png:base64:LENGTH=N>` instead of raw image/document bytes. OpenAI keeps its existing stream metadata and now gets the same gated dump on non-streaming and streaming Responses requests. Operator follow-up documented for Slice 0.5: configure Loki retention for `persai.debug.provider` at 3 days; no infra files changed in this slice.

- **ADR-119 Slice 0 landed (architecture inventory and reachability ledger):** read-only subagent produced `docs/ADR/119-prompt-inventory.md` — 1062-line ledger with 9 sections: 41 prompt-section writer rows (W1-W41 detailed), 15 template constants from `bootstrap-preset-data.ts`, 7 tool-descriptor surfaces, 5 volatile-context kinds traced end-to-end (memory + active_scenario exist today; retrieved_knowledge / environment / system-reminder do NOT exist as `volatileKind`), selection-guide single-seat verification, persona compiler duplication audit (`snapshotInstructions` rendered through both `personaInstructions` AND `instructionsBlock`), 12 future-slice hit lists (Slice 0.5 + Slice 1-11) with files-to-modify/create/delete plus tests, 10 risks R1-R10, and 8 reachability spot-checks with file:line citations. Risks folded back as actionable adjustments for executor subagents: **R1** (active_scenario `volatileKind` already exists since ADR-118 Slice 4 — Slice 4 rewrites format, doesn't introduce kind), **R2** (retrieved knowledge today is developer-tail Markdown via `buildRetrievedKnowledgeContextDeveloperSection` at `turn-execution.service.ts:2023-2027`, not on volatile rail — Slice 4/9 must migrate), **R3** (presence/environment compiled stable but rendered per-turn into developer tail, not via `volatileKind` — Slice 5 migrates, doesn't invent), **R4** (OpenAI today uses `payload.instructions = systemPrompt` at `openai-provider.client.ts:197-199,979-981` instead of `developer` role inside `input[]` — Slice 2 must include this request-shape migration), **R5** (OpenAI explicitly sets `parallel_tool_calls = true` whenever tools exist at `openai-provider.client.ts:203-206,985-988` — Slice 2 needs both enabled/disabled-Skills test cases), **R6** (Anthropic currently emits ONE `cache_control` marker on the whole `systemPrompt` at `anthropic-provider.client.ts:591-604` — Slice 2 needs compiled offset metadata to split into 3 BP boundaries safely), **R7** (`skill_state_classifier` prompt template still seeded at `bootstrap-preset-data.ts:226-240` and materialized into `promptDocuments.skillStateClassifier` despite ADR-118 deleting the caller path — orphan cleanup deferred to ADR-119 closure or separate slice), **R8** (Enabled Skills prefix still renders `card.body` + `guardrails` + `examples` at `enabled-skills-prompt-materialization.ts:128-140` — Slice 3 progressive disclosure must move all three into `skill({engage})` tool response in the same deploy), **R10** (`buildOpenAIInputItems` batches all volatile messages with same wrapper at `openai-provider.client.ts:1390-1393` — provider clients must group/sort volatile messages by `volatileKind` before adding `retrieved_knowledge` / `environment` / `system-reminder` kinds). Orchestrator audit: 8/8 spot-checks verified. Gate: format:check PASS, lint PASS. No code touched; only one new docs file created.

- **HOTFIX — Anthropic provider gateway: non-streaming refused for high `max_tokens` + `maxItems` rejected in structured-output schema:** production fire reported by the founder via Telegram bot — every "Собери pdf" request returned `Запрос принят. Готовлю документ…` then never delivered; subsequent retries returned `Ассистент временно недоступен. Попробуйте ещё раз.`. Cluster log dive on `persai-dev` surfaced TWO independent SDK errors hammering `provider-gateway` every few minutes: (1) `Streaming is required for operations that may take longer than 10 minutes. See https://github.com/anthropics/anthropic-sdk-typescript#long-requests for more details` — thrown synchronously by `@anthropic-ai/sdk@0.87.0` in `Anthropic.calculateNonstreamingTimeout` (client.js:425) when `Messages.create({...max_tokens, stream:false})` projects wall-time > 10 min based on `max_tokens`; (2) `400 invalid_request_error: output_config.format.schema: For 'array' type, property 'maxItems' is not supported` — Anthropic structured-output `output_config.format.schema` does not accept `maxItems` (or `minItems`) on `array` types. Bug 1 killed PDF content generation (`runtime-document-provider-adapter.service.ts` passes up to `DEFENSIVE_OUTPUT_TOKEN_CAP = 64_000` tokens) AND the LLM document failure-framing fallback (so the user got the generic "Ассистент временно недоступен" instead of a real reason). Bug 2 killed `AutoExtractToMemoryService` background memory extraction and `SessionCompactionService` shared compaction continuously (`AUTO_EXTRACT_OUTPUT_SCHEMA.items.maxItems` + `REUSABLE_SHARED_COMPACTION_OUTPUT_SCHEMA.properties[*].maxItems`); also would have killed `generate-skill-authoring-draft.service.ts` if routed to Anthropic. **Fix A (Bug 1):** in `apps/provider-gateway/src/modules/providers/anthropic/anthropic-provider.client.ts`, `generateText()` switched from `this.client.messages.create(payload, { signal })` to `this.client.messages.stream(payload, { signal })` + `await stream.finalMessage()` — same `Message` result shape (`content`/`stop_reason`/`usage`), but the underlying connection is streaming so the SDK skips the 10-min nonstream pre-check. Caller-facing API of `generateText` is unchanged; `streamText` untouched. **Fix B (Bug 2):** added private `sanitizeAnthropicStructuredOutputSchema(value)` that recursively walks the schema and strips `maxItems` and `minItems` keys without mutating the caller's input; `toAnthropicOutputConfig` now sanitizes before sending. Tool definitions (`tools[].input_schema`) untouched — Anthropic accepts `maxItems` there. Caller-side validators in `auto-extract-to-memory.service.ts` and `shared-compaction-state.ts` still enforce caps after the model returns. **Tests:** `apps/provider-gateway/test/anthropic-provider.client.test.ts` restubbed for the new `messages.stream(...).finalMessage()` path + new cases: high `maxOutputTokens=32_000` succeeds (was throwing before); structured schema with `maxItems: 5, minItems: 1` round-trips with both fields stripped and the input schema **not mutated** (asserted); deeply-nested `maxItems` stripped at every level; empty-completion warn (`anthropic_empty_completion`) still triggers under the new code path. `apps/provider-gateway/test/anthropic-empty-completion.test.ts` extended fake client to expose both `stream` and `create` so legacy stream-path tests still pass. Gate green: recursive lint PASS (14 packages) · format:check PASS · provider-gateway typecheck PASS · provider-gateway tests PASS (exit 0; both `anthropic_empty_completion` warns triggered as expected) · api typecheck PASS · web typecheck PASS. **Cache prefix invalidation:** none — provider-gateway internal change only, system-prefix bytes unchanged. **Out-of-scope follow-up (founder owns):** `SANDBOX_RUNNING_JOB_GRACE_MS = 15000` is too aggressive for legitimate workspace-hydrate-heavy `files` jobs (founder said they'll bump in `infra/helm/values-dev.yaml`); secondary sandbox-stale-job warnings observed during the same incident at 23:08:25 and 23:09:25 against `tool=files`.

## 2026-06-16

- **ADR-118 post-deploy hotfix (skill state write-race — tool engage was silently reverted by post-turn turnRouting echo):** model successfully called `skill({action:"engage", skillId, scenarioKey:"instagram_carousel"})` and got back the engaged payload, but on the NEXT turn no `<persai_active_scenario>` developer block appeared because `assistant_chats.skill_decision_state` was back to `{status:"inactive"}`. Root cause: `TurnRoutingService` (post-Slice-6) always echoes back `request.skillStateContext.decision` as `routeDecision.skillState` (no longer derives a new decision — by design), and `AutoSkillRoutingStateService.persistFromTurnRouting` then wrote that stale echo over the **active** state that `RuntimeSkillToolService` had just persisted via `POST /api/v1/internal/runtime/skill/state` inside the same turn. Two writers, last-write-wins, the post-turn write always lost the tool's update. Fix: split the write surface into two methods with explicit roles — (1) `persistFromTurnRouting` is now strictly read-only and returns `readChatSkillState(chatId)` (the freshest DB value, which the tool may have just written); (2) new `persistDecisionState({chatId, nextState})` is the single authoritative writer, performs the row write + retrieval-cache mismatch clear in one logical step. `InternalRuntimeSkillStateService` (the `/internal/runtime/skill/state` handler) switched from `persistFromTurnRouting({turnRouting:{skillState:next}})` to `persistDecisionState({chatId, nextState:next})` for both engage and release. `auto-skill-routing-state.service.test.ts` rewritten: asserts `persistFromTurnRouting` performs zero writes even when `turnRouting.skillState` disagrees with the DB; asserts `persistDecisionState` writes + calls `clearForChatWhenSkillMismatches` with the correct `activeSkillId`; asserts a post-engage `persistFromTurnRouting` echoes back the just-written DB state (not the stale runtime echo). `internal-runtime-skill-state.controller.test.ts` unchanged (stubs the service). Gate green: lint PASS · format:check PASS · api typecheck PASS · web typecheck PASS · `auto-skill-routing-state.service.test.ts` PASS · `internal-runtime-skill-state.controller.test.ts` PASS · `send-web-chat-turn.service.test.ts` PASS (11/11) · `stream-web-chat-turn.service.test.ts` PASS (14/14). Also: 3 marketer SkillScenario rows were seeded directly into `persai-dev` DB (`instagram_carousel`, `content_plan_monthly`, `landing_audit`) for the Маркетолог Skill (`131c1531-5566-4ad2-9422-3b9b76f6d666`) with `configDirtyAt = NOW()` bumped on 2 assigned assistants so the next turn rematerializes the bundle. No new ADR — fix is a direct ADR-118 D1 invariant repair (model owns activation; runtime turn-routing snapshot is not authoritative).

- **ADR-118 post-Slice-7 production hotfix (Skill ID rendering + selection-guide expansion + `routingExamples` Slice-6 residual cleanup + Clerk middleware admin scenarios registration):** Production caught the model passing `skillId: "Диетолог"` (display name) and `skillId: "1"` (random) to `skill({action:"engage", ...})` and getting `skill_not_enabled`. Root cause: `renderSkillCard` in `enabled-skills-prompt-materialization.ts` never rendered `card.id` — the tool description told the model "Must be one of the Skill ids listed in the Enabled Skills block" but the block contained no IDs. **Fix A**: each Skill card now starts with `- Skill ID: ${card.id}` followed by `- Display name: ${card.name}` (renamed from `- Skill: ...` for clarity); the `# Enabled Skills` block intro explicitly says the `Skill ID` field is the EXACT opaque identifier to pass as `skillId`; new regression test asserts `- Skill ID: accounting` etc. **Fix B**: `## Skills` section in `bootstrap-preset-data.ts` selection guide expanded from one prose paragraph to a bullet-list with concrete trigger logic — points the model at the `# Enabled Skills` block as source of truth, forbids substituting display name / category, gives concrete `scenarioKey: "instagram_carousel"` example, and references both engage/release call signatures. The sample preview string in `apps/web/app/admin/presets/page.tsx` (`skill_cards_block`) updated to match the new card format (`- Skill ID: skl_accounting_demo`, `- Display name: Accountant`). ADR-117 golden test (`apps/runtime/test/native-tool-projection.test.ts`) updated: 4 old single-line `**Skills.**` assertions replaced with 7 new assertions verifying `## Skills` heading occurrence (exactly 1), `# Enabled Skills` cross-reference, `Skill ID — exact opaque identifier` phrase, NEVER-substitute clause, engage/release signatures, and concrete scenarioKey example. **Fix C (Slice 6 ledger-gap follow-up)**: `routingExamples?: string[]` field removed from `AssistantRuntimeEnabledSkillSummary` (runtime-bundle) — it was a derived field (`card.examples.slice(0, 2)`) populated in `materialize-assistant-published-version.service.ts` and parsed into the local `EnabledSkillSummary` type in `turn-routing.service.ts`, but never read post-Slice-6 (its only consumer was the deleted `hasSkillLexicalMatch`). Removed from the type, the materialization, the parse in `resolveEnabledSkillSummaries`, and 3 test fixture files (`turn-routing.service.test.ts` 5 occurrences, `turn-execution.service.test.ts` 2 occurrences). Cache prefix bytes change once (deliberate). Grep audit: 0 active-code matches for `routingExamples`. **Fix D (production-blocking, Slice 3 follow-up)**: the 5 new scenario routes added in Slice 3 were never registered with `ClerkAuthMiddleware` in `IdentityAccessModule.configure`, so the API received the request, no middleware populated `req.resolvedAppUser`, and `AdminSkillsController.resolveRequestUserId` threw 401 "Authenticated user context is missing.". Production caught it on the admin Skill detail page → Scenarios section. Added all 5 paths (`GET/POST /api/v1/admin/skills/:skillId/scenarios`, `GET/PATCH/DELETE /api/v1/admin/skills/:skillId/scenarios/:scenarioKey`) to `ClerkAuthMiddleware.forRoutes(...)`. `identity-access.module.test.ts` extended with 5 new `hasRoute` assertions so any future scenario route addition surfaces the guardrail. Gate green: lint PASS; format:check PASS; all 5 typechecks PASS; api test PASS (incl. new Skill ID regression); runtime test PASS (ADR-117 golden test passes with expanded assertions); web test PASS (777/777); provider-gateway test PASS.

- **ADR-118 Slice 7 landed (UX engagement indicator + selection-guide rule):** `RuntimeSkillDecisionState` extended with `activeScenarioDisplayName: string | null` (resolved per-locale at engagement time). `AssistantChatSkillDecisionState` domain type + `parseSkillDecisionState` in `prisma-assistant-chat.repository.ts` and `web-chat-turn-attempt.service.ts` updated to include the new field. `auto-skill-routing-state.service.ts` updated: `createInactiveSkillDecisionState` factory + `normalizeDecisionState` + `shouldPersistSkillDecisionState` all include `activeScenarioDisplayName`. `deriveEngagementSummary` helper added to `web-chat.types.ts`; `AssistantWebChatEngagementSummary` interface added; `AssistantWebChatTurnState` extended with optional `engagementSummary`. `send-web-chat-turn.service.ts` and `stream-web-chat-turn.service.ts` derive and include `engagementSummary` on turn completion (SSE `turn_completed` event + non-streaming path). `use-chat.ts` `ChatMessage` interface extended with `engagementSummary`; `onCompleted` handler extracts it from transport payload. `WorkingTextBlocks` component (`chat-message.tsx`) gains `engagementSummary` prop; annotation renders inline to the right of the `Выполнено ▾` toggle as `<span data-testid="engagement-annotation">` — Skill name only or `SkillName · ScenarioName` (first span `whitespace-nowrap`, second span `truncate`), subdued color `text-text-subtle/60`, no annotation when `null`. New Skills rule added to `bootstrap-preset-data.ts` `tools` selection guide (after `## Files`, before `## Deferred media honesty`) — one-time deliberate cache prefix invalidation. ADR-117 golden test (`native-tool-projection.test.ts`) updated with 4 new assertions verifying the Skills rule is present and appears exactly once. New test files: `engagement-summary.derivation.test.ts` (7 API-side cases for `deriveEngagementSummary`). New web test cases (6) added to `chat-message.test.tsx` for the engagement annotation: renders skill-only, renders skill+scenario with `·`, absent when null/undefined, annotation is in same toggle row (not in block body). Existing tests updated across 7 files to include `activeScenarioDisplayName: null` in `RuntimeSkillDecisionState`/`AssistantChatSkillDecisionState` fixtures. Gate green: lint PASS; format:check PASS; all 5 typechecks PASS; api test PASS; runtime test PASS (ADR-117 golden test passes); web test PASS (777/777); provider-gateway test PASS. Not committed, not deployed.

- **ADR-118 Slice 6 landed — full dead-code sweep (classifier / cadence / HTTP route / lexical-gate stack):** Two-phase sweep; Slice 0 ledger missed the second batch because the methods were named differently — orchestrator audit caught them and they were deleted in the same slice.
  - **Phase 1 — classifier / HTTP route / caller chain:** `SkillStateRoutingService` class and its test file deleted outright (~606 lines across 2 files). `POST /api/v1/turns/skill-routing-check` HTTP route removed with full caller chain: `TurnsController.checkSkillRouting`, `TurnExecutionService.checkSkillRouting`, `WebRuntimeTurnClientService.checkSkillRouting` + `isRuntimeSkillStateCheckResult`. `AutoSkillRoutingStateService.persistFromSkillCheckResult` deleted (dead — no production callers since Slice 1). `RuntimeSkillStateCheckResult` interface deleted from `packages/runtime-contract/src/index.ts`. `SkillStateRoutingService` removed from `turns.module.ts`. `"checkSkillRouting"` removed from `assertSupportedTurnRequest` operation union. 4 test files updated.
  - **Phase 2 — lexical-gate residuals in `turn-routing.service.ts` (ledger-gap batch):** 10 methods deleted: `resolveActiveAutoSkill`, `carryForwardAutoSkillState`, `shouldReuseActiveSkill` (carry-forward heuristic with lexical-gate and short-follow-up special cases), `buildSkillRoutingMatchText`, `hasSkillLexicalMatch`, `buildSkillRoutingTerms`, `tokenizeForSkillRouting`, `skillRoutingStems`, `createAutoSkillStateOnClassifierFailure` (cadence-era failure synthesizer), `buildTopicSummary`. Callers updated: classifier failure paths now pass `input.request.skillStateContext?.decision ?? null` directly; active-skill routing simplified to `if (activeAutoSkill)` (trusts the persisted state, no lexical gate). `buildTopicSummary` deleted — nothing writes `topicSummary` server-side; the field is kept on `RuntimeSkillDecisionState` for the Slice 2 state-passthrough path. `RuntimeSkillStateContext` simplified to `{ decision: RuntimeSkillDecisionState | null }` — dead fields `recentMessages`, `currentUserMessageIndex`, `forceCheck` removed; `RuntimeSkillRoutingRecentMessage` type deleted. `buildRuntimeContext` in `AutoSkillRoutingStateService` collapsed from an async DB-querying method (fetched up to 30 recent messages + user message count) to a synchronous one-liner `return { decision: input.decisionState }` — 2 DB queries eliminated per turn. The `selectRecentRoutingRows` private helper deleted. Callers in `send-web-chat-turn.service.ts` and `stream-web-chat-turn.service.ts` updated from `await buildRuntimeContext(...)` to synchronous call. `driftRecheckDecision` test deleted (tested `forceCheck`-triggered lexical drift detection — behavior gone by design; model now owns release via `skill` tool). 3 test files updated to remove dead context fields; turn-routing test updated with simplified skill-routing assertions.
  - **Result:** All 26 required-zero symbols verified at 0 active-code matches in `apps/` and `packages/`. Net: 2 files deleted, ~16 files modified, estimated −1 100 net lines from active source tree. Gate green: lint PASS; format:check PASS; all 5 typechecks PASS; api/runtime/provider-gateway/web test suites all PASS (web 772/772). Not committed, not deployed.

- **ADR-118 Slice 5 landed (admin UI for SkillScenario authoring):** `apps/web/app/admin/skills/page.tsx` extended with an inline "Scenarios" section co-located below the existing Skill editor. When a Skill is selected, the section fetches all scenarios via `getAdminSkillScenarios` (orval-generated client from `packages/contracts`), displays them ordered by `displayOrder` with status badges (draft / active / archived), and provides a toggle to show/hide archived ones. "Создать сценарий" opens an inline collapsible editor with all ADR-118 D3 fields: `key` (slug-validated `^[a-z][a-z0-9_]{1,63}$`, readonly after create), `displayName.ru/en`, `description.ru/en`, `iconEmoji`, `displayOrder`, `status`, `intentExamples` (repeatable list, cap 10), `recommendedTools` (checkboxes for 9 native tool keys hardcoded as `NATIVE_SCENARIO_TOOL_KEYS` — no extractable constant exists in the codebase), `exitCondition`, and a structured `steps` array editor with auto-numbered steps, per-step `directive` (required), `recommendedToolCall` dropdown, `mayBeSkippedIf`, `negativeGuards` (repeatable), up/down reorder buttons, and add/delete step buttons. Inline validation: `key` regex, at least one step, non-empty `directive`, soft yellow warning if last step's directive omits `skill({` or `release`. Per-scenario actions: Edit, Activate (draft→active), Archive (active→archived), Reactivate (archived→active). Two live preview panes updating at 300 ms debounce: Pane A "Catalog rendering" mirrors `enabled-skills-prompt-materialization.ts` format with `ru`/`en` locale toggle; Pane B "Active Scenario developer block" mirrors `BuildActiveScenarioBlockService` output format (formatting duplicated in `renderActiveScenarioBlockPreview` with a comment pointing to the source file). All API calls pass `Authorization: Bearer <token>` directly through the orval client `options`; optimistic state + refetch on success, error display on failure. New pure-helper exports: `SCENARIO_KEY_REGEX`, `NATIVE_SCENARIO_TOOL_KEYS`, `validateScenarioDraft`, `scenarioDraftToCreatePayload`, `scenarioDraftToUpdatePayload`, `scenarioToDraft`, `renderScenarioCatalogLine`, `renderActiveScenarioBlockPreview`. `apps/web/app/admin/skills/page.test.tsx` extended with 10 new test cases covering round-trip `scenarioToDraft`, `scenarioDraftToCreatePayload` payload shape, `validateScenarioDraft` regex rejection, `validateScenarioDraft` empty-steps rejection, `scenarioDraftToUpdatePayload` key omission, `scenarioDraftToUpdatePayload` archived status, `renderActiveScenarioBlockPreview` directive reflection, `renderScenarioCatalogLine` format exactness, soft warning for last step without `skill({`/`release`, and `NATIVE_SCENARIO_TOOL_KEYS` membership. Gate green: lint PASS; format:check PASS; web typecheck PASS; api typecheck PASS; runtime typecheck PASS; api test PASS (exit 0); runtime test PASS (exit 0); web test PASS (772/772 including all 10 new scenario cases). No backend changes. Not committed, not deployed.

- **ADR-118 Slice 4 landed (scenario catalog materialization + active-scenario volatile developer block):** Scenarios for each enabled Skill are now materialized into the runtime bundle and rendered in the cached system-prompt prefix (`Available scenarios:` section per Skill, up to `SCENARIO_CATALOG_RENDER_LIMIT = 8`, with `... +N more` footer for overflow). New types `RuntimeBundleSkillScenarioStep` + `RuntimeBundleSkillScenario` added to `packages/runtime-contract/src/index.ts`; `AssistantRuntimeEnabledSkillSummary` extended with `scenarios?: RuntimeBundleSkillScenario[]` in `packages/runtime-bundle/src/index.ts`. `materialize-assistant-published-version.service.ts` fetches active scenarios from Prisma, converts via locale resolution, attaches to both the prompt cards (cached prefix catalog) and the bundle `skills.enabled[i].scenarios`. New `BuildActiveScenarioBlockService` (`apps/runtime/src/modules/turns/build-active-scenario-block.service.ts`) composes the `## Active Scenario` volatile developer block per ADR-118 D4 when `skillDecisionState.activeScenarioKey !== null`; degrades gracefully (null + log) when the scenario is no longer in the bundle. Block wired into `TurnExecutionService.prepareTurnExecution` before the memory block (scenario first, memory second). Volatile wrapper widening: `ProviderGatewayTextMessage` extended with `volatileKind?: "memory" | "active_scenario"`; Anthropic client wraps `active_scenario` blocks with `<active_scenario>` tags; OpenAI client wraps with `<persai_active_scenario>` tags; `"memory"` (or missing `volatileKind`) still produces the original literal strings byte-for-byte (no real cache invalidation — see R3 note). `RuntimeSkillToolService` `executeEngageWithScenario` method swaps the Slice 2 stub: validates `scenarioKey` against `bundle.skills.enabled[i].scenarios`, returns honest `availableScenarios` in `scenario_not_found`, or calls internal API + returns full `engaged` payload with steps/tools/exitCondition. Tests: `build-active-scenario-block.service.test.ts` (8 cases), extended `runtime-skill-tool.service.test.ts` (scenario happy-path, `availableScenarios` honesty), extended `enabled-skills-prompt-materialization.test.ts` (catalog rendering, 0-scenario omit, 10-scenario overflow, `resolveEnabledSkillScenariosForBundle` locale, cache-prefix byte-stability), extended Anthropic + OpenAI provider tests (back-compat memory + new `active_scenario` wrapper assertions). Gate green: prisma generate PASS; lint PASS; format:check PASS; api/web/runtime/provider-gateway/runtime-contract typecheck all PASS; api test PASS; runtime test PASS; provider-gateway test PASS; web test PASS (762/762). One-time cache invalidation note: the memory wrapper bytes are byte-identical in the new parameterized code path (same literal strings emitted for `volatileKind: "memory"` or missing `volatileKind`), so NO actual user-facing cache invalidation occurred for existing memory blocks. Not committed, not deployed.

- **ADR-118 Slice 3 landed (`SkillScenario` entity + admin API):** new Prisma model `SkillScenario` (table `skill_scenarios`) with enum `SkillScenarioStatus` (`draft|active|archived`), FK to `skills` with `CASCADE`, unique `(skillId, key)`, index `(skillId, status, displayOrder)`. Migration `20260616140000_adr118_skill_scenario` (rollback: `DROP TABLE "skill_scenarios"; DROP TYPE "SkillScenarioStatus";`). New `skill-scenario.types.ts` with full parser/serializer logic (key regex `^[a-z][a-z0-9_]{1,63}$`, required `ru`+`en` locales, step validation). `ManageSkillScenariosService` implements `listScenarios`, `getScenario`, `createScenario`, `updateScenario`, `archiveScenario` using `WorkspaceManagementPrismaService` directly (matches existing Skills pattern; no separate domain entity/repository files — ADR-referenced `skill.entity.ts`/`skill.repository.ts` do not exist in codebase). Status transitions enforced: `draft→active`, `active→archived`, `archived→active`; other transitions throw `BadRequestException`. `archiveScenario` is idempotent. Every successful mutation calls `markAssignedAssistantsDirty(skillId)` (actual helper name in codebase; ADR called it `markAssistantsConfigDirtyForSkill`). 5 scenario routes added to `AdminSkillsController` (`GET/POST /:skillId/scenarios`, `GET/PATCH/DELETE /:skillId/scenarios/:scenarioKey`; DELETE = archive, returns 200 with archived state). `ManageSkillScenariosService` wired into `workspace-management.module.ts`. OpenAPI: added `AdminSkillScenario`, `AdminSkillScenarioStep`, `AdminSkillScenarioResponse`, `GetAdminSkillScenariosResponse`, `AdminCreateSkillScenarioRequest`, `AdminUpdateSkillScenarioRequest`, `SkillScenarioStatus` schemas + 5 paths; `contracts:generate` run to regenerate TS models. Tests: `manage-skill-scenarios.service.test.ts` (happy CRUD, lifecycle, validation, dirty-marker spy), `admin-skill-scenarios.controller.test.ts` (auth scoping, DELETE returns archived state, NotFoundException propagation). Gate green: prisma generate PASS; contracts:generate PASS; lint PASS; api typecheck PASS; web typecheck PASS; runtime typecheck PASS; provider-gateway typecheck PASS; runtime-contract typecheck PASS; api test PASS; runtime test PASS; web test PASS. `format:check` 667 pre-existing failures in `packages/contracts/src/generated/` (orval output, unchanged by this slice); 0 failures in newly-authored files. Not committed, not deployed.

- **ADR-118 Slice 2 landed (`skill` tool):** new `skill` tool allowing the model to explicitly engage or release an enabled Skill for the current chat. Tool catalog row added (`policyClass: "platform_managed"`, `capabilityGroup: "workspace_ops"`, `toolClass: "utility"`). Runtime-tool-policy extended with `skill: "inline"` execution mode and `hasNativeModelExecution` flag. `createSkillToolDefinition` in `native-tool-projection.ts` — flat discriminated-union schema by `action: "engage" | "release"` with optional `skillId` and `scenarioKey`; tool is projected only when `bundle.skills.enabled` is non-empty, and the schema is byte-stable per turn. `RuntimeSkillToolService` created with strict argument parsing (rejects unknown keys, validates `action`, verifies `skillId` against `bundle.skills.enabled`, returns `scenario_not_found` for any `scenarioKey` in Slice 2 — Slice 4 will fill in real catalog). Internal API seam: `PersaiInternalApiClientService.updateSkillState` POSTs `assistantId + channel + surfaceThreadKey + action + skillId + scenarioKey` to `POST /api/v1/internal/runtime/skill/state` (internal port 3002, guarded by `PERSAI_INTERNAL_API_TOKEN`). `InternalRuntimeSkillStateService` resolves `chatId` from `AssistantChatRepository.findChatBySurfaceThread`, reads current `skillDecisionState`, applies engage/release transition via `AutoSkillRoutingStateService.persistFromTurnRouting`, and fetches Skill display name from Prisma. `InternalRuntimeSkillStateController` registered in `workspace-management.module.ts`. Tool wired into `TurnExecutionService` `SKILL_TOOL_CODE` case and `turns.module.ts` providers/exports. Tests: `runtime-skill-tool.service.test.ts` (9 cases: happy engage, happy release, skill_not_enabled, scenario_not_found, invalid_arguments ×3, API-error ×1, release-with-extra-args), `internal-runtime-skill-state.controller.test.ts` (7 cases: engage OK, release OK, idempotent re-engage, NotFoundException propagation, bad-auth, missing assistantId, bad action), native-tool-projection test extended (projected with enabled Skills / absent without), seed-tool-catalog assertion added. Gate green: lint, format:check, all 5 typechecks, API test suite, runtime test suite. Not committed, not deployed (deploy together with Slice 1).
- **ADR-118 Slice 1 landed (decision state shape + cadence persistence trim):** `RuntimeSkillDecisionState` reshaped to `{status, activeSkillId, activeSkillName, activeScenarioKey, topicSummary}` in `packages/runtime-contract/src/index.ts`; `confidence` and `checkedAtMessageIndex` removed end-to-end (production code: 0 matches; remaining matches are docs + immutable migration history only). R1 inline re-declarations updated to the new shape with `activeScenarioKey` in all three sites (`apps/api/.../assistant-runtime.facade.ts`, `apps/web/app/app/_components/use-chat.ts`, `apps/api/.../web-chat.types.ts`). R7 atomic writers fix: `assistant_chats.skill_cadence_state` column dropped via new Prisma migration `20260616120000_adr118_drop_skill_cadence_state` (rollback comment included), and the two services that previously wrote `skillCadenceState` atomically with the decision row (`manage-admin-skills.service.ts`, `manage-assistant-skills.service.ts`) drop those writes in the same commit — Prisma would have failed with `Unknown field` otherwise. `AutoSkillRoutingStateService` trimmed: cadence helpers deleted (`shouldRunBackgroundCheck`, `runBackgroundCheck`, `markBackgroundCheckQueued`, `markBackgroundCheckFailed`, `createBackgroundCheckContext`, `createNewChatSkillCadenceState`, `createEnabledSkillBootstrapCadenceState`, `createMigrationRepairSkillCadenceState`); decision-row persistence helpers kept for Slice 2 `skill` tool reuse. Cadence constants (`DEFAULT_SKILL_ROUTING_*`) deleted from `platform-runtime-provider-settings.ts`; `routerPolicy.skillRoutingPolicy` removed from OpenAPI (`packages/contracts/openapi.yaml`) + admin runtime UI (`apps/web/app/admin/runtime/page.tsx`) + generated contract models. `SkillStateRoutingService` kept as a file (re-used by Slice 2 single-shot fallback path stays a future option), but its call from `TurnExecutionService` is removed so the classifier is inert until Slice 2 ships the new `skill` tool. Tests updated across 12 suites; 29 files modified plus 6 generated models deleted. Activation is intentionally inert post-Slice-1 — Slice 2 (the `skill` tool itself) must land in the same deploy or model loses Skill engagement until then. Gate green (format + lint + 4-package typecheck + api/runtime test suites; one pre-existing web `use-chat.test.tsx` resume-polling flake that passes in isolation, unrelated to Slice 1 — diff in `use-chat.ts` is shape-only). Not deployed, not committed beyond this commit (deploy waits for Slice 2).

## 2026-06-15

- **HOTFIX — `image_edit` deferred jobs failed 100% on `persai-dev` (ADR-117 cleanup regression from `4a0baa39`):** every deferred `image_edit` (worker path) failed with `invalid_arguments: Unexpected arguments: referenceImageAlias`, then burned ~7.5 min of exponential-backoff retry (30/60/120/240/480s) before surfacing failure to model/user — observed live as 3 consecutive failed turns (2× with 2 input images + 1× with 1 input image) inside one Marketer-themed chat. Root cause: commit `4a0baa39` removed legacy singular `referenceImageAlias` from the parser whitelist (input) but left it as a derived field on the parsed `RuntimeImageEditRequest` (output) and in the contract — the deferred worker rehydrates the persisted parsed request through the same parser, which now rejects its own previous output. End-to-end single-path cleanup landed: removed `referenceImageAlias` / `referenceFilename` / `referenceImage` (singular) from parser output (`apps/runtime/.../runtime-image-edit-tool.service.ts` — `ResolvedImageEditSelection`, parser output, worker selection shape, all 13 tool-result composition sites, plus the rehydrate fallback `(request.referenceImageAlias === null ? [] : [request.referenceImageAlias])` that bridged legacy single → plural), from `RuntimeImageEditRequest` / `RuntimeImageEditToolResult` / `ProviderGatewayImageEditRequest` (`packages/runtime-contract/src/index.ts` — now plural-only `string[] | null` required), from the OpenAI image-edit client + the image-generation service normalize path (`apps/provider-gateway/.../openai-provider.client.ts`, `provider-image-generation.service.ts` — both used the singular as a fallback for the plural array). Adjacent hardening: `invalid_arguments` for image jobs now non-retryable (new `NON_RETRYABLE_IMAGE_REASONS` set in `runtime-media-job-run.service.ts` mirroring the existing video pattern, plus six other model-correctable image reasons) — surfaces as terminal `BadRequestException` in seconds, no more 7.5-min retry theatre for any deterministic image failure. Regression guards added: `runtime-media-request-parsing.test.ts` now has a `persisted RuntimeImageEditRequest passes worker rehydrate parse` round-trip test (parse model args → simulate enqueue → re-parse rehydrated shape) that would have caught this in CI; `runtime-media-job-run.service.test.ts` has a `treats invalid_arguments image jobs as non-retryable bad requests` test asserting `BadRequestException` with the reason + message preserved; `turn-execution.service.test.ts` now contains explicit absent-key assertions (`referenceImageAlias === undefined`, `referenceFilename === undefined`) so a future reintroduction can't slip past. All affected tests updated to the plural-only shape (8 test files total). Gate green: format + lint + 4-package typecheck + runtime/provider-gateway/api affected test suites all pass. **Out of scope (separate fixes):** `messageToUser` honesty gap (`"Accepted... being prepared"` emitted at enqueue, before the worker actually accepts the job — model can't tell from the tool result alone that a downstream worker failure happened); separate Prisma `runtimeSession.upsert ... value too long for the column's type` regression visible in the same log window.
- **ADR-118 Slice 0 landed (engagement inventory and reachability ledger):** read-only subagent produced `docs/ADR/118-skill-engagement-inventory.md` — 37-row ledger (vs ADR-118's ~10 expected, +27 additional callsites uncovered), 35/35 delete verdicts with caller reachability proven by file:line, 0 unproven. Sections 1-7 complete: heuristics inventory + keep verdicts + proof that `Enabled Skills` block already renders independent of `skillDecisionState` (filter is `assignmentStatus === "active"`, ADR-118 activation effect is only retrieval-priority + cache + scenario block, not instruction visibility) + volatile-context end-to-end trace (Anthropic `<recent_short_memory>` + OpenAI `<persai_contextual_memory>` wrappers, **memory-specific today**) + Slice 6 hit list + 10 risks R1-R10. Orchestrator audit: 4 spot-checks against real code all matched ledger claims. 4 risks folded back as actionable ADR-118 adjustments: **R1** (inline re-declarations of decision-state shape in `assistant-runtime.facade.ts` + `use-chat.ts` — Slice 1), **R3** (memory-specific volatile-context wrappers need parameterization with `volatileKind` for `active_scenario`, one additional one-time cache invalidation for the memory wrapper bytes — Slice 4), **R7** (two `skillCadenceState` writers in `manage-admin-skills.service.ts` and `manage-assistant-skills.service.ts` must be dropped in the same commit as the column drop or Prisma fails — Slice 1), **R9** (Slice 6 hit list extended with `POST /api/v1/turns/skill-routing-check` route, `TurnExecutionService.checkSkillRouting`, `WebRuntimeTurnClientService.checkSkillRouting`, downstream `*-web-chat-turn.service.ts` callers). Gate: `lint` PASS, `format:check` PASS. No code touched; only one new docs file created. Not committed.
- **ADR-118 opened (Skill scenarios + model-owned activation):** new open program ADR `docs/ADR/118-skill-scenarios-and-model-owned-activation.md` running in parallel to ADR-117 (does not block, does not supersede). Replaces the hidden Skill activation stack (`SkillStateRoutingService` background classifier + `matchesSkillLexically` foreground substring-gate + cadence intervals `initialCheck=3` / `recheck=5`) with **model-owned activation via a single `skill({ engage | release })` tool**, and adds a **new first-class product concept** — admin-authored `SkillScenario` — so Skills can carry reusable structured workflows like "Instagram-карусель: 8 slides via image_generate series". Three-level engagement (Enabled → Active → Running scenario), single discriminated-union tool, scenario steps injected via existing `cacheRole: "volatile_context"` pattern (cached system prefix stays byte-stable across engage/release), and quiet UX indicator as inline annotation **in the `:::working` block header row, to the right of the `Выполнено ▾` toggle** (Skill name + optional `· Scenario`; nothing when no active Skill; not a line inside the block body). Dead-code sweep mandatory (Slice 6 deletes the entire cadence/classifier/lexical-gate stack — `SkillStateRoutingService`, `tryForegroundActivation`, `runBackgroundCheck`, cadence constants, `skillCadenceState` column, `routerPolicy.skillRoutingPolicy` admin field). Adherence to scenario steps is judgment-based (structured developer block with numbered imperatives + recommended-tool hints + negative guards); JSON-schema args constraints and post-call validation explicitly rejected on cache-discipline + simplicity grounds. Inline single-shot fallback classifier explicitly deferred ("пока без"). 9 slices for orchestrator-driven execution by subagents (Slices 1 and 2 must land in the same deploy). One additive line will be contributed to the ADR-117 `tools` selection guide in Slice 7 (ADR-117 is in closure mode — this contribution lands as a post-closure additive rule against the same canonical guide). Authored only; no Skill-engagement code changed yet.
- **HOTFIX — `@persai/runtime-contract` startup crash (ADR-117 Slice 3 regression):** the prior push (`260837c2`) crash-looped the new `api`/`runtime` rollout pods with `ERR_MODULE_NOT_FOUND` resolving `./media-prompt-fragments` imported from `runtime-contract/src/index.ts`. Root cause: the contract package is consumed as **un-built TypeScript source** (`main` → `src/index.ts`, no build step), and Slice 3's `export … from "./media-prompt-fragments"` was the first relative import — Node 22's type-stripping ESM loader cannot resolve an extensionless relative specifier, `.ts` extension fails the emit typecheck (TS5097), and no `.js` sibling exists. Fix: inlined the provider-conditioning fragments (`ANTI_COLLAGE_RULE`, `STANDALONE_*`, `referenceGuidanceRule`, `seriesItemHeaderLine`) directly into `packages/runtime-contract/src/index.ts` and deleted the sibling file, restoring the single self-contained module. No consumer imports changed (all import from `@persai/runtime-contract`); the ADR-117 golden single-source test now anchors on `index.ts`; `API-BOUNDARY.md` / `ARCHITECTURE.md` / `TEST-PLAN.md` / ADR-117 closure updated. Provider output and model-facing text unchanged. Gate green: runtime-contract typecheck + **api emit build** + runtime/gateway typecheck + lint + golden single-source/projection tests.
- **ADR-117 Slice 5 landed (golden single-source test + docs + ADR closure):** strengthened `apps/runtime/test/native-tool-projection.test.ts` into the ADR-117 invariant lock. The golden test now reads the real production sources from disk and fails if the collage/contact-sheet/diptych rule is re-inlined outside `packages/runtime-contract/src/media-prompt-fragments.ts`, if runtime/provider media paths stop referencing the shared fragments, if `tool-catalog-data.ts` reintroduces `action="deferred"` or cross-tool comparison prose, or if `bootstrap-preset-data.ts` loses the Native Tool Runtime selection guide / reintroduces an `agents` Tasks Policy. `docs/API-BOUNDARY.md`, `docs/ARCHITECTURE.md`, and `docs/TEST-PLAN.md` now record the shipped D1 precedence rule and the three-concern seam. ADR-117 now has a closure section with the final owner table, reachability-proof citations back to the inventory ledger Section 4, the intentionally kept document-tool `action !== "deferred"` guard, and `cache-prefix rollout SHA: PENDING` until materialization rollout + deploy happen. No runtime behavior changed. Not deployed, not committed.
- **ADR-117 Slice 4 landed (dead-code & drift sweep):** removed the proven-dead legacy TOOLS-markdown path — `buildRuntimeToolPoliciesMarkdown` (`runtime-tool-policy.ts`), `buildPromptToolMarkdownEntry` + its now-orphaned helper `joinPromptToolInstruction` (`prompt-constructor-tool-metadata.ts`), and the `generateToolsPrompt` `else` fallback (`compile-prompt-constructor.service.ts`). The DB `tools` template is always seeded, so the fallback was unreachable in every seeded env; the cold-migration-only missing-template case now emits an empty tools block + a single `warn` log (no markdown re-derivation, no stub). Removed the four dead ghost-verb sanitizers (`containsLegacyScheduledActionGuidance`/`resolveSanitizedScheduledActionDescription`, `containsGhostBackgroundTaskAction`/`resolveSanitizedBackgroundTaskDescription`) from `native-tool-projection.ts` — re-confirmed zero live matches for the legacy tokens; `createScheduledActionToolDefinition`/`createBackgroundTaskToolDefinition` now call `resolveToolDefinitionDescription` directly. Reconciled the hardcoded `files` description in `runtime-tool-policy.ts` to include the real `preview` action. Kept the document-tool `action !== "deferred"` backward-compat guard (`turn-execution.service.ts`). Tests updated (`compile-prompt-constructor.service.test.ts` asserts the warned empty-block path; `runtime-tool-policy.test.ts` drops the markdown builder + asserts `preview`; `native-tool-projection.test.ts` fixture uses raw live descriptions). Gate green (lint/format/api+web+runtime+gateway typecheck + affected tests). Not deployed, not committed.
- **ADR-117 Slice 3 landed (provider-conditioning constants — single source):** new shared module `packages/runtime-contract/src/media-prompt-fragments.ts` is now the one definition of the provider-facing media rules — `ANTI_COLLAGE_RULE` (Rule A, complete variant naming diptych/triptych), `STANDALONE_IMAGE_RULE` + `STANDALONE_GENERATED_IMAGE_RULE`/`STANDALONE_EDITED_IMAGE_RULE` (Rule B), `referenceGuidanceRule({ multiple })` (Rule C single/multi), and `seriesItemHeaderLine(index,total)` (Rule D). Both runtime composers (`runtime-image-generate-tool.service.ts`, `runtime-image-edit-tool.service.ts`) and the gateway builder (`openai-provider.client.ts` `generateImage`/`buildImageEditPrompt`) import these instead of re-stating their own wording (the contract package is the only shared boundary across the separate `@persai/runtime` and `@persai/provider-gateway` packages). Provider output is semantically unchanged (wording unified to the most complete variant; series header byte-identical). **Model-facing cleanup:** the pure provider-hygiene collage/grid/multi-panel clause was removed from the `image_generate`/`image_edit` tool descriptions in `native-tool-projection.ts`; the count/series intent the model needs (`count=N` → N images, `outputMode='series'`) and the `referenceImageAliases` "rooted in source / references only guide" parameter-choice guidance were kept. Added a small single-source sanity test (down-payment on Slice 5's golden test). Provider-conditioning is intentionally NOT admin-editable (TS-only). Gate green (lint/format/all-package typecheck + provider-gateway suite + runtime projection/sanity). The model-facing tool-descriptor trim changes the per-tool definition surface (folds into the same one-time prefix/tool-surface refresh already required by Slices 1/2 on rollout); the provider-prompt centralization is provider-only. Not deployed, not committed.
- **ADR-117 Slice 2 landed (catalog consolidation + agents reduction):** stripped all cross-tool selection sentences from catalog `modelUsageGuidance` (S4/S5/S7/S9–S11/S13–S16) — now owned solely by the selection guide. Fixed the `action="deferred"` → `action="pending_delivery"` factual drift in all three media entries (R1). Fixed `referenceImageAlias` → `referenceImageAliases` (plural multi-reference API) in `image_edit.modelUsageGuidance` (R5/A4). Annotated `files.modelUsageGuidance` as policy-overridden (A5). Reduced `agents` template from "Memory and Task Governance" + Tasks Policy to "Memory Policy" only (D5); Tasks selection lives solely in the `tools` guide. Updated `createScheduledActionToolDefinition` fallback in `native-tool-projection.ts` (removed "Use background_task" cross-tool reference). Admin presets `agents` label → "Memory Policy". Tests updated (seed-tool-catalog + native-tool-projection). Changes seeded `agents` default → second deliberate one-time prompt-cache prefix invalidation on rollout. Gate green. Not deployed, not committed.
- **ADR-117 Slice 1 landed (Native Tool Runtime selection guide):** the DB `tools` system-prompt block was promoted from 3-line boilerplate to a compact (~36-line) cross-tool **selection guide** — the single canonical place that tells the model WHICH tool to use and WHEN (images vs vision-only describe, knowledge/web local-first, document vs inline, memory/tasks, files alias-first + real-delivery honesty, global "call don't narrate", and deferred-media honesty stated against the real `action="pending_delivery"` token). Admin presets UI relabels the block as the selection guide and drops the stale `tools_catalog_block` variable chip (`apps/web/.../presets/page.tsx` + test). **Additive-first:** `tool-catalog-data.ts` and the `agents` template were NOT edited (duplicate removal + Tasks-Policy move happen in Slice 2), so net model guidance only increased. Changes the cached system prefix → one-time prompt-cache invalidation on rollout. Gate green (lint/format/api+web+runtime typecheck/affected tests). Not deployed, not committed.
- **ADR-117 opened (tool-instruction source-of-truth, Мир 2):** new open program ADR `docs/ADR/117-...` to consolidate model-facing tool instructions into three single-owner concerns — selection guide (DB `tools` block = "Native Tool Runtime"), per-tool descriptor (catalog→policy→projection), and a provider-conditioning constants module — plus reconcile instructions against real code (e.g. fix `action="deferred"`→`pending_delivery` drift) and remove dead paths. Authored only; no Мир-2 code changed yet. Executed in 6 slices by subagents.
- **image_edit multi-reference inputs (up to 16 total):** `image_edit` now accepts multiple reference images via a new sticky-alias array `referenceImageAliases` (the legacy single `referenceImageAlias` still works and is merged in). The source image plus up to `MAX_RUNTIME_IMAGE_EDIT_REFERENCE_IMAGES` (15) references are sent to OpenAI `images.edit` as one ordered `image[]` set (16-input ceiling per gpt-image-1). Wired end-to-end: contract (`RuntimeImageEditRequest.referenceImageAliases`, `ProviderGatewayImageEditRequest.referenceImages`, `MAX_RUNTIME_IMAGE_EDIT_INPUT_IMAGES`/`..._REFERENCE_IMAGES`), tool projection schema/descriptions, runtime parser (dedupe + drop source-collision + cap), `resolveImageSelection` (loads N references), provider-gateway normalize, and OpenAI client `image=[source, ...references]` + plural prompt wording. Single-reference prompt wording preserved verbatim for backward compatibility.
- **Web stream silent-cut fix (cadence watchdog):** once any native tool starts in a stream span, the `slow_avg` cadence signal is now disabled for the rest of that span (`apps/api/.../cadence-watchdog.ts`). Root cause from `persai-dev` logs (`alex@agse.ru`, turn `c8c44383`): on media turns the model streams a short post-tool wrap-up answer slower than the 200 ms/token threshold, `slow_avg` fired and aborted the runtime fetch with **no error**; because tool side-effects make the turn unsafe to retry, the reply was left truncated and the user had to re-ask. Mirrors the silent-timer philosophy already documented for `recordToolFinished` (slow post-tool answers are the job of provider/runtime stream timeouts). Pure-text-turn `slow_avg` behavior is unchanged.
- **Media `prompt` optional in series mode (image_edit + image_generate):** when `outputMode="series"` carries non-empty `seriesItems`, a missing top-level `prompt` is no longer rejected as `invalid_arguments` (which silently skipped the whole media request and produced no job). The parser now synthesizes an overall prompt; non-series calls still require `prompt`. Same `persai-dev` incident: the carousel `image_edit` call shipped 4 `seriesItems` and no `prompt`, so the job was never enqueued. Added `requestId`-tagged `skipped` warn logs to the previously silent `invalid_arguments` / selection branches for future diagnosis.

## 2026-06-14

- **Current-message attachment ordinals:** multi-attach composer shows ephemeral 1/2/3 watermarks on pending thumbnails; runtime labels direct-input image/PDF blocks as `Current message attachment N of M` (separate from Working Files `image #N` aliases).
- **ADR-116 closed:** runtime file re-view program complete (116.0–116.3). `files.inspect` / `files.read` / `files.preview` with plan-owned preview limits, ephemeral provider injection, and unified hydration limits; live acceptance PASS on `persai-dev` (`ff9e4cbb`).
- **ADR-116 Slice 116.2 (preview + injection):** `files.preview` for `image/*` and native PDF with plan-owned byte/edge limits; tool JSON ack only, ephemeral multimodal via `toolFollowUpUserContent` after tool history; `turn-context-hydration` uses bundle effective preview limits for current-turn attachments.
- **ADR-116 Slice 116.0 (file re-view foundation):** added `files.inspect` / contract `files.preview`, plan-owned `maxFilePreviewBytes` + `maxFilePreviewEdgePx` on `files` tool activation (Prisma + Admin Plans UI + materialized `RuntimeToolPolicy`), `@persai/config` preview defaults/ceiling helpers, and `files.inspect`/`get` metadata responses with `capabilities` + effective preview limits.
- **ADR-116 Slice 116.1 (read hardening):** `files.read` on PDF/DOCX now surfaces `charCount`, `extractionQuality`, `readNote`, `extractionCached`; sanitizer sets `truncated` + `charCount` when clipping to 16k; internal extract API returns `cached: true` on metadata cache hits.
- **Doc hygiene — program closure:** retired stale “active program” references to ADR-078 / ADR-102 across `AGENTS.md`, `ARCHITECTURE.md`, `ROADMAP.md`, `DATA-MODEL.md`, `SESSION-HANDOFF.md`, and cursor rules. All orchestration programs through ADR-115 are closed archive; new work requires explicit user priority + new ADR.
- **Media job completion framing:** plan-level `mediaCompletionVisionEnabled` toggles vision review on the existing `/media-jobs/complete` LLM call for `image_generate` / `image_edit` (up to 10 hydrated images, required non-empty reply). Cheap plans stay text-only but prompts now require a real delivery reply instead of optional silence + hardcoded «Медиафайл отправлен.» / `Media sent.`
- **Media completion follow-up:** vision input limited to job output images only; completion max output tokens raised to 1000; `seriesItems` must be unique per frame in image tool projection.
- **ADR-115 closed:** inbound safety program complete (115.0–115.7 + follow-through). Outbound moderation explicitly not planned.
- ADR-115 polish: sidebar assistant-card safety icons (warn amber / restrict red) with modal + `userSafety` bootstrap; warn copy reframed to prior messages in chat/thread; TG warn delivery idempotent per `triggerKey`.
- ADR-044 abuse cleanup: removed obsolete quota-pressure coupling from distributed abuse enforcement (`quotaDecision` input, unused quota deps in `EnforceAbuseRateLimitService`); repository now clears legacy `quota_pressure_*` block rows on next attempt. Quota limits stay in `EnforceAssistantCapabilityAndQuotaService` / ADR-087 only. removed obsolete quota-pressure coupling from distributed abuse enforcement (`quotaDecision` input, unused quota deps in `EnforceAbuseRateLimitService`); repository now clears legacy `quota_pressure_*` block rows on next attempt. Quota limits stay in `EnforceAssistantCapabilityAndQuotaService` / ADR-087 only.
- ADR-115 slice 115.5: `safety_user_restricted` admin_system notification on auto-moderation `block_user` and admin manual restrict; audit→admin_system mapping with user-email enrichment for all user-scoped events (no UUID fallback label).
- ADR-115 admin safety auth fix: registered `/api/v1/admin/safety-policy/*` and `/api/v1/admin/safety-controls/*` in API `ClerkAuthMiddleware` (fixes live `401 auth_required` / “Session expired” on Runtime Inbound Safety and Ops safety unblock/restrict). Reverted incorrect web `buildAdminFetchOptions` BFF workaround; admin UI uses standard Bearer + `getAdminSessionToken` again.
- ADR-115 slice 115.7: user-visible safety warn (system thread notice + localized web card), pack-aware moderation thresholds, rolling warn strikes per `reasonCode`, repeat offense blocks at inbound; OpenAPI `platformNotice` on chat messages.
- ADR-115 follow-up BFF auth (superseded): web `buildAdminFetchOptions` attempt did not fix live safety admin — root cause was missing API middleware route registration.
- ADR-115 follow-up UX + ops fix: step-up challenge now accepts `admin.safety_user.restrict` (fixes Ops manual safety restrict 400), web chat shows localized `safety_restricted` banner with support CTA, ops user directory highlights safety-restricted rows in warning tone, inbound safety admin panel polish + `getAdminSessionToken`, and GKE live-moderation smoke scripts under `apps/api/scripts/`.
- ADR-115 Slices 3–4 and 6 landed on `main` at `e797a172` (full repo gate: lint, typecheck, test, test:step2, build).
- ADR-115 Slice 0 (inbound safety gate skeleton): added `user_restrictions` and `moderation_cases` Prisma models + migration, `EnforceInboundSafetyGateService` read-only gate, and canonical inbound reorder to `safety -> abuse -> quota` in web prepare and Telegram inbound paths. Active safety restrictions deny with `403 safety_restricted`; empty table preserves prior behavior aside from the intentional abuse-before-quota reorder.
- ADR-115 Slice 1 (contour-1 heuristics): added `safety_heuristic_rules`, `safety_policy_settings`, and `safety_moderation_review_jobs`; `EvaluateInboundSafetyPrecheckService` with seeded RU/EN packs; async contour-2 enqueue on defer/block; admin safety-policy API at `/api/v1/admin/safety-policy/*`. Inbound order is now `safety -> abuse -> contour-1 -> quota -> runtime`.
- ADR-115 Slice 2 (contour-2 async moderation): added `ProcessSafetyModerationReviewService` + scheduler worker, OpenAI Moderation API client, `moderation_cases` writes, and auto `user_restrictions` upsert on `block_user`. Config keys `SAFETY_MODERATION_*` live in `packages/config` api config.
- ADR-115 Slice 3 (inbound safety deny): added `hold_and_defer_contour_2_sync` route, sync moderation hold before runtime for high-risk packs, unified `safety_restricted` inbound error + optional system thread notice stub, and web client mapping distinct from `rate_limited`.
- ADR-115 Slice 4 (ops safety controls): added `/api/v1/admin/safety-controls/*` (list restrictions/cases, unblock, manual restrict with step-up), ops cockpit `safetyRestriction` summary + user-directory `safetyStatus`, and `/admin/ops` safety panel.
- ADR-115 Slice 6 (runtime policy UI): added OpenAPI/contracts for `/api/v1/admin/safety-policy/*` and `/admin/runtime` **Inbound Safety** section (heuristic packs + routing knobs).

## 2026-06-13

- CI test fix + final UI polish follow-up: investigated the failed post-push GitHub Actions `CI` run and traced it to two brittle `ImageLightbox` native-inline expectations that depended on jsdom image/canvas behavior rather than the intended product contract. The lightbox tests now explicitly simulate a loaded image + canvas extraction path before asserting inline native transfer, which restores deterministic full-suite CI coverage without changing runtime behavior. The same commit also includes the remaining founder-requested web polish still pending in the tree: tighter responsive integrations cards and calmer shared pill-button shadows.
- Legacy `mediaClasses` cleanup: removed the dead plan-entitlement `mediaClasses` dimension from the active API/domain/persistence path and dropped `plan_catalog_entitlements.media_classes` via Prisma migration. Effective capability/routing/enforcement truth now derives from `toolClasses`, `channelsAndSurfaces`, tool activations, and quota state instead of a stale parallel media-class layer. Admin plan parsing/state, plan catalog persistence, and model-visible assistant knowledge no longer round-trip or expose `mediaClasses`, which removes the false "image/audio/video/file disabled" signal the model previously saw even when media tools were active.

## 2026-06-12

- Mobile bridge binding fix + APK refresh: `ImageLightbox` native save/share now invokes `window.PersaiNative` methods with the native bridge object as the call context instead of detaching the function first, which keeps Android `addJavascriptInterface` calls on the real native object and stops the WebView from falling through to the dead `blob:` browser path. Added focused regression coverage for the binding contract, rebuilt/exported the Android APK again, and re-ran the full AGENTS verification gate plus full monorepo tests before push.
- Media auth path + APK refresh: the same-origin `assistant-file` BFF now accepts a fresh `X-PersAI-Session-Token` like the other protected web BFF routes, chat inline image previews now use the authenticated blob loader instead of a raw protected `<img>` URL, lightbox transfer fetches/native payloads now carry the same fresh session token, Android/iOS native media transfer forwards that header alongside cookies, and the Android APK artifact was rebuilt/exported again. This closes the auth seam that reproduced in Chrome/Capacitor but not in Cursor browser and also stabilizes native save/share against `/api/assistant-file/...`.
- HeyGen voice curation follow-up: the Admin Tools `Model` checkbox now implies `approved + enabled` instead of silently saving a contradictory state that disappears after refresh, and the private-imported HeyGen catalog path now enriches missing preview URLs from the legacy `/v2/voices` response when `/v3/voices?type=private` omits them.
- Post-interruption UI/API fixpack: Admin HeyGen `Edit voices` curation routes now pass through Clerk auth middleware and surface load failures honestly, legacy raw HeyGen voice cache rows can still populate the curation table, home recent chats show smart/project icons plus a soft active-work dot and preview text while the redundant Telegram quick action is gone, the expanded sidebar account footer has safer top spacing, assistant-file downloads prefer canonical file MIME over storage `octet-stream`, chat history falls through to canonical `assistant_files.metadata.mediaDerivatives` when attachment metadata is stale so thumbnails/posters do not unnecessarily fall back to full-size media, and the Android APK artifact was rebuilt from the fixed `persai-mobile` shell.
- Full repo founder verification fixpack: the current wide working tree was brought back to a green AGENTS gate before commit/push. Web chat soft-detach/reattach continuity now carries a distinct `terminal_status` path so the new stream reconciliation stays type-safe, and stale API HeyGen test doubles were updated to the approved-catalog contract (`getApprovedVoiceCatalogEntries`) so the full monorepo test suite passes again on the combined founder web/API/mobile tree.
- Admin HeyGen voice approval landed for talking-avatar voices: Admin Tools now has an `Edit voices` table beside `Refresh voices` where operators can search/filter all cached public/private HeyGen voices, preview audio, approve/enable voices, correct `RU` / `EN` / `Other` / `Multi` language and gender, and separately mark approved voices as model-selectable. Manual curation is stored in `platform_heygen_voice_curation` and is not overwritten by provider refresh; user-facing Characters catalogs now expose only approved + enabled voices, while model-facing talking-avatar shortlists additionally require the model checkbox.
- Fixpack: completed chat working notes now keep their `:::working` structure across final/history/status/reattach reconciliation, remove duplicated working text from the final answer, and render as a quiet collapsed `Done` / `Выполнено` disclosure after completion while streaming notes use a single left-rail style. Admin Tools now reads HeyGen voice catalog metadata from the active `heygen-voices-avatar-v` cache key, and the HeyGen refresh path now includes imported `type=private` voices, classifies them as ElevenLabs-quality entries, and projects unknown-language private imports into RU/EN via multilingual handling so imported voices can appear in the Characters picker/model shortlist.
- Chat media derivatives landed as additive file/runtime truth: canonical chat media now stays on full/master `fileRef`, while image `thumbnail` and video `poster` refs can be generated asynchronously as hidden non-billable `assistant_files` system variants tracked in canonical file metadata and mirrored into attachment payloads. Web chat bubbles now prefer those lightweight refs when present but keep automatic legacy fallback to `fileRef`, and ordinary multimodal runtime image inputs now use an explicit transient resize path (about `2048px` max edge only when needed) while `image_edit` and media-tool reference images still use full/master bytes.
- Chat media gallery polish: image attachments that arrive as a small same-message set now open as one lightbox gallery. Mobile can swipe horizontally between images while keeping vertical swipe-to-close, and desktop gets quiet previous/next controls plus keyboard arrows. Video lightbox behavior is unchanged.
- HeyGen voice catalog quality hardening: the platform HeyGen voice cache now enriches each voice with derived source (`elevenlabs` / `heygen` / `gemini`), quality tags (`professional` / `natural` / `lifelike`), preview availability, locale/pause support, and a server-side quality rank. The model-facing 24-voice shortlist now prefers RU/EN ElevenLabs-backed voices, then high-quality HeyGen voices, then safe fallbacks instead of alphabetical filler. The Characters voice picker keeps the same RU/EN/OTHER/MY + gender filters, but sorts each bucket by quality, labels ElevenLabs/Pro/Natural/Lifelike voices inline, and does not offer broken static Gemini previews as playable.
- Chat working-text stream cleanup: assistant live "working" notes no longer rely on text regex heuristics to split progress markers from the final reply. Web chat now wraps already-streamed pre-tool prefixes into persisted `:::working` markdown blocks when the turn enters another tool/retrieval/project pass, renders those blocks as quieter working notes above the live/final answer, keeps the inline status beside the cursor alive even after the first visible answer text arrives, and removes the old progress-line normalization hack. Runtime project guidance was updated accordingly so visible working notes stay short/natural instead of newline/bullet-formatted for UI parsing.
- Founder setup polish tail: the recreate/setup flow is quieter and more human. The upload-avatar card no longer shows an extra bottom banner, the temporary recreate/recover explanation banner was removed from the user path, the `Shape the personality` / `Настрой характер` surfaces now use the same calmer card/input language as assistant settings in both themes, and recreate-confirmation copy now describes the reset in plain language instead of exposing programmer terms like `runtime bundles/specs` or `media artifacts`.
- Characters / HeyGen persona identity follow-up: workspace video personas now persist a fixed `videoFormat` (`16:9` / `9:16` / `1:1`) as part of avatar identity instead of treating talking-avatar aspect ratio as request-only truth. Persona creation now center-crops/normalizes the uploaded portrait to that stored format before HeyGen avatar creation, the Characters create modal exposes `Auto / 9:16 / 1:1 / 16:9` with auto-detect from source image dimensions, edit keeps avatar immutable and shows the stored format read-only, public/internal persona APIs now carry `videoFormat`, and runtime talking-avatar generation now resolves aspect ratio in the order `explicit request -> stored persona videoFormat -> provider/admin default`.

## 2026-06-11

- Founder UI polish tail follow-up: several last-mile web settings surfaces were normalized after live review. `Payment settings` and `Clone voice` now sit on the same compact pill baseline as the rest of the CTA system; the shared pill-button shadow was reduced to a calmer half-strength treatment; Files rows became truly one-line and compact by dropping the redundant origin/type subline for ordinary files while keeping only meaningful document-output chips (`Current`, `vN`); the Characters voice language selector now matches the Gender segmented control height/style; and the Memory drawer was tightened so tabs, search/add controls, row spacing, expand actions, and right-edge delete affordances read as one dense premium list instead of mixed ad hoc controls.
- Founder UI polish final follow-up: payment settings in assistant limits now stays quiet by default but escalates to a primary CTA only when billing truth indicates real attention/action (`grace/past_due`, auto-renew restore/off, scheduled change, recurring migration trouble, provider-managed recovery, or an explicit warning). The Knowledge base list no longer keeps reloading on incidental rerenders, gets a separate round manual status-refresh button beside `Upload documents`, and swaps the per-row reindex affordance away from the generic refresh glyph so indexing checks are less confusing. Locked `Characters` plans no longer show the demo avatar card at all; instead they show only the pricing gate copy (`Available on Plan Pro and above` / `Доступно на тарифе Pro и выше`) plus the clarified avatar-video explanation. The compact token progress bar inside the expanded sidebar account menu also had its track/background restored so it matches the fuller quota style again.
- CTA polish follow-up: shared primary/secondary pill buttons are now shorter and more unified across the user-facing web surfaces. The base pill height was reduced by about 18%, secondary pills were lightened a tone in both dark and light themes, and the local duplicate CTA styles in assistant settings and the home dashboard were collapsed back onto the shared button helper so pairs like `Сохранить / Настроить` read as one calmer system.
- Chat UI polish follow-up: normal and project chats no longer render separate activity banners inside the timeline. Live tool/project progress now appears only as one quieter inline status beside the streaming cursor in the active assistant bubble, with a brief left-to-right fade on appearance, and the terminal `Response ready` / `Ответ готов` banner path was removed so completion leaves no extra status pill after the answer.
- Web UI polish follow-up: the assistant-settings/sidebar cleanup now uses shared soft-outlined inputs and pill buttons across the touched user surfaces, keeps knowledge/files/support rows flatter and quieter, and localizes Telegram group-refresh feedback beside the `Refresh` control instead of rendering it as a page-level status block.
- Billing UI follow-up: recurring paid copy no longer says `Next`. Shared billing-summary resolution now renders ordinary active recurring states as `Charge {date}` / `Списание {date}`, while an already scheduled plan downgrade renders the future plan and switch date instead (`BASIC c 26 июн.` style) across the sidebar/account surfaces and payment/settings UI. The web bootstrap/app-data seam now includes billing-subscription management state so scheduled downgrade truth can be shown consistently outside the payment modal without guessing from plan visibility alone.
- Billing/quota truth correction: `grace_period` after an already-effective scheduled downgrade now keeps the downgraded current plan and its limits, but preserves the last paid subscription window as the quota period until `payment_recovered` / `renewal_succeeded`. `TrackWorkspaceQuotaUsageService` no longer rewrites grace reads back to the previous paid plan; it now uses the stored current plan for limits while still recovering the previous paid period boundaries from `previousPaidPlanCode` / matching lifecycle history so usage counters do not reset or open a new period early. `expired_fallback` still starts a new fallback period.
- UI follow-up: workspaces in `grace_period` / `past_due` now show a calm payment-issue badge in the three intended visible surfaces instead of leaking the token-budget light-mode badge there: the collapsed sidebar mini-profile, the expanded sidebar account card, and the settings payment/plan surfaces (current-plan summary plus payment-settings modal status area). The badge copy is localized as `Payment issue` / `Сбой оплаты`.
- ADR-114 is now closed for its decided scope after live operator validation. Reserve OpenAI-compatible transport for `image_generate` / `image_edit` is confirmed working end-to-end with the intended primary-then-reserve behavior; OpenAI video fallback remains explicitly out of scope and deferred pending separate compatibility proof and a future decision.
- Billing lifecycle fix: overdue provider-managed paid subscriptions no longer depend solely on a timely CloudPayments renewal webhook to leave stale `active` state. When PersAI reads an `active`/`past_due` paid row whose `currentPeriodEndsAt` is already in the past and the row still has `providerSubscriptionRef`, it now first reconciles against CloudPayments `subscriptions/get`: an `Active` provider subscription with an advanced `NextTransactionDateIso` self-heals through `recoverPayment()` (missed success webhook), while a `Cancelled` provider subscription self-heals through the normal canceled-period-end fallback. If provider reconciliation cannot prove the renewed period, the existing grace-start safety path remains the fallback so stale recurring rows do not keep infinite paid access.
- Billing lifecycle fix: effective subscription resolution now self-heals expired paid grace periods the same way it already self-heals expired trials. If a workspace subscription is still `grace_period` after `graceEndsAt`, the read path now attempts `expireGrace()`, reloads the row, and returns `expired_fallback`; if fallback application is misconfigured, it logs `expired_grace_fallback_skipped` and returns the stale row instead of breaking admin/runtime reads with a user-facing error.

## 2026-06-09

- ADR-114 forced-fallback hotfix: intentionally breaking the primary OpenAI image key with an `Incorrect API key` / `invalid_api_key` response now triggers reserve OpenAI-compatible image fallback instead of failing before ProxyAPI is tried. Live dev confirmed the failed job had reached provider-gateway and stopped on primary OpenAI `401`; provider-gateway classification now treats that account-access failure as reserve-eligible, with focused coverage for both `image_generate` and `image_edit`.
- ADR-114 live hotfix: enabling the reserve OpenAI-compatible media key no longer breaks the primary OpenAI image path during materialization. The internal runtime secret resolver now recognizes `tool/image_generate/reserve/api-key`, and a regression test locks that mapping so direct OpenAI image generation can continue before any reserve fallback is needed.
- ADR-114 Slice 1 implemented for OpenAI image generation/edit fallback only. `Admin > Tools` now carries reserve OpenAI-compatible media transport settings (enable, API key, base URL), materialization forwards the config to runtime/provider-gateway image refs, provider-gateway retries once on strict primary OpenAI transport/account availability failures, and successful reserve fallback logs plus emits the existing admin notification path. OpenAI video fallback remains deferred; billing/quota semantics stay on the existing OpenAI image path with no transport-only billing-facts metadata.
- ADR-114 replaced the withdrawn realtime live voice artifact and superseded fal draft with the active reserve OpenAI-compatible media transport program. Direct OpenAI media behavior, model selection, Admin Runtime, and Admin Plans stay unchanged; the only intended product surface is an `Admin > Tools` Media reserve block with enable boolean, reserve API key, and reserve base URL (default `https://api.proxyapi.ru/openai/v1`). Slice 1 is image generation/edit fallback only; OpenAI video fallback is deferred pending separate route compatibility proof. The ADR defines strict retry criteria, keeps reserve attribution in logs plus one existing-platform admin notification per successful fallback, avoids new billing-facts metadata solely for attribution, and keeps the orchestrator-run GPT-5.4 subagent execution model.
- Android/Capacitor video preview follow-up: real-device smoke on `com.persai.app` showed inline WebView video elements can paint Android's grey native play fallback even while the same file opens correctly in the video viewer. Chat video cards are now about 30% smaller and extract a real decoded frame into a canvas-backed thumbnail image; Android/native shells keep the inline `<video>` surface hidden while showing that true thumbnail and preserving click-to-play behavior.
- Chat media polish: web video attachment cards now use stable portrait/square/landscape preview presets, reveal the real inline video frame after frame data loads, and keep the same same-origin `/api/assistant-file/...` path used by desktop and Capacitor playback. Working Files prompt text also now separates PDF-only document anchors from the general chat-file list and exposes a general `LAST_DELIVERED_FILE` anchor.
- ADR-112 formally closed after the live video-tool follow-up: the ADR status now reflects Slices 1-10 complete, the stale "Slices 7-8 pending" wording is removed, and the handoff points the separate HeyGen VC catalog/settle residual to future API/accounting work rather than ADR-112.
- ADR-112 live video-tool follow-up: `video_generate` guidance now draws a hard boundary between ordinary/cinematic video and `talking_avatar`. Silent/no-speech/gesture/image-animation requests route to `mode="cinematic"`; `talking_avatar` is reserved for explicit spoken-avatar narration with non-empty `speechText`. Runtime validation errors for empty `speechText` now tell the model to use cinematic or ask for a real script, model-correctable async video failures (`invalid_arguments`, bad reference/portrait aliases, missing voice/persona/config) are non-retryable so jobs stop immediately instead of burning the retry window. Media/document worker attachment payloads now use Working Files sticky aliases as the single source of truth; the old separate recent-image/recent-document-source attachment resolvers (`last 6` / `last 4`) were removed from the runtime path instead of being kept as hidden fallback namespaces.
- ADR-112 Slice 8 completed in the working tree: background model-call prompt hygiene now has clearer names and classifications. Runtime bundles expose `promptDocuments.backgroundTaskEvaluation` while keeping legacy `heartbeat` as a compatibility alias; skill-state classifier calls now include `checkReason` in the prompt and use the `skill_state_classifier` provider classification; quota advisory evaluation is split from real background-task evaluation via `quota_advisory_evaluation`; and compaction-exhausted follow-ups no longer invoke the two-phase background-task evaluator, instead creating a bounded static notification intent from durable compaction facts after the existing suppression checks.
- ADR-112 Slice 7 completed in the working tree: background job visibility now splits runtime prompt truth into real in-progress jobs vs post-completion delivery updates. API/web continuity remains unchanged (`activeMediaJobs` and web document chips still keep `completion_pending` / `ready_for_delivery`), but runtime-facing `openMediaJobs` / `openDocumentJobs` now exclude those states and instead receive a compact internal `jobDeliveryUpdates[]` seam on `RuntimeTurnRequest`. Runtime renders one `## Job Delivery Updates` developer section that says generation/rendering is already finished, distinguishes `finalizing_delivery` from `delivered_recently`, prevents stale "still generating" replies during the delivery race, and explicitly closes the async audio lane: voice replies use synchronous `tts`, not background `audio_generate`.
- ADR-112 prompt-context follow-up: internal memory hydration now returns each memory row's existing `chatId`, and runtime renders slim source markers (`this chat` / `past chat`) in durable-memory labels only when the current chat can be compared. Past-chat memory blocks add one short instruction telling the model to use chat/context search for missing details rather than assuming the fact happened in the current conversation. Unresolved `open_loop` short memories no longer duplicate into the recent short-memory prompt block because the operational surface is `Open Loop Refs`. Telegram channel context is now the same centralized developer section for private and group Telegram turns, with a concise voice/audio hint when the inbound Telegram message includes audio and group-only privacy cautions kept only for groups. `Working Files` was slimmed by removing the format legend, compressing file-tool guidance, and suppressing historical audio/voice attachments from the default prompt surface. No database migration was needed.

## 2026-06-08

- ADR-112 Slice 9 completed in the stacked working tree: runtime model-facing tool descriptors/instructions were cleaned without changing execution architecture. `document` guidance now consistently uses `docId` and states it is UUID-only, `memory_write` no longer exposes ADR-id implementation cruft, `scheduled_action` projection is sanitized to user-visible reminders and no stale `assistant_check` hidden-follow-up copy, `background_task` no longer advertises a ghost `update` action, pending-delivery honesty copy is shared across image/video/document tools, video persona guidance consistently says `Settings -> Characters`, and `tts` now has one concise rule to call the tool for spoken replies/voice notes and not claim audio exists unless the same turn returns `action='generated'`. Focused projection/scheduled-action/TTS tests plus runtime lint/typecheck passed.
- ADR-112 Slice 5/6 independent audit follow-up: four read-only auditors reviewed runtime Working Files, media/document/file consumers, descriptors/docs, and test coverage. The real production residual was closed: image/video tool attachments now reuse Working Files sticky aliases instead of a second ephemeral `image #N`/`file #N` renumbering path; media tool result/log aliases prefer sticky labels; files-tool model-visible alias arrays keep sticky aliases first and drop legacy ordinal aliases; non-UUID `docId` values are nulled/logged instead of forwarding alias-like strings; `docId` tool projection is camelCase + UUID-only; and stale `RuntimeTurnResult.discoveredFileRefIds` / ADR-081 alias docs were corrected to the sticky Working Files truth.
- ADR-112 Slice 6 residual developer-block cleanup completed in the same stacked flow: the Working Files title/tool vocabulary alignment had already landed with Slice 5, and the remaining unused `showCurrentTurnImageOrdinals` / direct-attachment counter plumbing was removed from runtime hydration. Source audit confirms no active runtime turn assembly still emits `## File history` or carries the dead current-turn image ordinal path.
- ADR-112 Slice 5 completed in the working tree: runtime Working Files now uses stable sticky `file #N` / `image #N` handles instead of recomputed current/previous/found/listed/read aliases. The developer section is titled `## Working Files`, separates sticky labels from recency/role markers, keeps PDF-specific document anchors (`DOC_CURRENT_SOURCE` / `DOC_LAST_DELIVERED_PDF`) visible, and preserves same-name files with short-hash disambiguation. Hydration, tool-loop discovery merging, `files`, `document`, `image_edit`, `image_generate`, and native tool projection guidance now resolve/teach the sticky scheme; focused runtime tests plus runtime lint/typecheck passed.
- ADR-112 follow-up tightening on the Slice 10 auto-extractor: runtime `AutoExtractToMemoryService` now asks for at most `3` high-confidence items (or zero), rewrites the extraction prompt toward concise neutral evidence-backed notes, restricts `open_loop` to concrete unresolved follow-ups instead of vague ongoing interests/product direction, defaults ordinary open loops to `short`, and adds a small parser guard that skips obvious test-voice/demo/ephemeral summaries plus vague `open_loop` outputs, downgrades non-explicitly-durable `long` open loops to `short`, and skips numeric-confidence `long` fact/preference candidates below `0.85`. Focused runtime tests lock the prompt rules and parser behavior; no runtime/API contract shape changed.
- ADR-112 Slice 10 is now complete in the working tree: the model-facing `memory_write` contract uses a simple `layer` choice (`long` | `short`) instead of `durability` / `stability`, with deterministic routing `long -> core + identity/stable` and `short -> contextual + episodic/time_bound` on the API side; the raw `web_chat` post-turn memory writer is removed from the active path; contextual hydration is a bounded recent short-memory window instead of lexical pre-turn search; and runtime planning drops retrieved-knowledge memory items that would duplicate that already-rendered short-memory block. Anthropic now projects that volatile recent-memory block as app-provided `<persai_runtime_context><recent_short_memory>...` immediately before the real user request, while OpenAI keeps the provider-native `developer` item. The remaining follow-up also landed: `runtime_sessions` now carries an explicit `memoryExtractionWatermark`, compaction auto-extract advances that same watermark while processing only the unwatermarked compacted prefix, and a new API idle-session scheduler reuses the existing background-compaction queue to run one compact-model idle extraction pass only after `20` idle minutes and `>=10` new hydratable messages after the watermark. Idle/provider failures leave the watermark unchanged and are retry-bounded by the existing job scheduler; successful/no-item evaluations advance the watermark so the same message delta cannot be processed twice. Background queue dedupe now separates `compaction` and `idle_extract` lanes so an idle job cannot suppress a real compaction job.
- ADR-113 opened and Slice 1 (expressive delivery core) implemented: chat voice replies are now structured and controllable instead of flat tone steering. The `tts` worker tool drops the single `toneTag` argument and accepts structured expressive intent (`delivery`, `emotion`, `pace`, `intensity`, `pause`, `nonVerbal`); the runtime derives the legacy tone for Yandex/OpenAI baselines and forwards the structured intent to provider-gateway. A new safe, deterministic compiler turns that intent into a small conservative set of ElevenLabs `eleven_v3` audio tags (`[whispers]`, `[dramatic]`, `[excited]`, `[sad]`, `[curious]`, `[laughs]`/`[chuckles]`/`[sighs]`/`[clears throat]`, `[short pause]`/`[long pause]`) with conflict avoidance (whisper suppresses loud emotion/intensity), a hard 3-tag budget, and model-authored-tag stripping so the model can never inject raw tags through `text`. The ElevenLabs provider client now defaults the chat quality path to `model_id: "eleven_v3"` (honoring an explicit catalog `modelKey` override per ADR-110), prepends compiled tags, and sends conservative discrete-stability v3 voice settings; non-`eleven_v3` ElevenLabs models and non-ElevenLabs providers ignore v3 tags and keep prior tone-based behavior. The saved ElevenLabs `voiceId` and the existing media/job delivery flow are unchanged. Voice catalog v2 backend/cache and the premium picker UI are designed in ADR-113 as Slices 2-3.
- ADR-113 Slice 2 (voice catalog v2 backend/cache) implemented: ElevenLabs voices for the assistant voice settings are now served from a platform-wide read-through cache (`platform_elevenlabs_voice_catalog_cache`, new additive migration) via `ElevenLabsVoiceCatalogService` instead of a live per-request fetch. The cache refreshes lazily on a 24h TTL, upserts on a successful fetch, and serves the last known (stale) list with a warning if a refresh fails, so the settings UI stays responsive and rate-limit-safe. Entries are normalized with `gender`, `category`, `language` + a derived `languageBucket` (`ru`/`en`/`other`), and `previewUrl`. `GET assistant/voice/settings` keeps its existing fields and additively exposes per-entry `language`/`languageBucket`; `unavailable` is now only reported when the cache is empty and a live refresh fails. The premium picker UI remains Slice 3.
- ADR-113 Slice 3a (premium voice picker UI) implemented: the three plain voice dropdowns in assistant settings are replaced by a shared `VoicePicker` card component unified across ElevenLabs, Yandex, and OpenAI. It adds voice search, gender/language/category filter chips (each shown only when it discriminates), selectable cards, and ElevenLabs stock-preview playback (play/stop). ElevenLabs cards are sourced from the Slice 2 cached catalog (with `language`/`languageBucket`/`category`/`previewUrl`); Yandex/OpenAI cards come from their fixed enums. Honest loading/`not_configured`/`unavailable` states are surfaced inline, the saved per-provider voice still writes into `persai.assistantVoiceProfile.v1`, and the existing assistant-gender constraint on the selectable set is preserved. The `eleven_v3` test-phrase generation is split out as Slice 3b (needs metered backend synthesis).
- ADR-113 closed: Slice 3b (`eleven_v3` test-phrase synthesis) was dropped as unnecessary complexity. A paid on-the-fly preview synthesis (with its billing/quota and anti-abuse plumbing) is not justified for voice selection — the Slice 3a stock card preview covers "how does this voice sound", and real intonation is verified live by the assistant's actual voice notes in chat. TTS 2.0 ships on Slices 1 + 2 + 3a.
- ADR-113 voice picker follow-up: the ElevenLabs catalog refresh now uses the shared voice library (`/v1/shared-voices`) and stores a versioned cache key for a bounded picker set per `RU|EN|OTHER` bucket and assistant gender, mixed 50/50 from `featured` voices and locally popularity-ranked candidates (`cloned_by_count + liked_by_count`). The web picker was simplified to the intended premium list: ElevenLabs shows only `RU | EN | OTHER` plus voice name + preview play, while Yandex/OpenAI keep the same compact name + play surface without marketplace chips.
- ADR-113 admin voice curation follow-up: ElevenLabs now separates an expanded admin candidate catalog from the public picker list. Admin users can browse the larger cached candidate set, select a voice for their own assistant, mark voices as public/hidden, and flag broken demos; regular users only receive the admin-approved public voices (capped at 24 per language bucket + gender). The selected voice row now has an explicit check marker and ElevenLabs `voiceId` is no longer silently cleared by the settings filter before save/publish.
- ADR-113 fallback hotfix: Yandex SpeechKit v3 TTS now handles multi-JSON/NDJSON audio chunk responses instead of assuming a single JSON document, and OpenAI TTS rejects foreign provider model keys such as `eleven_multilingual_v2`, falling back to `gpt-4o-mini-tts` so fallback speech does not call OpenAI with an ElevenLabs model id.

## 2026-06-07

- ADR-111 closed as completed after live HeyGen voice-clone smoke and post-smoke repair. The completed program now records workspace-scoped cloned voices, VC-on-success debit, preset/cloned voice persona selection, runtime voice precedence, Characters/My voices UI, upload/record clone creation, mobile video preview/lightbox polish, Clerk route auth coverage, browser recording WAV conversion, and HeyGen `audio/x-wav` projection. Custom audio lip-sync, HeyGen Studio API, broad passthrough tuning, and chat TTS remain out of ADR-111 scope.
- ADR-112 Slice 4 completed in the working tree (held from push for the memory-block audit): safe assistant-scoped memory backfill now follows the established dangerous-admin two-phase pattern. API added `POST /api/v1/admin/memory-backfill/preview` (read-authorized dry-run) and `POST /api/v1/admin/memory-backfill/apply` (step-up required via new dangerous action `admin.memory_backfill.apply`). The backfill scans up to `1000` active rows for one assistant and reports/applies two exact cleanup rules only: legacy episodic `core` rows (`memoryClass = core` but not `identity + stable`) are reclassified to `contextual`, and trivial `web_chat` rows whose stored summary fails the durable-summary guardrail are soft-pruned with `forgottenAt` via `markForgottenById`. If a row matches both rules, prune wins. Repository support (`listActiveForBackfill`, guarded `reclassifyMemoryClassById`) and focused API tests landed, and OpenAPI was hand-edited minimally for the new endpoints/action without regenerating contracts.
- ADR-112 Slice 3 completed (two sub-slices, local commits only, held from push): durable-memory normalization + consolidation + lifecycle. 3a (`56613bff`) added nullable `superseded_at` / `superseded_by_memory_id` columns, a `markSupersededById` repository method, and `supersededAt: null` exclusion on every active memory read/hydration/search path so a superseded fact stops resurfacing while its audit row is retained. 3b (`8433dd6d`) added lazy memory embeddings (`embedding_vector` JSONB + `embedding_model_key` + `embedding_generated_at`, model key reused from the knowledge embedding policy) and a best-effort `ConsolidateAssistantMemoryService` that runs after each successful background compaction (reusing the existing scheduler lease/cadence, never failing the compaction job): it lazily embeds new memories, merges same-kind near-duplicates (cosine >= 0.92 -> supersede loser, survivor priority core > confidence > recency), and decay-prunes time_bound contextual rows idle for > 45 days. Unresolved open loops are never merged/pruned; identity/stable/core rows are never decay-pruned; with no embedding model available it skips merge and still runs decay. Mechanical cosine merge, not a positive classifier.
- ADR-112 Slice 2 completed: durable-memory retrieval/render quality. Runtime-facing hydration now drops trivially-non-durable contextual hits (reusing the Slice 1 negative guardrail, so legacy greeting/ack rows that pass the lexical exact-token floor no longer hydrate into the prompt) and de-duplicates contextual memories against core by normalized text (not just id), while preserving search relevance order. The per-turn contextual block is rendered as one volatile block under the existing header, deterministically grouped by kind (`Preferences -> Facts -> Open loops -> Other`), still flagged `cacheRole: "volatile_context"` and placed last in the prefix. A provider-gateway regression test now locks the ADR-110 invariant that a `volatile_context` block is re-projected as a non-cacheable `user` block before the current question and never carries `cache_control`; the core stable cache token is asserted invariant under contextual rotation. The earlier per-turn `cache_creation` worry was investigated directly against live `persai-dev` receipts (session `78ae7b44`) and confirmed resolved: post-deploy short Anthropic turns keep history cache reads growing with only tiny new-tail writes, versus pre-deploy turns that re-wrote a 3k-10.5k segment every turn.
- ADR-112 Slice 1 completed: `memory_write` and compaction auto-extract now require model-emitted `durability` (`identity|episodic`) and `stability` (`stable|time_bound`) with optional `confidence`; API persistence added nullable memory durability/stability/confidence columns plus semantic routing (`identity+stable -> core`, otherwise contextual, negative guardrails -> soft `not_durable` skip); trivial web-chat greeting/ack turns are no longer stored; and runtime `memory_write` guidance/schema/tests now reflect the tightened durable-memory contract.
- ADR-112 opened as an orchestrator-run quality program for context, memory, and the tool surface. Grounded in five read-only GPT-5.4 audits (durable memory, prompt/developer assembly, file aliases, background jobs/turns, tool descriptors), it defines seven workstreams (A memory formation, B prompt/block formation, C developer block + file/media, D sticky file aliases, E background jobs visibility, F background model-call prompt hygiene, G tool descriptor/instruction hygiene) and a 10-slice subagent-coded backlog. Memory formation is decided by model judgment with embedding dedup, periodic consolidation, and lifecycle/supersession - heuristics only as a guardrail, never the deciding layer. The parent agent orchestrates and audits; GPT-5.4 subagents implement.
- ADR-111 post-smoke UX/video cleanup: mobile chat video previews now use a deterministic premium play card with metadata-driven aspect geometry instead of a forced `16:9` frame, the video lightbox chrome can be recovered by tapping the playing video, the locked demo character prefers a female localized voice, talking-avatar generation no longer lets a generic hard landscape `16:9` default win when no explicit aspect is requested and defers that case to provider `auto`, and cloned-voice cards/modal copy are quieter.
- Prompt-cache discipline (PROD): the per-turn `durable_memory_contextual` block is now repositioned out of the cached prompt prefix in both providers via a typed contract flag instead of fragile header string-matching. `ProviderGatewayTextMessage` gained `cacheRole?: "volatile_context"`, which the runtime sets when it hydrates relevance-retrieved contextual memory. Live `persai-dev` Anthropic receipts showed that contextual memory is intentionally query-dependent and was sitting before conversation history, so every Anthropic history breakpoint included a changing prefix and rewrote roughly `10.5k` cache-creation tokens on short turns. Both provider clients now detect the flagged block, drop it from the cacheable prefix, and re-project it as a `user` block immediately before the current user question (symmetric Anthropic/OpenAI schema) so the question keeps the highest recency while the stable system/tools cache and Anthropic moving-history breakpoint stay hot. OpenAI per-turn developer instructions remain a provider-native trailing `developer` suffix.
- ADR-111 post-audit cleanup completed: cloned-voice recording now ignores stale microphone permission resolutions and duplicate starts, clone/persona preview blob URLs are revoked on replace/close, failed cloned-voice rows count toward the UI limit gate to match API active-row truth, and archiving a cloned voice best-effort marks workspace assistants config-dirty so materialized persona guidance refreshes.
- ADR-111 Slice 5 implementation completed: Settings -> Characters now includes a compact `My voices` subsection, cloned-voice upload/record modal with visible quality and rights guidance, ready-only cloned-voice persona selection, inline clone-and-attach flow, cloned-voice API client helpers, and runtime persona guidance that surfaces only safe cloned-voice labels from materialized catalog truth. Cross-docs now record the cloned-voice API/data/test boundaries. Local focused checks and full verification passed; authenticated live dev smoke remains outstanding because no signed-in dev session/local app was available.
- ADR-111 Slice 4b completed: personas can now link a workspace cloned voice through `clonedVoiceId` while retaining preset HeyGen fallback fields. API create/update validates linked cloned voices as same-workspace, active, ready, and provider-backed; internal runtime reads expose usable clone metadata; materialized persona catalogs keep provider ids out of user/model-facing labels; and talking-avatar voice resolution now follows explicit `voiceKey` -> linked cloned voice -> preset persona voice.
- ADR-111 Slice 4a completed: provider-gateway now submits/polls HeyGen voice clones with exact `complete` success semantics and stable clone-limit/plan-upgrade/auth/rate-limit error mapping. API now exposes workspace-scoped cloned voice create/list/archive/set-default endpoints, validates multipart audio uploads, enforces clone limits and duplicate names, marks failed clone attempts without debit, and debits VC once through `voice_clone_creation` only after a clone is marked ready.
- ADR-111 Slice 4 was split after the first GPT-5.4 implementation pass identified missing persona linked-clone substrate for the runtime precedence rule. Execution now proceeds as 4a provider-gateway/API cloned-voice CRUD + successful-completion VC debit, then 4b persona linked-clone substrate + runtime resolve.
- ADR-111 Slice 3 completed: API substrate now includes workspace-scoped `workspace_video_cloned_voices` persistence with Prisma migration, repository port/adapter, platform settings for cloned voice workspace limit and VC creation cost (`5` / `50` defaults, hard cap `10` for limit), and the typed VC ledger discriminator `voice_clone_creation`. No HeyGen HTTP call, REST endpoint, runtime resolve change, wallet debit, or web UI was added.
- ADR-111 Slice 2 completed: Characters now use a compact premium responsive grid with shared card geometry for unlocked personas, locked saved personas, and the locked demo record. The create action is a compact grid slot that disables for plan/limit gating without opening the modal, locked cards expose no edit/delete/portrait actions, and voice labels now use `Voice - ...` / `Голос - ...` copy.
- ADR-111 Slice 1 completed: inline chat video previews now render a deterministic compact play placeholder instead of depending on Android WebView decoding the first frame, while preserving metadata duration and click-to-lightbox behavior. The video lightbox hero play overlay now disappears once playback starts instead of staying mounted when chrome is visible, video chrome auto-hides after playback and returns on tap, and the same-origin assistant-file BFF is regression-covered for Capacitor-relevant `Range`/validator passthrough plus upstream `206`, `Content-Range`, `Accept-Ranges`, `Content-Length`, and `Content-Type: video/mp4` preservation. No API or `persai-mobile` change was needed.

## 2026-06-06

- History compaction: `docs/CHANGELOG.md` now keeps only current entries and concise recent summaries. Detailed 2026-06-05-and-earlier entries moved to `docs/CHANGELOG.archive-2026-06-05-details-and-earlier.md`; older pre-existing archive remains unchanged.
- ADR-103/104/106/107 closure sweep: old non-active tails are now explicitly closed. ADR-103 keeps the stubbed interactive landing demo as completed scope and cancels/defer-indefinitely the public unauthenticated LLM demo endpoint; ADR-104 keeps D1/D2 as completed deploy-resilience scope and cancels/defer-indefinitely the auto-mutating drift-repair cron while leaving prod atomicity as a future design note; ADR-106 is marked completed; ADR-107 is marked closed as a partial program with its existing Program closure as source of truth.
- ADR-110 closed as completed after operator verification. The active execution order is finished: embedding truth moved to `Admin > Knowledge`, runtime text failover and retrieval-helper fallback execute on the native path, TTS model truth no longer depends on the old OpenAI hardcode, and Anthropic prompt-cache/accounting landed with the later stream-accounting hotfix. Deferred STT/web-search provider-model work stays out of ADR-110 and should return only as a separate future ADR/slice if product priority requires it.
- ADR-110 Anthropic stream-accounting hotfix: provider-gateway no longer double-counts Anthropic streaming usage by summing `message_start` and `message_delta` token snapshots as if they were additive deltas. Later stream usage payloads now replace earlier fields instead of accumulating them, which brings PersAI's Anthropic ledger/session token counts back in line with Anthropic's own logs and prevents false context-pressure / compaction signals on short chats.
- ADR-109 closed as completed after operator verification. The HeyGen talking-avatar foundation is accepted as done; remaining voice cloning, Characters UI v2, and video-player polish work is explicitly owned by ADR-111 and must not reopen ADR-109.
- ADR-111 opened as the orchestrator-run program for HeyGen voice cloning and Characters UX v2. It keeps ADR-109 closed, supersedes only ADR-109's deferred voice-cloning non-goal, excludes custom audio lip-sync / broad HeyGen passthrough tuning, and defines a 5-slice GPT-5.4 subagent execution plan covering video-player polish, premium Characters UI cleanup, cloned-voice substrate, HeyGen voice-clone backend/runtime wiring, voice-clone UI, docs, and live smoke.
- ADR-110 Anthropic prompt-cache fix: runtime now passes prompt-cache intent to Anthropic text calls instead of limiting prompt caching to OpenAI-only requests. Anthropic provider-gateway already projects that intent as `cache_control: { type: "ephemeral" }` on the stable system block, so live Claude calls can now produce `cacheCreationInputTokens` / `cachedInputTokens` in usage instead of always recording zero cache usage.
- ADR-110 live hotfix: Admin Runtime settings now tolerate persisted token-metered catalog rows created before `cacheCreationInputPer1M` existed by defaulting the missing cache-write price to `0` during normalization. The dev cluster's persisted runtime catalog was patched in place so existing OpenAI/Anthropic/image/video rows no longer break Admin Runtime reads or materialization rollouts.
- Billing lifecycle fix: CloudPayments recurrent `check` webhooks can now resolve an existing PersAI subscription by `SubscriptionId`/`AccountId` even when there is no local renewal payment intent, so provider retries are not rejected as an unknown payment intent. Manually/admin-activated paid subscriptions with no provider subscription reference now enter the same `active -> grace_period -> expired_fallback` lifecycle when their paid period expires, preserving the standard grace notifications, materialization rollout, and fallback behavior.
- ADR-110 Slice 4: Anthropic prompt cache is now provider-specific and economically honest. The Anthropic provider client keeps the stable `systemPrompt` cacheable via `cache_control: { type: "ephemeral" }` only when prompt caching is enabled, leaves volatile `developerInstructions` as an uncached suffix system block, and never emits a literal `developer` role to Anthropic. Usage/accounting now has a separate `cacheCreationInputTokens` bucket in addition to normal `inputTokens`, cache-read `cachedInputTokens`, and `outputTokens`; token pricing metadata adds `cacheCreationInputPer1M`, with Anthropic cache writes priced separately from ordinary input and cache reads in both quota weighting and the cost ledger.
- ADR-110 Slice 3: TTS keeps its existing runtime provider-chain fallback, but OpenAI TTS model truth is no longer hardcoded on the execution path. The API now materializes the OpenAI `text_to_speech` catalog entry onto the TTS credential ref, runtime/provider-gateway forward that `modelKey`, and OpenAI speech billing facts now reflect the actual successful model from the catalog-driven path. Yandex `speechkit-v3` remains intentional code-owned API-version truth in the active TTS path.
- ADR-110 scope correction: STT and web-search provider/model truth are deferred out of the active ADR-110 execution order because they are not current production blockers and need a later full vertical contract/runtime/provider-gateway/UI/test slice. The next active ADR-110 slice is now TTS model/default truth cleanup.
- ADR-110 Slice 2: runtime text failover now executes on the active runtime path for eligible text-model failures instead of stopping at the primary provider/model. `apps/runtime` now performs one-shot fallback for ordinary turn text calls plus router/background system-tool calls when the primary text request fails with timeout/5xx-style provider execution errors, and logs primary failure plus fallback success/failure explicitly. The API knowledge rerank helper keeps `retrievalModelKey = null` as graceful-off, but when configured it now retries once onto the runtime fallback provider/model for the same retryable helper failures. Embedding fallback, generated media, STT, TTS, and web-search behavior were left unchanged in this slice.
- ADR-110 Slice 1: embedding model truth moved out of Plans and into `Admin > Knowledge`. Plan payloads/contracts/UI no longer write `embeddingModelKey`; assistant-owned knowledge now resolves embedding/index/search model choice from `adminKnowledgeRetrievalPolicy`, preserving lexical graceful-off when admin embedding is unset or same-model embedding generation fails. Admin Knowledge policy updates now expose embedding-change impact (`affectedSourceCount`, chunks, bytes, by-source breakdown), require dangerous-action step-up confirmation when the embedding model changes, and enqueue backfill across all Admin Knowledge-governed sources: global uploaded Product KB, Product KB text entries, Skill documents, Skill knowledge cards, and assistant-uploaded knowledge. OpenAPI/generated contracts, admin-web tests, and focused API tests were updated to match the new source of truth.
- ADR-109 live delivery hotfix: three recent talking-avatar jobs in `persai-dev` were proven complete on HeyGen, but chat delivery still hung because HeyGen sometimes serves the finished `.mp4` with `Content-Type: binary/octet-stream`. The provider-gateway now normalizes empty / octet-stream HeyGen video downloads back to a real video MIME based on the download URL suffix (defaulting `.mp4` to `video/mp4`), so runtime no longer rejects completed HeyGen renders as “unsupported MIME type”.
- ADR-109 Characters edit flow: saved persona cards now open the same shared modal in `Edit` mode, but avatar replacement is intentionally forbidden there. Operators can update only the persona name and voice, preview the existing portrait in a larger view, and see a quiet hint that changing the face requires creating a new character. The API now exposes a bounded `PATCH /api/v1/workspaces/:workspaceId/video-personas/:personaId` path that updates only `displayName + heygenVoiceId`, preserving the existing `heygenAvatarId` and portrait identity instead of mutating HeyGen avatar state in place.
- ADR-109 live voice-catalog hotfix: HeyGen `GET /v3/voices` uses the opaque `next_token` response field, but the follow-up request must send it back as query parameter `token` per the official docs. The previous refresh path incorrectly sent `next_token=...`, and live dev verification from the `api` pod proved HeyGen kept returning the same first 20 EN voices with an unchanged cursor. The cache refresh now uses `token=...`, requests up to `100` rows per page, and stops early if HeyGen repeats the same cursor to avoid looping on a broken upstream page stream.
- ADR-109 live hotfix: fixed the Admin HeyGen voice refresh route so it goes through Clerk auth like the other admin tool-credentials endpoints; the live dev cluster root cause was a real `401 userId=null` on `POST /api/v1/admin/runtime/tool-credentials/heygen-voice-catalog/refresh`, which meant the spinner ran but the cache never refreshed.
- ADR-109 Admin Tools voice refresh feedback now shows `Last refresh ... · N voices` and a concrete post-refresh status like `Updated: 300 voices.` instead of a spinner-only transition with no visible outcome.
- ADR-109 HeyGen voice catalog truth-hardening: the shared HeyGen cache now follows provider pagination and stores the full deduped catalog, while runtime still materializes a short prompt-safe talking-avatar shortlist (`10 EN + 10 RU` target, balanced by gender as available) for model-facing instructions.
- ADR-109 Admin Tools now exposes a manual HeyGen voice refresh action with a visible last-refresh timestamp, so operators can force a catalog sync without waiting for the 24h cache TTL.
- ADR-109 Characters voice picker now reads the full HeyGen catalog instead of the model shortlist, and the `OTHER` bucket adds inline language search so non-EN/RU voices are browseable/listenable without bloating the runtime prompt.
- ADR-109 Characters UI polish: locked plans now show a single premium demo character with the provided demo portrait, a real voice preview, and a direct `https://persai.dev/app/pricing` upsell link; unlocked plans hide the demo card, move `Create character` below the saved list, add a quiet usage hint, and open saved portraits in a lightbox on click.
- ADR-109 voice-picker cleanup: fixed the RU/EN regression by normalizing human-readable HeyGen language labels like `English` / `Russian` into the same `en` / `ru` buckets used by the filter, and removed the nested-button markup in the create-persona voice list so the voice preview control no longer emits invalid HTML hydration warnings in tests.
- ADR-109 root-cause fix (saved-persona render): persona create/archive now best-effort mark workspace assistants `configDirtyAt` so the materialized spec re-builds and the model actually sees saved characters with their real `personaId` (previously the model saw an empty `videoPersonaCatalog`, told users "no saved characters", and improvised `personaId="alexey"` from the display name); and the persona repository `findById`/`archive` now guard against non-UUID ids and resolve to `null` (honest `persona_not_found`) instead of letting Prisma throw a raw "Error creating UUID" — that raw DB error was the cause of the "UUID error" the model paraphrased on saved-persona renders.
- ADR-109 fix (10-minute banner on a permanent error): the async media-job worker `assertVideoToolResultAccepted` threw `503 ServiceUnavailable` (retryable) for every `isError` video reason except `requested_mode_unsupported`, so permanent talking-avatar config/input failures (`persona_not_found`, `talking_avatar_persona_unavailable`, `talking_avatar_provider_unavailable`, `talking_avatar_plan_disabled`, `voice_not_found`, `voice_required`) were retried through the exponential-backoff budget (30s→60s→120s→240s→480s) for ~10–15 min before the banner cleared. These reasons are now classified non-retryable `400` and fail fast; genuinely transient reasons keep their `503`/retry budget.

## 2026-06-05

- ADR-109 follow-up fixpack: made saved persona portraits actually load through a same-origin authenticated proxy path, improved the HeyGen voice shortlist so `ru` and `en` both survive shortlist materialization, added a `RU | EN` filter in the Characters create-persona form, clarified that cinematic `voice_control` shortlist entries are not for talking-avatar voice selection, and changed large HeyGen video delivery so files over the `100MB` inline ceiling fall back to a direct download link instead of failing the job.
- ADR-109 follow-up fixpack: fixed workspace voice catalog auth to resolve membership canonically, accepted HeyGen fractional-duration responses in runtime, whitelisted HeyGen in scheduler recovery, and added `talkingAvatarAspectRatio` as a talking-avatar-only user/model intent field that overrides aspect only when the HeyGen admin row is set to `auto`.
