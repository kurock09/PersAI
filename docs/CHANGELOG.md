# CHANGELOG

> Archive: entries from 2026-05-19 and earlier moved to `docs/CHANGELOG.archive-2026-05-19-and-earlier.md`. Keep using this file for current work, including all 2026-05-20 ADR-099 entries.

- **ADR-109 Slice 10c — Live integration fixup: URL double-prefix + prompt validation + talking-avatar credential routing + tool description (`apps/api`, `apps/web`, `apps/runtime`, `packages/contracts`; 2026-06-05):** closed four blocking production behaviors discovered by operator's live dev validation of the audit-pass deploy (`77512ef7`). Pre-Slice-10c, talking-avatar end-to-end was unreachable from chat despite all 11 prior slices being green: voices weren't loading in Settings, "Create persona" was disabled, and chat attempts errored with `prompt must be a non-empty string` → `Exactly one of personaId or portraitImageAlias is required` → `talking_avatar_provider_unavailable`. **Fix #1 — URL double-prefix:** the 4 Slice 9 web client methods in `apps/web/app/app/assistant-api-client.ts` (`getWorkspaceVideoPersonas`, `getWorkspaceVoiceCatalog`, `createWorkspaceVideoPersona`, `deleteWorkspaceVideoPersona`) prepended `/api/v1/workspaces/...` to the path, but `getApiBaseUrl()` already returns `/api/v1` (web default) or `http://localhost:3001/api/v1` (SSR default), so every fetch hit `/api/v1/api/v1/workspaces/.../...` → 404. Dev API logs confirmed (`2026-06-05 14:07–14:08 UTC` 4× 404s). Persona list ALSO 404'd, but `loadPersonas()` catch is silent → UI showed empty list. Slice 10c drops the `/api/v1` prefix from all four methods to match the rest of the file's `${base}/path` convention. **Fix #2 — `prompt` validation order:** `runtime-video-generate-tool.service.ts::readVideoGenerateArguments` validated `prompt` non-empty BEFORE parsing `mode` (line ~886 pre-Slice-10c). Talking-avatar requests legitimately omit `prompt` (the LLM reasons "speechText IS the speaking content"), and Slice 10's tool description did not document that `prompt` was also required, so the LLM's reasoning was correct given the description but broke the validator. Slice 10c parses `mode` first; `prompt` is REQUIRED for `mode === "cinematic"` or absent/null (existing behavior unchanged); OPTIONAL for `mode === "talking_avatar"` — when omitted, runtime synthesizes the structural placeholder `"Talking-avatar render"` (literal — NO content from `speechText` or any user-supplied text leaks into the placeholder; invariant #15 preserved). Slice 10's tool description `prompt` doc updated to: "Required for cinematic mode. Optional for talking_avatar — provide a one-line scene context for observability, or omit." **Fix #3 — Variant B: separate talking-avatar credential routing (Slice 2b × Slice 7 architectural hole closed).** Pre-Slice-10c, Slice 7's `executeTalkingAvatarDispatch` required `credential.providerId === "heygen"`, but Slice 2b's plan validation EXPLICITLY refused HeyGen models as `plan.videoGenerateModelKey` / `videoGenerateFallbackModelKey` (cinematic-only error). So through the plan editor NO setup path could land `credential.providerId === "heygen"` in the bundle; runtime always saw cinematic provider (dev log `provider=kling`) and rejected with `talking_avatar_provider_unavailable`. **Operator-approved architecture (Variant B in the AskQuestion form, 2026-06-05):** add dedicated `plan.talkingAvatarModelKey` + `plan.talkingAvatarFallbackModelKey` fields. Plan editor exposes a separate HeyGen-only selector (filtered to `kind === "talking_avatar"` rows) under the existing Video Generation group; disabled when `talkingVideoEnabled === false` with hint text. Slice 2b refusal text updated `"...via the workspace plan toggle (Slice 8)"` → `"...via the plan's \`talkingAvatarModelKey\` field"`. New `assertTalkingAvatarModelKeysAvailable` refuses cinematic rows on the new field, refuses inactive rows, refuses missing-from-catalog rows. Materializ-assistant: new exported constant `VIDEO_GENERATE_TALKING_AVATAR_TOOL_KEY = "video_generate_talking_avatar"`; new private `buildTalkingAvatarCredentialRef` returns null when any prerequisite is missing (HeyGen secret unconfigured / `plan.talkingVideoEnabled !== true` / no active HeyGen catalog rows); modelKey resolution: `plan.talkingAvatarModelKey` if active, else first active HeyGen row (**Variant C fallback** kept as permissive default so initial deploy works without forcing operators to set the new field per plan); attaches HeyGen voice catalog + workspace persona shortlist to this new ref. Voice + persona catalogs REMOVED from the cinematic `video_generate` ref (the cinematic ref was attaching them in Slice 10 but the cinematic credential never legitimately had providerId === "heygen"; Kling voice catalog continues to attach via the existing branch). Runtime Slice 7: when `request.mode === "talking_avatar"`, looks up the new credential key (`bundle.governance.toolCredentialRefs["video_generate_talking_avatar"]`); when null/missing/not-configured → returns refined `talking_avatar_provider_unavailable` payload pointing operators at "Plan editor → Video Generation → Talking Avatar Model". Cinematic dispatch path unchanged. **Fix #4 — Slice 10 tool description re-materializes from the new ref.** `createVideoGenerateToolDefinition` signature change: `talkingVideoEnabled: boolean` → `talkingAvatarCredential: AssistantRuntimeBundleToolCredentialRef | null`. Structural truth derived from ref presence (`talkingAvatarEnabled = ref !== null`); persona + voice catalog hints sourced from the talking-avatar credential ref; cinematic `voiceCatalogHint` continues to source from the cinematic ref. Slice 8's HeyGen filter (`!isTalkingAvatarVideoProvider(providerId) || talkingVideoEnabled`) is preserved semantically: when talking-avatar credential is null, the LLM does NOT see talking-avatar schema fields nor the 7-section description block. **Persistence:** the new plan fields persist via the existing `billingProviderHints` JSON column at top level — NO Prisma migration. Mirrors how `videoGenerateModelKey` etc are stored. **Contracts:** `packages/contracts/openapi.yaml` adds `talkingAvatarModelKey: string nullable` + `talkingAvatarFallbackModelKey: string nullable` next to `videoGenerateFallbackModelKey` on both `AdminPlanInputBase` and `AdminPlanState`; generated TS models hand-edited (no `orval generate` run — preserves the Slice 8/10b drift-isolation discipline). **Admin Plans editor:** `availableTalkingAvatarModelKeys: ModelOption[]` computed from `availableModelCatalogByProvider.heygen.models` filtered by `active === true` AND `kind === "talking_avatar"`. Split off from `rawVideoKeys` so HeyGen talking-avatar rows no longer appear in the cinematic selector. `PlanDraft` extended; `emptyDraft`/`planToDraft`/`draftToPayload`/`isPlanDraftDirty` round-trip the new fields. **Tests (15 new total across 6 files):** `assistant-api-client.test.ts` +4 URL-shape assertions (each of the 4 Slice 9 methods receives a fetch URL that does NOT contain the substring `api/v1/api/v1` AND DOES contain `/workspaces/<id>/video-personas`); `runtime-video-generate-tool.service.test.ts` +4 prompt-validation cases (talking_avatar without prompt → passes with placeholder; talking_avatar with prompt → user-provided value preserved; cinematic without prompt → throws; cinematic with empty-string prompt → throws) + 2 credential-routing cases (talking_avatar with credential ref resolves; talking_avatar without credential ref → `talking_avatar_provider_unavailable`); `materialize-assistant-published-version.service.test.ts` +5 cases (with/without secret, with/without toggle, with/without active HeyGen catalog row, `plan.talkingAvatarModelKey` set vs default Variant C fallback, voice/persona attachment on talking-avatar ref only); `manage-admin-plans.service.test.ts` +3 cases (cinematic row refused on `talkingAvatarModelKey`; talking_avatar row accepted on `talkingAvatarModelKey`; existing Slice 2b refusal still fires with updated message text); `native-tool-projection.test.ts` +2 cases (description includes talking-avatar block when ref present; description omits it when ref null); `admin/plans/page.test.tsx` +1 round-trip test (selectors save+load correctly). **Pre-existing technical-debt repair:** `apps/runtime/test/native-tool-projection.test.ts` was using an obsolete self-executing pattern and was NOT registered in `run-suite-isolated.ts`, so the 2 projection tests added in the prior Slice 10 session were never actually being executed by `pnpm test`. Slice 10c subagent exported `runNativeToolProjectionTest` and registered it in the suite runner — justified per AGENTS.md "no dead stubs" because the test file is part of the Slice 10c deliverable. **Subagent:** Claude Sonnet 4.6 medium thinking, single hire, clean exit. Subagent honored "no docs edits" rule (eleventh in a row to do so cleanly; orchestrator owned all SESSION-HANDOFF / CHANGELOG / ADR updates including the prior `39207360` E14 spec commit). **Verification (8/8 gates orchestrator-run):** lint (all 6 packages Done), format:check ("All matched files use Prettier code style!"), 3 typechecks (api/web/runtime-contract all exit 0), web suite **670/670** PASS across 65 files (was 665/665 pre-Slice-10c; +5 new), api full suite all assertions PASS, runtime full suite all assertions PASS. **Cross-slice invariants 1–15 re-verified true:** #11 ADR-107 carve-out preserved (no Runway/Kling/OpenAI provider-client edits); #12 no keyword routing (`talkingAvatarEnabled = ref !== null` structural; `=== true` boolean equality at every site; zero regex / fuzzy / phrase parsing on user text); #14 REST-only persona mutation preserved (only materializ-assistant reads personas via Slice 5b port; zero persona writes anywhere new in this slice); #15 NON-NEGOTIABLE (the `"Talking-avatar render"` placeholder is a literal — no `speechText` or user text concatenation; provider check is structural; voice-catalog lookup is exact equality). NO Prisma schema or migration. NO HeyGen API client changes (Slice 6 unchanged). NO `apps/provider-gateway/**` edits. NO new files except generated-TS hand-edits. NO `orval generate`. 17 files modified (13 src + 4 tests). Deploy: API + WEB + RUNTIME + CONTRACTS (mirrors Slice 8/10b deploy shape). The contract widening flows backward-compat — old API instances simply omit the new plan fields; web reads them as `null` and falls through to Variant C default. After this slice, talking-avatar workflows end-to-end on dev are unblocked: voices load, persona create round-trips, chat-initiated talking-avatar renders dispatch to HeyGen honestly. Slice 11 (live smoke + cross-doc updates + final ADR-109 closure) is unblocked.

- **ADR-109 Slice 10b — Talking-video banner UX (time-based) on the chat-input chip surface (`apps/api`, `apps/web`, `packages/contracts`; 2026-06-05):** landed the user-facing rotating banner copy for HeyGen talking-avatar renders so 1–5-minute waits no longer read as a hang. HeyGen's poll endpoint returns only `status: pending|processing|completed|failed` with no real progress/ETA, so the rotation is purely client-side time-based — honest UX about the wait, not a fake progress bar. Thresholds (per E3 erratum): `<30s` "Preparing avatar…" / "Готовим аватар…", `<120s` "Synthesizing voice…" / "Синтезируем голос…", `<300s` "Rendering video…" / "Видео рендерится…", `>=300s` "Final pass, almost there…" / "Финальный проход, скоро будет готово…". **Surface decision:** landed on the user-visible `activeMediaJobs` chip in `apps/web/app/app/_components/chat-input.tsx` (lines ~1058–1079 above the composer; ticks every 1s via the parent's `mediaJobNowMs` state) — NOT the `ActivityEvent` activity-badge feed, which is suppressed in production for video jobs via `HIDDEN_MEDIA_ACTIVITY_LABEL` in `use-chat.ts:345` (subagent's first pass honestly diagnosed this; orchestrator pivoted via same-subagent resume to the correct surface). **Detection field (option (b) from ADR E3 line 767, explicitly permitted by the spec):** new optional + nullable `displayKind: "cinematic" | "talking_avatar" | null` on `AssistantWebChatActiveMediaJobState`. OpenAPI source-of-truth in `packages/contracts/openapi.yaml` (enum, nullable, non-required, descriptive doc text); hand-edited generated TS `assistantWebChatActiveMediaJobState.ts` + new sibling enum file `assistantWebChatActiveMediaJobStateDisplayKind.ts` (orval-style mirror of `assistantWebChatActiveMediaJobStateOperation.ts` / `runtimeVideoModelKind.ts`); generated `model/index.ts` re-export; API-side internal type in `apps/api/src/modules/workspace-management/application/web-chat.types.ts`. **API mapper helper:** new `toWebOpenMediaJobDisplayKind({ requestJson })` in `apps/api/src/modules/workspace-management/application/assistant-media-job.service.ts` (sibling to existing `toWebOpenMediaJobOperation`); returns concrete union `"cinematic" | "talking_avatar"` (never undefined on the wire) derived structurally from `(requestJson as AssistantMediaJobRequestPayload | null)?.directToolExecution?.request?.mode === "talking_avatar"`; all defensive paths (non-video toolCode, missing requestJson, missing mode, explicit non-talking_avatar mode) return `"cinematic"`. Wired into the `listOpenJobsForWebChat` row mapper. **Web rendering:** `resolveMediaJobLabel(t, job, nowMs)` in `chat-input.tsx` signature gained `nowMs: number`; new pre-switch early-return: `if (job.operation === "video_generate" && job.displayKind === "talking_avatar")` computes `resolveMediaJobElapsedSeconds(job, nowMs)` and returns one of 4 i18n keys at `<30s`/`<120s`/`<300s`/`>=300s` thresholds. Legacy switch untouched; cinematic chip rendering byte-identical when `displayKind !== "talking_avatar"` (Kling/Runway/OpenAI/`displayKind: "cinematic"`/legacy missing/null). Chip call site (line ~1073) passes `mediaJobNowMs` as the third arg. The `formatDuration(...)` chip half (`resolveMediaJobElapsedSeconds(job, mediaJobNowMs)`) stays as-is. **i18n keys:** new flat keys `chatTalkingAvatarBannerStage1`/`Stage2`/`Stage3`/`Stage4` under the existing `chat` namespace in `en.json` + `ru.json` — chose flat per file convention (audit-pass precedent: `charactersWarnStorageFailedTitle`); ADR spec at line 774 suggested nested `chat.talkingAvatarBanner.stage*` but flat matches the file. **Tests (6 new total):** `apps/web/app/app/_components/chat-input.test.tsx` adds `describe("active media job chip — talking-avatar banner (Slice 10b)")` with 3 cases: (1) cinematic video job with explicit `displayKind: "cinematic"` shows `mediaJobVideoGenerate` legacy copy at t=10s AND t=10min (proves rotation does NOT apply to cinematic regardless of elapsed time); (2) talking-avatar job rotates Stage1→Stage2→Stage3→Stage4 at threshold boundaries (t=0s, t=31s, t=121s, t=301s) using `vi.useFakeTimers` + "fresh mount per stage" pattern; (3) legacy fixture without `displayKind` field defensively defaults to cinematic (validates defensive fallback at the React layer). `apps/api/test/assistant-media-job-open-context.test.ts` adds 3 new tests: talking_avatar `mode` → `displayKind === "talking_avatar"`; cinematic `mode` → `displayKind === "cinematic"`; three defensive-default rows in one stub (video without `mode`, fully null `requestJson`, image job) → all three resolve to `"cinematic"`. **Mid-slice pivot:** first subagent pass invested in adding `providerId` + `startedAtMs` + `useTalkingAvatarBannerStage` hook to `ActivityEvent` / `activity-badge.tsx`, then honestly reported the surface was suppressed in production (`HIDDEN_MEDIA_ACTIVITY_LABEL` for all video tool activities). Orchestrator resumed the same subagent with corrected scope — revert activity-badge changes (verified: zero `providerId`/`startedAtMs`/`useTalkingAvatarBannerStage` matches in `activity-badge.tsx`; file back to pre-pivot state with zero diff vs HEAD), keep the i18n keys (correct + needed), patch the chat-input chip surface, add the minimum DTO field. Both passes preserved all 15 cross-slice invariants. Orchestrator's initial prompt had been too strict on "no contract changes" — the ADR E3 spec line 767 explicitly permits the `displayKind` DTO field as one of two detection options. **Cross-slice invariants 1–15 verified true:** #1 cinematic UX byte-identical (Tests 1 + 3 confirm at t=10s, t=10min, and with missing field; switch block in `resolveMediaJobLabel` is unchanged); #11 ADR-107 carve-out preserved (no Runway/Kling/OpenAI client edits); #12 no keyword routing (boolean `===` equality on enum strings throughout; no regex/fuzzy/keyword on user input anywhere); #14 REST-only persona mutation N/A (no persona code touched); #15 NON-NEGOTIABLE (strict structural enum equality; defensive type-safe casts on `requestJson` paths). Subagent: Claude Sonnet 4.6 medium thinking; resumed once for the surface pivot; honored "no docs edits" rule (tenth subagent in a row to do so cleanly). Verification (6/6 gates orchestrator-run): lint (all 6 packages Done), format:check ("All matched files use Prettier code style!"), 3 typechecks (web/api/contracts all exit 0), web suite **665/665** PASS across 65 files (was 662/662 pre-Slice-10b; +3 chat-input tests), api full suite all assertions passed across all suites including the 3 new mapper assertions. NO `apps/runtime/**` edits, NO `apps/provider-gateway/**` edits, NO Prisma schema/migration, NO module wiring changes, NO `orval generate` run, NO new Runtime DTO field. 10 modified files + 1 new generated enum file. Deploy: API + WEB + CONTRACTS (the OpenAPI/TS contract widening flows backward-compat — old API instances simply omit the field; web reads `undefined`/`null` and falls through to the cinematic legacy switch byte-identical; fully consistent rotation only when both API and web are on Slice 10b). After this slice, only **Slice 11 (tests + docs + verification + live smoke)** remains in ADR-109.

- **ADR-109 Audit-Pass — full independent audit of slices 0–10 + targeted reconciliation (`apps/api`, `apps/web`, `apps/provider-gateway`, `apps/runtime`, `packages/contracts`, `docs/ADR/109-heygen-talking-avatar-on-vcoin.md`; 2026-06-05):** completed a 4-agent independent audit of ADR-109 (slices 0–10) at operator request "найти баги хвосты и параллельные пути почистить все". 4 read-only audit subagents (Claude Opus 4.7) ran in parallel scoped to (1) backend substrate integrity, (2) architectural invariants + keyword routing + dead code, (3) contracts + types + parallel paths, (4) web UI + i18n + error mapping; produced 66 findings (8 BUG, 14 HOLE, 4 PARALLEL, 12 STALE, 1 DRIFT, 27 MINOR) across all 11 completed slices. Orchestrator triaged to **8 must-fix items** and 4 acknowledged-limitation entries; landed via 2 parallel write-capable fixup subagents (Claude Sonnet 4.6 medium thinking per operator's "use fast agents where no risk" directive). **Backend / contract fixes (Fixup A):** (1) **`heygenAvatarId` removed from user-facing list API** — `manage-workspace-video-personas.service.ts::toListItem` mapper drops the field; `PersonaListItem` type updated; OpenAPI `WorkspaceVideoPersonaState.heygenAvatarId` property removed (description updated to clarify this is the user-facing shape); generated TS `workspaceVideoPersonaState.ts` field dropped with invariant #5 doc comment; web `PersonaListItemDto` field dropped. Internal-runtime persona read endpoint continues to return `heygenAvatarId` + `heygenVoiceId` (runtime needs them, platform-token-authenticated, not user-visible). Closes invariant #5 violation. (2) **HeyGen 4xx vs 5xx classifier at provider-gateway** — new exported `HeyGenProviderClientError { code, httpStatus, providerMessage }` class in `heygen-provider.client.ts`; `createPhotoAvatar` throws it on non-OK responses with the HTTP status preserved. `provider-heygen-avatars.service.ts` catch block inspects `httpStatus`: 4xx → `BadRequestException(heygen_avatar_create_failed)`, 5xx / network / timeout → `ServiceUnavailableException(heygen_unavailable)`. Pre-audit, the gateway wrapped EVERY HeyGen failure in 503, collapsing HeyGen bad-input rejections into "transport outage" at the API. (3) **Concurrent persona-create wallet over-debit closed** — `manage-workspace-video-personas.service.ts` replaces the read-then-debit pair (`findUnique` + `vcoinBalanceRepository.debit`) with a single atomic conditional `tx.workspaceVcoinBalance.updateMany({ where: { workspaceId, balanceVc: { gte: cost } }, data: { balanceVc: { decrement: cost } } })`. If `updateMany.count === 0`, throws `vcoin_balance_exhausted` honestly. Race window collapses: only one concurrent debit can succeed. (4) **Orphan-avatar warning widened to all tx failure modes** — previously gated on `_personaError` (limit/duplicate/balance) `BadRequestException` only; now fires for any error caught after the HeyGen `createPhotoAvatar` call succeeds (constraint errors, infra errors, unexpected debit failures). Original error always re-thrown; warning format unchanged for greppability. (5) **`UserPlanVisibilityEntitlements.talkingVideoEnabled` tightened to required** — OpenAPI already had it required with `default: false`; hand-edited generated TS had it as `talkingVideoEnabled?: boolean`. Updated to `talkingVideoEnabled: boolean` (required). Prevents the next `orval generate` from flipping requiredness; `resolve-plan-visibility.service.ts` already sets the field explicitly on every path so no runtime change needed. (6) **6 stale slice-reference comments cleaned up** — `TODO(slice8)` block in `runtime-video-generate-tool.service.ts` (Slice 8 landed); "Slice 8 territory" comment for multi-character rule (Slice 10 owns it LLM-side); `buildGatewayTalkingAvatarFields` "until Slice 6" comment (Slice 6 landed); `provider-video-generation.service.ts` "Slice 2a placeholder until Slice 6" comment (Slice 6 landed); Prisma `WorkspaceVideoPersona.heygenAvatarId` doc comment ("NULL at creation; Slice 6 will lazily populate" → "Set eagerly on persona POST per E12. NEVER exposed on user-facing API"); Prisma archived-row name reuse comment ("Slice 6 will address" → "Known limitation acknowledged in ADR-109"). **Web UI fixes (Fixup B):** (1) **Error envelope parsing aligned across 4 client methods** — pre-audit, `assistant-api-client.ts::createWorkspaceVideoPersona` read `body?.code` from non-OK responses, but the API's `ApiExceptionFilter` returns `{ error: { code, category, message } }`. Result: ALL persona-create error code mappings (`heygen_unavailable`, `heygen_avatar_create_failed`, `persona_limit_reached`, `persona_duplicate_name`, `voice_not_found`, `vcoin_balance_exhausted`) were unreachable — users saw a generic `create_failed` toast for every kind of failure. Audit-pass aligns all 4 Slice 9 methods (`getWorkspaceVideoPersonas`, `getWorkspaceVoiceCatalog`, `createWorkspaceVideoPersona`, `deleteWorkspaceVideoPersona`) on the pre-existing `readApiErrorEnvelope(response)` helper + `throw new ApiStructuredError(envelope.message, envelope.code, envelope.details)`. `ApiStructuredError` newly exported from `assistant-api-client.ts`. UI catch block uses `err instanceof ApiStructuredError ? err.code : null` so all 6 mapped i18n branches now flow through end-to-end. (2) **E4 locked Characters now shows real workspace personas (disabled)** — pre-audit, `loadPersonas()` was gated on `talkingVideoEnabled === true`, so locked state rendered only a mock disabled "Маша" card; real workspace personas were never fetched. Violated E4 "persona cards visible but disabled — even existing personas". Audit-pass removes the gate from `loadPersonas`; locked state renders the real persona list as `opacity-60` disabled cards with NO create/edit/delete affordances and NO voice preview (preview button receives `previewAudioUrl={null}`). Zero-persona locked state shows section header + upsell hint + zero cards (mock placeholder removed). Voice catalog fetch stays gated on the unlocked branch. (3) **`storageWarning: "persona_created_storage_failed"` now surfaced honestly** — pre-audit, the API returned HTTP 200 with `{ persona, storageWarning?: "persona_created_storage_failed" }` when persona creation succeeded but post-tx portrait storage failed; UI ignored `storageWarning` and showed unconditional success toast (user saw a persona with a broken/missing portrait silently). Audit-pass branches the create-success path on `result.storageWarning === "persona_created_storage_failed"` and renders a new amber warning `FeedbackLine` (added `ActionFeedback` widened from `"ok"|"err"` to `"ok"|"err"|"warn"`). New i18n keys: `charactersWarnStorageFailedTitle` ("Persona created, portrait failed" / "Персонаж создан, портрет — нет") and `charactersWarnStorageFailedMessage` ("{name} was created but the portrait could not be saved..." / "{name} создан, но портрет не сохранён..."). **Tests:** Fixup A added Tests 15–17 in `manage-workspace-video-personas.service.test.ts` (list strips `heygenAvatarId`; conditional debit race; non-guard infra error orphan path); `updateMany` Prisma stub added; Test 1 updated to track conditional debit; Tests 7–8 in `provider-heygen-avatars.service.test.ts` (4xx → BadRequest, 5xx → ServiceUnavailable). Fixup B added 4 new test cases in `assistant-settings.test.tsx` (locked state with real personas; `storageWarning` warning feedback; `persona_limit_reached` mapped to i18n message; `persona_duplicate_name` mapped to i18n message); inverted 2 existing locked-state tests to assert `getWorkspaceVideoPersonas` IS called; updated 5 mock persona objects to drop `heygenAvatarId`; added `talkingVideoEnabled: false` to base entitlements mock. Web suite went from 658/658 → **662/662 PASS** across 65 files. **Doc reconciliation (orchestrator-side):** ADR-109 `feature_unavailable` → `talking_avatar_plan_disabled` in § Plan toggle, § Acceptance checklist item 8, and Slice 9 spec text (E13.1). `needs_disambiguation` acceptance-checklist item removed (E8 architecturally unreachable per Slice 10 status, no chat disambiguation card UI was built). New E13 erratum block appended (12 numbered sub-items E13.1–E13.12 + 4 acknowledged limitations) documenting all audit reconciliations: canonical error codes (`talking_avatar_plan_disabled`, `vcoin_balance_exhausted`, `talking_avatar_persona_unavailable`), `heygenAvatarId` user-facing removal, gateway 4xx semantics, conditional debit, orphan warning widening, error envelope parsing, E4 locked state, `storageWarning` surfacing, requiredness alignment, stale comment cleanup. Acknowledged limitations: archived personas retain HeyGen-side avatar slots; `RuntimeVideoVoiceCatalog` has no schema tag; `persai.runtimeVideoPersonaCatalog.v1` written but never validated; lazy-create branch unreachable from production; web client uses hand-typed DTOs (not orval). **Verification (9 gates orchestrator-run):** lint, format:check, 4 typechecks (api/web/runtime/provider-gateway), Fixup A focused tests (API + provider-gateway + full API suite), Fixup B web test suite (662/662). All PASS. **Cross-slice invariants 1–15 re-verified true** post-fixup with explicit greps: #5 (zero `heygenAvatarId` matches in `apps/web/**` outside test fixtures and the internal-only DTO note), #12 (zero keyword-routing patterns in new diff), #14 (zero `personaRepository.(create|update|archive)` in `apps/runtime/src/**`), #15 (all flag checks remain strict `=== true` / `=== false` equality). NO Prisma schema or migration changes. NO module wiring changes. NO `orval generate` run. NO new files. 16 files modified total. Subagents: 4 read-only audits (Claude Opus 4.7 default — no model override) + 2 write fixups (Claude Sonnet 4.6 medium thinking). Both fixup subagents honored "no docs edits" rule (eighth and ninth in a row to do so cleanly); orchestrator owned all doc updates. **After this pass, ADR-109 has no known bugs, no known invariant violations, and 4 acknowledged limitations documented in E13.** Slice 11 (live smoke + E2E + cross-doc updates) is the only remaining work and is unblocked.

- **ADR-109 Slice 10 — Chat UX for talking video (tool description + persona materialization substrate) (`apps/api`, `apps/runtime`, `packages/runtime-contract`, `packages/runtime-bundle`; 2026-06-05):** landed the LLM-facing context for talking-avatar workflows — the model now sees the workspace persona shortlist inline in the `video_generate` tool description plus a 7-section structured instruction block when `talkingVideoEnabled === true`. **Substrate:** new `RuntimeVideoPersonaCatalogEntry` (minimal shape `{ personaId, displayName, voiceLabel }` — no portrait URL, no voiceLanguage BCP-47 normalization), `RuntimeVideoPersonaCatalog`, and `isRuntimeVideoPersonaCatalog` type guard in `@persai/runtime-contract`; new `videoPersonaCatalog?: RuntimeVideoPersonaCatalog | null` field on `AssistantRuntimeBundleToolCredentialRef` in `@persai/runtime-bundle` mirroring `videoVoiceCatalog` exactly. **Materialization:** new private `attachMaterializedVideoPersonaCatalog(ref, workspaceId, talkingVideoEnabled)` method on `MaterializeAssistantPublishedVersionService` gated by exact `providerId === "heygen"` AND `talkingVideoEnabled === true`; reads active personas via the existing Slice 5b `WorkspaceVideoPersonaRepository.listActive(workspaceId)` port; chronological order preserved; attaches `{ provider: "heygen", schema: "persai.runtimeVideoPersonaCatalog.v1", personas: [] }` honestly when workspace has zero active personas; wired into `resolveRuntimeToolCredentialRefs` right after the existing `attachMaterializedVideoVoiceCatalog` so the bundle carries both catalogs on the same credential ref. `resolveRuntimeToolCredentialRefs` signature extended with `workspaceId` + `talkingVideoEnabled` params. Repository injection reuses the `WORKSPACE_VIDEO_PERSONA_REPOSITORY` already registered for Slice 7's read path. **Tool description:** new `describeVideoPersonaCatalogHint(credential)` helper in `native-tool-projection.ts` renders the inline persona shortlist as a readable table (`- personaId="<uuid>", displayName="<name>", voiceLabel="<label>"` per line, capped at 10 entries defensively even though the Slice 5 platform limit defaults to 10); or `"Available saved characters (videoPersonas): none yet. Suggest the user create one via Settings → Characters when they want a named character."` when empty. `createVideoGenerateToolDefinition(policy, credential, talkingVideoEnabled)` extended (Slice 8 added the `talkingVideoEnabled` parameter; Slice 10 fills the previously-1-line stub with the full 7-section block): (1) when to use `mode='talking_avatar'` — structural triggers: explicit speaking-video request + portrait OR named persona; `mode='cinematic'` default for everything else; (2) persona resolution from the inline `videoPersonas` block via exact case-insensitive `displayName` match (LLM-side, structural; leverages Slice 5's unique-name guarantee — name match is always unambiguous within a workspace); (3) "You cannot create personas yourself" — directs users to Settings → Characters (preserves invariant #14, ADR-109 erratum E1); (4) single-character-per-call rule with multi-speaker-→-multi-clip guidance (replaces the operator-superseded Slice 3 multi-character refusal in code with LLM-side instruction — invariant #15 honored); (5) voice selection on portrait path (model picks from voice shortlist by context; `voice_required` failure if omitted per erratum E9); (6) voice selection on persona path (omit `voiceKey` to use persona's stored voice; only pass to deliberately override); (7) inline persona shortlist table or "none yet" stub. Total description char count for canonical fixture (talkingVideoEnabled=true, 1-voice shortlist, 2 personas): 3,566 chars (was 3,612 before the orchestrator fixup below). **E8 `needs_disambiguation` is architecturally unreachable** given Slice 5's `UNIQUE INDEX (workspaceId, displayNameLower)` — duplicate persona names cannot exist within a workspace, so the LLM's exact name lookup is always unambiguous. NO new discriminated-union member added to `RuntimeVideoGenerateToolResult`; NO chat-side disambiguation card UI built; NO `<PersonaCard>` chat component. Documented in the ADR's Slice 10 status block. **Orchestrator fixup mid-slice (architectural cleanup):** subagent's first pass introduced `resolveVoiceLanguageFromLabel` with a 16-entry hardcoded language-keyword prefix-match table (`["Russian", "ru-RU"], ["English", "en-US"], …` + `lower.startsWith(lang.toLowerCase())`) used to derive a display-only `voiceLanguage` BCP-47 hint on each persona catalog entry. Strictly not a #15 violation (input is HeyGen's structured voice label, not user input; output is display-only doesn't drive behavior). But the prefix-match-against-keyword-list pattern is exactly what the operator has repeatedly said to avoid ("честно и чисто"). Orchestrator resumed the subagent and instructed to drop the entire `voiceLanguage` field rather than fix the lookup — the LLM understands "Russian (Female)" via `voiceLabel` without normalization; the BCP-47 code was redundant. Fixup removed: `voiceLanguage` from contract type, materialization map, description renderer, all test fixtures. `resolveVoiceLanguageFromLabel` and `VOICE_LABEL_TO_LANGUAGE` no longer exist anywhere in the codebase (verified via explicit grep across `materialize-assistant-published-version.service.ts`). Final catalog entry shape: `{ personaId, displayName, voiceLabel }` — minimal LLM-facing surface. Subagent: Claude Sonnet 4.6 medium thinking; single hire + one resume for the fixup; honored "no docs edits" rule both passes (seventh subagent in a row). **Tests:** `apps/runtime/test/native-tool-projection.test.ts` +5 Slice 10 cases + helper `makeHeygenTalkingBundle` (talking enabled with 2-persona catalog asserts all 7 description sections + per-persona lines + voice shortlist hint coexistence; empty catalog → "none yet" stub appears; undefined catalog → defensive default same as empty; talking disabled → none of the 7 sections appear; snapshot via substring assertions for diff-visibility per spec); `apps/api/test/materialize-assistant-published-version.service.test.ts` +4 Slice 10 cases simulating `attachMaterializedVideoPersonaCatalog` gate logic (HeyGen+enabled+2personas → catalog attached with exact shape; HeyGen+disabled → no catalog field; HeyGen+enabled+0personas → empty-personas catalog attached; non-HeyGen → no catalog regardless of toggle). All Slice 5b/7/8/9 regressions clean. Verification: 12/12 gates green (lint, format:check, 5 typechecks, 4 focused tests + Slice 5b regression + Slice 7 regression + full web suite 658/658 PASS across 65 files). **One transient flaky web run** mid-verification showed `3 failed | 655 passed` at elapsed time 106s vs the healthy 61s — confirmed not a Slice 10 regression on re-run (the 3 failures were unrelated tests in pre-existing files); re-run was 658/658 clean. Documented in handoff for future calibration. **Cross-slice invariants 1–15 verified true:** #11 ADR-107 carve-out preserved (no Runway/Kling/OpenAI provider-client edits); #12 no keyword routing (LLM does name → personaId resolution from materialized shortlist; explicit grep confirms zero `.match`/`.includes`/regex on user input anywhere new; the original keyword-prefix-match wart from first subagent pass was removed before commit); #14 REST-only persona mutation preserved (materialization READS personas only via `listActive(workspaceId)`; explicit grep across `materialize-*` confirms zero `personaRepository.(create|update|archive)` calls); #15 NON-NEGOTIABLE (exact `=== "heygen"` and `=== true` equality checks throughout). NO `apps/provider-gateway/**` edits. NO `apps/web/**` edits (this slice is server-side only — no chat UI changes per canonical Slice 10 spec). NO HeyGen API calls. NO new tools registered. NO Prisma schema/migration. NO `orval generate` run. NO chat-side UI changes. Deploy: API + RUNTIME + CONTRACTS (the runtime contract widening flows through generated types; backward-compat ensures stale runtime instances reading old materialized bundles still work — they see `videoPersonaCatalog` as undefined which renders the "none yet" stub). After this slice, the LLM has all the context it needs to drive talking-video workflows end-to-end: it sees the workspace persona list inline, knows when to use `talking_avatar` vs `cinematic`, knows to direct users to Settings → Characters for persona creation, and knows the single-speaker constraint. Slice 11 (live smoke + E2E + cross-doc updates) is unblocked.

- **ADR-109 Slice 9 — Assistant Settings UI: Characters (locked-with-upsell + unlocked persona management) + 3 substrate additions to expose existing data to user-facing UI (`apps/web`, `apps/api`, `packages/contracts`; 2026-06-05):** landed the FIRST user-visible HeyGen talking-avatar UI surface — end-users can now manage their workspace's video personas from the assistant settings page. Three substrate additions expose existing platform data through user-facing API surfaces. (1) `UserPlanVisibilityEntitlements.talkingVideoEnabled: boolean` — defensive structural read of `billingProviderHints.talkingVideoEnabled === true` from `resolve-plan-visibility.service.ts`; default `false` for legacy plans; exposes the Slice 8 plan toggle to the user-facing visibility API so the UI can choose locked-with-upsell vs unlocked mode. (2) New workspace-scoped voice catalog endpoint `GET /api/v1/workspaces/:workspaceId/video-personas/voice-catalog` on the existing `WorkspaceVideoPersonasController`; new application service `ReadHeygenVoiceCatalogForWorkspaceService` wraps the platform `HeyGenVoiceCatalogService.getMaterializedVoiceCatalog()` and re-projects to UI shape (`{ voiceId, name, language, gender, previewAudioUrl }`); workspace ID accepted at controller layer for auth-scoping only (data is platform-wide); returns empty `voices: []` (HTTP 200) honestly when the Slice 4 cache is unavailable (no HeyGen credential or empty shortlist). (3) `WorkspaceVideoPersonaListState.creationVcoinCost: integer` — sourced from `PlatformRuntimeProviderSettings.heygenPersonaCreationVcoin` (Slice 5 platform setting); `ManageWorkspaceVideoPersonasService.listPersonas` extended to read and return it so the UI renders "Create for N VC" without a second roundtrip. **New shared web component** `apps/web/app/_components/voice-preview-button.tsx`: HTML5 `<audio>` playback with module-level coordination (only one preview plays at a time across all instances via module-level `currentlyPlayingAudio` + `currentlyPlayingSetPlaying` refs, no Redux/context); when `previewAudioUrl` is non-null AND non-empty → active Play/Pause toggle button (Lucide `Play`/`Pause` icons); when null OR empty → grey disabled icon (`aria-disabled="true"`, `cursor-not-allowed`, `opacity-40`) with tooltip "Preview unavailable" — **NO TTS fallback substrate** in this slice (per operator directive — voices without native HeyGen `previewAudioUrl` show grey disabled icon; Slice 9b may add `POST /v3/voices/speech` blob caching later if real-world voices have a high null-rate). **New Settings UI section** in `apps/web/app/app/_components/assistant-settings.tsx` between Character (1) and Limits (2), with two visual states gated structurally on `data.plan?.entitlements?.talkingVideoEnabled === true`. **Locked-with-upsell state** (when `false`/missing per E4): section header in normal color + italic upsell hint "Доступно на тарифе X+" / "Available on Plan X+" with inactive-styled `/pricing` link + 1 mock disabled demo card ("Маша" with gray-circle placeholder portrait, voice label "Russian (Female)", `opacity-60`, no buttons) + banner "Эти персонажи будут доступны при активации тарифа" / "These characters will be usable again when the plan is reactivated". Conversion-oriented but quiet per E4 "не шумно". **Unlocked state** (when `true`): persona list fetched via `getWorkspaceVideoPersonas` with portrait/name/voice-label/preview-button per card (preview button looks up `previewAudioUrl` by exact `voices.find(v => v.voiceId === persona.heygenVoiceId)` against single-fetched voice catalog — no N+1); top-right "Create persona" button (disabled with tooltip "Достигнут лимит персонажей ({limit})" when `personas.length >= limit`); Create modal (drag-and-drop portrait upload + name input + voice picker dropdown with inline `VoicePreviewButton` per option + VC cost line "Cost: N VC. Balance: M VC. After: (M-N) VC" + submit disabled with "Insufficient balance — top up" link to `/app/packages` when `balance < creationVcoinCost`); Delete confirm modal "Удалить персонажа «{name}»? VC не возвращаются." → `DELETE /api/v1/workspaces/:workspaceId/video-personas/:personaId` → refresh list. Honest error code → UI message mapping for `persona_limit_reached`, `persona_duplicate_name`, `voice_not_found`, `insufficient_balance`, `heygen_avatar_create_failed`, `heygen_unavailable`. **New web client methods** in `apps/web/app/app/assistant-api-client.ts`: `getWorkspaceVideoPersonas`, `getWorkspaceVoiceCatalog`, `createWorkspaceVideoPersona` (multipart `FormData`, mirroring existing `uploadAssistantAvatar` pattern), `deleteWorkspaceVideoPersona`. **i18n** keys under `settings.characters.*` namespace in both `apps/web/messages/en.json` and `ru.json` (title, lockedHint, lockedBanner, create, empty, deleteConfirm, form.name/portrait/voice/cost/insufficient/limitReached, errors.duplicateName/voiceNotFound/heygenUnavailable, preview.unavailable, mockPersonaName/mockPersonaVoice). OpenAPI: extended `UserPlanVisibilityEntitlements` with `talkingVideoEnabled`, added `/workspaces/{workspaceId}/video-personas/voice-catalog` GET path, added `WorkspaceHeygenVoiceCatalogState` + `WorkspaceHeygenVoiceCatalogEntry` schemas, extended `WorkspaceVideoPersonaListState` with `creationVcoinCost`. Generated TS models: 2 new files (`workspaceHeygenVoiceCatalogEntry.ts`, `workspaceHeygenVoiceCatalogState.ts`) + `userPlanVisibilityEntitlements.ts`/`workspaceVideoPersonaListState.ts` augmented + `model/index.ts` updated. NO `orval generate` run (drift-isolation discipline preserved). **Tests:** `resolve-plan-visibility-vcoin.test.ts` +4 cases (`talkingVideoEnabled: true` from hints / `false` / missing → defaults `false` / `billingProviderHints: null` → defaults `false`); `read-heygen-voice-catalog-for-workspace.service.test.ts` 4 assertions (happy path with non-empty cache, null catalog, empty voices array, entry shape includes `previewAudioUrl` when present and `null` otherwise); `voice-preview-button.test.tsx` 5 cases (active button when URL non-null, disabled when URL null, disabled when URL empty string, click triggers `audio.play()`, second click pauses); `assistant-settings.test.tsx` +8 cases under `describe("characters section")` (State A locked with no plan, State A locked with `talkingVideoEnabled: false`, State B empty with create button enabled, State B with 2 personas rendered, Create modal opens, insufficient balance disables submit, persona limit disables top-level Create button, delete flow triggers DELETE call). **Subagent honesty wart:** Claude Sonnet 4.6 medium thinking; single run; honored "no docs edits" rule (sixth subagent in a row to do so cleanly); the response summary contained "From prior session" hallucinated phrasing for files actually created in THIS session (similar to Slice 5b's "previous implementation attempt" hallucination); orchestrator verified all 19 files actually got created/modified correctly with 1427 insertions; no impact on functionality. Verification: 12/12 gates green (lint, format:check, 5 typechecks, 4 focused tests + Slice 5b regression + Slice 7 regression + full web suite 658/658 PASS across 65 test files — was 643/643 pre-Slice-9, +15 new test cases). **Cross-slice invariants 1–15 verified true:** #11 ADR-107 carve-out preserved (no Runway/Kling/OpenAI provider-client edits); #12 no keyword routing (boolean `=== true` equality everywhere; persona-name uniqueness delegated to existing REST API structural check; voice picker matches by exact `voiceId === heygenVoiceId`); #14 REST-only persona mutation preserved (UI calls REST endpoints only via `createWorkspaceVideoPersona`/`deleteWorkspaceVideoPersona`; no runtime persona writes); #15 NON-NEGOTIABLE (voice catalog lookup is exact equality `find((v) => v.voiceId === persona.heygenVoiceId)`; flag check is structural `=== true`; zero regex/fuzzy/keyword match anywhere new). NO `apps/runtime/**` edits. NO `apps/provider-gateway/**` edits. NO HeyGen API calls from web (all HeyGen interaction stays in API service via existing Slice 5b flow). NO new tools registered. NO Prisma schema/migration changes. NO feature flag. Deploy: API + WEB + CONTRACTS (all touched workspaces ship together). After this slice, end-users on plans with `talkingVideoEnabled: true` can create/list/delete personas via Settings without leaving the assistant settings page; end-users on plans with the toggle off see the feature exists as a quiet conversion hint via the locked-with-upsell mock card. Slice 10 (chat UX + tool description) is unblocked and can now teach the LLM about persona usage in conversations.

- **ADR-109 Slice 8 — Plan toggle `talkingVideoEnabled` + materialization gate + LLM tool-schema projection (`apps/api`, `apps/web`, `apps/runtime`, `packages/runtime-contract`, `packages/contracts`; 2026-06-05):** wired the per-plan `talkingVideoEnabled` boolean end-to-end so operators can enable/disable HeyGen talking-avatar mode for each plan. New field on `AdminPlanInput` + `AdminPlanState` (persisted into the existing `billingProviderHints` JSON column at the top level under the `talkingVideoEnabled` key — no new Prisma column, default `false` for legacy plans). New `parseBooleanInput(value, fieldName)` helper in `manage-admin-plans.service.ts` (defaults `null`/`undefined` to `false`; throws on non-boolean); input parser, `toWriteInput`, and `toAdminPlanState` round-trip the flag through `billingProviderHints`. Slice 2b capability-refusal message updated `(Slice 9)` → `(Slice 8)` since this slice IS the toggle landing. Admin Plans editor (`apps/web/app/admin/plans/page.tsx`) renders a sibling checkbox next to the existing `videoGenerateModelKey` / `videoGenerateFallbackModelKey` fields; `PlanDraft` type, default draft, load-from-plan path, save payload, sub-component props, and `isPlanDraftDirty` all carry the new field. Materialization (`materialize-assistant-published-version.service.ts`) gained new private `resolvePlanTalkingVideoEnabled(planCode)` (mirrors the existing `resolvePlanBillingHintString` pattern with a defensive structural check `(hints as Record<string, unknown>).talkingVideoEnabled === true`); the resolved `toolPolicies` are post-processed via `.map(p => p.toolCode === "video_generate" ? { ...p, talkingVideoEnabled: planTalkingVideoEnabled } : p)` to inject the flag onto the bundle's `video_generate` policy specifically. Runtime contract: `RuntimeToolPolicy` in `@persai/runtime-contract` gained typed `talkingVideoEnabled?: boolean` (optional for backward compat — Slice 7's defensive cast `(policy as unknown as Record<string, unknown>).talkingVideoEnabled` still works). LLM-facing tool projection (`native-tool-projection.ts`) is now gated structurally on the flag in TWO places: (1) the Slice 2b HeyGen filter `!isTalkingAvatarVideoProvider(providerId)` is OR-ed with `talkingVideoEnabled` — HeyGen-credentialed assistants are projected as `video_generate` ONLY when the operator enabled the toggle; (2) `createVideoGenerateToolDefinition(policy, credential, talkingVideoEnabled)` conditionally spreads the 6 talking-avatar JSON-schema properties (`mode`, `speechText`, `speechLanguage`, `personaId`, `portraitImageAlias`, `voiceKey`) AND the talking-avatar description hint into the tool definition only when the flag is `true`. When `false`/missing/undefined, the LLM sees the pre-Slice-3 cinematic-only surface — no description mention, no schema fields. OpenAPI: `AdminPlanState` + `AdminPlanInputBase` schemas gained `talkingVideoEnabled: boolean (default false)`; generated TS models (`adminPlanState.ts`, `adminPlanInputBase.ts`) hand-edited (no `orval generate` run, preserves Slice 2a's drift-isolation discipline). Tests: `manage-admin-plans.service.test.ts` +4 assertions (parse true / parse false / missing→false / non-boolean→throw); `materialize-assistant-published-version.service.test.ts` +4 assertions (toggle true materializes / toggle false materializes / legacy bundle absence → undefined permissive / explicit `false` write for legacy plans); `native-tool-projection.test.ts` +3 scenarios (HeyGen + `true` projects all 6 fields + description copy; Runway + `false` stays cinematic-only; undefined defaults to cinematic-only); `apps/web/app/admin/plans/page.test.tsx` +4 assertions (legacy default `false` / `true` round-trip / `false` round-trip / `isPlanDraftDirty` detects toggle change) plus fixed existing `ToolActivationsEdit` render call that was missing the new required props. Slice 7's TODO-stub gate (`policy.talkingVideoEnabled === false` → `talking_avatar_plan_disabled`) is no longer a stub — the materialization writes the flag and the runtime gate fires automatically when an operator disables the toggle on a plan; `mode: "talking_avatar"` requests get blocked with `talking_avatar_plan_disabled` honestly. Subagent: Claude Sonnet 4.6 medium thinking; single run; honored "no docs edits" rule (fifth subagent in a row to do so cleanly). Subagent's summary claimed one pre-existing flaky failure in `use-chat.test.tsx > soft-detach resume refresh` — orchestrator re-verified on (a) the clean Slice 7 baseline (with Slice 8 stashed) and (b) Slice 8 applied: `use-chat.test.tsx` passes 82/82 in BOTH states, and the full web suite passes 643/643 (64 files) with Slice 8. Subagent claim was a hallucination; documented for future calibration. Verification: 12/12 gates green (lint, format:check, 5 typechecks, 4 focused tests + Slice 7 regression + full web suite). **Cross-slice invariants 1–15 verified true:** #11 ADR-107 carve-out preserved (no Runway/Kling/OpenAI provider-client edits); #12 no keyword routing (boolean check is strict `=== true` equality at every site: projection, materialization, runtime gate; zero regex / fuzzy / phrase parsing); #14 REST-only persona mutation preserved (`apps/runtime/src/**` untouched for persona writes; only `native-tool-projection.ts` adjusted, no persona-table interaction); #15 NON-NEGOTIABLE (flag-presence check is purely structural). NO `apps/provider-gateway/**` changes (Slice 6 ownership preserved). NO HeyGen API calls. NO new tool registered (only existing `video_generate` projection adjusted). NO Prisma schema change (uses existing `billingProviderHints` JSON column). NO migration. NO feature flag. Deploy: API + WEB + RUNTIME + CONTRACTS (all touched workspaces ship together; runtime contract widening backward-compat ensures stale runtime instances reading old materialized bundles work — they just see `talkingVideoEnabled` as `undefined`/permissive, which is the original Slice 7 default-on behavior). After this slice, operators can per-plan enable/disable talking-avatar without code changes; the LLM tool surface adapts to the plan toggle automatically at bundle materialization time. Slice 9 (Assistant Settings UI: Characters) is unblocked and can now read the plan toggle to decide locked-with-upsell vs full persona management mode.

- **ADR-109 Slice 7 — Runtime talking_avatar execution (persona / portrait-alias resolution, HeyGen dispatch, plan toggle TODO-stub) (`apps/runtime`, `apps/api`; 2026-06-05):** wired the runtime execution path for `mode === "talking_avatar"` end-to-end. `runtime-video-generate-tool.service.ts` now branches immediately after Slice 3 validation succeeds for talking-avatar requests, invoking a new private `executeTalkingAvatarDispatch` helper (~130 LOC). Helper performs (in order): (1) structural HeyGen provider check via the existing `isTalkingAvatarVideoProvider(providerId)` helper (Slice 2b) — no fallback to Kling/Runway/OpenAI; fails honestly with `talking_avatar_provider_unavailable` when provider isn't HeyGen; (2) plan toggle TODO-stub — reads `talkingVideoEnabled` from the materialized tool policy, blocks ONLY when the field is exactly `=== false` (missing / `undefined` / `true` is permissive — Slice 8 will land the materialization side); (3) branches on the Slice 3 XOR(`personaId`, `portraitImageAlias`); (4a) **persona path**: fetches the persona row via new read-only internal API call (`GET /api/v1/internal/runtime/workspaces/:workspaceId/video-personas/:personaId` with `Authorization: Bearer <PERSAI_INTERNAL_API_TOKEN>` fail-closed auth), uses `persona.heygenAvatarId` (always populated post-E12) for `cachedHeygenAvatarId`, resolves voice as either explicit `voiceKey` validated against the materialized HeyGen shortlist OR persona's stored `heygenVoiceId` (already validated at persona create time by Slice 5b), sets `portraitImageBytesBase64: null` (HeyGen's `type: "avatar"` doesn't need portrait at render); (4b) **portrait alias path**: resolves alias via existing media-alias path, sets `portraitImageBytesBase64` and `portraitImageMimeType` from the resolved bytes, `cachedHeygenAvatarId: null`, requires explicit `voiceKey` (no persona to fall back to — fails with `voice_required` per erratum E9); (5) dispatches via existing `provider-gateway.client.service.ts::generateVideo` which Slice 6 already wired to route `credential.providerId === "heygen"` to `HeyGenProviderClient.generateVideo`. The legacy `prompt` field on the gateway DTO mirrors `speechText` defensively to satisfy existing type shape without changing Slice 6 client behavior. Result echo (Slice 3 erratum E8) preserved — all `requested*` fields (`requestedSpeechText`, `requestedSpeechLanguage`, `requestedPersonaId`, `requestedPortraitImageAlias`, `requestedVoiceKey`) round-trip into `RuntimeVideoGenerateToolResult`. Defensive `cachedHeygenAvatarId === null` case: emits `WARN` log and fails with `talking_avatar_provider_unavailable` rather than silently forwarding null to HeyGen (shouldn't happen post-E12 but defensively handled). Honest failure codes: `persona_not_found`, `voice_required`, `voice_not_found`, `portrait_alias_unavailable`, `talking_avatar_provider_unavailable`, `talking_avatar_plan_disabled`. **NO writes to `workspace_video_personas` from `apps/runtime/**`** — explicit grep of new diff for `personaRepository.(create|update|archive)` returns zero matches; cross-slice invariant #14 (REST-only persona mutation) preserved in original strict form, no erratum needed. **Cross-slice invariant #12 (no keyword routing)** preserved — explicit grep for `speechText.(match|regex|includes|test|split)` returns zero matches; `speechText` appears only in Slice 3's structural type/presence checks. **Cross-slice invariant #15 NON-NEGOTIABLE** preserved — defensive structural parsing throughout: provider check is structural helper, plan toggle is exact `=== false` equality, voice resolution is exact `providerVoiceId === voiceKey` against the shortlist; zero regex / fuzzy match / keyword list anywhere. NO multi-persona refusal in code — operator-superseded by Slice 3 erratum (single-character rule lives only in the LLM-facing tool description, owned by Slice 10). New API side: read-only `ReadWorkspaceVideoPersonaService` (workspace-scoped lookup; returns null for missing / cross-workspace / archived — fail-closed isolation) + `InternalRuntimeWorkspaceVideoPersonasController` mirroring the `internal-runtime-knowledge.controller.ts` auth pattern. New runtime client method `PersaiInternalApiClientService.fetchWorkspaceVideoPersona({ workspaceId, personaId })` with 404 → null, 5xx/network/timeout → `ServiceUnavailableException`, schema-tag validation on response. Tests: augmented `runtime-video-generate-tool.service.test.ts` with `HEYGEN_VIDEO_MODEL_PARAMETERS` constant + `heygenBundle` fixture + `fetchWorkspaceVideoPersona` mock on `FakePersaiInternalApiClientService` + 11 new assertions (persona path happy with default voice; persona path with explicit `voiceKey` override matched + invalid → `voice_not_found`; persona not found → `persona_not_found`; portrait alias happy; portrait alias without `voiceKey` → `voice_required`; portrait alias unavailable → `portrait_alias_unavailable`; plan toggle `false` → `talking_avatar_plan_disabled`; plan toggle missing → permissive; HeyGen credential missing → `talking_avatar_provider_unavailable`; cinematic regression — existing cinematic dispatch unchanged; result echo regression for both paths); new `read-workspace-video-persona.service.test.ts` with 5 assertions (happy path, not found, cross-workspace isolation, archived rejection, `heygenAvatarId` non-null post-E12). Subagent: Claude Sonnet 4.6 medium thinking; single run; honored "no docs edits" rule (fourth subagent in a row to do so cleanly). Verification: 12/12 gates green (lint, format, 5 typechecks, 5 focused/regression tests including the 156s Slice 6 polling regression and the Slice 5b regression). NO `apps/provider-gateway/**` changes (Slice 6 ownership preserved). NO `media-delivery.service.ts` changes (ADR-108 Slice 2 ownership preserved). NO `native-tool-projection.ts` changes (Slice 3 ownership preserved; Slice 8/10 will tune the description). NO `orval generate` run. NO synchronous portrait blob load from object storage in the persona path (the gateway DTO carries `portraitImageBytesBase64: null` for persona path; HeyGen's `type: "avatar"` body uses `avatar_id` only). Deploy: RUNTIME + API (no provider-gateway redeploy, no migration). PersAI can now render HeyGen talking-avatar videos end-to-end on both the persona reuse path (Scenario C) and the ad-hoc photo path (Scenario A). Slice 8 (plan toggle materialization) lights up the `talkingVideoEnabled` gate that Slice 7 already enforces.

- **ADR-109 Slice 5b — Eager HeyGen avatar creation at persona POST (E12 retrofit) (`apps/api`, `apps/provider-gateway`, `packages/config`, `packages/runtime-contract`; 2026-06-05):** retrofits the persona POST flow so HeyGen `POST /v3/avatars` fires synchronously at persona create time (BEFORE the DB transaction). Implements the binding E12 amendment that supersedes the original lazy-create design. The previous lazy-create code path in `HeyGenProviderClient.generateVideo` (Slice 6) is preserved as a defensive fallback — it now delegates to a new public `createPhotoAvatar(input, options): Promise<{ avatarId: string }>` method extracted from the existing inline asset-upload + avatar-create code. The new method is ALSO exposed via a new provider-gateway HTTP endpoint `POST /api/v1/providers/heygen/create-photo-avatar` (controller `ProviderHeyGenAvatarsController` + service `ProviderHeyGenAvatarsService`) that the API service calls during persona POST. API-side client `HeyGenProviderGatewayClient` mirrors the existing `sync-provider-gateway-warmup.service.ts` pattern (reads `PERSAI_PROVIDER_GATEWAY_BASE_URL` + `PERSAI_PROVIDER_GATEWAY_HEYGEN_AVATAR_TIMEOUT_MS` directly from `process.env` for lightweight HTTP, not via the full `loadApiConfig` schema validation), maps 4xx → `BadRequestException(heygen_avatar_create_failed)`, 5xx/network/timeout → `ServiceUnavailableException(heygen_unavailable)`, and defensively parses the response schema tag + non-empty `avatarId`. `ManageWorkspaceVideoPersonasService.createPersona` reordered to: (1) settings + voice-catalog pre-checks (unchanged), (2) validate + normalize portrait to 1024×1024 JPEG (unchanged), (3) **pre-checks (limit, duplicate name, balance — best-effort racy reads OUTSIDE tx)** to short-circuit obvious violations and avoid the $1 HeyGen spend, (4) **HeyGen call OUTSIDE tx** via `heygenProviderGatewayClient.createPhotoAvatar({...})` using `TOOL_CREDENTIAL_IDS.tool_video_generate_heygen` (the platform-wide HeyGen API key established in Slice 1 and already consumed by Slice 4's `HeyGenVoiceCatalogService`), (5) **tx with authoritative race-guard re-checks + persona insert WITH `heygenAvatarId` populated + ledger event + balance check + debit** — all inside one `prisma.$transaction` block, (6) portrait save to object storage AFTER tx commits (orphan-blob safe, same as Slice 5). Orphan-avatar warning log fires only when the tx-level race guards reject after a successful HeyGen call (rare): `[persona] Orphan HeyGen avatar created but tx rejected. avatar_id=... persona_id=... workspace_id=... error_code=... The HeyGen avatar will remain unused. No compensation is performed (Slice 5b trade-off).` Prisma schema change: `WorkspaceVideoPersona.heygenAvatarId` `String?` → `String` (NOT NULL going forward). New Prisma migration `20260605000000_slice5b_persona_heygen_avatar_id_required` performs `UPDATE workspace_video_personas SET heygen_avatar_id = 'unset_legacy' WHERE heygen_avatar_id IS NULL` (defensive backfill — no production rows exist yet, affects zero rows in practice) followed by `ALTER COLUMN heygen_avatar_id SET NOT NULL`. Domain repository record + Prisma adapter `toRecord` mapping + service DTO `PersonaListItem.heygenAvatarId` all widened from `string | null` to `string`. New contract interfaces `ProviderGatewayHeyGenCreatePhotoAvatarRequest` (`schema: "persai.providerGatewayHeyGenCreatePhotoAvatarRequest.v1"`, fields: `credential: { secretId, providerId: "heygen" }`, `name`, `portraitImageBytesBase64`, `portraitImageMimeType`) and `ProviderGatewayHeyGenCreatePhotoAvatarResult` (`schema: "persai.providerGatewayHeyGenCreatePhotoAvatarResult.v1"`, fields: `provider: "heygen"`, `avatarId`, `respondedAt`) added to `@persai/runtime-contract`. **Cross-slice invariant #14 (REST-only persona mutation) preserved in its original strict form, no erratum needed** — `apps/runtime/src/**` untouched; persona writes happen exclusively from the API service via the REST controller. **Cross-slice invariant #12 (no keyword routing)** preserved — the only regex in new code is `/^\d+$/.test(rawTimeout)` parsing a numeric env var (NOT user-supplied strings); persona name handling stays `.toLowerCase()` equality. **Cross-slice invariant #15 NON-NEGOTIABLE** preserved — defensive structural parsing throughout (response schema tag check on gateway responses, exact `providerId === "heygen"` equality, type-safe `normalizeInput`). Tests: augmented `manage-workspace-video-personas.service.test.ts` with Tests 10–14 (pre-check limit/duplicate/balance reject → HeyGen never invoked; HeyGen fail → tx never opens; tx-race orphan warning log; happy-path now asserts `heygenAvatarId === fixedMockId`); new `heygen-provider-gateway.client.test.ts` with 8 assertions (200 happy path, 4xx → `heygen_avatar_create_failed`, 5xx → `heygen_unavailable`, network error, malformed response, timeout, missing base URL, request body shape); augmented `heygen-provider.client.test.ts` with Test 13 (standalone `createPhotoAvatar`: asset upload + avatar create, no video submit, `Idempotency-Key` on both POSTs); new `provider-heygen-avatars.service.test.ts` with 6 assertions (happy path, providerId rejection, missing-portrait rejection, empty-name rejection, secret resolution failure, HeyGen client error mapped to `heygen_avatar_create_failed`). Subagent: Claude Sonnet 4.6 medium thinking; single run; honored "no docs edits" rule (third subagent in a row to do so cleanly). Verification: 14/14 gates green (lint, format, 5 typechecks, 5 focused/regression tests, prisma validate). No `apps/runtime/**` changes. No `orval generate` run. NO HeyGen v1/v2 endpoint calls. NO compensation logic (orphan HeyGen avatars on tx-race accumulate at the rare-race rate, logged honestly). NO synchronous BLOCKING fetch inside `prisma.$transaction` (HeyGen call is strictly OUTSIDE the tx). Deploy: API + PROVIDER-GATEWAY + MIGRATION (Prisma migration required to land before API deploy). PersAI persona creation is now eager-HeyGen end-to-end; Slice 7 (runtime talking_avatar execution) can now assume `heygen_avatar_id` is always populated and reads-only from `workspace_video_personas`.

- **ADR-109 Slice 6 — Provider-gateway HeyGen client (v3 endpoints, polling, lazy avatar creation, defensive status parsing, billing facts) (`apps/provider-gateway`, `apps/runtime`, `packages/runtime-contract`; 2026-06-05):** landed the full HeyGen v3 HTTP integration end-to-end on the provider-gateway. New `HeyGenProviderClient` covers four HeyGen v3 endpoints: `POST /v3/videos` (submit, with `type: "image"` for ad-hoc Scenario A and `type: "avatar"` for Scenario C persona reuse), `POST /v3/avatars` (lazy avatar creation when a persona has no cached `heygen_avatar_id`), `POST /v3/assets` (portrait pre-upload returning `asset_id` consumed by the avatar create body), and `GET /v3/videos/{video_id}` (poll, 10s cadence per erratum E10). All calls use `X-Api-Key` header auth. Every submit POST (`/v3/videos` and `/v3/avatars`) carries a fresh `Idempotency-Key: <crypto.randomUUID()>` header per erratum E6; the key is never reused across attempts and never applied to poll GETs or asset uploads. Submit failures fail-fast on all HTTP errors (mirroring Kling client). Polling tolerates transient transport failures up to 3 consecutive, then throws `PERSAI_VIDEO_POLLING_LOST::<json>` with `provider: "heygen"`, `providerStage: "accepted"`, `code: "accepted_primary_unconfirmed"` — exact mirror of Kling format. **Defensive status parsing (invariant #15 NON-NEGOTIABLE):** terminal SUCCESS = exact `status === "completed"`; terminal FAILED = exact `status === "failed"`; everything else (including `"pending"`, `"processing"`, the create-response-only `"waiting"` value, any future undocumented value) → continue polling. Zero regex, zero `.includes(...)`, zero keyword list, zero fuzzy match anywhere — documented in a code comment referencing the invariant. **`acceptedTask` resume:** when `input.acceptedTask.provider === "heygen"` and `providerStage === "accepted"` with a non-empty `providerTaskId`, the client skips submit and resumes polling directly with the persisted `video_id`. **Missing-duration honesty:** when poll returns `status: "completed"` but `data.duration` is missing / non-positive / non-numeric, the client throws `heygen_duration_missing` honestly — NO fake `billingFacts` are emitted (per Slice 6 forbidden pattern). On success, `billingFacts` carries `providerKey: "heygen"`, `capability: "video"`, `modelKey: <input.model ?? "heygen-photo-avatar-v3">`, and `metering: { meteringKind: "time_metered", durationMs, durationSeconds }` populated from the HeyGen response (NOT from the input request). Contract widening (`packages/runtime-contract/src/index.ts`): `ProviderGatewayVideoGenerateRequest` gained 3 new optional fields (`cachedHeygenAvatarId?: string | null`, `portraitImageBytesBase64?: string | null`, `portraitImageMimeType?: string | null`) — all backward-compatible; `ProviderGatewayVideoGenerateResult` gained `lazyCreatedHeygenAvatarId?: string | null` (default null, populated only when Scenario C lazy-create happens, returned for Slice 7 to persist on the persona row). **Field decision rationale:** the new `portraitImageBytesBase64` field does NOT reuse the existing `referenceImage.bytesBase64` slot — `referenceImage` semantics on Kling/Runway is "visual reference for cinematic generation" while HeyGen's portrait is "the talking face"; conflating would break per-provider contract honesty. Optional field — Kling/Runway/OpenAI ignore it. **Image transport choice:** for Scenario A (ad-hoc) the portrait travels base64-inline in the `POST /v3/videos` body via HeyGen's documented `image: { type: "base64", media_type, data }` shape; for Scenario C lazy-create, the portrait is uploaded once to `POST /v3/assets` (multipart) and the returned `asset_id` is used in the `POST /v3/avatars` body (`file: { type: "asset_id", asset_id }`) — this split avoids duplicate base64 embedding. **Dispatch wiring:** `provider-video-generation.service.ts` injected `HeyGenProviderClient`, replaced the Slice 2a placeholder throw with real dispatch, and extended `normalizeInput` to defensively parse + forward the new fields with type-safe 400s. `HeyGenProviderClient` registered in `provider-gateway.module.ts`. **Subagent honesty:** Claude Sonnet 4.6 medium thinking, single run, clean exit. Subagent used `WebFetch` to confirm exact HeyGen v3 body shapes for `POST /v3/videos`, `POST /v3/avatars`, and `POST /v3/assets` before writing code, producing a tighter implementation than the ADR truth section alone would have. Honored the orchestrator's "no docs edits" instruction (contrast with Slice 5 where the subagent self-edited docs). Files modified (6, all Scope IN): `packages/runtime-contract/src/index.ts`, `apps/provider-gateway/src/modules/providers/provider-video-generation.service.ts`, `apps/provider-gateway/src/modules/providers/provider-gateway.module.ts`, `apps/provider-gateway/test/provider-video-generation.service.test.ts`, `apps/provider-gateway/test/run-suite.ts`, `apps/runtime/test/provider-gateway.client.service.test.ts`. New files (2): `apps/provider-gateway/src/modules/providers/heygen/heygen-provider.client.ts`, `apps/provider-gateway/test/heygen-provider.client.test.ts` (12 assertion groups: Scenario A happy-path + billingFacts shape, Scenario C cached avatar, Scenario C lazy-create with `lazyCreatedHeygenAvatarId` return, `Idempotency-Key` on every POST, defensive `"waiting"` + `"unknown_future_value"` → in-progress, terminal `"failed"` → honest error, polling-loss after 3 transport failures, 4xx submit fails-fast no retry, missing duration throws honest `heygen_duration_missing`, resume via `acceptedTask` skips submit, missing API key honest error, `voice_required` when `voiceKey` missing). Test timing note: HeyGen client test uses real 10s `setTimeout` delays during polling — one full run takes ~155 seconds. Not a blocker (exit 0 reached cleanly); a future cleanup slice may swap to fake-timer harness. Verification: all 12 gates PASS (lint, format:check, 5 typechecks, 5 focused tests including Kling regression + runtime regressions). Cross-slice invariants 1–15 verified true; specifically #11 ADR-107 carve-out preserved (no Runway/Kling/OpenAI provider-client edits), #14 REST-only persona mutation preserved (`apps/runtime/src/**` untouched; only `apps/runtime/test/provider-gateway.client.service.test.ts` augmented with HTTP-serialization assertions), #15 NON-NEGOTIABLE preserved (terminal-status check is pure string equality, structurally bounded). NO `apps/api` changes (no persona writes from the gateway — Slice 7 owns persistence). NO ADR-108 wallet code touched. NO `orval generate` run (preserves the pre-existing ~580-file repo-wide drift isolation). ADR-109 Status updated: Slice 6 Completed. Deploy: PROVIDER-GATEWAY + RUNTIME (provider-gateway gets the new client; runtime gets the contract widening; no API or migration). No feature flag. PersAI can now actually call HeyGen v3 end-to-end; the runtime side (Slice 7) wires `mode = "talking_avatar"` to populate the new gateway fields and persist `lazyCreatedHeygenAvatarId` back to the persona row.

- **ADR-109 Slice 5 — Workspace persona registry (substrate + REST + wallet integration + Admin knobs) (`apps/api`, `apps/web`, `packages/contracts`; 2026-06-04):** landed the full workspace video persona registry stack. New Prisma model `WorkspaceVideoPersona` (`workspace_video_personas` table) with soft-delete (`archived` flag + `archivedAt`), unique index on `(workspaceId, displayNameLower)`, and composite index on `(workspaceId, archived)`. `heygenAvatarId` is NULL at creation; Slice 6 will lazily populate it on first HeyGen render. Two new platform knob columns on `platform_runtime_provider_settings`: `heygen_persona_workspace_limit` (default 10) and `heygen_persona_creation_vcoin` (default 20 = $1.00 at 1 VC = $0.05). Migration `20260604230000_slice5_workspace_video_persona`: adds persona table + ALTER for new knob columns. Domain port `WorkspaceVideoPersonaRepository` with transaction-aware `create`/`archive`/`findBy*` methods. Prisma adapter `PrismaWorkspaceVideoPersonaRepository`. Application service `ManageWorkspaceVideoPersonasService` implements: (a) `createPersona` — voice ID validated against cached HeyGen shortlist (`providerVoiceId` exact match, no regex — invariant #15), portrait normalized to JPEG 1024×1024 via sharp, single `prisma.$transaction` enforcing limit check + duplicate-name guard + persona insert + ledger event (if cost > 0) + balance check + wallet debit; portrait saved to object storage AFTER transaction; `storageWarning` surfaced on failure without rollback; (b) `listPersonas` — non-archived rows + platform limit for UI; (c) `archivePersona` — soft-delete only, no HeyGen API call, no VC refund. REST controller `WorkspaceVideoPersonasController` at `api/v1/workspaces/:workspaceId/video-personas` — `POST /` with `FileInterceptor("portrait")`, `GET /`, `DELETE /:personaId`; fail-closed workspace authorization via `req.workspaceId` identity check. `WorkspaceVcoinLedgerEventKind` union widened to include `"persona_creation"`. `PlatformRuntimeProviderSettingsState` / `PlatformRuntimeProviderSettingsRecord` / `UpdatePlatformRuntimeProviderSettingsInput` extended with the two new knob fields. `resolve-platform-runtime-provider-settings.service.ts` and `manage-admin-runtime-provider-settings.service.ts` updated to read/write new columns. OpenAPI: 3 new paths (`POST/GET /workspaces/{workspaceId}/video-personas`, `DELETE /…/{personaId}`), 3 new schema components (`WorkspaceVideoPersonaState`, `WorkspaceVideoPersonaCreateResponse`, `WorkspaceVideoPersonaListState`), `AdminRuntimeProviderSettingsState` + `AdminRuntimeProviderSettingsRequest` extended with `heygenPersonaWorkspaceLimit`, `heygenPersonaCreationVcoin`, and `vcoinExchangeRate` (request type). 3 new manually-authored generated TS model files + `index.ts` updated. Admin > Runtime page: new "Vcoin Economy" fold with 3 operator inputs (VC exchange rate, persona limit, persona creation cost); properly wires `vcoinExchangeRate` into form state + save payload (pre-existing display-only bug fixed). Module wiring: controller, service, and repo provider registered. Tests: `manage-workspace-video-personas.service.test.ts` (9 assertions: happy-path with VC debit, limit reached, duplicate name, voice not found, insufficient balance, archive soft-delete, archive not found, cost=0 skips ledger, storage after tx). Implicit scope expansion (both additive/typecheck-forced): `platform-runtime-provider-settings.test.ts` pre-existing `"heygen"` key assertion fix (Slice 4 fixture omission) + new knob assertions; `adminRuntimeProviderSettingsRequest.ts` extended to include new optional fields in save path. Soft-delete decision documented: archived rows kept for Slice 6 HeyGen cascade; no hard-delete in Slice 5; no VC refund on archive; partial unique index deferred to Slice 6. Verification: all 12 gates pass (lint, format, api/web/runtime/provider-gateway/contracts typechecks, 5 focused test runners). Cross-slice invariants #11 #12 #14 #15 confirmed.

- **ADR-109 Slice 4 — HeyGen voice catalog cache (substrate, symmetric with Kling) (`apps/api`, `packages/runtime-contract`; 2026-06-04):** landed the HeyGen voice catalog cache substrate as a structural mirror of the existing Kling voice catalog pattern. Pure data-plane substrate: new `PlatformHeygenVoiceCatalogCache` Prisma model (migration `apps/api/prisma/migrations/20260604220000_slice4_heygen_voice_catalog_cache/migration.sql`, hand-authored mirror of the Kling table — table `platform_heygen_voice_catalog_cache`, PK on `cache_key VARCHAR(64)`, `voices_json JSONB`, `fetched_at TIMESTAMPTZ`), new `HeyGenVoiceCatalogService` injectable at `apps/api/src/modules/workspace-management/application/heygen/heygen-voice-catalog.service.ts` mirroring `KlingVoiceCatalogService`'s public surface — `getMaterializedVoiceCatalog(): Promise<RuntimeVideoVoiceCatalog | null>` with 24h TTL, lazy refresh, fetches `GET https://api.heygen.com/v3/voices` with `X-Api-Key` header sourced from `TOOL_CREDENTIAL_IDS.tool_video_generate_heygen` (registered in Slice 1) via `PlatformRuntimeProviderSecretStoreService`. Defensive multi-alias parsing of HeyGen v3 response shape: handles both `{data:[...]}` wrapper and flat array; field aliases `voice_id`/`voiceId`, `language`/`voice_language`, `gender`/`voice_gender`/`sex`, `preview_audio_url`/`preview_audio`/`previewAudioUrl`. Failure modes documented: missing API key → returns null + WARN log; HTTP error → falls back to stale cached row + WARN log; empty shortlist refresh → falls back to cached row. `RuntimeVideoVoiceCatalogEntry` in `packages/runtime-contract/src/index.ts` gained optional `previewAudioUrl?: string | null` (backward-compatible — Kling entries continue to work; existing rows hydrate to `null`); `RuntimeVideoVoiceCatalog.provider` widened from `"kling"` to `"kling" | "heygen"` (substrate widening symmetric to Slice 2a). `KlingVoiceCatalogService` augmented to extract `previewAudioUrl` from HeyGen-symmetric field aliases (`preview_audio_url`/`preview_audio`/`previewAudioUrl`) in `parseVoiceRow` and to hydrate it from cached JSON in `parseCachedVoices` — Kling regression test confirms existing behavior intact. Materialization wiring: `MaterializeAssistantPublishedVersionService` constructor gained a `HeyGenVoiceCatalogService` injection; the `attachMaterializedVideoVoiceCatalog` branch was refactored from `if (ref.providerId !== "kling") return ref` early-return to explicit parallel branches for `"kling"` and `"heygen"`, each calling its own service and attaching `videoVoiceCatalog` on non-empty shortlist. `HeyGenVoiceCatalogService` registered in `workspace-management.module.ts`. **Subagent honesty (no false-PASS regression):** while implementing Slice 4 the subagent (Claude Sonnet 4.6 medium thinking) discovered that `apps/api/test/materialize-assistant-published-version.service.test.ts` was already failing at HEAD — when Slice 2a added `"heygen"` to `MANAGED_CATALOG_PROVIDERS`, the test fixture's `availableModelCatalogByProvider` literal was not updated to include `heygen: { models: [] }`. Slice 3 verification did NOT include running this specific test (Slice 3's focused suite was `runtime-video-generate-tool`, `provider-gateway.client`, `provider-video-generation`), so this is not a Slice 3 false-PASS — it is a latent Slice 2a fixture omission that survived the implicit-scope expansion. Fixed in Slice 4 as a 3-line addition to the fixture. NO HeyGen render wiring (Slice 6 owns; `provider-video-generation.service.ts::generateVideo` still throws `"ADR-109 Slice 6: HeyGen runtime execution not yet implemented"` on the `heygen` case). NO portrait/persona handling (Slice 5 + 6 own). NO LLM tool description text (Slice 8). NO plan toggle (Slice 9). Files modified (6 modified + 4 new, all Scope IN, ~+48 lines): `packages/runtime-contract/src/index.ts`, `apps/api/prisma/schema.prisma`, `apps/api/src/modules/workspace-management/application/kling/kling-voice-catalog.service.ts`, `apps/api/src/modules/workspace-management/application/materialize-assistant-published-version.service.ts`, `apps/api/src/modules/workspace-management/workspace-management.module.ts`, `apps/api/test/materialize-assistant-published-version.service.test.ts`; new files `apps/api/prisma/migrations/20260604220000_slice4_heygen_voice_catalog_cache/migration.sql`, `apps/api/src/modules/workspace-management/application/heygen/heygen-voice-catalog.service.ts`, `apps/api/test/heygen-voice-catalog.service.test.ts` (7 assertions: fresh→network+upsert, warm→no network, expired→refresh, missing creds→null, HTTP 401→stale fallback, flat array, `voiceId` alias), `apps/api/test/materialize-heygen-voice-catalog.test.ts` (5 assertions: heygen catalog with `previewAudioUrl`, heygen ref→catalog attached, runway/openai refs→no attach, null catalog→ref unchanged, Kling regression). Verification (all 10 PASS via orchestrator shell): recursive lint, root format:check, api/web/runtime/provider-gateway typechecks (Prisma `generate` confirms `platformHeygenVoiceCatalogCache` model is structurally valid; all `tsc --noEmit` exit 0), `tsx test/heygen-voice-catalog.service.test.ts` (7 assertions PASS), `tsx test/kling-voice-catalog.service.test.ts` (exit 0, regression clean), `tsx test/materialize-assistant-published-version.service.test.ts` (exit 0, pre-existing fixture omission fixed), `tsx test/materialize-heygen-voice-catalog.test.ts` (5 assertions PASS). Cross-slice invariants 1–15 verified true; specifically #11 ADR-107 carve-out preserved (HeyGen recognized as talking-avatar provider, Slice 2b `kind=talking_avatar` constraint unchanged), #12 no keyword routing introduced, #14 persona REST-only N/A (no persona code touched), #15 NON-NEGOTIABLE — all parsing in the new service is purely structural JSON field inspection of the **HeyGen API response** (zero regex on user input, zero message-body parsing, zero keyword routing). ADR-109 Status updated: Slice 4 Completed. Deploy: API (migration `20260604220000_slice4_heygen_voice_catalog_cache` must run via `Dev Image Publish` migration approval gate per AGENTS.md before downstream slices). No feature flag. HeyGen voice catalog is now materialized into the assistant runtime bundle whenever a video-generate credential resolves to `providerId === "heygen"`; voice picker is uniform with Kling for the future Slice 6/7 talking-avatar render path.

- **ADR-109 Slice 3 — Mode contract + talking-avatar request fields + structural validation + gateway pass-through (`apps/runtime`, `apps/provider-gateway`, `packages/runtime-contract`; 2026-06-04):** added the `RuntimeVideoGenerateMode = "cinematic" | "talking_avatar"` request-mode axis and the talking-avatar transport fields (`mode`, `speechText`, `speechLanguage`, `personaId`, `portraitImageAlias`, `voiceKey`) on `RuntimeVideoGenerateRequest` + symmetric `requested*` echoes on `RuntimeVideoGenerateToolResult` + pass-through fields on `ProviderGatewayVideoGenerateRequest`. Pure structural contract widening + tool-execution validation; NO HeyGen HTTP wiring (Slice 6), NO tool projection JSON Schema or tool description text changes (Slice 8 + 9), NO plan toggle (Slice 9), NO multi-character refusal in code (operator directive — single-speaker rule moves to Slice 8 tool description). Baseline SHA at session start: `1331c2e9` (Slice 2b closure). Tree clean. Subagent: GPT-5.4 medium (first hire of this model on the project). **First run honestly stopped on a false-positive "dirty tree" blocker** — subagent interpreted the COMMITTED Slice 2b changes in `runtime-contract/src/index.ts` and `native-tool-projection.ts` as uncommitted working-directory drift and refused to proceed; orchestrator confirmed `git status: nothing to commit, working tree clean` and resumed the subagent with explicit clarification that the files were Slice 2b's committed substrate (not pending changes). Second run delivered cleanly within Scope IN. Useful contrast vs Claude Sonnet 4.6 medium thinking which handled Slices 2a + 2b without false blockers: GPT-5.4 is honest but less precise on git mental model. Files modified (6, all Scope IN, +568 / −6): `packages/runtime-contract/src/index.ts` (added `RUNTIME_VIDEO_GENERATE_MODES`, `RuntimeVideoGenerateMode`, `isRuntimeVideoGenerateMode` type guard; extended 3 interface shapes); `apps/runtime/src/modules/turns/runtime-video-generate-tool.service.ts` (defensive structural parsing of new args using existing `asNonEmptyString` helper — no regex / no string matching / no message-body parsing; structural XOR validation `hasPersonaId === hasPortrait` for personaId/portraitImageAlias mutual exclusion; mode-gated forwarding to gateway request payload ONLY when `mode === "talking_avatar"`; new private helpers `buildRequestedTalkingAvatarEchoes` and `buildGatewayTalkingAvatarFields`; symmetric `requested*` echo additions to all 14 `payload:` sites in the file); `apps/provider-gateway/src/modules/providers/provider-video-generation.service.ts` (`normalizeInput` accepts the new fields with defensive type-rejection returning honest 400s for malformed values; HeyGen `case` retains the Slice 2a placeholder throw `"ADR-109 Slice 6: HeyGen runtime execution not yet implemented"`). Tests augmented (no new test files): `apps/runtime/test/runtime-video-generate-tool.service.test.ts` 7 focused scenarios (persona happy path, portrait happy path, cinematic-ignores-extras, missing-speechText-throws, missing-speechLanguage-throws, both-persona-and-portrait-throws-XOR, neither-persona-nor-portrait-throws-XOR); `apps/runtime/test/provider-gateway.client.service.test.ts` extended to assert talking-avatar fields are JSON-serialized into the gateway HTTP body; `apps/provider-gateway/test/provider-video-generation.service.test.ts` augmented with pass-through assertions, cinematic-non-injection assertion, defensive type-rejection assertions, and an explicit regression test for the Slice 2a HeyGen-placeholder throw. Verification (all 10 PASS via orchestrator shell): recursive lint, root format:check, api/web/runtime/provider-gateway typechecks (all `tsc --noEmit` exit 0), `tsx test/runtime-video-generate-tool.service.test.ts`, `tsx test/provider-gateway.client.service.test.ts`, `tsx test/provider-video-generation.service.test.ts`, `tsx test/native-tool-projection.test.ts` (Slice 2b sanity — still green). Cross-slice invariants 1–15 verified true; specifically #15 NON-NEGOTIABLE — zero regex, zero string matching, zero message-body parsing introduced anywhere; XOR personaId/portraitImageAlias check is pure boolean equality on `null`-vs-non-`null` field presence; `mode` is an explicit field set by the LLM, never inferred from any text content. ADR-109 Slice 3 spec stamped with `**Status (2026-06-04): Completed.**` block including the operator-supersedence note (no multi-character refusal in code) and the orchestrator-decided deferral of tool projection JSON Schema to Slice 8 + 9. ADR-109 Status updated: Slice 3 Completed. Deploy: RUNTIME + PROVIDER-GATEWAY. No Prisma migration. No feature flag. Request contract is talking-avatar-ready and structurally validated end-to-end through runtime + provider-gateway pass-through; not callable yet (Slice 6 lands HeyGen HTTP client, Slice 7 wires execution).

- **ADR-109 Slice 2b — Catalog row capability axis (`cinematic` | `talking_avatar`) + plan validation + chat tool projection filter + Admin UI badge (`apps/api`, `apps/web`, `apps/runtime`, `packages/runtime-contract`, `packages/contracts`; 2026-06-04):** introduced a structural per-row capability axis on the runtime catalog so HeyGen rows are intrinsically `kind="talking_avatar"` and Runway/Kling/OpenAI/Anthropic rows are intrinsically `kind="cinematic"`, with provider-locked enforcement at parse time. Baseline SHA at session start: `f999d889` (Slice 2a closure). Subagent: Claude Sonnet 4.6 medium thinking, single run, no resume needed. Type/schema: `apps/api/src/modules/workspace-management/application/runtime-provider-profile.ts` added `RuntimeVideoModelKind = "cinematic" | "talking_avatar"` type alias + `kind` field on `RuntimeProviderModelProfileBase` + `defaultVideoModelKindForProvider(provider)` helper (HeyGen→`talking_avatar`, others→`cinematic`); `parseRuntimeProviderModelProfiles` derives `kind` from input row OR provider default AND throws when `provider === "heygen" && kind !== "talking_avatar"` OR `provider !== "heygen" && kind === "talking_avatar"` (provider-locked structurally); `createDefaultModelProfiles` and `parseLegacyCapabilityCatalog` extended symmetrically. `packages/contracts/openapi.yaml` gained `RuntimeVideoModelKind` enum schema component (`cinematic`, `talking_avatar`) and `kind` field added to `RuntimeProviderModelProfileCommonState` required + properties — clean architecture because all 5 `oneOf` billing-mode variants of `RuntimeProviderModelProfileState` already `allOf`-extend the common base, so the single edit propagates to all 5 variants without duplication. Generated contracts: new file `packages/contracts/src/generated/model/runtimeVideoModelKind.ts` (manually mirroring orval const-enum style), `runtimeProviderModelProfileCommonState.ts` augmented with `kind` field + import, `model/index.ts` augmented with one new `export * from "./runtimeVideoModelKind"`. Pre-existing repo-wide generated-files drift NOT touched (orchestrator kept Slice 2b's contracts edit scope to exactly 3 generated files + 1 new file; `orval generate` not run). `packages/runtime-contract/src/index.ts` added `PERSAI_RUNTIME_TALKING_AVATAR_VIDEO_PROVIDER_IDS = ["heygen"] as const`, `PersaiRuntimeTalkingAvatarVideoProviderId` type, and `isTalkingAvatarVideoProvider(providerId)` helper — pure structural enum membership check, never user-input parsing. Plan validation: `apps/api/src/modules/workspace-management/application/manage-admin-plans.service.ts::assertCapabilityModelKeysAvailable` includes HeyGen in video-capability catalogs, builds `videoModelKindMap: Map<modelKey, kind>` from active video models across all 4 providers, and refuses `videoGenerateModelKey` / `videoGenerateFallbackModelKey` with `BadRequestException` "is a talking_avatar (cinematic_only) model and cannot be used as a plan videoGenerateModelKey or videoGenerateFallbackModelKey. Talking-avatar models are exposed separately via the workspace plan toggle (Slice 9)." when the resolved row has `kind === "talking_avatar"`. Chat-side tool projection: `apps/runtime/src/modules/turns/native-tool-projection.ts::projectRuntimeNativeTools` adds `!isTalkingAvatarVideoProvider(videoGenerateCredential.providerId)` guard to the cinematic `video_generate` tool exposure block, with ADR-109 comment `// ADR-109 Slice 2b: talking_avatar rows are exposed via the Slice 9 plan toggle, not via the cinematic video_generate tool surface`. Admin Runtime UI: `apps/web/app/admin/runtime/page.tsx` added `kindForProvider(provider)` helper, included `kind` in `createModelProfile()` + `rebuildProfileForBillingMode()` payloads, and added a read-only `aria-label="Capability kind"` badge per row that displays "Talking Avatar" for HeyGen rows and "Cinematic" otherwise (sky-500 border styling symmetric to existing badges). Subagent self-reported 7-file implicit scope expansion (all pure-additive `kind` data defaults; no behavioral logic): `platform-runtime-provider-settings.ts` (3 sites with provider-aware default), `tool-path-pricing-catalog.ts` (1 site, `kind: "cinematic"` for ledger pseudo-profile), `runtime-provider-settings-admin.ts` (1 site), `runtime-provider-settings-admin.test.ts` (3 sites), `assistant-api-client.test.ts` (3 sites), `admin/knowledge/page.test.tsx` (6 sites), `admin/runtime/page.test.tsx` (3 sites) — orchestrator diff-reviewed every implicit-scope edit and confirmed pure data-default pattern (same precedent as Slices 1 and 2a). Tests augmented (no new test files): `apps/api/test/runtime-provider-profile.test.ts` 5 new assertions (default cinematic / default talking-avatar / explicit kind accepted / incompatible HeyGen+cinematic throws / incompatible Runway+talking-avatar throws); `apps/api/test/manage-admin-plans.service.test.ts` new block via `talkingAvatarCatalogService` mock (HeyGen-v2 row with `kind: "talking_avatar"` rejected with `cinematic_only` error; runway-gen-4 row with `kind: "cinematic"` passes); `apps/runtime/test/native-tool-projection.test.ts` 2 new assertions (`video_generate` absent for heygen provider bundle, present for runway); `apps/web/app/admin/runtime/page.test.tsx` 1 new test asserting "Cinematic"/"Talking Avatar" badges via `aria-label="Capability kind"` query. 18 files modified + 1 new file total (+445 / −5). Verification (all 10 PASS via orchestrator shell): recursive lint, root format:check, api/web/runtime/provider-gateway typechecks (all `tsc --noEmit` exit 0), `tsx test/runtime-provider-profile.test.ts`, `tsx test/manage-admin-plans.service.test.ts`, `tsx test/native-tool-projection.test.ts`, `vitest run app/admin/runtime/page.test.tsx` (18 tests, all pass — including new HeyGen badge test). Cross-slice invariants 1–15 verified true; specifically: #11 ADR-107 line 39-40 carve-out enforced parser-side (Runway/Kling rows structurally cannot be `talking_avatar`); #12 no keyword routing introduced anywhere; #15 NON-NEGOTIABLE remains true — capability derivation is purely structural via `providerId` enum membership + the explicit `kind` field, never via user-input parsing, regex, or model-name string matching. ADR-109 Status updated: Slice 2a + 2b Completed. Deploy: API + WEB. No Prisma migration. No feature flag. Admin can now configure HeyGen catalog rows with talking_avatar capability; plan editors that attempt to select HeyGen rows as cinematic primary/fallback fail validation with a clear honest error; chat-side LLM tool surface hides talking-avatar models from the cinematic `video_generate` tool until Slice 9 plan toggle exposes them per workspace.

- **ADR-109 Slice 2a — HeyGen catalog provider substrate + Admin Runtime UI card (`apps/api`, `apps/web`, `apps/provider-gateway`, `packages/runtime-contract`, `packages/contracts`; 2026-06-04):** widened TypeScript union and runtime contract to recognize HeyGen as a video catalog provider symmetric to Runway/Kling; Admin Runtime page renders an empty HeyGen card with placeholder copy `"Talking-avatar video provider for ADR-109. Catalog rows arrive in Slice 2b (capability axis pending). Save is supported now; rows remain empty."`. Baseline SHA at session start: `d18d0064` (Slice 1 closure). Tree clean. Executed under the ADR-109 orchestrator execution model with one implementation subagent — first hire under the operator directive to retire Opus 4.8 from implementation slices in favor of Sonnet 4.6 medium thinking (or GPT-5.4). Original Slice 2 spec was split during orchestrator planning into 2a (this slice — pure substrate widening) + 2b (capability axis on catalog rows + plan validation — next slice). Substrate changes: `apps/api/src/modules/workspace-management/application/runtime-provider-profile.ts` (`MANAGED_CATALOG_PROVIDERS`, `VIDEO_GENERATE_PROVIDERS`, `isVideoOnlyCatalogProvider`, 2 catalog-default blocks); `packages/runtime-contract/src/index.ts` (`PERSAI_RUNTIME_VIDEO_GENERATE_PROVIDER_IDS`); `apps/web/app/admin/runtime/page.tsx` (~11 sites: `providerLabel()`, local `MANAGED_CATALOG_PROVIDERS`, `isVideoOnlyCatalogProvider()`, `normalizeCatalogForSlice2()`, `createEmptyCatalog()`, `withDerivedCatalogWeights()`, `buildCatalogFallback()`, `selectedCatalogIndexByProvider`, `newCatalogCapabilityByProvider`, `clampCatalogIndex` useEffect, card-footer copy branch). Placeholder branches added in 2 of 5 anticipated sites where typecheck demanded: `apps/provider-gateway/src/modules/providers/provider-video-generation.service.ts::generateVideo()` switch gained `case "heygen": throw new Error("ADR-109 Slice 6: HeyGen runtime execution not yet implemented")`, and `apps/api/src/modules/workspace-management/application/materialize-assistant-published-version.service.ts::VIDEO_PROVIDER_CREDENTIAL_KEY` gained `heygen: "tool_video_generate_heygen"` (references the Slice 1 credential ref symmetric to Runway/Kling); other 3 anticipated files (`runtime-video-generate-tool.service.ts`, `runtime-tool-policy.ts`, `native-tool-projection.ts`) left UNTOUCHED because typecheck passed without modification. Contracts package: `packages/contracts/openapi.yaml` added `"heygen"` to `ManagedRuntimeCatalogProvider` enum + `RuntimeProviderModelCatalogByProviderState` required array + properties; `packages/contracts/src/generated/model/managedRuntimeCatalogProvider.ts` + `packages/contracts/src/generated/model/runtimeProviderModelCatalogByProviderState.ts` received targeted Slice 2a additions only — orchestrator deliberately rolled back the ~580-file repo-wide generated-files drift that `pnpm --filter @persai/contracts run generate` surfaced (pre-existing contracts/generated drift is independent repository tech debt and not in scope for ADR-109; Slice 2a commit stays clean and targeted). Subagent self-reported a 5-file implicit scope expansion (`platform-runtime-provider-settings.ts`, `runtime-provider-settings-admin.ts`, `runtime-provider-settings-admin.test.ts`, `knowledge/page.test.tsx`, `assistant-api-client.test.ts`) — all pure-additive `heygen: { models: [] }` data defaults symmetric to existing `kling: { models: [] }` sites with no behavioral logic; orchestrator diff-reviewed every implicit-scope edit and confirmed pure data-default pattern (same as Slice 1 `loadToolKeyMetadata()` refactor precedent). Tests augmented (no new files): `apps/web/app/admin/runtime/page.test.tsx` adds `heygen: { models: [] }` to mock catalog payload + new focused test `"renders the HeyGen catalog card with the empty-rows placeholder copy"`; `apps/api/test/runtime-provider-profile.test.ts` includes `heygen` in sorted-keys assertion + asserts `availableModelCatalogByProvider.heygen.models === []`. 15 files modified total (+111/−28). Verification (all 8 PASS via orchestrator shell): recursive lint (apps/web, apps/api, apps/runtime, apps/provider-gateway, apps/sandbox, scripts/smoke all Done); root format:check ("All matched files use Prettier code style!"); api/web/runtime/provider-gateway typechecks clean exit 0; `vitest run app/admin/runtime/page.test.tsx` (17 tests pass including new HeyGen card test); `tsx test/runtime-provider-profile.test.ts` (exit 0). Cross-slice invariants 1–15 verified true: no HeyGen URL / HTTP detail anywhere (Slice 6 owns the HTTP layer), no keyword routing / message-body parsing introduced (invariant #15), no Prisma schema or migration changes, no plan-validation logic changes (deferred to 2b), no capability-axis field on catalog rows (deferred to 2b), no behavioral change for OpenAI/Runway/Kling rows. ADR-109 Slice 2 spec stamped with split note + Slice 2a `**Status (2026-06-04): Slice 2a Completed.**` block. Deploy: API + WEB. No migration. No feature flag. Admin can now open `/admin/runtime`, see the HeyGen card, and save an empty HeyGen catalog cleanly; no runtime execution change yet — Slice 2b (capability axis), Slice 3 (mode contract + tool projection), Slice 4 (persona registry table), Slice 5 (persona REST service), Slice 6 (HeyGen HTTP client), Slice 7 (runtime talking-avatar execution path), Slice 8 (tool description + admin presets), Slice 9 (plan toggle), Slice 10 (Settings UI), Slice 10b (banner UX), Slice 11 (live smoke) follow sequentially.

- **ADR-109 Slice 1 — HeyGen credential + Admin Tools UI (`apps/api`, `apps/web`; 2026-06-04):** added a per-platform encrypted credential slot for HeyGen video provider symmetric to existing Runway/Kling slots. Baseline SHA at session start: `c4fa3825f64827ca21ffb10703fc2dc8a7456f9b` (post-Slice 0 closure). Tree clean. Executed under the ADR-109 orchestrator execution model — read-only orchestrator + one implementation subagent (Claude Opus 4.8 first run + same subagent resumed once after surfacing a Scope OUT typecheck blocker). Future Slice subagents will use Sonnet 4.6 / GPT-5.4 per operator directive ("opus 4.8 дорого и бессмысленно"). Changes: `apps/api/src/modules/workspace-management/application/tool-credential-settings.ts` registered `tool_video_generate_heygen: "tool/video_generate/heygen/api-key"` in `TOOL_CREDENTIAL_IDS`, mapped to tool code `video_generate` in `TOOL_CODE_BY_CREDENTIAL_KEY`, added display label `"Video Generation API Key (HeyGen)"`, and updated the operator-facing `notes` array to mention HeyGen alongside Runway and Kling; `ToolCredentialKey`, `ALL_TOOL_CREDENTIAL_KEYS`, and `CREDENTIAL_KEY_BY_SECRET_ID` automatically pick up the new key. `apps/api/src/modules/workspace-management/application/manage-admin-tool-credentials.service.ts::loadToolKeyMetadata()` refactored: the 14-entry hardcoded `Record<ToolCredentialKey, PlatformRuntimeProviderKeyMetadata>` literal replaced with derived `Object.fromEntries(ALL_TOOL_CREDENTIAL_KEYS.map((key) => [key, { configured: false, lastFour: null, updatedAt: null }]))` — semantic-preserving (all 14 prior entries used identical defaults), and prevents the same exhaustive-record typecheck blocker from re-occurring on every future provider credential addition. `apps/web/app/admin/tools/page.tsx` added third element to `VIDEO_PROVIDER_CREDENTIAL_KEYS` constant array (`"tool_video_generate_heygen"`) and updated the Video Providers section copy from "Runway and Kling" → "Runway, Kling, and HeyGen"; HeyGen falls through the default `"Enter API key..."` placeholder branch (no JSON placeholder — that branch stays Kling-only, byte-identical). Tests: `apps/api/test/tool-credential-settings.test.ts` added `runHeygenCredentialRegistration` function asserting `TOOL_CREDENTIAL_IDS.tool_video_generate_heygen === "tool/video_generate/heygen/api-key"`, `TOOL_CODE_BY_CREDENTIAL_KEY` mapping, `ALL_TOOL_CREDENTIAL_KEYS` membership, `CREDENTIAL_KEY_BY_SECRET_ID` reverse-resolve, and `buildToolCredentialSecretRef` shape; bumped `state.credentials.length` assertion 13 → 14 (`12 visible tool credentials + 2 notification credentials`). `apps/api/test/manage-admin-tool-credentials.service.test.ts` added HeyGen entry to save input + `updatedCredentials` rollout array (symmetric to Runway/Kling). `apps/web/app/admin/tools/page.test.tsx` added HeyGen entry to `credentialsPayload`, updated the existing copy assertion to the new sentence, added focused test asserting HeyGen row renders with default `"Enter API key..."` input placeholder (not the Kling JSON-shape placeholder). Verification (all 7 PASS via orchestrator shell): `corepack pnpm -r --if-present run lint` (apps/web, apps/api, apps/runtime, apps/provider-gateway, apps/sandbox, scripts/smoke — all Done); `corepack pnpm run format:check` ("All matched files use Prettier code style!"); `corepack pnpm --filter @persai/api run typecheck` (Prisma generate + tsc --noEmit clean exit 0); `corepack pnpm --filter @persai/web run typecheck` (tsc --noEmit clean exit 0); `corepack pnpm --filter @persai/api exec tsx test/tool-credential-settings.test.ts` (exit 0); `corepack pnpm --filter @persai/api exec tsx test/manage-admin-tool-credentials.service.test.ts` (exit 0); `corepack pnpm --filter @persai/web exec vitest run app/admin/tools/page.test.tsx` (3 tests pass, 1.77s). Cross-slice invariants 1–15 verified true: no HeyGen URL / HTTP detail anywhere (pure storage, Slice 6 owns the HTTP layer), no keyword routing / message-body parsing introduced (invariant #15), no behavioral change for Runway/Kling/OpenAI rows, no schema/migration/contract/runtime/provider-gateway/docs file touched, no ADR-108 wallet code touched. ADR-109 Slice 1 spec carries `**Status (2026-06-04): Completed.**` block. Deploy: API + WEB. No migration. No feature flag. Admin can now store the HeyGen API key in `/admin/tools` Video Providers section; no runtime callable behavior change yet (Slices 2–11 progressively wire catalog, contract, voice cache, persona registry, provider client, runtime execution, plan toggle, Settings UI, tool description, banner UX, and live smoke).

- **ADR-109 Slice 0 — Baseline + HeyGen API truth + UX erratum (`docs`; 2026-06-04):** docs-only baseline slice closing ADR-109 Slice 0. Baseline SHA `24d1d6ca89b92149a94d77c87c4c3af18cbbbd6c` (post-ADR-108 closure, tree clean). Read-only explore subagent (Claude Opus 4.8 thinking high) audited `developers.heygen.com` and `docs.heygen.com` and returned a cited 8-section HeyGen v3 API truth covering: (1) **Photo Avatar Video** = `POST /v3/videos` with `type: "image"` (Scenario A ad-hoc, no avatar entity) or `type: "avatar"` (Scenario C persona reuse via `avatar_id`); body carries `image|avatar_id`, `script` (1–5000 chars), `voice_id`, `resolution: 4k|1080p|720p`, `aspect_ratio: 16:9|9:16`, optional `voice_settings`, `background`, `Idempotency-Key` header; duration derived from script length, not user-controlled; response `{data: {video_id, status, output_format}}` with initial `status: "waiting"` not in poll enum (defensive parse required). (2) **Voices listing** = `GET /v3/voices` cursor-paginated (default 20, max 100), filter by `language`, `gender`, `type`, `engine`; voice item shape includes **`preview_audio_url: string | null`** — preview works without TTS generation for most voices. Russian officially supported (specific `voice_id`s and RU count not documented — enumerate empirically in Slice 4). (3) **Sync TTS-only** = `POST /v3/voices/speech` (Starfish engine only, filter `?engine=starfish`); returns `{audio_url, duration, request_id, word_timestamps}`; $0.000667/sec ≈ negligible — used as fallback when `preview_audio_url === null`. (4) **Photo avatar lifecycle** = `POST /v3/avatars` body `{type: "photo", name, file}` returns `data.avatar_item.id` (sync return per examples; readiness latency NOT documented); NO documented TTL (avatars persist until deleted); delete `DELETE /v3/avatars/looks/{look_id}` + `DELETE /v3/avatars/{group_id}` (look-delete does NOT auto-delete group — cascade caveat for Slice 5/6). (5) **Poll** = `GET /v3/videos/{video_id}` returns status enum `pending | processing | completed | failed` ONLY — **NO `progress`, NO `eta_seconds`, NO `stage`/`phase` sub-field**. HeyGen strongly recommends webhooks over polling (`callback_url` on create or `POST /v3/webhooks/endpoints`); for MVP PersAI uses polling for consistency with Runway/Kling (cadence 10s per v3 quick-start). Result: `{id, status, video_url, thumbnail_url, gif_url, captioned_video_url, subtitle_url, duration, created_at, completed_at, failure_code, failure_message, video_page_url}`; `video_url` is presigned with ≈7-day expiry. Typical render time NOT officially documented (third-party signals 1–3 min per 1 min output). (6) **Pricing** = per-second USD on self-serve PAYG: Photo Avatar (Avatar IV) 720p/1080p = **$0.05/sec**; 4K = $0.0667/sec; Avatar III legacy = $0.0167/sec (deprecated path, NOT used); avatar creation = **$1.00/call** (separate billable event — matches default `heygenPersonaCreationVcoin = 20 VC` at default `vcoinExchangeRate = 20`). Poll/result response does NOT carry `credits_used` — cost computed client-side as `duration × rate` (matches existing `record-model-cost-ledger.service.ts::calculateTimeMeteredCostMicros` shape from ADR-099/108); account balance via `GET /v3/users/me`. (7) **Rate limits / regions** = 10 concurrent video jobs on PAYG (20+ Enterprise); 429 with `Retry-After`; per-minute RPM caps not published; `Idempotency-Key` header on `POST /v3/videos` supported (1–255 chars, replays within 24h, in-flight retry → 409 `request_in_progress`); regions / data residency NOT documented; no free API tier since Feb 2026 (Slice 11 smoke costs real USD). (8) **Open questions** for empirical follow-up in later slices: status enum exhaustiveness (`waiting` vs 4-value enum), RU voice inventory + Starfish overlap, `preview_audio_url` null-rate, photo-avatar create readiness latency, delete cascade behavior, typical render time for banner sizing, `GET /v3/users/me` shape. Orchestrator + user discussion produced **6 binding UX decisions** recorded as Slice 0 erratum in `docs/ADR/109-heygen-talking-avatar-on-vcoin.md` (new `## Slice 0 erratum (2026-06-04)` section appended; original slice specs untouched; erratum wins on conflict): **E1** Scenario B retired from runtime (persona creation is REST-only via Settings → Characters form; runtime never mutates `workspace_video_personas`; model in chat _advises_ persona creation, has no `create_persona` tool — affects Slices 5/7/10). **E2** Voice preview is a hard requirement everywhere `voice_id` appears (Settings create form, Settings persona cards, chat disambiguation cards) via shared `<VoicePreviewButton voiceId={...}/>`; primary path = HeyGen-native `preview_audio_url`; fallback when null = `POST /v3/voices/speech` generated once per 24h TTL cached in PersAI blob storage — **platform-paid, never debited to user wallet** (≈$0.002 per voice per refresh). **E3** Talking-video banner UX is time-based (not phase-mapped) — new **Slice 10b** specified (web-only, no backend changes, no `media_jobs.phase` column); rotates banner copy by elapsed time (`0-30s "Готовим аватар…"` → `30s-2min "Синтезируем голос…"` → `2-5min "Видео рендерится…"` → `5+ min "Финальный проход…"`); cinematic banner unchanged (preserves cross-slice invariant #1). **E4** Settings Characters section is locked-with-upsell when toggle off (not hidden — was originally hidden in Slice 9 spec); section visible with quiet "Доступно на тарифе X+" hint + inactive link to `/pricing`; existing persona cards remain visible but disabled (no edit/delete/use); tone "не шумно" per user directive. **E5** Persona creation REST-only → new **cross-slice invariant #14**. **E6** HeyGen integration targets v3 only (no v1/v2 endpoints in PersAI code; `Idempotency-Key` on every `POST /v3/videos` submit; avoids 2026-10-31 v1/v2 sunset). **E7** Anti-keyword-routing reinforcement → new **cross-slice invariant #15**: no keyword routing, no message-body parsing, no string-matching for behavior decisions anywhere in PersAI code; mode selection is set by the model, persona resolution is exact-name equality (case-insensitive Unicode), multi-character detection is structural (>1 personaId field or strict structural parse), persona-creation intent is detected by the model via tool description, disambiguation is structured `{status: "needs_disambiguation", candidates: [...]}`, HeyGen status parsing treats any non-terminal value as in-progress. **E8** `video_generate` result type extends to a `needs_disambiguation` discriminated-union member with candidate cards (Slice 3 contract + Slice 7 runtime + Slice 9 UI). **E9** (revised same-day after user feedback) Ad-hoc voice is **model-picked explicitly** following the proven ADR-107 Slice 4 Kling `voice_control` pattern (materialized bundle exposes HeyGen voice shortlist with `voice_id`/`name`/`language`/`gender`/`preview_audio_url`/tags; tool description teaches the model to pick `voiceKey` by request context — gender, language, brand fit; runtime fails honestly with `voice_required` when Scenario A omits `voiceKey` instead of defaulting; Scenario C keeps the persona's `heygen_voice_id` unless the model passes a per-call override) — replaces an earlier draft that proposed runtime hardcoding the first RU preset as a default (rejected as runtime "guessing" violating invariant #15). **E10** Webhook vs polling = polling for MVP (consistency with Runway/Kling), 10s cadence, webhook adoption deferred to a post-program optimization slice. Acceptance checklist gets 11 erratum-induced items appended. ADR-109 § Status updated to "Proposed (2026-06-03). Slice 0 Completed (2026-06-04)" with pointer to erratum. ADR-109 § Slice 0 spec stamped with `**Status (2026-06-04): Completed.**`. Verification: docs-only; no code changed; `git diff` confirms only `docs/SESSION-HANDOFF.md`, `docs/CHANGELOG.md`, `docs/ADR/109-heygen-talking-avatar-on-vcoin.md` modified; zero changes under `apps/`, `packages/`, `prisma/`, `infra/`, `scripts/`. Deploy: NO. Next: ADR-109 Slice 1 — HeyGen credential + Admin Tools UI (API + WEB).

- **ADR-108 Slice 9 + closure — Admin UI VC polish, ADR closure, repo cleanup (`apps/web`, `docs`; 2026-06-04):** closed ADR-108 (Status: Completed) after live dev verification confirmed end-to-end VC flow for `alex@agse.ru` (Kling kling-v2-6 5s clip → `usdMicros=700000`, `vcDebited=14`, balance `999 → 985`; CloudPayments package webhook → `vcCredited=200`, balance `0 → 200`; `/quota_status` LLM tool returns VC-shaped row with `kind: "vcoin"`). Slice 9 admin-UI polish: `apps/web/app/admin/plans/page.tsx` no longer routes `video_generate` through the legacy `MONTHLY_MEDIA_QUOTA_TOOL_CODES` "monthly delivery-confirmed quotas" hint — the tool card now renders a video-specific "Paid video usage is gated by the workspace VC wallet (per-second pricing from the model catalog)" hint while image/edit retain the existing copy. `apps/web/app/admin/plans/_components/MediaPackagesSection.tsx` (`PackageRow`, `PackageForm`): preset cards now show `VC` instead of `u` for video packages, and the form `Units` field becomes a `VC` field (placeholder `200`, tip `Vcoins credited on purchase, e.g. 200`); title placeholders switch to `200 VC` for video. `packageType` plumbed through `PackageForm` props for the create/edit branches. Image/edit/document presets and forms byte-identical. Repo cleanup: removed all 14 transient `.tmp-*` debug artifacts (`.tmp-vcoin-probe.js`, `.tmp-pricing-audit.js`, `.tmp-toolpath-audit.js`, `.tmp-observe-latest.js`, `.tmp-check-balance.js`, `.tmp-query.sql`, plus their `.txt`/`.log` siblings) from the repo root. ADR-108 ADR doc updated: `Status: Completed (2026-06-04)`; `Slice 9 - Tests + docs + verification gate` gained a "Status: Completed" block with the live-verification numbers; acceptance checklist all 18 items checked, plus 2 new bullets covering the 2026-06-04 pricing-math correctness fix and the Slice 9 admin-UI polish. ADR-109 (HeyGen talking-avatar on Vcoin) is unblocked and is the next ADR in this lineage. Verification: PASS `corepack pnpm -r --if-present run lint`; PASS `corepack pnpm run format:check`; PASS `corepack pnpm --filter @persai/api run typecheck`; PASS `corepack pnpm --filter @persai/web run typecheck`; PASS `corepack pnpm --filter @persai/runtime run typecheck`; PASS `corepack pnpm --filter @persai/api run test` (full suite); PASS `corepack pnpm --filter @persai/runtime run test` (full suite); PASS `corepack pnpm --filter @persai/web run test` (full suite). Deploy: web only, no migration.

- **ADR-108 Slice 8 — Full retirement of `videoGenerateMonthlyUnitsLimit` (`apps/api`, `apps/web`, `apps/runtime`, `packages/runtime-contract`, `packages/contracts`, `docs`; 2026-06-04):** removed the deprecated per-unit video plan field from every active-path type, contract, projection, UI, and persisted JSON row. After the 2026-06-04 hotfix (`32ce5408`) the runtime path was already VC-only, but live admin saves on `agse-pro` proved the field could still re-enable per-unit gating in `enqueue-runtime-deferred-media-job` whenever an operator wrote a positive integer back into `billing_provider_hints`. Per the user directive (2026-06-04 02:37 MSK), Slice 8's original docs-only scope was expanded to the full retirement so ADR-109 (HeyGen talking-avatar) has a clean substrate. **API types:** `WorkspaceQuotaPlan`, `AdminPlanInput.quotaLimits`, `AdminPlanState.quotaLimits`, and `PlanQuotaHints` no longer carry the field; `MONTHLY_TOOL_QUOTA_TOOLS` for `video_generate` is now `limitKey: null` (VC-priced, no per-month unit counter); `track-workspace-quota-usage.service.ts::resolveBaseLimitUnits` now short-circuits when `limitKey === null`. **Projections:** `read-internal-runtime-quota-status.service.ts`, `resolve-plan-visibility.service.ts`, `quota-offers.ts` no longer emit/read the field. `quota-grounded-limit-copy.service.ts` continues to render the vcoin branch added in Slice 7. **Contracts:** `packages/runtime-contract/src/index.ts::RuntimeQuotaStatusVisiblePlanLimits`, `packages/contracts/openapi.yaml`, and `apps/runtime/src/modules/turns/persai-internal-api.client.service.ts::isMonthlyToolQuotaStatusVisiblePlanLimits` no longer carry the field; the contract package was regenerated. **Web UI:** `apps/web/app/admin/plans/page.tsx` had `PlanDraft.videoGenerateMonthlyUnitsLimit`, the `NumericDraftField` union member, the `NUMERIC_DRAFT_RULES` entry, the `planToDraft` / `draftToPayload` mappings, the dirty-state slot, and the visually-muted deprecated input row removed; `videoVcoinMonthlyGrant` is now the sole knob for `video_generate`. `apps/web/app/_components/pricing-page-view.tsx::derivePlanFacts` no longer falls back to the legacy `else if (videoGenerateMonthlyUnitsLimit > 0)` branch — the video chip is sourced exclusively from `videoVcoinMonthlyGrant` (+ optional `videoVcoinApproxVideosPerMonth`). **DB cleanup:** new idempotent migration `apps/api/prisma/migrations/20260604030000_adr108_drop_video_generate_monthly_units_limit/migration.sql` strips `videoGenerateMonthlyUnitsLimit` from the `billing_provider_hints` JSONB column on `plan_catalog_plans` (top-level + nested `quotaAccounting`) using `#-` operators, no table/column drop because the field never had its own column. **Tests:** `track-workspace-quota-usage` no longer reserves / settles / reconciles a unit counter for `video_generate`; the affected suites (`assistant-media-job-scheduler.service.test.ts`, `media-delivery-video-vcoin-settle.test.ts`, `media-delivery.service.test.ts`, `quota-accounting.test.ts`) now assert zero legacy-counter operations on the video path; cross-suite fixtures (`manage-admin-plans`, `plan-visibility`, `quota-offers`, `read-internal-runtime-quota-status`, `runtime-quota-status-tool`, `turn-execution`, `manage-assistant-payment-intents`) had the field stripped; `pricing-page-view.test.tsx` exercises both VC branches (with approx, without approx) and the zero-grant case (no chip). ADR-108 cross-slice invariant 10 was rewritten — the rollback-insurance carve-out is removed; `video_generate` reads only `workspace_vcoin_balance` + the catalog row. Verification: PASS `corepack pnpm --filter @persai/api run typecheck`; PASS `corepack pnpm --filter @persai/web run typecheck`; PASS `corepack pnpm --filter @persai/runtime run typecheck`; PASS `corepack pnpm -r --if-present run lint`; PASS `corepack pnpm run format:check`; PASS `corepack pnpm --filter @persai/api run test` (full suite, exit 0); PASS `corepack pnpm --filter @persai/runtime run test` (full suite, exit 0); PASS `corepack pnpm --filter @persai/web run test` (636 tests). Deploy: API + WEB + CONTRACTS + RUNTIME (migration `20260604030000_adr108_drop_video_generate_monthly_units_limit` must run via `Dev Image Publish` migration approval gate per AGENTS.md before downstream slices write to plans).

- **ADR-108 Slice 7 — Quota status tool + runtime advisor, vcoin variant (`packages/runtime-contract`, `apps/api`, `apps/runtime`, `docs`; 2026-06-04):** converted the `quota_status` tool's `video_generate` row from the per-unit shape to a vcoin-flavored shape, and added runtime-advisor copy that references VC balance and typical video cost. `packages/runtime-contract/src/index.ts`: `RuntimeMonthlyToolQuotaStatusToolRow` is now a discriminated union — `RuntimeMonthlyToolQuotaStatusToolRowUnits` (`kind: "units"`, for `image_generate`, `image_edit`, `document`) with all prior fields preserved byte-identical (optional `effectiveLimitUnits` carry-over for backward compat); `RuntimeMonthlyToolQuotaStatusToolRowVcoin` (`kind: "vcoin"`, for `video_generate`) with fields `balanceVc`, `monthlyGrantVc`, `typicalVideoCostVc`, `typicalVideoSeconds`, `typicalCostFromPlatformFallback`, `status: "ok" | "balance_exhausted"`. New `ComputeTypicalVideoVcoinCostService` (injectable, `apps/api/src/modules/workspace-management/application/vcoin/compute-typical-video-vcoin-cost.service.ts`): queries rolling 30-day arithmetic mean `durationSeconds` from `model_cost_ledger_events` via `$queryRaw` for the workspace and purpose `video_generation`; falls back to `TYPICAL_VIDEO_SECONDS_FALLBACK = 5` (= `TYPICAL_VIDEO_SECONDS` from Slice 6a) when no workspace history; returns `{typicalSeconds: null, typicalCostVc: null}` when no active time-metered video catalog pricing; uses BigInt ceil division (mirrors `compute-video-vcoin-cost.ts`). `read-internal-runtime-quota-status.service.ts`: gains three new injections (`WorkspaceVcoinBalanceRepository`, `ResolvePlatformRuntimeProviderSettingsService`, `ComputeTypicalVideoVcoinCostService`); return type updated to `RuntimeAwareMonthlyToolQuotas` (tools array typed as `RuntimeMonthlyToolQuotaStatusToolRow[]`); `execute()` transforms raw snapshot — units tools get `kind: "units"` added, `video_generate` gets full vcoin shape; `computeAdvisoryCandidates` narrows `tool.kind !== "units"` to skip vcoin rows; parallel reads via `Promise.all` when `video_generate` is active. `quota-grounded-limit-copy.service.ts::buildMonthlyToolCopy`: new `video_generate` + `kind === "vcoin"` branch — "You have N VC remaining. A typical video costs about K VC." (when balance > 0 and typicalCostVc != null) / "Your video credits are exhausted. Top up to continue." (when exhausted); units path narrowed to `kind === "units"` before reading unit fields. `persai-internal-api.client.service.ts::isMonthlyToolQuotaStatusTool`: routes validation by `kind` — vcoin variant checks `balanceVc`, `monthlyGrantVc`, `typicalVideoCostVc`, `typicalCostFromPlatformFallback`, `status: "ok" | "balance_exhausted"`; units variant checks the existing flat fields. `enqueue-runtime-deferred-document-job.service.ts`: excludes `kind === "vcoin"` rows before unit-field access (backward-compat with test stubs that have no `kind`). `workspace-management.module.ts`: `ComputeTypicalVideoVcoinCostService` registered. ADR-108 invariants: `model_cost_ledger_events` read-only (aggregation only, no write); `RuntimeBillingFacts` unchanged; image / image-edit / document quota rows byte-identical (other than added `kind: "units"`); no feature flag; no migration; `RuntimeBillingFacts` currency-neutral; Slice 8 untouched. Consumers narrowed (all within `apps/api` and `apps/runtime`): `read-internal-runtime-quota-status.service.ts`, `quota-grounded-limit-copy.service.ts`, `enqueue-runtime-deferred-document-job.service.ts`, `persai-internal-api.client.service.ts`. No web/UI file touched. Tests: `apps/api/test/read-internal-runtime-quota-status.service.test.ts` augmented with 6 new cases (kind:units image row, kind:vcoin video row, workspace history, fallback, no pricing, balance=0 exhausted); `apps/runtime/test/runtime-quota-status-tool.service.test.ts` augmented with vcoin passthrough test (kind:vcoin row in mock outcome, validator pass). Verification: PASS all typechecks (api, web, runtime, provider-gateway); PASS lint; PASS format:check; PASS focused API test; PASS focused runtime test; PASS full API test suite; PASS full runtime test suite. Deploy: API + runtime worker; no migration.

- **ADR-108 Slice 6b — User UI rendering for VC (`apps/web`, `docs`; 2026-06-04):** rendered the VC data plumbed in Slice 6a across three user-facing web surfaces. `apps/web/app/app/_components/assistant-settings.tsx` (`buildMonthlyCard`): `video_generate` monthly card now shows `"Remaining N VC"` as the value and `"1 VC ≈ $X"` (from `workspaceVcoinBalance.vcoinExchangeRate`) as the secondary/tooltip line; all other media cards (image_generate, image_edit, tts, stt) byte-identical. `apps/web/app/_components/pricing-page-view.tsx` (`derivePlanFacts`): new VC branch before the legacy video branch — emits `"X VC / month ≈ Y videos"` when `videoVcoinApproxVideosPerMonth` is present or `"X VC / month"` when it is absent, for plans with `videoVcoinMonthlyGrant > 0`; legacy `videoGenerateMonthlyUnitsLimit` fallback branch retained for un-migrated plans (Slice 8 owns retirement). Image/token/skill fact chips byte-identical. `apps/web/app/app/packages/page.tsx` (`formatPackageLabel`, now exported for test access): `video_generate` packages render `"N VC"` instead of `"N units"` / `"N единиц"`; all other package types byte-identical. Translation keys added to `apps/web/messages/en.json` and `ru.json`: `settings.monthlyVideoVcRemaining` ("Remaining {count} VC"), `pricing.factVideosVc` ("{vc} VC / month"), `pricing.factVideosVcWithApprox` ("{vc} VC / month ≈ {count} videos"). New test file `apps/web/app/app/packages/page.test.tsx` (5 cases covering `formatPackageLabel` for video_generate VC, image_generate units, and title override). Updated `pricing-page-view.test.tsx` (+4 new `derivePlanFacts` VC tests: VC with approx, VC without approx, legacy fallback when grant=0, no fact when grant=0 and units=null). Updated `assistant-settings.test.tsx` (+2 new VC card tests: `"Remaining 250 VC"` + `"1 VC ≈ $0.05"` with image card byte-identical, `"Remaining 0 VC"` with zero balance; updated 1 existing assertion from `"1 / 5"` to `"Remaining 0 VC"` per new rendering behavior). Server is the source of truth for `videoVcoinApproxVideosPerMonth`; no client-side "≈ N videos" math. ADR-108 cross-slice invariants preserved: `apps/api` unchanged; `packages/contracts` unchanged; image/tts/stt cards byte-identical; legacy `videoGenerateMonthlyUnitsLimit` fallback retained; Slice 7/8 untouched. Verification: PASS `corepack pnpm --filter @persai/web run typecheck`; PASS `corepack pnpm --filter @persai/api run typecheck`; PASS `corepack pnpm -r --if-present run lint`; PASS `corepack pnpm run format:check`; PASS `corepack pnpm --filter @persai/web exec vitest run` (637 tests, 64 files). Deploy: web only, no migration.

- **ADR-108 Slice 6a — Public pricing + plan visibility data plumbing for VC (`packages/contracts`, `apps/api`, `docs`; 2026-06-03):** plumbed workspace VC balance and plan Vcoin grant onto the user-facing API surfaces so Slice 6b can render them. `PublicPricingPlanState` (returned by `GET /api/v1/public/plans/pricing`) gained three new fields: `videoVcoinMonthlyGrant` (required integer ≥ 0), `vcoinExchangeRate` (required integer ≥ 1, sourced from `PlatformRuntimeProviderSettings`), and `videoVcoinApproxVideosPerMonth` (optional integer ≥ 0, server-precomputed `floor(grant / ceil(avgUsdPerSecond × 5 × rate))`; omitted when grant is 0 or no active time-metered video catalog rows). `UserPlanVisibilityState` gained a required top-level field `workspaceVcoinBalance: { balanceVc: integer, videoVcoinMonthlyGrant: integer ≥ 0, vcoinExchangeRate: integer ≥ 1 }`, with `balanceVc` from `WorkspaceVcoinBalanceRepository.getOrCreate(workspaceId)` and the anonymous/no-workspace sentinel `{ balanceVc: 0, videoVcoinMonthlyGrant: 0, vcoinExchangeRate: 20 }`. New shared constant `TYPICAL_VIDEO_SECONDS = 5` in `apps/api/src/modules/workspace-management/application/vcoin/typical-video-seconds.ts` (same value as Slice 5 admin UI). `ManageAdminPlansService.listPublicPricingPlans()` injected `ResolvePlatformRuntimeProviderSettingsService` (already registered) and reads `vcoinExchangeRate` plus active time-metered video catalog rows to compute `avgUsdPerSecond`. `ResolvePlanVisibilityService.getUserVisibility()` injected `WorkspaceVcoinBalanceRepository` (already registered) and `ResolvePlatformRuntimeProviderSettingsService` (already registered). `packages/contracts/openapi.yaml` updated with the new fields; `corepack pnpm --filter @persai/contracts run generate` regenerated 4 files in `packages/contracts/src/generated/**`. `apps/web/app/_components/pricing-page-view.test.tsx` and `apps/web/app/app/_components/assistant-settings.test.tsx` received mechanical stub additions to satisfy the new required fields in generated types (no UI logic changed). `apps/api/test/plan-visibility.service.test.ts` updated with two new constructor mocks (`workspaceVcoinBalanceRepository`, `resolvePlatformRuntimeProviderSettingsService`) to match the updated constructor positional order. Verification: PASS `corepack pnpm --filter @persai/contracts run generate`; PASS `corepack pnpm --filter @persai/api run typecheck`; PASS `corepack pnpm --filter @persai/web run typecheck`; PASS `corepack pnpm -r --if-present run lint`; PASS `corepack pnpm run format:check`; PASS `corepack pnpm --filter @persai/api exec tsx test/manage-admin-plans.service.test.ts`; PASS `corepack pnpm --filter @persai/api exec tsx test/resolve-plan-visibility-vcoin.test.ts`; PASS `corepack pnpm --filter @persai/api exec tsx test/plan-visibility.service.test.ts`; PASS `corepack pnpm --filter @persai/api run test` (full API suite). ADR-108 cross-slice invariants preserved: Slice 7/8 untouched; settle/enqueue/quota paths byte-identical; `model_cost_ledger_events` shape unchanged; `RuntimeBillingFacts` unchanged; `videoGenerateMonthlyUnitsLimit` still present in all contracts and projections; `TYPICAL_VIDEO_SECONDS = 5` matches Slice 5; no web production file logic touched (test stubs only). Deploy: API + CONTRACTS (no migration; no web server change; Slice 6b will follow with web UI).

- **ADR-108 Slice 5 — Admin Plans UI (`apps/web`, `docs`; 2026-06-03):** replaced the "Monthly video generations" field in the Admin Plans `Plan limits` section with a new "Monthly VC grant" field wired to `AdminPlanInputBase.videoVcoinMonthlyGrant` (top-level, not nested under `quotaLimits`). Legacy `videoGenerateMonthlyUnitsLimit` field remains fully wired and editable but is visually muted (CSS `opacity-60`, strikethrough title, `(deprecated — use Monthly VC grant)` note) — Slice 8 owns retirement. The new field includes two inline contextual hints: `1 USD = N VC` (read from `runtimeData.vcoinExchangeRate` with fallback `20`) and `≈ N videos` computed as `floor(grant / ceil(avgUsdPerSecond × 5s × exchangeRate))` where `TYPICAL_VIDEO_SECONDS = 5` is a UI-heuristic-only constant; hint shows `≈ — videos` when no active time-metered video profiles exist or pricing is absent. `avgVideoUsdPerSecond` is collected inside `load()` by walking active `time_metered` video-capable profiles in `runtimeData.availableModelCatalogByProvider` (minutes converted to per-second); arithmetic mean across all matching profiles. `vcoinExchangeRate` is stored in a new `useState<number>(20)` initialized inside `load()` from `runtimeData?.vcoinExchangeRate ?? 20`. `PlanForm` now accepts and forwards `vcoinExchangeRate` and `avgVideoUsdPerSecond` props and is exported for test access. `apps/web/app/admin/runtime/page.tsx`: `ModelProfileEditor` and `PriceMetadataEditor` gained `vcoinExchangeRate?: number` prop; the `time_metered` pricing block renders `<span className="text-xs text-muted-foreground">1 USD = {vcoinExchangeRate} VC</span>` next to the "Price / unit" field — layout and all other pricing fields unchanged. Augmented `apps/web/app/admin/plans/page.test.tsx` (+7 new cases) and `apps/web/app/admin/runtime/page.test.tsx` (+1 new case). Verification: PASS `corepack pnpm --filter @persai/web run typecheck`; PASS `corepack pnpm --filter @persai/api run typecheck`; PASS `corepack pnpm -r --if-present run lint`; PASS `corepack pnpm run format:check`; PASS `corepack pnpm --filter @persai/web exec vitest run app/admin/plans/page.test.tsx` (13 tests); PASS `corepack pnpm --filter @persai/web exec vitest run app/admin/runtime/page.test.tsx` (16 tests); PASS `corepack pnpm --filter @persai/web exec vitest run` (626 tests). ADR-108 cross-slice invariants preserved: apps/api unchanged; image plan fields byte-identical; legacy `videoGenerateMonthlyUnitsLimit` present in all wire positions; `videoVcoinMonthlyGrant` maps to top-level payload; runtime page layout preserved. Deploy: web only, no migration.

- **ADR-108 Slice 4 — Packages crediting flip (video_generate only) (`apps/api`, `docs`; 2026-06-03):** wired the unconditional VC credit path for `video_generate` package purchases and a new VC debit path for `video_generate` package refunds, with no feature flag and no schema migration. `manage-media-package-purchase.service.ts::fulfillPackagePaymentIntent` was refactored from a batch-array `prisma.$transaction([...])` to an interactive `prisma.$transaction(async tx => {...})`: video items no longer write `WorkspaceMediaPackageGrant` rows — instead, a single `recordEvent({kind: "package_purchase", amountVc: videoVcCreditTotal, referenceKey: paymentIntentId})` + `credit({amountVc: videoVcCreditTotal, kind: "package_purchase"})` pair runs inside the same `tx` (ledger-first → credit-second, idempotent via the `(workspaceId, "package_purchase", paymentIntentId)` unique index). Non-video items (image_generate, image_edit, document) continue to upsert `WorkspaceMediaPackageGrant` rows with byte-identical payload inside the same transaction. New public method `reversePackagePaymentIntent({paymentIntentId, workspaceId})` reads package items from `WorkspacePaymentIntent.metadata.packageItems` (snapshot, not the catalog row), computes the `videoVcDebitTotal`, and opens a single `prisma.$transaction` that records a `kind: "package_refund"` ledger event with `amountVc: -videoVcDebitTotal` (signed negative, correct for the audit ledger) and calls `debit({amountVc: videoVcDebitTotal})` (absolute value). Idempotency by `(workspaceId, "package_refund", paymentIntentId)`. `handle-cloudpayments-webhook.service.ts`: after `updatePaymentIntent` and before `deriveLifecycleEvent`, a new block hooks the `notificationType === "refund"` path for `media_package_purchase` intents by calling `reversePackagePaymentIntent`; the `payment_reversed` subscription lifecycle event flow is unchanged. Audit log lines emitted: `adr108_vcoin_package_purchase_credited …` on successful credit; `adr108_vcoin_package_refund_debited …` on successful debit. **Admin operator action required before deploy:** existing `video_generate` catalog package rows must have their `units` field re-encoded in VC semantics (e.g. the entry previously labelled "+10 videos" should be updated to the VC equivalent such as 1000). Residual: image/audio package refunds still do not reverse the `WorkspaceMediaPackageGrant` row (pre-existing bug, Scope OUT of this slice). ADR-108 Slice 4 spec carries a `**Status (2026-06-03): Completed.**` block. Verification: PASS `corepack pnpm --filter @persai/api run typecheck`; PASS `corepack pnpm --filter @persai/web run typecheck`; PASS `corepack pnpm -r --if-present run lint`; PASS `corepack pnpm run format:check`; PASS augmented `corepack pnpm --filter @persai/api exec tsx test/manage-media-package-purchase.service.test.ts` (11 cases); PASS augmented `corepack pnpm --filter @persai/api exec tsx test/handle-cloudpayments-webhook.service.test.ts` (+2 refund cases); PASS augmented `corepack pnpm --filter @persai/api exec tsx test/workspace-vcoin-ledger-event.repository.test.ts` (+1 negative-amountVc case); PASS `corepack pnpm --filter @persai/api run test` (full API suite). ADR-108 cross-slice invariants preserved: `model_cost_ledger_events` unchanged; `RuntimeBillingFacts` unchanged; image/image-edit/document paths byte-identical (batch→interactive refactor does not alter non-video upsert behavior); video purchase no longer writes `WorkspaceMediaPackageGrant`; video refund shares ONE transaction for ledger event + wallet debit; no feature flag. Deploy: API only (no migration; `20260603220000_adr108_vcoin_ledger_event` from Slice 3 is the only prior schema change; admin catalog re-encode required before switchover).

- **ADR-108 Slice 3 — Monthly grant on subscription period boundary (`apps/api`, `docs`; 2026-06-03):** wired an idempotent monthly Vcoin grant that credits `videoVcoinMonthlyGrant` (Slice 1 plan field) into `workspace_vcoin_balances` exactly once per `(workspaceId, periodStartedAt)` pair on every subscription period rollover, with strict transactional discipline. New Prisma model `WorkspaceVcoinLedgerEvent` (`workspace_vcoin_ledger_events`, PK on `id` UUID, signed `amount_vc`, `kind` TEXT, `reference_key` TEXT, optional `plan_code`) landed via migration `apps/api/prisma/migrations/20260603220000_adr108_vcoin_ledger_event/migration.sql`. UNIQUE index on `(workspace_id, kind, reference_key)` is the idempotency surface: P2002 on insert ⇒ "already granted". New `WorkspaceVcoinLedgerEventRepository` port (`WORKSPACE_VCOIN_LEDGER_EVENT_REPOSITORY`) + Prisma impl exposes only `recordEvent({workspaceId, kind, amountVc, referenceKey, planCode?, tx})` returning `{recorded: bool}`. Shared parse helper `parseVideoVcoinMonthlyGrant` extracted from `manage-admin-plans.service.ts` into `apps/api/src/modules/workspace-management/application/vcoin/parse-video-vcoin-monthly-grant.ts`; both admin call sites and the grant service now import the same implementation (no parse logic duplication). `WorkspaceVcoinBalanceRepository` (port + Prisma impl) extended with `credit({workspaceId, amountVc, kind, tx?})` — zero is no-op, negative throws, positive increments `balance_vc` via `{increment: amountVc}`; `credit` does NOT write the ledger event (ledger-first → credit-second order enforced by the service). New `GrantMonthlyVcoinService.creditPeriod({workspaceId, planCode, periodStartedAt, tx: Prisma.TransactionClient})` — `tx` is always required; zero-grant plans return no-op without writing the idempotency mark (so a future config bump from 0 to positive can credit retroactively); plan-not-found throws `PlanNotFoundForVcoinGrantError` to force transaction rollback. Rollover hook wired in `ManageWorkspaceSubscriptionLifecycleService.applyActivePaidTransition` INSIDE the existing `prisma.$transaction` block after `appendLifecycleEvent` — a failed grant rolls back the entire subscription upsert + lifecycle event + grant atomically; the post-transaction side effects (dirty-mark / materialization / producer emit) remain unchanged in shape and order. Audit log line `adr108_vcoin_grant_credited workspaceId=… planCode=… periodStartedAt=… vcGranted=… previousBalanceVc=… balanceVc=…` emitted on successful credit; quiet on `alreadyGranted=true`. `GrantMonthlyVcoinService` and `WORKSPACE_VCOIN_LEDGER_EVENT_REPOSITORY` registered in `workspace-management.module.ts`. This table is independent of and parallel to `model_cost_ledger_events` (USD COGS ledger — ADR-108 cross-slice invariant 2: that table unchanged in shape and write site). ADR-108 Slice 3 spec carries a `**Status (2026-06-03): Completed.**` block. Verification: PASS `corepack pnpm --filter @persai/api run pretypecheck`; PASS `corepack pnpm --filter @persai/api run typecheck`; PASS `corepack pnpm --filter @persai/web run typecheck`; PASS `corepack pnpm -r --if-present run lint`; PASS `corepack pnpm run format:check`; PASS new `corepack pnpm --filter @persai/api exec tsx test/grant-monthly-vcoin.service.test.ts` (7 cases); PASS augmented `corepack pnpm --filter @persai/api exec tsx test/workspace-vcoin-balance.repository.test.ts` (+5 credit cases); PASS new `corepack pnpm --filter @persai/api exec tsx test/workspace-vcoin-ledger-event.repository.test.ts` (4 cases); PASS augmented `corepack pnpm --filter @persai/api exec tsx test/workspace-subscription-lifecycle.service.test.ts` (+3 grant hook cases); PASS `corepack pnpm --filter @persai/api run test` (full API suite). ADR-108 cross-slice invariants preserved: `model_cost_ledger_events` unchanged; `RuntimeBillingFacts` currency-neutral; image/TTS/STT settle and enqueue paths byte-identical; post-tx side effects unchanged; no web / runtime / provider-gateway / Stripe / payment-intent / package-purchase change. Deploy: API (migration `20260603220000_adr108_vcoin_ledger_event` must run via `Dev Image Publish` migration approval gate).

- **ADR-108 Slice 2 — Settle path debit (video only) (`apps/api`, `docs`; 2026-06-03):** wired the user-facing Vcoin (VC) settlement for `video_generate` and the advisory wallet pre-check at enqueue, with strict transactional discipline. New pure helper `apps/api/src/modules/workspace-management/application/vcoin/compute-video-vcoin-cost.ts` exports `computeVideoVcoinCost({billingFacts, profile, vcoinExchangeRate}): {vcCost, usdMicros}` — mirrors `record-model-cost-ledger.service.ts::calculateTimeMeteredCostMicros` for the USD-micros leg (cross-slice invariant 2: USD COGS ledger shape stays the source of truth) and applies per-job `ceil` from Decision §53 at the VC level via BigInt math. Throws on non-time-metered facts, non-time-metered profile (catalog/runtime drift), non-positive duration, and non-positive-integer rate — silent 0-VC fallback is forbidden by Slice 2 forbidden patterns. `WorkspaceVcoinBalanceRepository` (port + Prisma impl) gained `debit({workspaceId, amountVc, tx?})`: zero is no-op, negative throws, positive decrements the row's `balance_vc`; when `tx` is supplied all reads/writes share the caller's transaction (Slice 2 settle uses this), when omitted runs against the default client; the repo deliberately permits a one-shot below-zero write so the outer tx can commit cleanly when an artifact's VC cost overshoots the wallet — the lifecycle rejection lives at the next enqueue. `WorkspaceQuotaAccountingRepository.settleMonthlyMediaQuota(input, tx?)` and `TrackWorkspaceQuotaUsageService.settleAssistantMonthlyMediaQuota({…, tx?})` gained an optional `tx`: when supplied the Prisma implementation skips opening its own `$transaction` and skips the serializable retry loop (the outer tx owns isolation and retry); when omitted the image / image-edit / TTS / STT call sites observe byte-identical behavior (cross-slice invariant 1 / 6). `MediaDeliveryService` now branches on `artifact.sourceToolCode === "video_generate"` after successful artifact persistence: it resolves `PlatformRuntimeProviderSettings`, looks up the `(providerKey, modelKey, occurredAt)` time-metered profile via the existing `findRuntimeProviderCatalogProfileForTimestamp`, computes VC cost via the new helper, then opens ONE `prisma.$transaction(async tx => …)` that runs `settleAssistantMonthlyMediaQuota({tx})` followed by `workspaceVcoinBalanceRepository.debit({tx})` so retries cannot double-debit and a failed write rolls both back; image / image-edit branches keep the existing best-effort settle. Audit log line `adr108_video_settle workspaceId=… provider=… model=… durationSeconds=… usdMicros=… vcDebited=… previousBalanceVc=… balanceVc=…` is emitted on every successful debit. `EnqueueRuntimeDeferredMediaJobService` now performs an advisory pre-check on `video_generate` only after the concurrency / activation guards but before the monthly-unit-counter reservation: when `balance_vc <= 0` the enqueue rejects with `code: "vcoin_balance_exhausted"`, `limitKind: "vcoin_balance_exhausted"`, and a user-facing message; existing `videoGenerateMonthlyUnitsLimit` reservation remains as the secondary plan-feature guard for the transitional period. Image / image-edit / TTS / STT enqueues never read the wallet — proven by tests that pass throwing wallet stubs through those paths. Failed deliveries (terminal worker failure, missing billingFacts, settle throw inside the tx) trigger the existing `markMonthlyMediaQuotaReconciliationBestEffort` exactly as before; the wallet is NOT debited on any failure. ADR-108 Slice 2 spec carries a `**Status (2026-06-03): Completed.**` block in the ADR-106 format. Verification: PASS `corepack pnpm --filter @persai/api run typecheck`; PASS `corepack pnpm --filter @persai/web run typecheck`; PASS `corepack pnpm -r --if-present run lint`; PASS `corepack pnpm run format:check`; PASS new `corepack pnpm --filter @persai/api exec tsx test/compute-video-vcoin-cost.test.ts`; PASS augmented `corepack pnpm --filter @persai/api exec tsx test/workspace-vcoin-balance.repository.test.ts` (added 6 debit cases on top of the 4 Slice 1 cases); PASS augmented `corepack pnpm --filter @persai/api exec tsx test/enqueue-runtime-deferred-media-job.service.test.ts` (added 3 VC pre-check cases for empty wallet rejection, positive wallet proceed, image_generate never consulting wallet); PASS new `corepack pnpm --filter @persai/api exec tsx test/media-delivery-video-vcoin-settle.test.ts` (6 cases — Runway + Kling + OpenAI cost paths, image_generate no-VC, settle failure rolls both writes back, missing billingFacts triggers reconciliation); PASS full API test suite via `corepack pnpm --filter @persai/api run test`. Cross-slice invariants preserved: `model_cost_ledger_events` shape and write site unchanged; `RuntimeBillingFacts` stays currency-neutral; image / image-edit / TTS / STT settle paths byte-identical; no chat-routing / OpenAI-image / runtime / provider-gateway / web change; no Stripe / payment-intent / webhook change. Deploy: API only (Slice 1 migration was the only schema change; no new migration in this slice).

- **ADR-108 Slice 1 — Schema + platform contract for VC wallet (`apps/api`, `packages/runtime-contract`, `packages/contracts`, `docs`; 2026-06-03):** landed the Vcoin (VC) wallet substrate without changing any runtime behavior. New Prisma model `WorkspaceVcoinBalance` (`workspace_vcoin_balances`, PK on `workspace_id`, `balance_vc` integer default 0, FK to `workspaces` with `ON DELETE RESTRICT`) and new scalar column `platform_runtime_provider_settings.vcoin_exchange_rate` (integer NOT NULL DEFAULT 20, single platform-level course `1 USD = N VC`, default `1 USD = 20 VC` ⇔ `1 VC = $0.05`) landed via migration `apps/api/prisma/migrations/20260603190000_adr108_workspace_vcoin_balance/migration.sql`. `PlatformRuntimeProviderSettings` read path now defaults `vcoinExchangeRate` to 20 when persisted JSON omits the field and round-trips the value through admin upsert with a positive-integer guard. Plan `billingProviderHints` gains `videoVcoinMonthlyGrant` (integer, default 0) wired through admin plan parse / save / load. New pure helper `apps/api/src/modules/workspace-management/application/vcoin/convert-usd-micros-to-vcoin.ts` exports `convertUsdMicrosToVcoin(micros: bigint, rate: number): number` using round-half-up at the half-VC midpoint (per ADR-108 Decision §51-52); the per-job ceil from Decision §53 is intentionally NOT applied here (it belongs to Slice 2's settle-path wrapper). New read-only-with-create wallet repository at `apps/api/src/modules/workspace-management/infrastructure/persistence/prisma-workspace-vcoin-balance.repository.ts` (port at `apps/api/src/modules/workspace-management/domain/workspace-vcoin-balance.repository.ts`) exposes `getOrCreate(workspaceId)` only; no debit / credit / mutation methods exist yet. `videoGenerateMonthlyUnitsLimit` is JSDoc-`@deprecated` in `packages/runtime-contract/src/index.ts` and `apps/api/src/modules/workspace-management/application/admin-plan-management.types.ts`, and `deprecated: true` plus an explanatory `description` in `packages/contracts/openapi.yaml`; the field stays present on the row for one release cycle as rollback insurance per ADR-108 Non-goals. ADR-108 cross-slice invariants preserved: `model_cost_ledger_events` shape unchanged, `RuntimeBillingFacts` stays currency-neutral, `image_generate` / `image_edit` / `tts` / `stt` per-unit quota counters in `workspace_media_monthly_quota_counters` untouched, no chat-routing / OpenAI-image / runtime / provider-gateway / web change, no Stripe / payment-intent / webhook change. `docs/DATA-MODEL.md` updated with the new wallet substrate paragraph; ADR-108 Slice 1 spec carries a `**Status (2026-06-03): Completed.**` block in the ADR-106 format. Verification: PASS `corepack pnpm contracts:generate`; PASS `corepack pnpm --filter @persai/contracts run typecheck`; PASS `corepack pnpm --filter @persai/api run typecheck`; PASS `corepack pnpm --filter @persai/web run typecheck`; PASS `corepack pnpm --filter @persai/runtime run typecheck`; PASS `corepack pnpm --filter @persai/api exec tsx test/convert-usd-micros-to-vcoin.test.ts`; PASS `corepack pnpm --filter @persai/api exec tsx test/workspace-vcoin-balance.repository.test.ts`; PASS `corepack pnpm --filter @persai/api exec tsx test/platform-runtime-provider-settings.test.ts`; PASS `corepack pnpm --filter @persai/api exec tsx test/manage-admin-plans.service.test.ts`; PASS `corepack pnpm --filter @persai/api run lint`; PASS `corepack pnpm run format:check`. Deploy: API + CONTRACT (Prisma migration must run via `Dev Image Publish` migration approval gate per AGENTS.md before downstream slices can write to the new table).

- **ADR-108 Slice 0 — ADR-107 program closure (`docs`; 2026-06-03):** closed ADR-107 as a program in a new `## Program closure (2026-06-03)` section of `docs/ADR/107-provider-native-video-audio.md`. **Landed** items recorded with CHANGELOG references (slices 1, 4, 5 ledgered in this file on 2026-06-02) plus `file:line` anchors for the code-only slices on the active path: Slice 2 (admin selection and validation) at `apps/api/src/modules/workspace-management/application/platform-runtime-provider-settings.ts:430-433`, Slice 3 (materialization and runtime intent) at `apps/runtime/src/modules/turns/runtime-video-generate-tool.service.ts:1092-1307`. **Deferred indefinitely with no follow-up program**: Kling Omni provider route (`POST /v1/videos/omni-video`), broad Kling multi-image / multi-element generation beyond the bounded `image` + `image_tail` two-image case, Runway voice/avatar APIs being routed through `video_generate` (line 39-40 of ADR-107 stays binding for Runway), the `preserve_reference_audio` and `reference_voice_or_track` audio modes mentioned in the ADR-107 contract sketch, audio-priced ledger dimensions distinguishing silent vs native-audio vs voice-control cost, and delivery copy that narrates produced audio / input-mode details. **Accepted residuals** recorded in the same section: the `omni` capability is honestly representable on a video catalog row but the runtime/save-API hard-rejects `omni` for execution rows (catalog-vs-runtime split is intentional and stays this way until a future ADR opens the `/omni-video` route); OpenAI video continues to share the existing `tool_image_generate` OpenAI media credential entry (no dedicated `tool/video_generate/openai/api-key`; future change requires its own ADR). The ADR-107 acceptance checklist was updated to partial — 6 items `[x]` (landed), 5 items `[~]` (deferred indefinitely with explicit "no follow-up program" note). Cross-links: ADR-108 is the program closure track for ADR-107; ADR-109 is the talking-avatar program with a single HeyGen-only named exception to ADR-107 line 39-40 (line 39-40 stays binding for Runway, OpenAI, Kling, and any future provider not opened by its own ADR). ADR-108 Slice 0 in `docs/ADR/108-video-vcoin-economy-and-pre-talking-avatar-cleanup.md` carries a `**Status (2026-06-03): Completed.**` block in the ADR-106 format. ADR-102 pre-PROD path `0 → 1 → 2 → 9 → 10` remains unblocked. Verification: docs-only; no code changed; no enum cleanup performed. Deploy: NO.

- **ADR-108 + ADR-109 opened — Video Vcoin economy and HeyGen talking-avatar mode (`docs`; 2026-06-03):** opened two new ADR programs as parallel tracks to ADR-102. **ADR-108** introduces Vcoin (VC) as the user-facing settlement currency for `video_generate` only (image / TTS / STT keep per-unit quotas). Course `1 USD = 20 VC`, integer VC only, minimum step `$0.05 = 1 VC`. New `workspace_vc_balance` table; wallet accumulates across periods; settle-only debit on delivery success; USD COGS ledger (`model_cost_ledger_events`) unchanged. ADR-106 Slice 9 ("media quota settlement unchanged") is explicitly superseded for `video_generate` only. ADR-108 Slice 0 closes ADR-107 as a program (docs-only): landed items stay landed; Kling Omni, broad multi-image, Runway voice/avatar in `video_generate`, `preserve_reference_audio` / `reference_voice_or_track` audio modes, audio-priced ledger dimensions, and delivery narration are deferred indefinitely with no follow-up program. Migration is manual through Admin UI. **ADR-109** adds HeyGen as the 4th `video_generate` provider with a new top-level `mode: "cinematic" | "talking_avatar"`. Talking-avatar is HeyGen-only (no fallback to Runway / Kling / OpenAI). ADR-107 line 39-40 is preserved for those providers; this ADR carves a named exception for HeyGen. Voice MVP = HeyGen presets only (cloning deferred). Multi-character per clip deferred. New workspace persona registry (`workspace_video_personas`, default limit 10/workspace, configurable). HeyGen avatar id created lazily on first persona reuse. Persona creation costs a fixed VC amount (default 20 VC); render cost is variable VC through ADR-108 settle path. Plan toggle `talkingVideoEnabled` on `video_generate` tool activation card. Both ADRs run in parallel to ADR-102 and do not block its pre-PROD path. Both ADRs carry a full orchestrator-agent execution model: read-only-for-code orchestrator spawns one implementation subagent per slice with a documented prompt template (Scope IN / Scope OUT / Forbidden patterns / Required tests / Verification commands / Return structure), and a per-slice acceptance gate blocks closing the slice until every gate item passes. Risk-sensitive slices (ADR-108 slices 1/2/4/7; ADR-109 slices 3/5/6/7/10) carry inline Scope OUT and Forbidden patterns. Verification: docs-only; no code changed. Deploy: NO.

- **ADR-107 Slice 4 — bounded Kling voice control + 2-image tail mapping (`packages/runtime-contract`, `apps/runtime`, `apps/provider-gateway`, `docs`; 2026-06-02):** implemented the next honest Kling execution slice on the current standard Kling video path. Runtime `video_generate` now accepts explicit `voiceIds[]` and `voiceKeys[]` and, for Kling only, maps `audioMode:"voice_control"` to the documented `voice_list` path when the request carries real ordered voices; this now covers both the bounded prompt-only `text2video` path and the image-backed `image2video` path, with provider-gateway forcing `sound:"on"` per the official schema. The first bounded multi-image-compatible path is also now live for Kling: when `inputMode:"multi_image"` and exactly two ordered image aliases are provided, runtime loads them as `image` + `image_tail` instead of refusing or silently degrading to one image. Broader multi-image/custom storyboard semantics are still intentionally unsupported: requests with more than two images or voice control without explicit voice ids/keys fail honestly with structured unsupported results. Runway audio and Kling Omni remained out of scope for this slice. Verification: PASS `corepack pnpm --filter @persai/runtime exec tsx test/runtime-video-generate-tool.service.test.ts`; PASS `corepack pnpm --filter @persai/runtime exec tsx test/native-tool-projection.test.ts`; PASS `corepack pnpm --filter @persai/provider-gateway exec tsx test/kling-provider.client.test.ts`; PASS `corepack pnpm --filter @persai/provider-gateway exec tsx test/provider-video-generation.service.test.ts`; PASS `corepack pnpm --filter @persai/runtime-contract run typecheck`; PASS `corepack pnpm --filter @persai/runtime run typecheck`; PASS `corepack pnpm --filter @persai/provider-gateway run typecheck`.

- **ADR-107 Slice 5 — bounded billing + unsupported-mode honesty verification (`apps/runtime`, `apps/api`, `docs`; 2026-06-02):** kept ADR-107 Slice 5 intentionally narrow. No new native-audio or voice-control pricing/accounting dimensions were introduced. Instead, the async video-job path now preserves current billing behavior while fixing one honesty bug: `requested_mode_unsupported` video outcomes are treated as terminal user/runtime rejections, not retryable runtime failures, so unsupported native-audio / voice-control / deferred input-mode requests do not churn through the scheduler or masquerade as ordinary video generation problems. Added focused regression coverage that async scheduler persistence keeps provider/model video billing facts unchanged for audio-capable rows (for example `runway/veo3.1`) and that unsupported video mode requests fail terminally instead of requeueing. Verification: PASS `corepack pnpm --filter @persai/runtime exec tsx test/runtime-media-job-run.service.test.ts`; PASS `corepack pnpm --filter @persai/api exec tsx test/assistant-media-job-scheduler.service.test.ts`; PASS `corepack pnpm --filter @persai/runtime run typecheck`; PASS `corepack pnpm --filter @persai/api run typecheck`.

- **ADR-107 Slice 1 — video capability contract/catalog truth (`packages/runtime-contract`, `packages/contracts`, `apps/api`, `apps/web`, `apps/runtime`, `docs`; 2026-06-02):** added the smallest additive capability model needed to represent honest video capability truth on catalog video rows without changing execution/materialization/provider-gateway wiring. `RuntimeVideoModelParameters` now carries `audioCapabilities` (`silent`, `provider_native_audio`, `voice_control`) and `inputCapabilities` (`text`, `single_reference_image`, `multi_image`, `omni`) alongside the existing duration/aspect/reference-image/provider-parameter metadata. API runtime-provider normalization and profile resolution now default legacy video rows to `audioCapabilities:["silent"]` plus `inputCapabilities:["text"]`, automatically adding `single_reference_image` when `referenceImageSupported=true`; this keeps existing rows valid while making Runway/Kling/OpenAI capability truth explicit. Runway/Kling remain video-only catalog providers and still do not enter chat routing; `availableModelsByProvider` remains chat-only OpenAI/Anthropic, and `image_generate` / `image_edit` remain OpenAI-only. OpenAPI and generated contracts now expose `videoModelParameters` and the new capability enums/shape, and the existing Admin Runtime web consumer was updated only enough to stay contract-compatible. Verification: `corepack pnpm contracts:generate`; PASS `corepack pnpm --filter @persai/api exec tsx test/platform-runtime-provider-settings.test.ts`; PASS `corepack pnpm --filter @persai/api exec tsx test/runtime-provider-profile.test.ts`; PASS `corepack pnpm --filter @persai/contracts run typecheck`; PASS `corepack pnpm --filter @persai/api run typecheck`; PASS `corepack pnpm --filter @persai/runtime run typecheck`; PASS `corepack pnpm --filter @persai/web exec vitest run app/admin/runtime/page.test.tsx --config vitest.config.ts`; PASS `corepack pnpm --filter @persai/web run typecheck`.

- **Hotfix follow-up — Kling live poll endpoint correction (`apps/provider-gateway`, `docs`; 2026-06-02):** direct live probing against `api-singapore.klingai.com` showed that PersAI's previous switch to `GET /v1/videos/status?task_id=...` was wrong for the deployed Kling video flow: create requests succeeded, but that status endpoint returned `404 Not Found` with `No message available`. The adapter now polls the live-working task endpoints `GET /v1/videos/text2video/{task_id}` and `GET /v1/videos/image2video/{task_id}` again, which return the actual `task_status` payload for accepted jobs. Verification: focused `@persai/provider-gateway` Kling test PASS locally; direct live probe reproduced create success plus `404` on `/v1/videos/status`, then confirmed `{task_id}` endpoints return the real processing payload.

- **ADR-107 truth correction — native audio vs voice control vs Kling multi-image/Omni (`docs`; 2026-06-02):** corrected ADR-107 after official-doc plus live-provider verification. The ADR now distinguishes provider-native audio from provider-side voice control, treats Kling multi-image as a separate capability family rather than the current single-image path, and records that Kling Omni is a distinct provider route (`POST /v1/videos/omni-video`) instead of a normal `text2video` / `image2video` model switch. Runway audio remains gated to documented audio-capable video models such as `veo3.1` / `veo3.1_fast`; ordinary Gen-4.5 / Gen-4 Turbo must not be treated as native-audio-capable by default. Verification: docs-only; no code changed.

- **Hotfix follow-up — bounded polling retry for async video providers (`apps/provider-gateway`, `docs`; 2026-06-02):** fixed the live `video_generate` fallback chain where Kling, Runway, or OpenAI could already have an accepted provider task but PersAI still abandoned the primary attempt on a transient poll-time transport failure such as `fetch failed`. Provider-gateway video polling for Kling, Runway, and OpenAI now keeps the existing provider `taskId`/`videoId`, tolerates up to 2 transient poll-fetch failures after the first one, and only fails the attempt after the bounded retry budget is exhausted; no provider task is recreated during those retries. Added focused regression coverage for Kling/Runway/OpenAI poll recovery on thrown `fetch failed`, alongside the existing OpenAI transient HTTP retry path. Verification: focused provider-gateway video tests PASS; recursive lint PASS; root `format:check` PASS; full repo test PASS; API/WEB/RUNTIME/PROVIDER-GATEWAY typechecks PASS.

- **Hotfix follow-up — Kling result parsing + Runway fallback delivery (`apps/provider-gateway`, `apps/runtime`, `docs`; 2026-06-02):** fixed the live `video_generate` failure chain where Kling primary could complete without producing a PersAI artifact because provider-gateway only recognized older Kling result URL shapes. Kling video polling now also accepts downloadable URLs from `data.response[0]`, `data.videoUrl`, and `data.video_url`. Runway fallback now routes prompt-only requests to `/text_to_video` instead of `/image_to_video` without `promptImage`, and runtime video response validation accepts provider-catalog durations such as Runway `seconds=5` instead of the stale OpenAI-only `4/8/12` set. Runtime now logs every video provider attempt with provider/model/seconds/fallback state so primary Kling failures are no longer hidden behind the final fallback outcome. Verification: focused provider-gateway/runtime video tests PASS; recursive lint PASS; root `format:check` PASS; full repo test PASS; API/WEB/RUNTIME/PROVIDER-GATEWAY typechecks PASS.

- **Hotfix follow-up — video catalog params UI + runtime bundle hash guard (`apps/api`, `apps/runtime`, `apps/web`, `docs`; 2026-06-01):** closed the live tail after catalog-driven video params landed. API runtime-boundary callers now derive `bundleHash` from the exact `runtimeBundleDocument` being sent/warmed, preventing stale DB hash drift from failing runtime validation with `bundleDocument hash does not match bundle.bundleHash`. Runtime failure finalization now retries a minimal failed receipt write when the full failure payload cannot be persisted, so accepted receipts are not left stuck. `Admin > Runtime` now creates, displays, edits, and saves `videoModelParameters` for video catalog rows; new Runway/Kling video rows receive provider defaults and the focused web test asserts the params are included in the saved catalog payload. `persai-dev` was recovered in place: mismatched materialized spec hashes were corrected, affected assistants were advanced to generation `922`, and their active runtime bundles now include `videoModelParameters`. Verification: recursive lint PASS; root `format:check` PASS; recursive typecheck PASS; focused API/runtime/web tests PASS. Full recursive test reached known web Vitest parallel-load timeouts; every timed-out file passed in isolated reruns.

- **Hotfix — catalog-driven video model parameters + Kling v3 (`apps/provider-gateway`, `apps/runtime`, `apps/api`, `packages`, `docs`; 2026-06-01):** fixed the live post-`344d0d1e` video-provider regressions by moving provider/model video constraints into managed model-catalog rows instead of runtime hardcoding. Video catalog profiles now carry `videoModelParameters` for duration constraints, aspect-ratio/size mappings, reference-image support, and narrow provider parameters such as Kling `mode=pro`; materialization copies those parameters into `video_generate` credential refs, and runtime normalizes omitted/invalid `seconds` and omitted `size` from the selected catalog model before calling the provider. Runway Gen-4.5 now uses `1280:720` / `720:1280` and catalog durations `[5, 8, 10]`; official Kling uses `kling-v3` with catalog range `3..15` and preferred `4/8/12`. Provider-gateway requests now carry provider parameters through to adapters, and Kling no longer defaults to stale v1. Verification: focused provider-gateway/runtime/API tests PASS; API/RUNTIME/PROVIDER-GATEWAY typechecks PASS; root `format:check` PASS. Deploy: API/RUNTIME/PROVIDER-GATEWAY plus persisted dev catalog rows updated to include `videoModelParameters` and `kling-v3`.

- **ADR-107 accepted — provider-native audio for generated video (`docs`; 2026-06-01):** added the executable ADR for adding provider-native audio to `video_generate` without building a PersAI-side TTS/mux pipeline. The ADR keeps silent video as the default, targets Kling first for native audio because current provider docs describe video-native audio/voice binding, gates Runway audio to documented audio-capable model/mode combinations, and preserves ADR-106 invariants: Runway/Kling stay video-only, chat routing stays OpenAI/Anthropic, and `image_generate` / `image_edit` stay OpenAI-only. Verification: docs-only; no code changed.

- **Kling official API correction — removed KIE proxy path (`apps/provider-gateway`, `apps/runtime`, `apps/api`, `apps/web`, `docs`; 2026-06-01):** replaced the active Kling implementation that incorrectly called KIE proxy endpoints (`api.kie.ai`, `kieai.redpandaai.co`) with the official Kling API flow on `api-singapore.klingai.com`. The provider-gateway Kling adapter now authenticates by generating a short-lived JWT from the operator's official Kling Access Key + Secret Key, calls the official `POST /v1/videos/text2video`, `POST /v1/videos/image2video`, and `GET /v1/videos/{task_id}` task endpoints, and sends reference images directly as official base64 `image` input instead of using any proxy upload path. The Kling credential slot remains `tool/video_generate/kling/api-key` but now stores Kling-only JSON (`{"accessKey":"...","secretKey":"..."}`) instead of a single bearer key. `Admin > Tools` copy and placeholders now describe the real Kling credential shape, active docs no longer describe KIE as the Kling runtime path, and runtime keeps the bounded video fallback improvement where provider-gateway 5xx failures are fallback-eligible but timeouts/unconfigured service failures are not blindly retried. Verification: focused provider-gateway/runtime/web tests PASS; touched-package typechecks PASS; root `format:check` PASS. Deploy: API/WEB/RUNTIME/PROVIDER-GATEWAY. Residual: live smoke with real official Kling credentials is still required after deploy to validate the exact enabled model names and any provider-side payload/version constraints.

- **ADR-106 Slice 10 — final verification and docs (`apps/api`, `apps/web`, `apps/runtime`, `apps/provider-gateway`, `docs`; 2026-06-01):** closed the Runway/Kling video provider program with an independent read-only audit, repo gates, and focused cross-app checks. Final cleanup removed stale/dead lint leftovers, made the legacy web runtime-provider helper preserve Runway/Kling video-only catalog buckets instead of dropping them, and made Runway/Kling polling loops lint-clean without changing their timeout-controlled behavior. The implemented state is code-live-callable for `video_generate` through OpenAI/Runway/Kling after deployment and real operator credentials; `image_generate` and `image_edit` remain OpenAI-only, and Runway/Kling remain excluded from chat routing. Live smoke with real Runway/Kling keys was not run in this local Slice 10 session and remains the next deploy/operator validation step. Verification: independent audit PASS with no blockers; recursive lint PASS; root `format:check` PASS; `@persai/api`, `@persai/web`, `@persai/runtime`, and `@persai/provider-gateway` typecheck PASS; focused API/web/runtime/provider-gateway suites PASS. Deploy: API/WEB/RUNTIME/PROVIDER-GATEWAY.

- **ADR-106 Slice 9 — video provider cost attribution (`apps/api`, `docs`; 2026-06-01):** fixed persisted billing-fact ledger pricing lookup so generated video cost uses the executing provider's catalog bucket and timestamp-matched row instead of searching model keys across providers. OpenAI, Runway, and Kling video billing facts now append provider/model/cost truth from the actual provider row, including inactive historical rows whose effective window covers `occurredAt`; no ledger row hardcodes OpenAI for video. Media quota settlement remains unchanged: monthly media units stay plan-owned user quota, while provider cost facts remain additive accounting truth. No runtime/provider-gateway execution changes and no Slice 10 E2E work were included. Verification: focused model-cost ledger test PASS; `@persai/api` typecheck PASS; root `format:check` PASS. Deploy: API.

- **ADR-106 Slice 8 — runtime provider-aware video execution and fallback (`apps/runtime`, `docs`; 2026-06-01):** runtime `video_generate` execution now uses the materialized provider/secret/model refs from the assistant bundle when calling provider-gateway. OpenAI video remains the default existing path, Runway/Kling refs send their provider ids and dedicated secret ids, and video fallback is now provider-aware and bounded: one configured materialized fallback ref is attempted after an eligible terminal primary failure, with honest warning text. Runtime provider-gateway response validation now accepts OpenAI/Runway/Kling video results and rejects provider mismatches. Image generation/edit paths were regression-tested and remain unchanged. No provider-gateway adapter changes, API materialization/gating changes, or billing ledger/pricing work was included; returned provider-gateway billing facts continue to flow into persisted runtime artifacts as before. Verification: focused runtime video and provider-gateway client tests PASS; runtime image generate/edit regressions PASS; `@persai/runtime` typecheck PASS; root `format:check` PASS. Deploy: RUNTIME.

- **ADR-106 Slice 7 — Runway/Kling provider-gateway video adapters (`apps/provider-gateway`, `docs`; 2026-06-01):** added provider-gateway Runway and Kling video clients plus `video_generate` dispatch by materialized provider id (`openai`, `runway`, `kling`). Runway uses the documented `X-Runway-Version: 2024-11-06` image/text-to-video task flow (`image_to_video` + task polling + download) and maps supported PersAI sizes to the version-specific `1280:768` / `768:1280` ratios. Kling now uses the official Kling API task flow with JWT auth from Access Key + Secret Key, `text2video` / `image2video` creation, unified task polling, and normalized video result download; no KIE proxy upload/task endpoints remain in the active adapter path. Provider-gateway returns provider/model keyed time-metered billing facts for the new adapters; downstream ledger attribution remains Slice 9. OpenAI video model validation remains Sora-only, while Runway/Kling accept non-empty catalog model ids. No runtime execution/fallback orchestration, API materialization/gating, provider billing ledger, or Slice 8+ work was included. Verification: focused provider-gateway dispatch/Runway/Kling tests PASS; full provider-gateway suite PASS; `@persai/provider-gateway` typecheck PASS; root `format:check` PASS. Deploy: PROVIDER-GATEWAY.

- **ADR-106 Slice 6 — video provider runtime gating (`packages/runtime-contract`, `apps/api`, `apps/runtime`, `docs`; 2026-06-01):** widened the shared `video_generate` provider allowlist and matching API/runtime native gates from OpenAI-only to OpenAI/Runway/Kling. Configured Runway/Kling video refs are now allowed by API runtime tool policy and projected by runtime native tool projection, while image generation/edit provider gates remain OpenAI-only and chat routing remains OpenAI/Anthropic-only. No provider-gateway clients, Runway/Kling dispatch/execution flow, billing ledger/facts, or provider adapters were added. Verification: focused API runtime tool policy test PASS; focused runtime native projection test PASS; root `format:check` PASS; `@persai/runtime-contract`, `@persai/api`, and `@persai/runtime` typecheck PASS. Deploy: runtime-contract consumers/API/RUNTIME.

- **ADR-106 Slice 5 — provider-specific video credential materialization (`apps/api`, `docs`; 2026-06-01):** published assistant bundle materialization now builds `video_generate` credential refs from the selected active video catalog provider instead of always cloning the image credential ref. OpenAI video continues to materialize through the existing OpenAI media credential (`tool/image_generate/api-key`), while Runway and Kling materialize through `tool/video_generate/runway/api-key` and `tool/video_generate/kling/api-key`; cross-provider video fallback refs carry their own provider/secret refs. Raw Runway/Kling tool credentials are skipped in the generic tool-credential loop so they only appear as provider-specific `video_generate` refs/fallbacks. `image_generate` and `image_edit` remain on the existing OpenAI image credential path. Missing selected video models now fail clearly during materialization. No runtime/provider-gateway execution gates, provider clients, or billing changes were included. Verification: focused materialization test PASS; root `format:check` PASS; `@persai/api` typecheck PASS. Deploy: API.

- **ADR-106 Slice 4 — plan video model validation across catalog providers (`apps/api`, `apps/web`, `docs`; 2026-06-01):** plan `videoGenerateModelKey` and `videoGenerateFallbackModelKey` now validate against active video catalog rows from OpenAI, Runway, and Kling while image model validation remains on the existing image-capable path. Because plan media selections still store bare model keys, Admin Runtime settings and plan save validation now reject duplicate active video model ids across OpenAI/Runway/Kling instead of allowing ambiguous provider resolution. `Admin > Plans` shows provider-labeled video options and disables duplicate active video ids with an explicit warning; saved values remain bare model keys, so no contract migration was introduced. No materialization, runtime/provider-gateway execution, provider clients, or billing changes were included. Verification: focused API plan/runtime-settings tests PASS; focused Admin Plans web test PASS; root `format:check` PASS; `@persai/api` typecheck PASS; `@persai/web` typecheck PASS. Deploy: API/WEB.

- **ADR-106 Slice 3 — Runway/Kling Admin Runtime catalog UI (`apps/web`, `docs`; 2026-06-01):** extended `Admin > Runtime` so the provider model catalog renders OpenAI, Anthropic, Runway, and Kling cards. Runway/Kling catalog editing is video-only in the UI, new video rows default to `time_metered`, and copy stays explicit that catalog readiness does not make live execution available. Primary/fallback/router chat provider selectors remain OpenAI/Anthropic-only. No API, plan selection, materialization, runtime/provider-gateway execution, provider clients, or billing changes were included. Verification: focused Admin Runtime web tests PASS; root `format:check` PASS; `@persai/web` typecheck PASS. Deploy: WEB.

- **ADR-106 Slice 2 — Runway/Kling Admin Tools video credentials (`apps/api`, `apps/web`, `docs`; 2026-06-01):** added separate encrypted Admin Tools credential entries for Runway and Kling video provider API keys (`tool_video_generate_runway` -> `tool/video_generate/runway/api-key`, `tool_video_generate_kling` -> `tool/video_generate/kling/api-key`) using the existing secret-store and masked metadata path. `Admin > Tools` now renders a dedicated Video Providers section while the existing OpenAI media slot remains `tool_image_generate` for image generation, image edit, and current OpenAI video behavior. No catalog UI, plan selection, materialization, runtime/provider-gateway execution, provider clients, or billing changes were included. Verification: focused API tool-credential tests PASS; focused web Admin Tools test PASS; root `format:check` PASS; `@persai/api` typecheck PASS; `@persai/web` typecheck PASS. Deploy: API/WEB.

- **ADR-106 Slice 1 — video-only Runway/Kling catalog providers (`packages/contracts`, `apps/api`, `apps/web`, `docs`; 2026-06-01):** split runtime provider concepts so chat routing remains `openai`/`anthropic` while the structured managed model catalog can now carry `openai`, `anthropic`, `runway`, and `kling` buckets. Runway/Kling are accepted only as video-only catalog providers; they do not enter `availableModelsByProvider`, primary/fallback/router chat selectors, credential storage, materialization, runtime execution gates, provider-gateway clients, or image generation/editing. OpenAPI and generated contracts now expose the four-bucket catalog shape, with minimal web compatibility updates so existing admin surfaces compile while Slice 3 UI work remains separate. Verification: `contracts:generate`; `format:check`; `@persai/contracts`, `@persai/api`, `@persai/web` typecheck; focused API tests for platform runtime provider settings, runtime provider profile, and admin runtime provider settings service PASS. Deploy: API/WEB if this catalog contract is shipped.

- **ADR-105 follow-up — idempotent runtime media-job run replay (root fix for duplicate execution) (`apps/runtime`, `apps/api`, `docs`; 2026-06-01):** replaced the temporary file-recovery mitigation with a root application fix for the live duplicate-execution bug. The proven failure mode was: API called `/internal/runtime/media-jobs/run`, transport returned retryable `fetch failed`, scheduler requeued the same durable job, and runtime re-executed side effects because `/run` itself was not idempotent. `apps/runtime/src/modules/turns/runtime-media-job-run.service.ts` now wraps async media worker execution in the same durable accepted/completed receipt protocol used by runtime turns: the first call claims a stable `media-job-run:<jobId>:...` execution key, writes terminal result into the runtime turn receipt, and any repeat call for the same durable job either replays the persisted completed result or returns an explicit in-flight conflict instead of executing the tool path again. In parallel, `apps/api/src/modules/workspace-management/application/assistant-media-job-scheduler.service.ts` no longer performs file-based artifact recovery on retryable runtime-run failures; retryable transport loss now requeues honestly, and the repeated `/run` is safe because runtime returns replay/in-flight semantics rather than duplicating artifacts. Added focused runtime coverage for accepted execution, completed replay, and in-flight duplicate suppression, and removed the obsolete API persisted-files recovery regression. Verification: focused PASS `@persai/runtime test/runtime-media-job-run.service.test.ts`; focused PASS `@persai/api test/assistant-media-job-scheduler.service.test.ts`; recursive lint PASS; root `format:check` PASS; `@persai/runtime` typecheck PASS; `@persai/api` typecheck PASS; `@persai/web` typecheck PASS; broad `@persai/api` suite PASS. Deploy: runtime + api images required.
- **ADR-105 follow-up — preserve partial series artifacts + make multi-image guidance series-first (`apps/runtime`, `apps/api`, `docs`; 2026-06-01):** fixed the two live media tails found in `persai-dev` without keyword routing. First, multi-image runtime execution no longer drops already persisted outputs when a later `series` item fails. `runtime-image-edit-tool.service.ts` and `runtime-image-generate-tool.service.ts` now treat any multi-image request as item-by-item single-image execution inside one job (explicit `series` still works; `variants`/plain multi-count now also execute through the same one-output-per-item path), and if a later item hits provider safety or another terminal failure after earlier artifacts were already persisted, runtime returns the produced artifacts plus a structural partial-warning instead of collapsing the whole worker result to `artifacts=[]`. That allows API scheduler truth to move the job into `completion_pending` and deliver the files that really exist, instead of falsely terminal-failing with files stranded only in the assistant file registry. Second, model-facing tool guidance is now `series`-first for any multi-image `image_generate` / `image_edit` request; `variants` remains in the schema only as a rare compatibility fallback and is no longer advertised as the normal path. Verification: focused runtime PASS (`runtime-image-edit-tool.service`, `runtime-image-generate-tool.service`, `native-tool-projection`, `runtime-media-job-run.service`); focused API PASS (`assistant-media-job-scheduler.service`). Deploy: runtime + api images required.
- **ADR-105 follow-up — invalid-arguments budget refund + ref-bound series guard (`apps/runtime`, `docs`; 2026-05-31):** closed the next two live carousel/runtime regressions without keyword routing. First, per-turn media budgeting now refunds a previously reserved media unit batch when the tool result is a structural `action:"skipped" + reason:"invalid_arguments"` outcome, so one malformed `image_generate` / `image_edit` / `video_generate` call no longer burns the whole turn budget and block a corrected same-turn retry with `tool_budget_exhausted`. Second, ref-bound multi-frame requests now stay structurally tied to the available source image: `turn-execution.service.ts` passes reusable image attachments into `image_generate`, and `runtime-image-generate-tool.service.ts` rejects `outputMode="series"` generation with `reason:"source_image_required"` when a reusable current-turn image already exists, instructing the model to switch to `image_edit` with `sourceImageAlias` instead of regenerating from scratch. Also strengthened series prompt composition in both generate/edit paths so each frame keeps one product/campaign identity and returns one final image, not a collage/grid. Verification: focused runtime suites PASS (`tool-budget-policy`, `runtime-image-generate-tool.service`, `runtime-image-edit-tool.service`, `native-tool-projection`, `turn-execution.service`); recursive lint PASS; root `format:check` PASS; `@persai/api` typecheck PASS; `@persai/web` typecheck PASS. Deploy: runtime image required.

- **Web chat follow-up — stop running-status attachment bleed + refresh landing market PNG assets (`apps/web`, `docs`; 2026-06-01):** fixed the remaining web-only media rendering tail where a new pending assistant bubble could visually inherit an older committed image while a turn was still `running`. Root cause was in `use-chat.ts`: `applyTurnStatusState()` reused `status.assistantMessage.attachments` while the primary stream still owned the live assistant placeholder, so a reattach/status payload that surfaced an older committed assistant message could stick its image onto the new pending bubble. The running/accepted live assistant path now treats turn-status as progress-only truth and never hydrates attachments from it; attachments appear only from terminal completed transport or committed history. Added a focused regression in `use-chat.test.tsx` that reproduces the exact stale-image case (old committed assistant with image attachment + new running turn) and asserts the live streaming bubble has no attachments. Also switched the founder-owned landing market demo assets from `jpg` to refreshed `png` files in `block-market.tsx` and added the six new localized PNG assets under `apps/web/public/landing/market/`. Verification: recursive lint PASS; root `format:check` PASS; `@persai/api` typecheck PASS; `@persai/web` typecheck PASS; focused web suite PASS (`app/app/_components/use-chat.test.tsx`, 82/82). Deploy: web image required.

- **Delivery honesty follow-up — media-aware localized delivered fallback (`apps/api`, `apps/web`, `docs`; 2026-06-01):** fixed the remaining post-ADR-105 delivery copy seam where attachment-only media replies could still collapse to the file-shaped fallback `File sent.` / `Файл отправлен.` after structural cleanup removed technical attachment lines and left no assistant prose. `final-delivery-honesty.ts` now chooses the empty-body delivered fallback structurally from `attemptedArtifactKind` instead of always assuming `"file"`: `media` returns `Media sent.` / `Медиафайл отправлен.`, while document/file delivery keeps `File sent.` / `Файл отправлен.`. Updated focused API coverage in `final-delivery-honesty.test.ts` and `stream-web-chat-turn.service.test.ts` so streamed attachment-only media replies assert the media-aware fallback. Included the founder-owned landing demo layout tweak already present in `apps/web/app/_components/landing/demo/block-market.tsx` in the same verified commit. Verification: recursive lint PASS; root `format:check` PASS; `@persai/api` typecheck PASS; `@persai/web` typecheck PASS; focused API tests PASS (`final-delivery-honesty`, `stream-web-chat-turn.service`). Deploy: api + web images required.

- **ADR-105 follow-up — semantic open-job context + explicit multi-image `series` mode (`packages/runtime-contract`, `apps/api`, `apps/runtime`, `docs`; 2026-05-31):** closed the two live media regressions diagnosed on the `сова -> лев` and carousel cases without keyword routing or legacy fallbacks. **Open-job truth fix:** `RuntimeOpenMediaJobContext` and `RuntimeOpenDocumentJobContext` now carry `sourceSummary`; API readers populate it from persisted request/source text; runtime developer sections render that summary plus explicit safety language that older open jobs are only server truth for prior tasks and are **not** proof that the current turn started a new async job. This closes the model-facing ambiguity where an existing owl job could be mistaken for acceptance of a new lion request. **Multi-image clean contract fix:** `RuntimeImageGenerateRequest` / `RuntimeImageEditRequest` gained `outputMode: "variants" | "series"` plus ordered `seriesItems[]`. Tool projection now instructs the model to use `variants` for alternate versions of one idea and `series` + `seriesItems[]` for distinct final frames/items (carousel slides, storyboard frames, etc.). Runtime generate/edit keeps ADR-105's invariant `one structured request = one durable media job`, but when `outputMode="series"` it executes multiple single-image provider calls (`count=1`) inside that same job/turn with one item-specific prompt per output. This removes the collage-prone "one shared prompt describing the whole carousel with n=count" shape without splitting into multiple jobs. Verification: focused runtime parsing PASS, runtime image-generate PASS (incl. new series execution case), runtime image-edit PASS (incl. new series execution case), native-tool-projection PASS, runtime turn-execution PASS, api assistant-media-job open-context PASS, api send-web-chat-turn PASS, runtime typecheck PASS, api typecheck PASS. Deploy: runtime + api images required.

- **Delivery honesty — restore structural full-undelivery notice + full-suite verification truth (`apps/api`, `docs`; 2026-05-31):** ran the complete AGENTS verification gate plus CI-like full suites over the whole ADR-105 + delivery-honesty + founder-UI working set and fixed every real failure honestly (no test-bending to mask defects). **Behavioral fix (corrects the prior over-removal):** the earlier delivery-honesty rework deleted the prose-meaning correction machinery but also dropped the _legitimately structural_ full-undelivery notice, which (a) left the `telegram-webhook-proxy.controller` suite red and (b) silently shipped "Готово, отправляю hello.txt"/"Here is your file." to the user when the runtime produced/attempted an artifact that delivery dropped (`attempted>0 && delivered===0`). `applyFinalDeliveryHonestyCorrection` now appends a count-driven, locale-aware, **type-aware** honest notice in exactly that case — EN/RU, "no file…" vs "no image or other media… was actually delivered in this reply." — with the file-vs-media kind resolved _structurally_ from the attempted artifacts' `type` (`resolveUndeliveredArtifactKind`: `document → file`, else `media`), never from the model's prose. Threaded `attemptedArtifactKind` through `telegram-channel-adapter.service.ts`, `complete-web-post-runtime-turn.ts`, and the two `assistant-media-job-completion-delivery.service.ts` sites (`"media"`); `assistant-document-job-delivery.service.ts` keeps the `"file"` default. The structural-only principle is preserved: a bare prose claim with **nothing** attempted (`attempted===0`, e.g. an image claim with `media: []`) is still left untouched — that gap is owned upstream by the `delivery_contract` instruction, so the prose-meaning regex stays gone. **Test-truth fixes uncovered by the full suite:** (1) `provider-image-generation.service.test.ts` accumulated `secretIds` expectation was stale (the earlier `editImage(count=3)` success adds a 3rd `image_generate` secret fetch) — and revealed that per-file `node --test` runs of the gateway are false-greens because those files only _export_ run-functions invoked by `run-suite.ts`; corrected the expectation and re-verified via the suite; (2) `enqueue-runtime-deferred-media-job.service.test.ts` out-of-range count bumped `9 → 11` for the new `MAX=10`; (3) `send-web-chat-turn` + `stream-web-chat-turn` undelivered tests set `welcomeLocale: "ru"` so the RU notice flows (the removed `containsCyrillic` means locale must be explicit now); (4) `telegram-webhook-proxy.controller.test.ts` image-claim-with-zero-media case updated to expect **unchanged** text (prose-meaning removed); added file+media notice unit coverage in `final-delivery-honesty.test.ts`. Verification: runtime-contract/runtime/api/web/provider-gateway typecheck PASS; recursive lint PASS; root `format:check` PASS; full suites GREEN — runtime, api (all files), provider-gateway (`run-suite`); web 609/611 with 2 _rotating_ failures (different tests each run) that all pass in isolation (13/13, 92/92) — pre-existing parallel-load flakiness in the web vitest env, not a regression; founder landing-demo UI edits verified (web typecheck + lint + landing/demo tests green). Deploy: api image required.

- **ADR-105 FIX A+B — count-ceiling split-job fix + partial under-delivery honesty (`packages/runtime-contract`, `apps/runtime`, `apps/api`; 2026-05-31):** (FIX A) raised `MAX_RUNTIME_IMAGE_GENERATE_COUNT` from `4` to `10` (gpt-image-1 provider batch ceiling) so a 10-image request with `perTurnCap=10` no longer splits into 4+4+2 = 3 jobs; `MAX_RUNTIME_IMAGE_EDIT_COUNT` follows by alias. Fixed `resolveImageCountCap` in `native-tool-projection.ts` to read `policy.perTurnCap` directly and fall back to `hardCap` when unset, instead of routing through `resolveAdvertisedPerTurnCap` which incorrectly fell back to `TOOL_HARD_CAP_PER_TURN["image_edit"]=1` (the call-loop cap, not the per-call batch-size cap). No stray hardcoded `4` found in any other image-path file. (FIX B) added `buildPartialDeliveryShortfallLine(produced, requested, locale)` to `final-delivery-honesty.ts` — locale-aware (EN/RU), returns a one-line honest notice when `1 ≤ produced < requested`, null otherwise; wired at both delivery resolution sites (web and Telegram) in `assistant-media-job-completion-delivery.service.ts` after `applyFinalDeliveryHonestyCorrection`, using `failureLocale` on web and `deliveryContext.locale` on Telegram. Fires only for the partial `1 ≤ M < N` case; M=0 (full failure) and M=N (full delivery) produce no shortfall line. Quota release (remainder N−M) was already correct from prior session. Verification: all four typechecks PASS (runtime-contract, runtime, api, provider-gateway); runtime lint + api lint + provider-gateway lint PASS; root `format:check` PASS; runtime native-tool-projection 1/1 (3 new count.maximum assertions), tool-budget-policy 1/1, image-generate 4/4, image-edit 5/5; provider-gateway image-generation 1/1, openai-client 1/1; api completion-delivery 15/15 (4 new shortfall tests: EN web, RU telegram, full delivery no-shortfall, M=0 no-shortfall). Deploy: runtime + api images required.

- **ADR-105 follow-up — delete dead HTTP media-quota reservation path (`apps/api`, `apps/runtime`; 2026-05-31):** removed the now-dead HTTP-based monthly-media-quota path left over from the ADR-105 single-owner migration (reservation/release/reconcile now happen in-process in the API via the quota-accounting repository; the old worker→HTTP→service path had zero src callers). Verify-then-delete confirmed deadness by grep (only definitions + the dead controller + test mocks referenced the symbols; the identically-named **repository** methods on the live `TrackWorkspaceQuotaUsageService` path were untouched). Deleted `reserve-internal-runtime-monthly-media-quota.service.ts` and `mutate-internal-runtime-monthly-media-quota.service.ts`; removed the 3 dead `media-monthly/{reserve,release,reconcile}` routes + their 2 injections/imports from `internal-runtime-tool-quota.controller.ts` (kept the live `consume`/`check`/`quota-status/checkout` routes), removed the 2 provider registrations from `workspace-management.module.ts`, and removed the 3 dead worker-client methods (`reserveMonthlyMediaQuota`/`releaseMonthlyMediaQuota`/`markMonthlyMediaQuotaReconciliationRequired`) + the now-unused `ReserveMonthlyMediaQuotaOutcome` type and `mutateMonthlyMediaQuota` helper from `persai-internal-api.client.service.ts`; pruned obsolete quota mock surface from the runtime tool tests + controller test. No behavior change to any live path. Verification: runtime + api typecheck PASS, runtime + api lint PASS, `format:check` PASS; live single-owner path proven intact — API scheduler 10/10, completion-delivery 11/11, enqueue + quota-accounting + controller PASS; runtime image-generate 4/4, image-edit 5/5, video 1/1, turn-execution PASS. Deploy: runtime + api images required. **Honest correction to the prior residual list:** the `count=4` ceiling is NOT a stray hardcode — it is already plan-clamped (`min(perTurnCap, MAX_RUNTIME_IMAGE_GENERATE_COUNT)` via `resolveImageCountCap`, ADR-074 L1.1) with `4` as an absolute safety rail like every other tool's `MAX_*` guard; and `MAX_OPEN_MEDIA_JOBS_PER_CHAT=2` is a defensible global safety bound (concurrency rejection is already honest/structured) — turning it per-plan is a separate product feature (new plan field + Prisma migration + admin UI), not a cleanup, and is intentionally not done here.

- **Delivery honesty — structural truth replaces prose-meaning regex (`apps/runtime`, `apps/api`, `docs`; 2026-05-31):** reworked all delivery-honesty handling (media + document + file) so correctness is driven by structural delivery facts, not by guessing at the model's prose. The model was being trusted as the source of the _fact_ of delivery (then patched after the fact with brittle keyword/regex meaning detection); now the UI/system is the single source of that truth and the model is contractually told never to announce attachment/delivery in prose. **Runtime (`turn-execution.service.ts`):** added a `DELIVERY_HONESTY_CONTRACT` developer-instruction section (`delivery_contract`, present on every turn) — "do not write markdown links to local/internal file paths, do not state a file/image/video/document is attached/sent/uploaded; delivered files are shown structurally by the interface; for pending items only say it is being prepared and will arrive separately"; fixed a stream/sync asymmetry so the stream path now passes `deferredDocumentJobs: turnState.deferredDocumentJobs` to the follow-up provider request (parity with sync, both inject `deferred_document_follow_up`); **deleted** the prose-meaning symptom path `DELIVERY_CLAIM_PATTERNS` / `claimsAttachmentDelivery()` / `applyUndeliveredAttachmentCorrection()` / `buildUndeliveredAttachmentCorrection()`, keeping the structural deferred-acknowledgement corrections (`applyDeferredMediaAcknowledgementCorrection`, `applyDeferredDocumentAcknowledgementCorrection`, `sanitizeLegacyTechnicalAttachmentSummary`). **Runtime (`runtime-image-edit-tool.service.ts` + `native-tool-projection.ts`):** removed the `isLikelyAnalysisOnlyPrompt()` keyword guard and its `edit_intent_not_explicit` early-return (last word-parse heuristic from the ADR-105 follow-up list); sharpened the `image_edit` description to state intent structurally — "use this only when the user explicitly wants an image modified, never to describe/analyze/answer questions about an image (those are answered in text)". **API (`final-delivery-honesty.ts`):** reworked `applyFinalDeliveryHonestyCorrection` to be purely structural — strips technical attachment summaries, strips delivered-attachment markdown links, neutralizes undelivered phantom local-file links (href removed, link text kept), falls back to a localized "file sent" line only when the body is empty _and_ attachments were delivered; **removed** the prose-meaning machinery (`POSITIVE_MEDIA/FILE_DELIVERY_CLAIM_PATTERNS`, `detectUndeliveredClaimKind`, `buildUndelivered*Correction`, `containsCyrillic`, `UndeliveredClaimKind`). Behavior change: a bare prose claim with no link and no attachment (e.g. "Your image is ready.") is now left untouched instead of being regex-appended — the contract instruction prevents the false announcement upstream, and only structural artifacts are corrected downstream. Function signatures unchanged (no call-site churn). Verification: runtime + api typecheck PASS; runtime lint + api lint PASS; root `format:check` PASS; focused tests PASS — runtime deferred-media 4/4, deferred-document 8/8 (incl. new stream-parity test), runtime-image-edit 5/5, native-tool-projection exit 0, api final-delivery-honesty 14 structural assertions (incl. "bare prose claim left unchanged" cases). Deploy: runtime + api images required.

- **ADR-105 follow-up — media description de-bloat + prompt word-parse removal (`apps/runtime`, `docs`; 2026-05-31):** the ADR-105 media additions had bloated each tool's model-facing text by repeating the same facts 2–3× across the main description, the per-turn-cap hint, and the `count` param. Optimized `native-tool-projection.ts` so each fact appears once: cap hint = number + "each image/video is one unit" + `tool_budget_exhausted` (dropped the `count=N` clause, which also de-confuses `video_generate` since it has no count); main descriptions own the "set count for a series — one job, no extra calls" rule; `count` params own range + per-unit accounting. Caps remain policy-driven (`policy.perTurnCap`), semantics unchanged. Separately removed a hardcoded prompt word-parse heuristic from `runtime-image-edit-tool.service.ts` — deleted `SECOND_IMAGE_REFERENCE_PROMPT_MARKERS` (multilingual phrase list) and `inferReferenceGuidedSelection`; `resolveImageSelection` now uses the structural `sourceImageAlias`/`referenceImageAlias` the model supplies (all guards intact). Behavior change: a 2-image reference edit now requires the model to set `referenceImageAlias` explicitly (description strengthened accordingly) rather than inferring it from prompt words. `isLikelyAnalysisOnlyPrompt`/`DELIVERY_CLAIM_PATTERNS` and the `MAX_OPEN_MEDIA_JOBS_PER_CHAT=2` constant are left as documented follow-ups. Verification: runtime typecheck PASS, runtime lint PASS, format:check PASS; `native-tool-projection.test.ts` PASS (no media-prose assertions to update), `runtime-image-edit-tool.service.test.ts` 5/5 (2 new: explicit structural source+reference selection, `source_image_alias_required` guard). Deploy: runtime image required.

- **ADR-105 follow-up — `image_edit.count` truly end-to-end + partial-leak closure (`apps/provider-gateway`, `apps/api`, `docs`; 2026-05-31):** closed a confirmed gap where `image_edit(count=N>1)` reserved `N` units but the provider returned only `1`. The gateway `normalizeEditInput` dropped/never-validated `count`, the OpenAI `editImage` payload omitted `n`, and `buildImageEditPrompt` forced "return one edited version" — so the program's "end to end" §6 claim was untrue and, under single-owner, the never-produced `N−1` leaked. Fixes: `provider-image-generation.service.ts` `normalizeEditInput` now validates `count` (`MIN/MAX_RUNTIME_IMAGE_EDIT_COUNT`) and forwards it; `openai-provider.client.ts` `editImage` sends `n: input.count` and `buildImageEditPrompt` is count-conditional (single-output wording for `count=1`, "N distinct edited variations of the source image" for `count>1`) with **no carousel/content hardcoding** — the assistant still decides how to interpret a request; `assistant-media-job-completion-delivery.service.ts` adds `releaseUnproducedRemainderBestEffort` which, after the delivery loop resolves the `M` produced artifacts, releases the never-produced remainder `N−M` exactly once (no-op when `M≥N`, mutually exclusive with the pre-delivery `reconcile N` path). Genuine provider under-delivery no longer leaks quota; only user-facing under-count framing remains deferred (copy-only, not a leak). Verification: provider-gateway + api typecheck PASS; gateway `openai-provider.client` + `provider-image-generation.service` tests PASS (n-in-payload, count-conditional prompt, count forward/validate); api completion-delivery 11/11 (3 new: partial under-delivery releases 3, full delivery releases 0, pre-delivery failDelivery does not double-resolve); repo gate lint + format:check PASS. Deploy: provider-gateway + api images required.

- **ADR-105 implemented — media job truth + single-owner media-quota reservation (`packages/runtime-contract`, `apps/runtime`, `apps/api`, `docs`; 2026-05-31):** landed Slices 1–6 as an orchestrated, per-slice-reviewed program. Contract (`runtime-contract`): media accepted results unified on `action:"pending_delivery"` (`canSendFileNow:false`, `messageToUser`, `expectedResultCount`), `image_edit` gained `count` end-to-end, open-job/summary context carries `requestedCount`/`expectedResultCount`. Runtime: tool projection describes media `perTurnCap` as **total result units** (not calls) and exposes `image_edit.count`; `tool-budget-policy` budgets unit-aware (`reserve(requestedUnits)`, rejects a request that would exceed the cap as a whole instead of splitting); `turn-execution` resolves requested units per media call, tracks `hadRejectedMediaRequest`, and stops the blanket deferred-media correction from overwriting mixed accepted+rejected outcomes; the three media tool services emit `pending_delivery`. API: monthly media-quota **reservation moved to enqueue admission** (`reserveAssistantMonthlyMediaQuota({units:N})`, `N=count`/`1` for video) with compensating release on insert failure; third open media job in a chat gets an explicit structured `media_job_concurrency_limit` rejection (`limitKind`, `activeJobs`, `maxActiveJobs`, `requestedUnits`); open-job context populates `requestedCount`/`expectedResultCount`. **Single-owner reservation correction (Slice 5, changes long-term truth — see ADR-105 Implementation outcome):** the monthly counter is aggregate per `(workspace,tool,period)`, so leaving worker-side releases alongside enqueue reservation could cross-corrupt a concurrent job's reserved units; therefore **all worker-side `releaseMonthlyMediaQuotaReservationBestEffort` calls were removed** from the three runtime tool services (worker now has zero monthly-media-quota call sites) and the **API owns single-owner resolution**: scheduler `failJob` releases the job's full `N` once for every terminal failure path; completion `failDelivery` reconciles `N` once for pre-delivery-loop failures (guarded by `loopResolved` against post-`deliver()` double-count); the delivery loop still settles/reconciles per artifact. Invariant: each reserved unit is resolved exactly once (settle / release / reconcile), no worker involvement, no double/multi-release. Residuals (tracked): `assistant_not_found` in `failJob` can't release (logged for workspace reconciliation); genuine provider under-delivery framing deferred per Non-goals. Verification: repo gate green — recursive lint PASS, root `format:check` PASS, runtime/api/web typecheck PASS; focused tests PASS (runtime image-generate 4/4, image-edit 3/3, video pass, turn-execution + tool-budget-policy pass; API media-job-scheduler 10/10, completion-delivery 8/8, enqueue + open-context pass). Deploy: runtime + api images required.

- **ADR-105 accepted — media job truth and orchestrated cleanup (`docs`; 2026-05-31):** reviewed the media-path audit findings and added the source-of-truth ADR for clean PROD media behavior. ADR-105 does **not** change code in this session; it codifies the missing product/runtime truth on top of ADR-086: one structured media request = one durable media job, `perTurnCap` for media means total result units rather than tool calls, no silent split/trim of oversized requests, explicit concurrency rejection for the third open job, document-aligned media `pending_delivery` semantics, explicit `image_edit.count` support only when wired end to end, enqueue-time unit-aware async quota admission consistent with ADR-082 settlement, and a bounded six-slice execution plan for the orchestrator (`contract -> runtime projection/budgeting -> runtime tools -> API enqueue/reservation -> scheduler/completion honesty -> docs/verification`). Verification: docs-only; no runtime/API/web code changed.

- **ADR-103 Slice A — one-flow interactive landing demo (frontend, stubbed; `apps/web`, `docs`; 2026-05-30):** landed the full frontend demo system (no backend, no risk) as an orchestrator-led run of six diff-reviewed, gate-verified subagent tasks (A1–A6). Added `apps/web/app/_components/landing/demo/`: `chat-atoms.tsx` (real `chat-message.tsx` tokens/classes — `AssistantRow` no-bubble, right-aligned `UserBubble`, `ArtifactPill`, `MemoryChip`, `ChannelFrame`), `demo-window.tsx` (adaptive PersAI-replica shell: bento `bg-chrome`/`bg-bg`, desktop-only sidebar, thread, dual-mode composer), `demo-script.ts` (autoplay narrative steps + `classifyIntent`→`getStubReply` + `DEMO_INITIAL_MESSAGES` + suggested prompts + limits, all i18n keys), `use-demo-machine.ts` (`useReducer` state machine `idle→autoplay→takeover→thinking→reply→limitReached→softReset`), `use-idle-timer.ts`, `use-in-view-once.ts` (SSR-safe one-shot IntersectionObserver), `hero-demo.tsx` (the only Tier-1 live island: autoplay → user takeover → stubbed reply → ≤3-reply cap → inline CTA → soft reset, suggested-prompt chips, pause/replay), and the three Tier-2 scroll-triggered product-window blocks `block-project.tsx` (project mode + skills + document artifacts, B2B), `block-knowledge.tsx` (Sources panel + cited-source highlight), `block-media.tsx` (token-gradient before/after clip-path reveal). Mounted the island in `hero-section.tsx` (responsive 2-col grid, copy → demo → CTAs, CTAs un-duplicated); replaced the 6-scene `workflow-section` gallery with the 3 blocks and **deleted** the retired pseudo-3D `workflow-surface.tsx` (grep-confirmed sole importer; ~38 KB removed). Added `landing.demo.*` + `landing.blocks.*` copy to `en.json` + `ru.json`; moved 4 hardcoded English aria-labels to i18n; gated both ambient loops under `prefers-reduced-motion` (scroll-cue dot via `globals.css` `.scroll-cue-dot { animation:none }`, thinking pulse via `useReducedMotion()`); guaranteed a no-JS/pre-hydration static first frame (greeting + composer in SSR HTML). New tests: `chat-atoms` (8), `demo-window` (16), `use-demo-machine` (27), `hero-demo` (8), `blocks` (6); `page.test.tsx` updated from "6-scene" to "3-block" with client-island mocks. **Deviations (recorded in ADR-103):** `use-typewriter.ts` not built (framer-motion entrance + thinking indicator instead); `get-reply.ts` adapter not yet extracted (calls `getStubReply` directly — Slice B must introduce the seam); media block is a gradient placeholder (named swap layer for a real photo later). Verification: `@persai/web` typecheck PASS, lint PASS, root `format:check` PASS, full web vitest PASS (one unrelated `use-chat` soft-detach polling test flaked under full-suite parallel load and passes 81/81 in isolation); founder-facing both-theme browser pass done (light + dark hero, demo replica, autoplay narrative incl. Telegram beat, takeover chips, pause/replay, all 3 blocks). No backend, no new dependency, no public LLM surface. Deploy: web image only (cosmetic landing; non-blocking).

- **ADR-104 — deploy resilience: partial-build isolation + pin-only-succeeded (`.github/workflows`, `docs`; 2026-05-30):** fixed the failure mode where one failed service build stranded the others on dev. `dev-image-publish.yml` build matrix now sets `strategy.fail-fast: false` (D1) so a single service build failure no longer cancels the rest. Each successful build+push now uploads a per-service `built-<service>` marker artifact; the non-migration pin job runs with `if: always()` (gated to push + non-empty deploy + non-migration + successful detect), downloads the markers, computes the successfully-built CSV, and pins **only** that subset (D2) — so green services roll forward even when a sibling build fails. Happy-path invariant: when all builds succeed the subset equals the full affected set, so behavior is unchanged on the common case. The **migration** pin job stays atomic (success-only + verify-every-image). ADR-104 also records the planned dev drift-repair cron (D3, deferred — not shipping an auto-mutating `values-dev.yaml` cron yet) and the prod-atomic release policy (D4, design-only; no `values-prod.yaml` exists). Verification: workflow YAML parses (4 jobs, `fail-fast:false`); `pin-dev-image-tags.mjs --services api,web --dry-run` validates subset pinning.

- **ADR-102 Slice 8 — document-worker LLM economics (`apps/runtime`, `apps/api`, `docs`; 2026-05-30):** wired document-worker LLM token usage into the unified model-cost ledger. Runtime adapter now accumulates `generateText` response `.usage` across all worker calls per job (outline, sections, HTML, patch) via `mergeUsageSnapshots` and returns the merged snapshot as `RuntimeDocumentJobRunResult.usage`; the Gamma path (zero worker LLM calls) stays `usage: null`. Added `"document_generation"` to `ModelCostLedgerPurpose`, a new `recordDocumentGenerationUsageEvent()` method, and a human label. API document-job scheduler now injects `RecordModelCostLedgerService`, reads `userId` from the existing DB column (no schema change), and appends a `document_generation` ledger row (non-blocking try/catch) after successful job run. Three distinct ledger identities preserved: `document_render` (render op), `chat_helper` (completion framing), `document_generation` (worker LLM). No Prisma migration, no quota-path change. Verification: lint PASS, format:check PASS, runtime-contract/runtime/API typecheck PASS; runtime adapter 49/49 tests PASS (4 new Slice 8 cases: single-shot usage, null usage, Gamma null, patch-revise); scheduler 11/11 PASS (2 new); ledger 14/14 PASS (2 new).

- **ADR-102 safe cleanup inventory (`apps/api`, `apps/web`, `packages/runtime-contract`, `docs`, `services`; 2026-05-30):** removed dead code and dead scaffolding flagged by the ADR-102 cleanup inventory. Deleted the unused `WebRuntimeShadowComparisonService` (service + tests + DI provider + `overview-dashboard` types/field + admin-overview wiring); deleted the dead `uploadChatAttachment()` export from the web assistant API client; replaced the hardcoded `"Навык - "` skill-badge literal with a `skillBadgePrefix` i18n key (`en.json` "Skill", `ru.json` "Навык", `use-chat.ts` + test); narrowed `RuntimeDocumentToolResult.action` to drop the unused `"deferred"` member; fixed a stale economics phrase in `ARCHITECTURE.md`; removed the empty `services/openclaw/.gitkeep` placeholder. The `native-*`/`send-native-*` filename rename was **intentionally skipped** (cosmetic, high-churn, and `native` is the endorsed PersAI-native term). Verification: recursive typecheck + targeted tests PASS.

- **ADR-103 proposed — one-flow interactive landing demo system (`docs`; 2026-05-30):** added the source-of-truth ADR for a new premium landing whose first screen is a single, coherent, interactive demo flow (autoplay narrative → user takeover → guided suggestion chips → limited LLM → soft reset) rendered as a faithful live replica of the real PersAI chat UI (exact tokens/classes from `chat-message.tsx`/`app-shell.tsx`, adaptive shell, `CHAT_FILE_PILL` artifacts), plus three Tier-2 scroll-triggered product-window blocks (project mode + documents/B2B, knowledge base + source attribution, media before/after) replacing the retired pseudo-3D `WorkflowSurface` gallery. Codified the `useReducer` state machine (`idle → autoplay → takeover → thinking → reply → limitReached → softReset`), the `getReply()` reply-source abstraction (stub in Slice A → `POST /api/demo/turn` real capped LLM in Slice B), mandatory dark/light token discipline (ADR-076 re-bound `dark:` variant, no raw hex), motion/a11y/mobile/no-JS rules, the file structure under `apps/web/app/_components/landing/demo/`, and the two-slice plan (Slice A frontend stubbed, no backend/no risk; Slice B gated public unauthenticated endpoint requiring `API-BOUNDARY`/`DATA-MODEL` updates + ADR-044/ADR-055 abuse review). Verification: docs-only; no code changed.

- **ADR-102 CLOSED — pre-PROD cleanup complete + live smoke passed (`docs`; 2026-05-30):** founder (`alex@agse.ru`) ran the Slice 10 6-point human smoke on `persai-dev` (`e3c78b63`) — all green: web stream + history reconcile; document create/`revise_document` → honest "ещё рендерится" pending then a separate delivered PDF with `companionOriginalStatus=absent` (no same-turn old-file masquerade); image generate → honest pending then delivered; file open/download by `fileRef`; assistant switch isolates chats + refreshes plan UI; Admin Ops counts assistant-scoped. Log verification clean (`document-jobs/enqueue` 202 → `AssistantDocumentJobDeliveryService` delivered the revised PDF, `media-jobs` enqueue 202 → `Processed 1 assistant media job(s)`, zero error/warn api/runtime logs). ADR-102 status set to **Completed**; all PROD-blocking + recommended slices (0–7, 9, 10) done and deployed. Optional Slice 8 (document-worker LLM economics) and the cleanup inventory remain non-blocking. Verification: docs-only.

- **ADR-102 Slices 5+6 + dev deploy + Slice 10 agent preflight (`packages/contracts`, `apps/web`, `apps/runtime`, `docs`; 2026-05-30):** (Slice 5) synced OpenAPI with real API truth — added `AssistantWebChatMessageAttachmentState`/`...DocumentLink`, required message `attachments[]`, the assistant Files read surfaces, and `stage-attachment`; regenerated contracts (with the required follow-up `prettier --write` on generated files) and migrated web message-attachment + cleanup-summary types to generated. (Slice 6) `refreshAssistantScopedSlices` now also refetches `getAssistantPlanVisibility` and `setPlan` so per-assistant plan UI is no longer stale after assistant switch/create. The full ADR-102 batch (`5ccf9703`) plus a deferred image/edit test alignment (`e3c78b63`) were committed, CI-equivalent verified (recursive lint, `format:check`, recursive typecheck, runtime/api/web suites, `helm lint`+`template`, `detect-affected` 4/4 — `requiresFullCi` rebuilt all five services), pushed, and deployed: Dev Image Publish success → bot pin `9a6bfeaa` → Argo CD `persai-dev` Synced + Healthy, all five deploys `2/2` on image `:e3c78b63` with 0 restarts, runtime started clean, api serving `/health`+`/ready` 200. The deferred image/edit test fix corrected `turn-execution.service.test.ts` (the Slice 2 subagent had only updated `deferred-media-acknowledgement.test.ts`): `image_generate`/`image_edit` enqueue a deferred job so their assistant text is now normalized to the honest pending acknowledgement, while `video_generate`/referenced-edit do not enqueue in those scenarios and keep their text. Slice 10 agent preflight (`kubectl get deploy,svc,ingress,pods`) PASS; human UI smoke remains owed by founder before ADR-102 closes. Deploy: DONE (runtime + api + web + provider-gateway + sandbox on `e3c78b63`).

- **ADR-102 Slices 1+2 — runtime document & media honesty (`apps/runtime`, `docs`; 2026-05-30):** closed the same-turn delivery-honesty gaps before PROD test users. (1) `files.send` AND `files.write_and_send` are now both blocked while a document job from the same turn is pending delivery (`document_pending_delivery`), and the skipped result reports the real requested action. (2) Within one provider tool-call batch, `document` calls now always execute before any `files` call via a stable `reorderToolCallsDocumentFirst` reorder (applied at both stream and sync batch sites), so `[files.send, document]` emitted order can no longer ship an older file as the new result. (3) Model-facing document tool copy now reflects the real `pending_delivery` / `canSendFileNow=false` contract instead of `action='deferred'`. (4) Deferred-media acknowledgement correction now always normalizes delivery-claiming model text to an honest pending acknowledgement (aligned with the document path) instead of preserving "готово, держи результат" while the media job is still open. Verification: focused runtime tests PASS (deferred-document 7/7 incl. new write_and_send + batch-reorder cases, deferred-media 3/3, runtime-document-tool 11/11); `@persai/runtime` typecheck + lint PASS. Deploy: DEPLOY REQUIRED (runtime).

- **ADR-102 Slices 3, 4, 7, 9 — runtime/admin/CI cleanup tails (`apps/api`, `apps/runtime`, `packages/runtime-contract`, `.github`, `scripts/ci`, `infra/helm`, `docs`; 2026-05-30):** (Slice 3) added `RuntimeOpenDocumentJobContext` + `openDocumentJobs` to the runtime contract and wired it end-to-end (sourced via `AssistantDocumentJobReadService.listOpenJobsForRuntimeContext` in web sync/stream + Telegram, rendered as an `open_document_jobs` developer section) so follow-up turns get server truth about in-flight PDF/presentation renders, mirroring `openMediaJobs`. (Slice 4) admin ownership transfer is now plan-aware (`assistantPolicy.maxAssistants`) instead of hard "1 user = 1 assistant", the dead `P2002` catch left by ADR-101 Slice 1 was removed, and Ops cockpit web-chat counts (`activeWebChats`/`archivedWebChats`) are scoped to the selected `assistantId` instead of workspace-wide. (Slice 7) the Telegram completed-turn path now forwards `runtimeResponse.toolInvocations` to the tool-path ledger (parity with web sync). (Slice 9) root `format:check` and new `detect-affected.mjs` unit tests now run in the CI affected-quality lane, the contract/runtime-boundary→integration escalation policy and the values-dev per-service image-tag pin rule are documented in TEST-PLAN. Verification: focused runtime/api tests PASS; recursive lint, format:check, and api/web/runtime/runtime-contract typecheck all PASS. Deploy: runtime + api images required for Slices 1-4, 7 to take effect; Slice 9 is CI-only.

- **ADR-102 doc corrections + governance fix (`docs`, `.cursor/rules`; 2026-05-30):** orchestrator re-verified every ADR-102 code claim against the tree and applied three corrections: Slice 5 (OpenAPI/web contract drift) demoted from PROD-blocking to recommended (minimum path now `0 → 1 → 2 → 9 → 10`); Slice 9 problem restated (contract/runtime-boundary risks already escalate to the integration matrix, only `requiresFullCi` is missed); Slice 4 scope extended to remove the dead `P2002` catch left by ADR-101 Slice 1. Replaced the stale always-applied "ADR-078 Continuity" rule with "ADR-102 Continuity" so session-start guidance points at the active program (ADR-078 is closed archive).

- **Setup wizard step 2 — presets + skills for new users (`apps/web`, `docs`; 2026-05-30):** fixed `/app/setup` step 2 for fresh registration. Persona archetypes now load after Clerk auth is ready and no longer depend on a successful assistant/workspace fetch. Professional skills stop infinite spinner retry: step 2 ensures onboarding + assistant exist before `GET /assistant/skills`, dedupes concurrent prerequisite calls, and shows a manual retry on failure instead of looping. Chat mode dropdown menu items use vertical `gap-1` matching horizontal padding. Verification: setup page + chat-area tests PASS; lint/format/typecheck PASS.

- **Light mode chat mode guard (`apps/api`, `apps/web`, `docs`; 2026-05-30):** when paid token light mode is active, smart/project chat modes are reset on chat list load and inbound turn prepare, API rejects PATCH to elevated modes, and the web mode dropdown mutes smart/project with caption «лимит исчерпан» / «limit reached». Verification: focused API + web chat-area tests PASS.

- **Token-metered economics — absolute price weights (`packages/types`, `apps/api`, `apps/web`, `docs`; 2026-05-30):** fixed quota credit derivation so premium/reasoning models that cost more per token deduct proportionally more credits. Weights are now `provider price / $1 per 1M input reference` instead of per-model normalization (`input=1`), which had made models with identical price ratios (e.g. `gpt-5.4-mini` vs `gpt-5.4`) show `1× vs normal` and charge the same credits per token. Admin Plans multipliers and runtime quota accounting now reflect absolute provider cost. Verification: `@persai/types` build PASS; focused token-metered, platform-runtime-settings, and plan multiplier tests PASS.

- **Hotfix — API CrashLoop from `@persai/contracts` runtime import (`packages/types`, `packages/contracts`, `apps/api`, `apps/web`, `docs`; 2026-05-30):** moved token-metered credit helpers from `@persai/contracts` (Orval TS barrel, not built in API Docker) to `@persai/types` (`dist/`, built in every service image). Fixes `persai-dev` API `CrashLoopBackOff`: `ERR_MODULE_NOT_FOUND` for `generated/step2-client` when Node loaded `@persai/contracts` at runtime. Verification: workspace lint PASS; format check PASS; `@persai/api` and `@persai/web` typecheck PASS; focused token-metered, platform-runtime-settings, and web tests PASS.

- **ADR-102 accepted — pre-PROD architectural cleanup program (`docs`; 2026-05-30):** added the canonical agent-executable cleanup program after the 2026-05-30 read-only audit. ADR-102 defines eleven bounded slices (baseline gate, runtime document/media honesty, open document jobs context, multi-assistant admin tails, OpenAPI/web contract drift, web plan refresh, Telegram ledger parity, optional document-worker economics, CI/deploy hygiene, preflight smoke) with explicit PROD-blocking minimum path `0 → 1 → 2 → 5 → 9 → 10`, verification matrix, deploy discipline, and copy-paste agent prompt template. Does not reopen ADR-078 or block PROD on `document_generation` ledger wiring. Verification: docs-only slice; no runtime/API/web code changed.

- **Token-metered quota weights derived from provider prices (`packages/contracts`, `apps/api`, `apps/web`, `docs`; 2026-05-30):** token-metered runtime model profiles now derive quota weights from provider prices on read and save (`input=1`, cached/output = price ratios). Existing catalog rows with stale `1/1/1` weights recalculate automatically when runtime settings are loaded or saved—no separate migration script. Admin Runtime shows derived weights as read-only for token-metered models; Admin Plans shows credit multipliers for normal/premium/reasoning slots (`1× baseline`, `N× vs normal`) from the runtime catalog. Verification: workspace lint PASS; format check PASS; contracts/api/web typecheck PASS; focused token-metered and plan multiplier tests PASS.

- **Project mode files panel onboarding hint (`apps/web`, `docs`; 2026-05-30):** when a chat is switched into project mode, the web shell now guides users to the sidebar project files block. Mobile opens the left sidebar overlay; desktop keeps the sidebar open and only pulses the files panel. The hint uses a theme-aware accent animation (with reduced-motion fallback), scrolls the panel into view when needed, and shows once per chat per browser session. Verification: workspace lint PASS; format check PASS; `@persai/api` and `@persai/web` typecheck PASS; focused project-files hint + `chat-area` tests PASS.

- **Settings APK footer — pinned bottom + quiet utility tone (`apps/web`, `docs`; 2026-05-30):** moved the Assistant Settings Android download/update action out of the scroll body into a fixed SlideOver footer so it stays at the bottom of the settings drawer instead of floating in empty space. The shared banner now supports a quieter `utility` tone (flat border, no glow/blur) used in the settings footer and mobile sidebar; Capacitor keeps the existing `Update app` copy via `AssistantSettingsApkFooter`. Verification: workspace lint PASS; format check PASS; `@persai/web` typecheck PASS; focused `slide-over.test.tsx` and `AssistantSettingsApkFooter` tests PASS.

- **Support tickets UX — active list + modal dialogue (`apps/web`, `docs`; 2026-05-30):** refactored Assistant Settings support requests so the list shows only active tickets by default (`open`, `pending`, `answered`), sorts unread replies first, keeps the green unread dot, and hides closed tickets behind a quiet toggle link. Clicking a ticket now opens the full dialogue in a centered modal (backdrop blur, rounded surface, scrollable thread) instead of expanding inline inside the list card. Added RU/EN strings for the empty-active state, closed toggle, and modal close affordance. Verification: workspace lint PASS; format check PASS; `@persai/web` typecheck PASS; focused `assistant-support-section.test.tsx` PASS (`4` tests).

- **Light palette + landing background reset (`apps/web`, `docs`; 2026-05-29):** made only the requested baseline visual change after reverting the broader UI polish attempt: light theme tokens now use a warm cream/peach base with quiet sage accents, and the public landing page no longer renders the aurora/noise background layers. Landing hero/finale CTAs now use quieter reference-style buttons: calm sage primary pills without glow/shimmer and warm raised cream secondary pills with subtle edge/highlight beveling, including dark-theme-specific CTA material so the buttons keep the same raised edge on the dark landing. Chat file attachment pills now use the same compact raised material in both light and dark themes without changing surrounding message layout. Cards, settings, chat shell, and sidebar structure are otherwise unchanged. Verification: workspace lint PASS; format check PASS; `@persai/api` typecheck PASS; `@persai/web` typecheck PASS; full `@persai/web` test suite PASS.

- **Document pending-delivery honesty guard (`apps/runtime`, `packages/runtime-contract`, `docs`; 2026-05-29):** changed accepted async document-tool results from model-visible success/deferred semantics to explicit `pending_delivery` with `canSendFileNow=false`, `jobId`, `docId`, `versionId`, and user-facing pending copy. Runtime now normalizes the assistant's same-turn document reply to a pending acknowledgement and blocks `files.send` from sending an older file while a document job from the same turn is still pending. Structured document HTML rendering now suppresses the duplicate first heading block when it already matches the section heading. Verification: focused runtime document-tool, deferred document acknowledgement, deferred media acknowledgement, and document-structure tests PASS; `@persai/runtime` typecheck PASS.

- **Telegram group access mode (`apps/api`, `apps/web`, `apps/runtime`, `packages/contracts`, `docs`; 2026-05-27):** added assistant-scoped Telegram `telegramAccessMode` (`owner_only` | `group_members`) on the existing Telegram config surface while preserving `groupReplyMode` as the separate mention/all-messages trigger. Webhook access now ignores bot-originated messages, keeps private DMs owner-only, and allows non-owner group senders only from active linked `assistant_telegram_groups`. Group turns keep persisted user text clean while storing Telegram sender/chat metadata on the message row and sending structured `channelContext.telegram` to runtime; runtime renders current group context as developer instructions and labels prior Telegram group user messages with stored sender names. The Telegram settings UI now saves the access mode through the existing config PATCH. Verification: focused Telegram adapter/config/native-client/internal-turn API tests PASS; focused runtime hydration/turn-execution tests PASS; focused Telegram Connect UI test PASS; `@persai/api`, `@persai/runtime`, and `@persai/web` typechecks PASS; workspace lint PASS; `format:check` PASS.

- **Auth incident hotfix — Clerk profile failure falls back to existing AppUser (`apps/api`, `docs`; 2026-05-27):** hardened the authenticated request path for the live intermittent Clerk upstream failure after ADR-101 Slice 8. `ClerkAuthService` still requires a valid Clerk JWT and still rejects unknown Clerk subjects, but when `verifyToken` succeeds and `users.getUser(sub)` fails, it now looks up an existing PersAI `AppUser` by `clerkUserId = sub` and reuses that persisted email/displayName instead of turning the whole request into 401. This fallback does not create users, does not relax unknown-subject auth, and logs the Clerk profile failure plus explicit DB-fallback usage without logging tokens. Verification: focused `clerk-auth.service.test.ts` and `step2-auth-foundation.e2e.test.ts` PASS; `@persai/api` typecheck PASS; `@persai/web` typecheck PASS; workspace lint PASS; `format:check` PASS; `git diff --check` PASS.

- **ADR-101 legacy cleanup follow-through — remove user-only repository methods and honest multi-assistant tails (`apps/api`, `docs`; 2026-05-27):** finished the bounded post-Slice-8 ADR-101 cleanup by deleting the old user-only `AssistantRepository` methods entirely: `findByUserId`, `updateDraft(userId)`, and `markApply*(userId)` are now gone from the repository contract and Prisma implementation, and the ADR-101 guard test fails if any of those names return in active source. `BillingLifecycleProducerService` no longer invents assistant notification context by picking the first assistant inside a workspace/user pair: it now uses the member's active assistant when available, falls back only when exactly one assistant exists, and otherwise sends workspace-level email without ambiguous assistant-scoped push. `ResolveAdminOpsCockpitService` likewise no longer falls back to the first assistant for multi-assistant users without an explicit or active selection; the cockpit now returns the assistant options honestly and requires selection in that ambiguous state. Verification: focused `billing-lifecycle-producer.service.test.ts`, `resolve-admin-ops-cockpit.service.test.ts`, and `adr101-find-by-userid-guard.test.ts` PASS; `@persai/api` typecheck PASS; workspace lint PASS; `format:check` PASS; `git diff --check` PASS.

- **ADR-101 Slice 8 — active assistant plan/billing lookup cleanup (`apps/api`, `docs`; 2026-05-26):** removed the remaining active product/admin hot-path `AssistantRepository.findByUserId` callers. User/admin plan visibility now resolves the selected active assistant/workspace before reading effective plan, quota, monthly media, package offers, and entitlements, fixing the live multi-assistant `/assistant/plan-visibility` ambiguity that made the UI fall back to free/gray. Payment intents, media package checkout, Admin Plan Control, Admin workspace subscription edits, and Ops billing-support actions now resolve workspace context through `ResolveActiveAssistantService`; `findByUserId` remains only as the explicit legacy repository/interface method. Added a source guard test to prevent new active-source callers. Verification: focused plan visibility, payment intent, media package, admin override/subscription/billing-support, and guard tests PASS; `@persai/api` typecheck PASS; `format:check` PASS.

- **ADR-101 Ops admin display — compact multi-assistant support (`apps/api`, `apps/web`, `packages/contracts`, `docs`; 2026-05-26):** made `Admin > Ops` multi-assistant-aware without redesigning the cockpit. The user directory now keeps the Assistant column compact by showing no assistant, the existing single-assistant status, or `N assistants` for multi-assistant rows. `GET /admin/ops/cockpit` accepts optional `assistantId`, returns a compact `assistants[]` selector list, and resolves assistant-owned blocks for the selected assistant while leaving billing/subscription support visibly workspace-level. The cockpit top banner now owns the single assistant selector/dropdown, and Plan Control remains assistant-scoped because the override writes `AssistantGovernance.assistantPlanOverrideCode`. Verification: contracts regenerated; focused API ops cockpit/directory tests PASS; focused web Admin Ops page test PASS; API/web typechecks, recursive lint, and format check PASS.

- **ADR-101 Slice 7 live remediation — API stale-dist build/start guard (`apps/api`, `.github`, `docs`; 2026-05-26):** live `persai-dev` still showed `Assistant lookup by userId is ambiguous for multi-assistant users` on setup preview even after source-level preview resolution moved to `ResolveActiveAssistantService`. Cluster inspection showed the deployed API image source was correct but the compiled `dist/apps/api/.../preview-assistant-setup.service.js` was stale and still called `findByUserId`. The hotfix keeps `findByUserId` behavior unchanged, disables Docker build cache for API image publishes, deletes and rebuilds `apps/api/dist` in one Docker layer, and runs the same compiled-preview assertion during image build and API startup so stale preview JS fails hard instead of serving traffic. Verification: clean local `@persai/api` build after deleting `apps/api/dist` PASS; compiled-preview assertion PASS.

- **ADR-101 Slice 6 — quiet web switcher + assistant-scoped client state (`apps/web`, `docs`; 2026-05-26):** landed the bounded web-shell multi-assistant UX without turning the existing sidebar card into a loud permanent selector. Single-assistant UI stays unchanged; the sidebar assistant card remains the settings affordance and only gains a quiet premium accent when `assistantLimit.maxAssistants > 1`; assistant switching now lives inside Assistant Settings -> Character via `Switch assistant` / `Сменить ассистента`; the modal lists assistants with calm select/create states and respects the existing backend assistant-limit truth. Client chat/session/streaming thread state is now assistant-id scoped, and switch/create refreshes lifecycle/chat/Telegram/notification slices without changing backend contracts. Verification: focused web Vitest slice PASS (`5` files, `163` tests); repo `lint`, `format:check`, `@persai/api` typecheck, `@persai/web` typecheck, and `@persai/contracts` typecheck PASS.

- **ADR-101 Slice 5 — assistant-scoped product surfaces on active assistant context (`apps/api`, `docs`; 2026-05-26):** migrated the remaining user-facing assistant-owned API hot paths off legacy user-only assistant selection. Memory Center/workspace-memory reads and mutations, task/background-task reads and controls, Skill assignment reads and replacement, assistant knowledge uploads/list/get/inspect/reindex/delete, avatar upload/download, voice settings/runtime-tier resolution, direct/staged file upload plus voice transcription, Telegram connect/state/config/revoke/resend flows, and assistant lifecycle/settings mutations (`draft`, `publish`, `reapply`, `rollback`, `reset`, `setup preview`) now resolve the active assistant through `ResolveActiveAssistantService` instead of `findByUserId` or local `assistant.findFirst({ userId })` fallbacks. Assistant draft/apply mutations that must target a specific assistant now use assistant-id-backed repository writes so multi-assistant users no longer hit ambiguous user-keyed lifecycle mutations. Verification: focused memory/task/skills isolation tests PASS; focused avatar/media/knowledge/telegram/lifecycle tests PASS; final gate (`lint`, `format:check`, `@persai/api` / `@persai/web` / `@persai/contracts` typecheck) PASS.

- **ADR-101 Slice 4 — active-assistant web chat isolation (`apps/api`, `docs`; 2026-05-26):** moved the web chat hot paths off user-only assistant selection so chat list/bootstrap reads, inbound web runtime context resolution, turn status lookup, and the reattach/stop registries now scope to the resolved active assistant instead of `findByUserId`. `ManageWebChatListService` now lists and owns chats through `ResolveActiveAssistantService`, inbound turn runtime context now resolves the active assistant before selecting the published version/runtime bundle, `/assistant/chat/web/turns/:clientTurnId` only reads attempts for the resolved assistant, and the in-memory reattach/hard-stop registries are keyed by `assistantId + clientTurnId` so same-user assistant A/B turns cannot collide. Verification: focused manage-web-chat-list, inbound-runtime-context, web-turn-attempt, and hard-stop-registry tests PASS; existing send/stream/prepare web-turn tests PASS; final gate (`lint`, `format:check`, `@persai/api` / `@persai/web` / `@persai/contracts` typecheck) PASS.

- **ADR-101 Slice 3 — lifecycle/bootstrap assistant list + switch contracts (`apps/api`, `apps/web`, `packages/contracts`, `docs`; 2026-05-26):** exposed the first public multi-assistant contract layer on top of Slice 2. The API now has explicit assistant list and switch endpoints, `GET/POST /assistant` lifecycle responses include `assistants[]`, `activeAssistantId`, and `assistantLimit`, and bootstrap now returns the same active-assistant envelope instead of a singular assistant shell. Lifecycle/bootstrap reads in this slice now route through the active-assistant resolver path rather than `findByUserId`, while preserving the existing active assistant detail field for current web consumers. The web contract/client boundary now understands the richer lifecycle/bootstrap shape and adds list/switch helpers without yet introducing the Slice 6 switcher UI. Verification: focused bootstrap/lifecycle view/identity-access tests PASS; focused web client/use-app-data tests PASS; existing `create-assistant` + `switch-active-assistant` service tests PASS; `@persai/api` / `@persai/web` / `@persai/contracts` typecheck PASS; repo `format:check` PASS.

- **ADR-101 Slice 2 — active assistant resolution + creation limit enforcement (`apps/api`, `docs`; 2026-05-26):** added `ResolveActiveAssistantService` as the central workspace-member-first assistant resolver for explicit `assistantId`, active-pointer fallback, single-assistant bootstrap fallback, and honest multi-assistant failure; added `SwitchActiveAssistantService`; and added `EnforceAssistantCreationLimitService` so `CreateAssistantService` now blocks creation when workspace plan `assistantPolicy.maxAssistants` is reached and sets the creator's `WorkspaceMember.activeAssistantId` to the new assistant. Also replaced the small Slice 1 stopgap ambiguity checks already added for notification preference, Telegram group refresh/groups, and assistant indexing jobs so those paths now share the same active-assistant rules instead of duplicating local selection logic. Verification: focused resolver/limit/switch/create-assistant/notification-preference tests PASS; `@persai/api` typecheck PASS.

- **ADR-101 Slice 1 — schema unlock + plan assistant limit (`apps/api`, `apps/web`, `packages/contracts`, `docs`; 2026-05-26):** started the multi-assistant implementation by removing Prisma's root one-assistant ownership constraints. `Assistant.userId` and `(workspaceId, userId)` are no longer unique, `AppUser` / `WorkspaceMember` relations are plural, `WorkspaceMember.activeAssistantId` is added and backfilled from existing assistant rows, and the active pointer is constrained to an assistant in the same workspace. Plan truth now carries `assistantPolicy.maxAssistants` through Admin/Public plan contracts and Admin Plans UI; existing/default B2C plans resolve to `1`, while B2B/operator plans can store higher limits. The startup/default-plan seed also backfills the assistant policy for fresh environments. Temporary compile fallout from removed Prisma uniqueness now fails honestly on ambiguous multi-assistant user-only lookups instead of silently selecting a first/newest assistant; full active-assistant resolution remains Slice 2.

- **ADR-101 Slice 1 remediation — admin delete-user clears active assistant pointer (`apps/api`, `docs`; 2026-05-26):** fixed the post-migration delete blocker introduced by `workspace_members.active_assistant_id -> assistants(id, workspace_id)` with `ON DELETE RESTRICT`. `AdminDeleteUserService` now nulls any `WorkspaceMember.activeAssistantId` reference to the assistant inside the destructive transaction before deleting the assistant row, preserving the existing delete-user cleanup semantics. Focused regression coverage in `admin-delete-user.service.test.ts` now proves a migrated user with a populated active assistant pointer can still be deleted; focused API typecheck also passes.

- **ADR-101 accepted — multi-assistant workspace model (`docs`; 2026-05-26):** added the execution ADR for the clean `1 user = 1 workspace = N assistants` platform foundation. The decision keeps AI employee roles, role templates, work queues, and outstaffing UX out of scope; makes plan truth own assistant count (`maxAssistants = 1` for B2C, `>1` for B2B); defines `WorkspaceMember.activeAssistantId`, plural assistant relations, active assistant resolution, multi-assistant bootstrap, web switcher/state namespacing, assistant-scoped API migration, runtime isolation verification, and a mandatory final cleanup slice that removes temporary bridges and legacy `findByUserId` single-assistant assumptions. No runtime code changed in this docs-only architecture slice.

- **PDF document cleanup audit — remove dead recent-PDF hint plumbing (`apps/api`, `apps/runtime`, `packages/runtime-contract`, `docs`; 2026-05-26):** removed the stale `recentChatPdfs` / `RuntimeRecentChatPdf` turn-time contract tail that the runtime no longer consumed after the Working Files migration. API turn entrypoints no longer query or serialize assistant-wide recent PDFs on every send/stream/Telegram turn, the obsolete helper methods/tests were deleted, the runtime contract no longer carries the unused hint payload, and a dead internal document-operation enum member (`verbatim_transfer`) was dropped. Also aligned active comments/tool wording with the real structured-vs-patch revise paths and switched model-facing wording from `file_ref` / `doc_id` to `fileRef` / `docId` where the active tool contract already uses camelCase. Verification: `@persai/api` typecheck PASS; focused `send-web-chat-turn`, `stream-web-chat-turn`, and `handle-internal-telegram-turn` tests PASS.

- **Structured document prod path — explicit preserve/rewrite intent + preserve-first large-source routing (`apps/api`, `apps/runtime`, `packages/runtime-contract`, `docs`; 2026-05-26):** hardened the large attached-document create/revise path so runtime no longer guesses rewrite intent from user wording or defaults to content rewriting on big extracted-source files. The document tool/runtime contract now accepts additive `contentIntent` (`preserve_content | rewrite_content`), tool guidance defaults omitted intent to preserve, and the worker applies that as a real guardrail: large extracted-source creates now stay on the structured source-preserving path unless the tool explicitly allows rewrite, while structured revise now defaults to `style_only` when neither `editOperation` nor explicit rewrite intent is present. Chunked generation remains available for explicit rewrite requests and for large attachments whose text could not be extracted, using attachment size as the routing fallback. Verification: focused `runtime-document-provider-adapter.service.test.ts` PASS; `@persai/runtime` typecheck PASS; workspace lint PASS; `format:check` PASS; `@persai/api` / `@persai/web` typecheck PASS.

- **ADR-100 follow-up — Working Files honest chronological history (`apps/runtime`, `apps/sandbox`, `packages/runtime-contract`, `docs`; 2026-05-26):** replaced the model-facing `Working Files` role buckets with one newest-first `File history` journal so the model sees a single truthful line per reusable file: canonical `AssistantFile.createdAt`, strict author label (`user | model | sandbox`), primary alias, filename, and always-on microdescription when `semanticSummary` exists. `TurnExecutionService` now sorts by canonical file creation time, formats timestamps deterministically in UTC, keeps only a short PDF `CURRENT_SOURCE` / `LAST_DELIVERED_RESULT` reminder, removes the old `HISTORY` / `OTHER_FILES` primary display, appends an 8-char `fileRef` suffix when duplicate filenames need disambiguation, and preserves those PDF anchors even when an older delivered result would otherwise fall outside the model-visible 20-file window. `RuntimeFileRef` gained additive `createdAt` / `authorLabel`, registry + sandbox mappings now populate them, attachment-backed file upserts preserve existing metadata instead of erasing prior `semanticSummary` truth during hydration/update, recent discovered refs stop re-truncating semantic hints in hydration, and image edit/generate persistence now enables `allowWeakRequestFallback: true` so short media prompts can still surface a durable summary in the history block. Verification: focused runtime tests for working-files rendering, generated summary fallback, and registry mapping all PASS; workspace lint plus `@persai/api` / `@persai/web` / `@persai/runtime` / `@persai/sandbox` typecheck PASS; repo `format:check` remains blocked only by a pre-existing unrelated `apps/runtime/test/runtime-document-provider-adapter.service.test.ts` formatting issue outside this slice.

- **Structured document prod path — polish (`apps/api`, `apps/runtime`, `docs`; 2026-05-26):** follow-up after audit — persist `editStrategy: fast_small` on small create and patch-revise; honor explicit `metadata.preserveText` / `metadata.styleOnly` for structured `style_only`; stop applying `transferMode` on revise operation resolution; reuse prior `styleProfileJson` on lazy upgrade when present; strengthen `editOperation` tool guidance; add scheduler structured-field persistence test and runtime tests for large verbatim structured create + metadata style routing.

- **Structured document prod path — versioned structure/style editing for large PDFs (`apps/api`, `apps/runtime`, `packages/runtime-contract`, `docs`; 2026-05-26):** implemented ADR-098 structured document snapshots so large PDF jobs no longer depend on whole-HTML SEARCH/REPLACE as the primary revise path. `AssistantDocumentVersion` now stores additive `structureJson`, `styleProfileJson`, `editStrategy`, and `structureVersion`; `renderedHtml` remains a derived delivery artifact. Large create/revise routes build or lazy-upgrade deterministic section snapshots, apply `style_only` / `content_patch` / `section_rewrite` through bounded structured worker envelopes, and persist structure fields through the document scheduler. Routing is language-agnostic via explicit document-tool fields (`transferMode`, `editOperation`, `targetSectionIds`) plus persisted version state; small/`fast_small` documents keep the existing patch-revise fast path. Added migration `20260524120000_adr098_structured_document_versions`, new runtime helper `persai-document-structure.ts`, tool-contract extensions, and focused runtime/API tests for structured revise, lazy upgrade, and fast-path preservation.

- **Runtime background-turn economics follow-up — narrow worker prompts + system-tool slots for async framers (`apps/runtime`, `docs`; 2026-05-26):** audited the remaining runtime background/helper text-generation paths after the document verbatim fix and removed the clearest unnecessary premium/persona carry-over. `RuntimeBackgroundTaskEvaluationService` no longer starts the synthetic tool-enabled background run on `premium_reply`, now uses `system_tool`, and its evaluator request no longer prepends the full ordinary chat system prompt or heartbeat when returning structured `push/no_push/complete` JSON. `TurnExecutionService.createBackgroundTaskToolRun()` now runs under an explicit `background_worker` prompt mode with a short non-conversational worker system prompt instead of the ordinary chat persona prefix. Async `RuntimeDocumentJobCompletionService` and `RuntimeMediaJobCompletionService` also switched their short completion/failure framers from `normal_reply` to `system_tool` and stopped appending the ordinary heartbeat tail; they still keep the ordinary system prompt because those final messages remain user-facing assistant copy. Focused runtime coverage now asserts the new slot/prompt behavior. Verification: focused `runtime-background-task-evaluation.service.test.ts`, `runtime-document-job-completion.service.test.ts`, `runtime-media-job-completion.service.test.ts`, and `@persai/runtime` typecheck — all PASS.

- **ADR-091 follow-up — idle re-engagement greeting-first topic continuation (`apps/api`, `docs`; 2026-05-26):** adjusted the idle re-engagement LLM instructions after founder feedback so the model is no longer told to avoid continuing an older topic as active context. The scheduler brief and default instruction now explicitly require a brief natural greeting or soft check-in first, then allow returning to the earlier topic with wording that acknowledges time passed and gently asks whether the user still wants help or wants to continue. Updated focused scheduler coverage to assert the new greeting-first continuation guidance is present in the runtime evaluation brief. Verification: focused `persai-idle-reengagement-scheduler.service.test.ts`, workspace lint, `format:check`, `@persai/api` typecheck, `@persai/web` typecheck — all PASS.

- **ADR-097 / ADR-100 follow-up — honest image-provider safety rejection + one safer retry (`apps/runtime`, `apps/provider-gateway`, `docs`; 2026-05-26):** fixed the concrete production image failure seam where OpenAI safety rejects were collapsing into generic provider/runtime failures and sometimes later degrading into `media_job_artifacts_missing`. `OpenAIProviderClient` now detects image generate/edit safety rejections and returns a typed `image_provider_safety_rejected` payload with upstream request id/provider-status metadata instead of a generic transport failure. Runtime now preserves that as `ProviderGatewaySafetyRejectedError`, performs exactly one bounded safer-prompt paraphrase through the existing `systemTool` model slot, retries the provider call once, and if that retry succeeds stores the safer paraphrase in `revisedPrompt` with an honest warning. If the rewrite or single retry still fails, runtime returns the same typed safety-reject reason and `RuntimeMediaJobRunService` now converts that into an honest async media-job execution failure instead of letting the API later report "no deliverable artifacts". Added focused coverage in provider-gateway/runtime tests for typed safety errors, one rewrite+retry success, and final honest safety failure. Verification: workspace lint, `format:check`, `@persai/api` / `@persai/web` / `@persai/runtime` / `@persai/provider-gateway` typecheck, full `@persai/api` / `@persai/runtime` / `@persai/provider-gateway` / `@persai/web` test suites — all PASS.

- **ADR-097 / ADR-100 follow-up — Working Files document-role priority cleanup (`apps/runtime`, `docs`; 2026-05-26):** removed the separate `RECENT PDFS YOU CAN REVISE` developer block and folded its meaning into the `Working Files` section so the model sees one coherent document/file source of truth instead of competing hints. `TurnExecutionService` now labels document-relevant files with explicit roles such as `CURRENT_SOURCE`, `LAST_DELIVERED_RESULT`, `HISTORY`, `RECENT_DISCOVERED`, and `OTHER_FILES`, includes `fileRef` UUID anchors directly on revisable PDF lines, and adds priority guidance so current user-provided source files win over older delivered PDFs when the user is asking to create a new document. The same rendering also keeps important semantic hints visible even when filenames are weak and avoids mixing conflicting historical aliases on the same document-role line. Updated focused runtime tests for the new Working Files rendering rules. Verification: focused `working-files-developer-section` tests, plus workspace lint, `format:check`, `@persai/api` / `@persai/web` / `@persai/runtime` / `@persai/provider-gateway` typecheck, full `@persai/api` / `@persai/runtime` / `@persai/provider-gateway` / `@persai/web` test suites — all PASS.

- **Turn-entrypoint consolidation Slice 4 — honest internal web runtime session/compaction client naming (`apps/api`, `docs`; 2026-05-25):** completed the final bounded API turn-entry residue-cleanup slice requested after Slice 3 by renaming the two remaining misleading internal web session/compaction transport helpers to honest client-oriented names without changing behavior: `compact-native-web-chat-session.service.ts` -> `web-runtime-compaction-client.service.ts` (`WebRuntimeCompactionClientService`, `WebRuntimeCompactionClientInput`) and `resolve-native-web-chat-session-state.service.ts` -> `web-runtime-session-state-client.service.ts` (`WebRuntimeSessionStateClientService`, `WebRuntimeSessionStateClientInput`). Updated `ManageWebChatListService`, `workspace-management.module.ts`, and the two focused helper tests to use the new names consistently while keeping the test filenames stable for the current verification plan. Adapter-local error text now refers honestly to the internal web runtime compaction/session-state clients instead of "native runtime web" helpers. No public route, schema, Telegram behavior, config residue, or shadow-comparison cleanup changed in this slice. Verification: focused compaction-client + session-state-client + manage-web-chat-list tests, `@persai/api` typecheck, and focused API ESLint — all PASS.

- **Turn-entrypoint consolidation Slice 3 — honest internal web runtime client naming (`apps/api`, `docs`; 2026-05-25):** renamed the two misleading internal web runtime transport adapters without changing behavior: `send-native-web-chat-turn.service.ts` -> `web-runtime-turn-client.service.ts` (`WebRuntimeTurnClientService`, `WebRuntimeTurnClientInput`) and `stream-native-web-chat-turn.service.ts` -> `web-runtime-stream-client.service.ts` (`WebRuntimeStreamClientService`, `WebRuntimeStreamClientInput`). Updated `SendWebChatTurnService`, `StreamWebChatTurnService`, module wiring, and the four focused tests to use the new names consistently, while keeping test filenames stable for the current verification plan. Error text inside the renamed client adapters now refers honestly to the internal web runtime client/stream instead of "native runtime web sync/stream". No public HTTP/SSE route, schema, Telegram behavior, or shadow/config residue changed. Verification: focused send-client + stream-client + send-web + stream-web tests, `@persai/api` typecheck, and focused API ESLint — all PASS.

- **Turn-entrypoint consolidation Slice 2 follow-up — bounded late-path failure hardening (`apps/api`, `docs`; 2026-05-25):** tightened the new shared web post-runtime completion seam without widening scope. `complete-web-post-runtime-turn.ts` now treats quota/compaction follow-up delivery as best-effort, so a failure in `deliverIntentNow()` after the main assistant reply is already persisted no longer fails the turn. `send-web-chat-turn.service.ts` and `stream-web-chat-turn.service.ts` now also treat post-replay skill-state persistence / background-check queueing as best-effort, logging and continuing instead of downgrading a completed main reply. `stream-web-chat-turn.service.ts` additionally stops creating a second interrupted assistant message on late-path errors after the main assistant reply already exists. `web-chat-turn-attempt.service.ts` now guards terminal idempotency by allowing `markCompleted()` / `markFailed()` / `markInterrupted()` to update only non-terminal attempts, so a completed attempt cannot be overwritten by a later failure/interrupted write. Added focused regressions in `send-web-chat-turn.service.test.ts`, `stream-web-chat-turn.service.test.ts`, and `web-chat-turn-attempt.service.test.ts`. Verification: focused send + stream + turn-attempt tests, `@persai/api` typecheck, and focused ESLint on touched files — all PASS.

- **Turn-entrypoint consolidation Slice 2 — shared web post-runtime completion seam (`apps/api`, `docs`; 2026-05-25):** extracted `apps/api/src/modules/workspace-management/application/complete-web-post-runtime-turn.ts` and switched both `SendWebChatTurnService` and `StreamWebChatTurnService` to use it for the overlapping web-only completion path after assistant-message persistence. The new shared seam centralizes: web media delivery, final-delivery honesty correction persistence, memory/quota/model-cost/tool-path ledger recording from the finalized assistant text, quota/compaction follow-up delivery, replay completion writes, and post-turn skill-state persistence plus background-check kickoff. Stream-only behavior stayed local in `StreamWebChatTurnService` (stall retry, SSE callbacks, interrupted partial persistence, timing/metrics), and no public HTTP/SSE route, schema, Telegram path, or `send-native-web` / `stream-native-web` filename changed. A tiny follow-up cleanup removed one unused local in `stream-web-chat-turn.service.ts` so the workspace lint gate stayed green, with no behavior change. Verification: focused `send-web-chat-turn.service.test.ts`, focused `stream-web-chat-turn.service.test.ts`, workspace lint, `format:check`, `@persai/api` / `@persai/web` / `@persai/runtime` typecheck, full `@persai/api` / `@persai/runtime` / `@persai/web` test suites — all PASS. An earlier parallelized web run showed timeout-only noise under local machine contention, but the isolated full `@persai/web` rerun passed clean.

- **Turn-entrypoint consolidation Slice 1 — shared assistant-message persistence helper (`apps/api`, `docs`; 2026-05-25):** started the API-side cleanup for the ADR-072 turn-entrypoint residue without touching public routes or runtime routing. Added `persist-assistant-message.ts` as the single helper for the main assistant-reply persistence seam used by `SendWebChatTurnService`, `StreamWebChatTurnService`, and `HandleInternalTelegramTurnService`. The helper centralizes two behaviors that had been hand-copied across hot paths: (1) persisting `discoveredFileRefIds` onto assistant-message metadata, and (2) attaching acknowledgement message ids to queued deferred media jobs when the turn already produced an assistant reply. This keeps current HTTP/SSE/Telegram behavior unchanged while removing one of the main duplication seams that would otherwise make later entrypoint consolidation risky. Added focused coverage in new `apps/api/test/persist-assistant-message.test.ts`, kept existing send/stream/telegram service suites green, and ran the full repo verification gate plus full `api/web/runtime` suites. The first full web run hit one transient timeout in `app/admin/runtime/page.test.tsx`, but the file passed on isolated rerun and the repeated full web suite then passed clean.

- **ADR-097 hotfix — retrying DB-truth revision version allocation (`apps/api`, `docs`; 2026-05-24):** fixed the production race where two quick `revise_document` enqueues for the same document could both allocate the same `(doc_id, version_number)` pair and fail on Prisma unique constraint `assistant_document_versions_doc_version_number_key`. `AssistantDocumentJobService.enqueueRevision()` now computes the next `versionNumber` inside the transaction from the latest persisted `AssistantDocumentVersion` row for that `docId` instead of trusting the delivered `currentVersionNumber`, so pending same-chat and cross-chat revisions both advance from DB truth. The enqueue path now also retries the specific `(doc_id, version_number)` `P2002` conflict up to 3 times, re-reading the latest version number on each attempt; no schema change, no global lock, and no change to revision ancestry/current-version promotion semantics. Added focused API coverage for both behaviors in new `assistant-document-job.service.test.ts`: (1) allocate from latest persisted version when DB is already ahead of `currentVersionNumber`, and (2) simulate a unique-conflict retry where the second attempt advances to the next free version number. Verification: lint + `format:check` + 4× typecheck + 3× test suites — all PASS.

- **ADR-097 Slice 5 — cross-chat recent-PDFs hint + descriptor sharpening for fileRef adoption (`packages/runtime-contract`, `apps/api`, `apps/runtime`, `docs`; 2026-05-24):** closes the fileRef-adoption gap left by Slice 4: production DB showed zero UUID fileRef calls — the model kept passing aliases (`"last generated file"`, `"previous attachment #1"`, `"current attachment #1"`) and getting `file_alias_not_found`. Root cause: the `RECENT PDFS IN THIS CHAT` hint (Slice 3) only covered the current chat, leaving the model without any server-resolved UUID anchor for cross-chat revise. **Fix 1:** `AssistantDocumentJobReadService.listRecentAssistantPdfsForTurn()` added — queries PDFs across ALL chats of the assistant ordered by `updatedAt DESC`, cap 6, only non-null `renderedHtml`; returns `fileRef` (= `assistantFileId`), `filename`, `chatId`, `deliveredAt`. `RuntimeRecentChatPdf` extended with `fileRef?`, `chatRef?: "current_chat" | "other_chat"`, `relativeAge?: string`. All 5 API entry points updated (stream-web, send-web, send-native-web, handle-internal-telegram, send-native-telegram). Hint format updated to `fileRef: <uuid>  filename: <name>  origin: <chatRef>  age: <relativeAge>` with explicit anti-alias warning. **Fix 2:** `native-tool-projection.ts` `fileRef` field description rewritten to say "MUST be a UUID" with an example UUID and a list of alias patterns that are NOT valid. All `file_ref` (snake-case) in the supporting description replaced with `fileRef`. Sentence added: "When the developer-block lists fileRefs in RECENT PDFS YOU CAN REVISE, prefer revise_document with one of those fileRef UUIDs over create_pdf_document." **Fix 3 (no code):** verified `docIdReq = "last generated file"` magic alias still fires as a fallback. Added `[document-tool] fileRef-not-uuid` log line for future debugging. Tests: 5 new `listRecentAssistantPdfsForTurn` API tests, updated runtime hint tests + 2 new cross-chat cases, 3 new streamer contract tests, 4 new descriptor assertions, 1 log assertion. Verification: lint + `format:check` + 5× typecheck + 3× test suites — all PASS.

- **ADR-097 Slice 4 — cross-chat PDF revise via file_ref (`packages/runtime-contract`, `apps/runtime`, `apps/api`, `docs`; 2026-05-24):** `file_ref` (an `AssistantFile.id`) added as an alternative to `doc_id` on `revise_document`, enabling the model to revise PDFs produced in earlier chats. The API resolves `fileRef` via `AssistantDocumentDeliveredFile.assistantFileId` → `AssistantDocument` → latest `AssistantDocumentVersion`, security-checks `AssistantFile.assistantId === currentAssistantId` (never workspace-only), reads `renderedHtml`, and feeds the existing Slice 2 patch-revise loop. The new `AssistantDocumentVersion` is written to the **current chat** with `parentVersionId` pointing to the cross-chat ancestor — read crosses chats, write stays local. Three new typed errors: `revise_document_file_ref_not_found` (no delivered file for this fileRef under this assistant), `revise_document_file_ref_not_a_pdf_document` (resolves but not a PDF), `revise_document_ambiguous_source` (both `file_ref` and `doc_id` passed). Existing `document_revise_unsupported_legacy_version` guard also fires on the cross-chat path. `listRecentChatPdfsForTurn` unchanged (stays per-chat; cross-chat visibility already covered by ADR-100 Working Files). `native-tool-projection.ts` updated: `fileRef` field added to schema, descriptions updated for both `fileRef` and `docId`. New test file `apps/api/test/enqueue-runtime-deferred-document-job-file-ref-resolver.service.test.ts` with 9 cases. Verification: lint + `format:check` + 4× typecheck + 3× test suites — all PASS.

- **ADR-097 Slice 3 — single-shot timeout re-route + recent-PDFs developer hint (`apps/api`, `apps/runtime`, `apps/provider-gateway`, `packages/runtime-contract`, `docs`; 2026-05-24):** hardens two production gaps discovered during `persai-dev` manual test. Gap A: `ProviderGatewayTimeoutError` (typed error, replaces generic `ServiceUnavailableException`) is now caught on the single-shot PDF generation path and triggers an honest re-route to chunked generation (logs `[document-pdf-single-shot-timeout]`; counts against retry budget). Chunked-pipeline timeouts set `document_pdf_chunked_timeout` and break the loop — no further re-route. `ProviderGatewayTextGenerateRequest.timeoutMsHint?: number` added to runtime contract; worker passes `240_000ms` for `document_html_generation`, `document_pdf_outline`, `document_pdf_patch_revise`; OpenAI and Anthropic provider clients use `max(default, hint)` capped at `600_000ms`; gateway validates at `assertValidRequest`. Gap B: `AssistantDocumentJobReadService.listRecentChatPdfsForTurn()` queries up to 3 recent `pdf_document` rows with non-null `renderedHtml` within the last 10 chat messages; result flows as `RuntimeTurnRequest.recentChatPdfs`; `TurnExecutionService` injects a factual `RECENT PDFS IN THIS CHAT (server-resolved, not user-typed)` + `revise_document` guidance section into the developer block when the document tool is in scope and the list is non-empty (zero cost when empty). `native-tool-projection.ts` descriptor adds one reinforcement sentence. No keyword routing. No server-side reject of `create_pdf_document`. Verification: workspace lint, `format:check`, all four typechecks, 2 new adapter tests (timeout re-route + chunked-timeout fail), 4 new `runRecentPdfsHintTests` developer-block unit tests, 3 new `timeoutMsHint` validation tests in provider-gateway, 5 new `listRecentChatPdfsForTurn` API tests, full runtime + API + provider-gateway test suites.

- **ADR-100 follow-up — token-aware files.search + Working Files recovery (`apps/runtime`, `docs`; 2026-05-24):** fixed the next concrete file-handling production seam exposed by a live transcript. `RuntimeAssistantFileRegistryService.search()` no longer fails multi-token natural-language queries: it now lowercase-splits the query on whitespace, drops tokens shorter than 2 chars, dedupes, then OR-fetches candidates (`min(max(limit*5, 50), 200)` cap) across `displayName` / `relativePath` / `metadata.semanticSummary` (the latter via Prisma `string_contains` on a JSON path), and ranks in memory by the number of distinct tokens that substring-match across those three fields, with Postgres `createdAt desc` as natural tiebreaker. Empty token list (single-char queries) falls back to the prior single-substring `buildSearchWhere` path. Public method signature unchanged; no prisma migration; no `pg_trgm`/`tsvector` index. `TurnExecutionService.buildWorkingFilesDeveloperSection()` removes the closed-world `Use only these aliases ...` phrasing and adds a new recovery instruction at the end: `If the user refers to a file that is not in this list, do not assume it is unavailable. First call files.list ... and if needed follow up with files.search ... Only then, if nothing matches, tell the user the file is not available.` `files.list` is explicitly preferred over `files.search` for recovery because `list` returns the full corpus with `semanticSummaryHint` already attached, which the model can reason over directly. Hard constraints respected: no change to `turn-routing.service.ts`, `project-execution-profile.ts`, `orchestrate-runtime-retrieval.service.ts`, `read-assistant-knowledge.service.ts`, public files-tool contract, `RuntimeFileRef` schema, or any keyword matching. Verification: workspace lint, `format:check`, `@persai/api` / `@persai/web` / `@persai/runtime` typecheck, new focused `runtime-assistant-file-registry.service.test.ts` (6 tests: multi-token semantic match, createdAt tiebreaker on equal score, 3-token vs 1-token ranking, short-token fallback without throw, token dedupe, limit respected after ranking), extended `runtime-files-tool.service.test.ts` (multi-token search), extended `working-files-developer-section.test.ts` (closed-world phrasing gone + recovery instruction present), plus full `@persai/runtime` test suite. Two unrelated pre-existing `working-files-developer-section.test.ts` failures (tests 1 and 5) reproduce on clean `origin/main` without this slice and are out of scope.

- **ADR-097 Slice 2 — patch-revise loop for PDF documents (`apps/api`, `apps/runtime`, `packages/runtime-contract`, `docs`; 2026-05-24):** replaces full HTML regeneration on `revise_document` with an honest SEARCH/REPLACE patch loop on top of the `renderedHtml` persisted by Slice 1. One LLM call per revise job (`document_pdf_patch_revise` classification) returns a strict JSON envelope; patches are validated for exact uniqueness (no fuzzy match, no retry) and applied sequentially to the previous rendered HTML; result runs through `repairHtmlDocument` and is sent to PDFMonkey; the new HTML is persisted to the new `AssistantDocumentVersion.renderedHtml` through the same Slice 1 persistence seam. Removes the silent `revise_document → create_pdf_document` fallback from `RuntimeDocumentToolService`; PDF revise without a valid UUID docId now routes as `revise_document` and the API resolves via `findLatestRevisionContextForChat` or returns honest `revise_document_requires_existing_pdf`. Legacy versions without `renderedHtml` rejected at enqueue time with `document_revise_unsupported_legacy_version` — no silent full-regeneration. New error codes: `document_pdf_patch_revise_invalid_envelope`, `document_pdf_patch_revise_search_not_found`, `document_pdf_patch_revise_search_ambiguous`, `document_pdf_patch_revise_repair_failed`. Progress message: "Applying edits…" / "Применяю правки…" via existing delivery service. Timeout: `DEFAULT_DOCUMENT_TIMEOUT_MS` (6 min). Presentations / Gamma path untouched. Contract: `PERSAI_PROVIDER_REQUEST_CLASSIFICATIONS` extended with `"document_pdf_patch_revise"`; `RuntimeDocumentJobRunRequest` extended with `previousVersionRenderedHtml?: string | null`. Verification: runtime and API typecheck, 6 new adapter tests, 3 new tool-service tests, 3 new API enqueue tests, full runtime and API test suites.

- **ADR-097 Slice 1 — chunked PDF generation + sticky HTML persistence (`apps/api`, `apps/runtime`, `packages/runtime-contract`, `docs`; 2026-05-24):** adds outline-first sequential section-by-section generation for PDF documents with large source input (>20 KB inlined), persists the exact post-`repairHtmlDocument` HTML on `AssistantDocumentVersion.renderedHtml` so the upcoming patch-revise loop (Slice 2) can read it without re-generating, and resolves the output-token ceiling from the admin runtime bundle instead of the 16 K hardcoded constant. Single-shot path remains for small/no-source jobs. Routing is one deterministic decision per job (attachment bytes ≥ threshold → chunked) plus one allowed truncation-triggered switch (structurally truncated single-shot output → switch to chunked once, logged as `[document-pdf-single-shot-truncated]`). Chunked pipeline: outline call with strict JSON envelope (invalid/empty → `document_pdf_outline_invalid`, no fallback) → style anchor synthesized in worker → sequential section calls (1 LLM call each; no parallel calls; proportional source slice; tail summary ≤ 1 500 chars) → assembly (concat → DOCTYPE wrap → `repairHtmlDocument` → PDFMonkey). Progress milestones ("Outline ready", "Section K of N", "Assembling PDF") are emitted as structured log lines with localized en/ru text. `RuntimeDocumentJobRunResult.renderedHtml` carries the HTML across the worker → API boundary; `AssistantDocumentJobSchedulerService` persists it in the `ready_for_delivery` transaction. No retroactive backfill; no presentations/Gamma impact; no PDFMonkey API surface change. Verification: Prisma generate, workspace lint, `format:check`, `@persai/api` / `@persai/web` / `@persai/runtime` typecheck, focused `runtime-document-provider-adapter.service.test.ts` (5 new tests) and `assistant-document-job-scheduler.service.test.ts` (1 new test), full `@persai/runtime` and `@persai/api` test suites.

- **ADR-100 follow-up — files-tool discovery aliases + knowledge relevance floor (`apps/api`, `apps/runtime`, `docs`; 2026-05-24):** finalized the assistant's file search/send/edit loop so a `files.search` result reliably drives the next `files.send` / `image_edit` instead of the model defaulting to stale `previous attachment #N` ordinals that point to unrelated past uploads. Fix A — runtime files tool now emits `discoveredFileRefs: RuntimeFileRef[]` on its internal execution outcome for `search` / `list` / `get` / `read`, each carrying fresh ordinal/singular working-files aliases (`found image #N` / `found file #N` for search, `listed image #N` / `listed file #N` for directory listings, singular `fetched image` / `fetched file` for single-target `get`, `read image` / `read file` for `read`). The same aliases populate the already-optional `aliases` field on the model-visible `RuntimeFilesToolItem`. `TurnExecutionService.applyToolExecutionOutcome` merges discovered refs into `turnState.fileRefs` (push if absent, otherwise merge aliases case-insensitively). The existing `TurnContextHydrationService.upsertWorkingFileRef` already merges incoming `fileRef.aliases`, so the next iteration's Working Files developer block now lists discovered files with both the discovery alias and the standard ordinal. Fix C — `read-assistant-knowledge.service` now propagates whole-token `exactTokenHits` from `scoreFieldMatch` through `rankStructuredCandidate` into `RankedSearchCandidate.exactTokenHits`, and the four `.filter((row) => row.score > 0)` filter sites (text knowledge documents, memory rows, chat messages, product knowledge text entries) are replaced with one exported `passesRelevanceFloor` helper. Rules: `score <= 0` rejected; any exact whole-token hit always passes (recall protection); single-token queries reject fuzzy/trigram-only candidates; multi-token queries pass fuzzy-only only when `score >= 0.5 * topScore`. Scoring weights, ranking order, and `selectRankedCandidates` are unchanged. Hard constraints: no change to `turn-routing.service.ts`, `project-execution-profile.ts`, `orchestrate-runtime-retrieval.service.ts`, or any public schema; no keyword-matching anywhere. Verification: workspace lint, `format:check`, `@persai/api` typecheck, `@persai/web` typecheck, `@persai/runtime` typecheck, focused `turn-execution-discovered-file-refs.test.ts`, `runtime-files-tool.service.test.ts`, `turn-execution.service.test.ts`, `project-execution-profile.test.ts`, `read-assistant-knowledge.service.test.ts`, `orchestrate-runtime-retrieval.service.test.ts`, plus full `@persai/api` and `@persai/runtime` test suites.

- **ADR-100 follow-up — LLM-authored async media replies for Web/TG (`apps/api`, `apps/runtime`, `docs`; 2026-05-24):** mini-audited async media/document acknowledgement and completion copy across Web and Telegram, then removed the confirmed production seams where templates displaced model-authored text. Runtime deferred media/document acknowledgement correction now preserves non-empty LLM copy and uses canned acknowledgement only as an empty-output fallback. Media completion delivery now attempts fresh LLM framing even when an existing completion message id is present, updating that message instead of reusing old acknowledgement text; if framing fails, artifact delivery still falls back to stored text. Telegram now suppresses separate final text only when it exactly duplicates the delivered media caption, so a different LLM-authored final reply is sent even if media was already delivered with a caption. The temporary document-delivery placeholder now also uses existing document-job locale inference (`Готовлю документ...` for Russian requests, English otherwise). Telegram inbound uploads now enqueue the same canonical upload micro-description helper as web uploads after successful attachment persistence/runtime-readability checks, including finalized two-file albums, while preserving existing policy/dedupe and keeping queue failures best-effort. Verification: workspace lint, `format:check`, `@persai/api` typecheck, `@persai/web` typecheck, focused `turn-execution.service.test.ts`, `assistant-media-job-completion-delivery.service.test.ts`, `telegram-bot.client.service.test.ts`, `assistant-document-job-failure-copy.service.test.ts`, `assistant-document-job-delivery.service.test.ts`, `handle-internal-telegram-turn.service.test.ts`, plus `@persai/runtime` typecheck.

- **ADR-100 follow-up — files semantic-summary search + generated summary truth (`apps/api`, `apps/runtime`, `packages/runtime-contract`, `docs`; 2026-05-24):** extended the native Files contract so model-visible `files.list/get/search` results can carry `semanticSummaryHint`, and runtime file search now matches canonical `AssistantFile.metadata.semanticSummary` in addition to path/name. Generated outputs now also stop depending on final user-facing assistant text for this metadata: runtime writes a bounded durable `generation_request` summary directly onto canonical file truth for generated media/doc outputs when the request itself is good enough, while API delivery reuses the existing background micro-description job as a fallback when a generated file still has no durable summary after delivery. Verification: focused `generated-file-semantic-summary.test.ts`, `runtime-files-tool.service.test.ts`, `sanitize-tool-result-for-model.test.ts`, `assistant-upload-micro-description-job.service.test.ts`, `media-delivery.service.test.ts`, plus `@persai/runtime` and `@persai/api` typecheck.

- **ADR-100 live follow-up — OpenAI media false-abort hardening (`apps/provider-gateway`, `docs`; 2026-05-23):** tightened the two concrete long-running OpenAI media failure seams without widening scope. `editImage()` now uses a dedicated bounded `7 minute` timeout instead of sharing the shorter `5 minute` image-generation limit, while `pollOpenAIVideoJob()` no longer fails the whole job on a single transient poll-status response such as `408`, `429`, or `5xx`/`504`. Video polling still preserves the existing overall request timeout and terminal failure handling for explicit provider job statuses. Focused provider coverage now asserts both the `420_000 ms` image-edit timeout and successful recovery from a one-off video poll `504`. Verification: workspace lint, `format:check`, `@persai/api` typecheck, `@persai/web` typecheck, `@persai/provider-gateway` typecheck, and focused `openai-provider.client.test.ts` + provider image/video service tests.

- **ADR-100 live follow-up — remove project cadence abort and drop silent stall kills (`apps/api`, `docs`; 2026-05-23):** live cluster evidence showed the remaining project-chat cutoffs were no longer caused by `slow_avg` but by the same API-side cadence watchdog's `silent` path during long quiet follow-up spans after initial visible progress. The fix is now structural: `project` turns are completely removed from the API cadence-abort path, and ordinary web turns no longer let the `silent` timer abort the stream at all. Non-project web turns still retain `slow_avg` recovery for obviously dribbling text streams, while truly silent waits now defer to lower-level runtime/provider bounds instead of a fake API-side stall abort. Verification: focused `cadence-watchdog.test.ts`, focused `stream-web-chat-turn.service.test.ts`, and `@persai/api` typecheck.

- **ADR-100 live follow-up — upload micro-description binary limit raised (`apps/api`, `docs`; 2026-05-23):** increased the cheap background upload micro-description helper cutoff for binary files from `2 MB` to `4 MB` so moderately sized PNG/PDF uploads are not rejected before helper invocation. Added a focused API regression asserting `image/png` at exactly `4 MB` is still accepted while `4 MB + 1 byte` is rejected, and kept the rest of the helper path/MIME allowlist unchanged. Verification: focused `assistant-upload-micro-description.service.test.ts`, focused `assistant-upload-micro-description-job.service.test.ts`, and `@persai/api` typecheck.

- **ADR-100 live follow-up — project slow-mo guard + progress line breaks (`apps/api`, `apps/web`, `docs`; 2026-05-23):** closed the next concrete long-project-chat regression without widening scope. `StreamWebChatTurnService` now resolves cadence-watchdog options per chat mode and disables only the `slow_avg` recovery path for `project` turns, while keeping the silent watchdog and the ordinary non-project protection paths intact. `cadence-watchdog` now supports explicit `slowAvgEnabled` control with focused regressions, and web assistant markdown paragraphs now use `whitespace-pre-wrap` so runtime-authored project progress lines with single newlines render as separate lines instead of collapsing into one paragraph. Verification: focused `cadence-watchdog.test.ts`, focused `stream-web-chat-turn.service.test.ts`, focused `chat-message-blocks.test.tsx` + `chat-message.test.tsx`, workspace lint, `format:check`, and `@persai/api` / `@persai/web` typecheck.

- **ADR-100 live follow-up — pre-start stream abort hardening (`apps/api`, `apps/web`, `docs`; 2026-05-23):** fixed one more real hard-abort class in web chat streaming. The stream endpoint had still been awaiting `prepare()` before opening SSE headers, while the web client still treated arrival of `2xx` headers as both transport-open and accepted-turn truth. Heavy pre-start work could therefore trip the client pre-header timeout and look like an abrupt stop before the first real streamed chunk. `AssistantController.streamWebChatTurn()` now opens SSE immediately and sends an early keepalive before awaiting `prepare()`, and pre-start failures now terminate the stream via SSE `failed` instead of a late non-stream HTTP failure. On the client, `useChat` no longer clears pending-send state on `onHeadersOk`; acceptance now waits for real `started`/terminal progression, and an early `failed` before `started` is treated as a non-accepted turn rather than a partial aborted answer. Verification: focused `use-chat.test.tsx` + `assistant-api-client.test.ts`, workspace lint, `format:check`, and `@persai/api` / `@persai/web` typecheck.

- **ADR-100 project files sidebar follow-up — upload + delete actions (`apps/web`, `docs`; 2026-05-23):** the left `Project files` sidebar panel is no longer read-only for active project chats. It now exposes a compact `+` action that uploads directly into the current project chat, rejects batches larger than 3 files, and shows localized upload/delete error copy. Each file row also gets a trash action that reuses the existing global assistant-file delete path. The panel now refreshes through a small `project-files-changed` client event keyed by `chatId`, and the ordinary chat upload flow dispatches that event after staged attachments succeed, so sidebar file state updates without a full navigation/reload. Verification: focused `sidebar.test.tsx` + `use-chat.test.tsx`, workspace lint, `format:check`, and `@persai/api` / `@persai/web` typecheck.

- **ADR-100 live follow-up — follow-up pass abort + project-status localization (`apps/api`, `apps/web`, `apps/runtime`, `docs`; 2026-05-23):** live project-mode verification exposed a real continuation bug after the earlier orchestrator slice: synthetic retrieval/project status events were still counting as cadence activity in `StreamWebChatTurnService`, so the API-side watchdog could arm itself before the next provider pass and then abort a healthy follow-up iteration before headers arrived. The stream watchdog now ignores those synthetic status markers for stall detection, while still honoring real text/thinking/tool/media/done activity. On the web side, fixed runtime-authored project summary/status copy now resolves through `ActivityBadge` translation keys so Russian UI no longer shows those canned English project banners raw. Runtime project instructions were also tightened so visible progress should prefer one short line per update, avoid `Status 2/6`-style numbering, and avoid long narrated progress paragraphs when a lightweight marker is enough. Verification: `corepack pnpm --filter @persai/runtime exec tsx test/project-execution-profile.test.ts`, `corepack pnpm --filter @persai/runtime exec tsx test/turn-execution.service.test.ts`, `corepack pnpm --filter @persai/web test -- app/app/_components/activity-badge.test.tsx app/app/_components/use-chat.test.tsx`, `corepack pnpm --filter @persai/api exec tsx test/stream-web-chat-turn.service.test.ts`, and `corepack pnpm --filter @persai/api` / `@persai/web` / `@persai/runtime` `typecheck`.

- **ADR-100 post-6H follow-up — source progression + activity prioritization (`apps/runtime`, `apps/web`, `docs`; 2026-05-22):** tightened the already landed ADR-100 project-mode orchestrator without inventing a new routing tree. Project-mode precheck now allows web participation whenever the tool is available, so the model can escalate from local context to external verification inside the same bounded loop instead of being silently pre-narrowed away from web on knowledge-heavy turns. Runtime tool-loop follow-up now adds a dynamic `Source progression` developer block: after local/project context the model is told to continue to the next missing source when evidence is still partial, and after external lookup it is told to compare that evidence back against local files/Skills before finalizing. Project stream cadence was also reduced so early `plan/gather/analyze` banners no longer dominate before any real answer/tool work, and web now prioritizes concrete live tool/retrieval activity over generic project status while still preserving project `summary` / `detail` text. Verification: focused runtime project-profile/stream/turn-execution tests, focused `use-chat` + `activity-badge` web tests, workspace lint, `format:check`, and `@persai/api` / `@persai/web` / `@persai/runtime` typecheck.

- **ADR-100 Slice 6H live follow-up — retrieval helper pruning fix (`apps/api`, `docs`; 2026-05-22):** live `persai-dev` smoke on the already deployed `27541a81` build exposed a real retrieval post-processing bug: the hidden knowledge-retrieval helper could correctly return `rankedReferenceIds: []` or a strict subset for irrelevant Product KB/plan candidates, but API treated helper output as reorder-only semantics and collapsed empty output to `null`. `KnowledgeRetrievalHelperService.rerankCandidates()` now returns a real ranking result even for an empty allowlist, and both `ReadAssistantKnowledgeService` and `OrchestrateRuntimeRetrievalService` now treat helper output as an allowlist rather than a soft reorder hint. Helper-pruned references are dropped completely instead of surviving lower in the list. Focused regressions cover both `empty` and strict-subset helper outputs on direct knowledge search and orchestrated retrieval. Verification: focused `read-assistant-knowledge.service.test.ts`, focused `orchestrate-runtime-retrieval.service.test.ts`, workspace lint, `format:check`, `@persai/api` typecheck, and `@persai/web` typecheck.

- **ADR-100 doc reconciliation after Slice 6H (`docs`; 2026-05-22):** polished `docs/ADR/100-project-chat-mode-and-b2b-analysis-profile.md` into a cleaner continuation/source-of-truth document after the already landed Slice 6H closeout. The ADR now states plainly that Slices 2-6H are implemented, removes stale `working tree` and session-specific parent/subagent scaffolding, compresses the implementation ledger, clarifies the boundary between `normal | smart | project`, Skills, Product KB/subscription facts, explicit cross-thread recall, and project files on existing `AssistantFile` truth, and keeps the next honest step as deploy prep + live project verification before Slice 7. No code behavior changed.

- **ADR-100 Slice 6H — retrieval source admission and relevance hygiene (`apps/api`, `apps/runtime`, `apps/web`, `docs`; 2026-05-22):** closed the live project-mode retrieval-quality finding without globally disabling useful product awareness or human recall. Runtime now admits Product KB/subscription facts only on product intent, project-mode precheck follows the same rule, generic `plan` / `план` no longer triggers Product KB, and non-empty Admin Runtime `routerPolicy.precheckRuleOverrides` trigger lists now replace built-in defaults instead of silently extending them. API retrieval still searches ordinary user documents, but assistant-wide `memory` and `chat` are searched only when the runtime marks explicit recall intent. Project-file retrieved items now outrank ordinary user documents and Product KB during runtime hydration, and Admin Runtime copy documents the override semantics. Verification: focused runtime routing/profile/execution tests, focused API orchestration/read-knowledge tests, Admin Runtime page test, workspace lint, `format:check`, and `@persai/runtime` / `@persai/api` / `@persai/web` typecheck.

- **ADR-100 Slice 6F follow-up — internal upload micro-description себес (`apps/api`, `docs`; 2026-05-22):** completed the missing internal cost-accounting closeout for the new upload micro-description background helper without touching user quota semantics. `assistant_upload_micro_description_jobs` now durably stores helper `usageJson` plus `usageOccurredAt` from the provider response before any money write, `AssistantUploadMicroDescriptionService.describeCanonicalFile()` now returns `summary`, `usage`, `respondedAt`, and provider/model, and `AssistantUploadMicroDescriptionJobService` persists that seam in the same success transaction as any semantic-summary updates. After the durable job write succeeds, API appends a non-blocking `model_cost_ledger_events` row via the new narrow `RecordModelCostLedgerService.recordToolHelperEvent()` path with honest labels (`purpose=tool_helper`, `source=upload_micro_description`, `surface=background`, `sourceEventId=upload_micro_description_job:<jobId>`). If the helper spends tokens but yields no usable summary, the job still records usage/time and completes honestly; no user quota, plans, or UI behavior changed. Verification: focused `assistant-upload-micro-description-job.service.test.ts`, focused `record-model-cost-ledger.service.test.ts`, and `@persai/api` typecheck.

- **ADR-100 Slice 6F — cheap background upload micro-description (`apps/api`, `apps/web`, `packages/contracts`, `docs`; 2026-05-22):** added one more bounded pre-deploy project-file intelligence slice for uploads that still lack a deterministic semantic summary. Admin Runtime now exposes `routerPolicy.analyzeUploadsOnB2cUpload` (default `false`) on the existing runtime/provider settings surface; ordinary non-project/B2C uploads obey that toggle, while project mode always enqueues once canonical `fileRef` truth exists. API now owns durable `assistant_upload_micro_description_jobs` plus a lease-based scheduler/worker, reuses the existing `systemTool` model slot for the helper, and persists canonical summary truth on `AssistantFile.metadata.semanticSummary` / `semanticSummarySource` while mirroring attachment metadata when practical. The semantic-summary source enum now includes `upload_micro_description`. Enqueue remains bounded and pre-deploy only: existing project chats may enqueue during stage after `fileRef` exists, and prepared inbound turns enqueue only after staged attachments merge and final `chatMode` is known. Verification: focused `assistant-upload-micro-description-job.service.test.ts`, `manage-chat-media.stage-web-thread.test.ts`, `prepare-assistant-inbound-turn.service.test.ts`, `platform-runtime-provider-settings.test.ts`, `manage-admin-runtime-provider-settings.service.test.ts`, `apps/web/app/admin/runtime/page.test.tsx`, `apps/web/app/app/assistant-api-client.test.ts`, `apps/web/app/app/runtime-provider-settings-admin.test.ts`, workspace lint, `format:check`, `@persai/api` typecheck, and `@persai/web` typecheck.

- **ADR-100 Slice 6C/6D/6E — project-file intelligence core correction (`apps/api`, `apps/runtime`, `docs`; 2026-05-22):** completed the must-have pre-deploy project-file/runtime correction. Project chats now stage canonical attachment-backed files as a real source before KB in project gather, while deep extraction remains lazy and is cached on `AssistantFile.metadata` (`internalRuntimeFileExtractionCache`) after first use instead of rerunning heavy analysis. The existing developer/working-files seam stays the cheap selector layer with tiny semantic anchors; no second KB, no schema migration, and no UI expansion were added. Verification: focused `orchestrate-runtime-retrieval.service.test.ts`, focused `extract-internal-runtime-assistant-file.service.test.ts`, focused `turn-execution.service.test.ts`, workspace lint, `format:check`, and `@persai/api` / `@persai/web` / `@persai/runtime` typecheck.

- **ADR-100 Slice 6B — project-only retrieval ordering (`apps/api`, `apps/runtime`, `docs`; 2026-05-22):** added a narrow project-gated retrieval improvement so active-skill project turns keep the skill stage and still stage user knowledge before product knowledge, even when skill hits already exist. Ordinary non-project active-skill behavior stays unchanged. The change is gated by a small internal `gatherProfile: "project"` flag only; no pinned-skill schema, no chat-file retrieval stage, and no web changes were added. Verification: focused `orchestrate-runtime-retrieval.service.test.ts`, focused `turn-execution.service.test.ts`, workspace lint, `format:check`, and `@persai/api` / `@persai/web` / `@persai/runtime` typecheck.

- **ADR-100 Slice 6A — bounded semantic summaries (`apps/api`, `apps/runtime`, `packages/runtime-contract`, `docs`; 2026-05-22):** added a durable, token-safe `semanticSummary` / `semanticSummarySource` on existing attachment and canonical file metadata when cheap deterministic signals already exist (`textExtract`, `transcription`). Runtime now exposes only tiny capped working-file hints for weak/generic filenames, keeping `contentPreview` separate and out of steady-state prompt sections. No schema migration, no image vision captioning, and no parse-every-upload behavior were added in this slice. Verification: focused API semantic-summary/media-staging tests, focused runtime working-files hint test, workspace lint, `format:check`, and `@persai/api` / `@persai/web` / `@persai/runtime` typecheck.

- **ADR-100 correction — remaining Slice 6 is must-have before deploy (`docs`; 2026-05-22):** founder clarification tightened the target boundary: project-file intelligence cannot remain a soft follow-up. ADR-100 now explicitly requires three pre-deploy closeout blocks before Slice 7: `6C` project files must surface as a first-class source in the existing developer/working-files context, `6D` deep document/media analysis must run once and persist/cache truth on existing attachment/`fileRef` records instead of rerunning heavy analysis, and `6E` runtime core must treat project chat files as a real gather source rather than an opportunistic `files.read` fallback.

- **ADR-100 Slice 5 — project activity feed (`apps/runtime`, `apps/api`, `apps/web`, `docs`; 2026-05-22):** added additive project-only stream events for activity stages and safe visible reasoning summaries. Runtime emits bounded `project_activity` / `project_reasoning_summary` events only for `chatMode === "project"`; API maps them to new SSE event names; web appends them into the existing `ActivityBadge` timeline via `activities[]` instead of `ThoughtBlock`, and suppresses noisy generic tool live badges in project mode. The feed is intentionally session-ephemeral in this slice; no DB persistence was added. Verification: focused runtime stream tests, focused native stream tests, focused `use-chat` / `activity-badge` web tests, workspace lint, `format:check`, and `@persai/api` / `@persai/web` / `@persai/runtime` typecheck.

- **ADR-100 follow-up audit refinement (`docs`; 2026-05-22):** recorded founder-requested architecture clarifications after readonly code audits. Complex chat/project file analysis should reuse the existing API-owned `DocumentExtractionService` path instead of inventing a parallel project file/OCR stack; ordinary web upload still stores only a small local preview plus `fileRef`, so ADR-100 now explicitly prefers lazy extraction on demand during project `gather` rather than parsing every upload, with any future cache attached to existing `fileRef` truth. The same audit confirmed a real current gap for images/files with weak filenames: later turns do not get a durable semantic description, so ADR-100 now preserves a future clean path of tiny semantic summaries on canonical attachment/file truth, shared across B2C and project mode. Also captured that a future project-mode skill picker should use an optional pinned-skill field separate from ordinary `skillDecisionState`, so normal foreground/sticky/background auto-skill activation stays intact when no skill is pinned.

- **ADR-100 Slice 4 — project execution profile (`apps/runtime`, `apps/api`, `docs`; 2026-05-22):** taught runtime to treat `chatMode === "project"` distinctly from smart/deep chat. Added a bounded project execution profile with a retrieval-aware precheck that avoids the PDF/document `reasoning_request` empty-retrieval trap, a staged developer contract (`plan -> gather -> analyze -> replan -> synthesize`), and reasoning-budget reuse from the existing materialized bundle/tool-policy path rather than new hardcoded B2B limits. Also closed the native API bridge gap so helper-built runtime requests include `chatMode` consistently. Verification: focused runtime project/routing tests, focused native send/stream tests, workspace lint, `format:check`, and `@persai/api` / `@persai/web` / `@persai/runtime` typecheck.

- **ADR-100 proposed — Project chat mode + B2B analysis profile (`docs`; 2026-05-22):** documented the target architecture for professional/B2B project work after auditing current Skills, Plans, runtime, retrieval, and web surfaces. The decision keeps B2B as an effective-plan distinction only, adds a chat-scoped `project` mode alongside ordinary/current smart behavior, keeps domain specialization in Skills, and defines Cursor-like behavior as a staged project execution profile plus project-only visible activity and reasoning-summary feed rather than a complex routing tree or raw chain-of-thought exposure. The ADR also requires the final implementation program to create/update a hidden active B2B plan in the target cluster/admin control plane with explicit Skill, KB/storage, retrieval, context, sandbox, model, tool-cap, and loop-budget values, verified from live admin/runtime truth. No runtime code changed.

- **ADR-100 Slice 3 — project UI shell (`apps/web`, `docs`; 2026-05-22):** added lower-sidebar `ProjectFilesPanel` (attachment-derived, deduped by `fileRef`, paginated `getChatMessages`) for active project chats; moved chat mode control to composer area (desktop three-pill segmented control; mobile compact chip + 3-mode menu). Tests: sidebar 20/20, chat-area 14/14. No API/runtime changes.

- **ADR-100 Slice 2.1 — chat mode contract closeout (`packages/contracts`, `apps/api/test`, `docs`; 2026-05-22):** closed Slice 2 merge gaps without expanding scope. OpenAPI `AssistantWebChatTurnRequest` now documents optional `chatMode` and `deepModeEnabled`; contracts regenerated. Added `ManageWebChatListService.parseUpdateInput` tests for project/normal sync and chatMode/deepModeEnabled conflict rejection. Reverted out-of-scope `chat-input.tsx` mic styling. Parent verification gate green (lint, format:check, api/web typecheck; focused API tests 21/21).

- **ADR-100 Slice 2 — explicit chat mode contract (`apps/api`, `apps/web`, `apps/runtime`, `packages/contracts`, `packages/runtime-contract`, `docs`; 2026-05-22):** added persisted `assistant_chats.chat_mode` (`normal | smart | project`) plus API/OpenAPI/runtime contract plumbing while preserving `deepModeEnabled` as the compatibility boolean. Old `deepModeEnabled` writes map to `normal`/`smart`; new `chatMode` writes keep `deepModeEnabled` synchronized (`normal=false`, `smart/project=true`). Web chat mode control now exposes `Project` as the third state and sends `chatMode` on sends/updates; sidebar shows a project marker for project chats. `project` intentionally maps to the existing deep/smart runtime path until ADR-100's later dedicated project execution profile lands.

- **Support API auth correction + compact mobile voice cancel UX (`apps/api`, `apps/web`, `docs`; 2026-05-22):** an independent live-dev audit showed the previous support BFF explanation was incomplete: the real failing endpoints (`POST /api/v1/support/tickets/:ticketId/read`, `GET /api/v1/support/attachments/:attachmentId`, `GET /api/v1/admin/support/attachments/:attachmentId`) were missing `ClerkAuthMiddleware` registration in `IdentityAccessModule`, so `req.resolvedAppUser` never populated and both mark-read persistence and attachment preview/download returned `401 auth_required` even when `web` forwarded a fresh session token. Added those routes to the API middleware registration and locked them with `identity-access.module.test.ts`. Also tightened mobile voice recording UX again: replaced the wide banner/progress rail with a smaller centered status pill and changed cancel arming to require a stronger mostly-horizontal left swipe with larger slop/hysteresis and a vertical-drift guard. Verification: focused `identity-access.module.test.ts`, focused `chat-input.test.tsx` + `assistant-api-client.test.ts`, workspace lint, `format:check`, and `@persai/api` / `@persai/web` typecheck.

- **Support auth + quiet unread follow-up (`apps/web`, `docs`; 2026-05-22):** fixed the two post-deploy regressions in the new support surface and softened the unread UX. Support attachment URLs now go through dedicated same-origin BFF routes (`/api/support-attachment/:id`, `/api/admin-support-attachment/:id`) and the browser image fetch helper avoids adding a bearer header on same-origin routes, so admin thumbnails/lightbox use the session-backed proxy path instead of failing with `401`. User-side `mark read` now also goes through a dedicated BFF route (`POST /api/support-ticket/:ticketId/read`), which fixed `userLastReadAt` persistence and stopped unread dots from reappearing after refresh. The new support BFF routes were added to Clerk protected-route matching in `apps/web/middleware.ts`. The sidebar assistant card now shows a compact support-reply status when unread support exists, and the mobile menu button gets a small unread-count badge tied to the same support signal instead of a noisier live dot. Verification: workspace lint, `format:check`, `@persai/api` typecheck, `@persai/web` typecheck, focused `middleware.test.ts`, `sidebar.test.tsx`, `assistant-settings.test.tsx`, and `assistant-api-client.test.ts`.

- **Support BFF fresh-token fallback + mobile voice cancel rollback (`apps/web`, `docs`; 2026-05-22):** live dev logs after the `web` rollout showed the support bugs were still real: the specialized support BFF routes were forwarding only server-side `auth().getToken()` and API still logged `401 userId: null` for admin attachment previews and `mark read`. Same-origin support BFF requests now prefer a browser-supplied `x-persai-session-token` before falling back to Clerk server auth, `authenticated-attachment-image` sends that header on same-origin `/api/...` blob fetches, and browser `mark read` sends it on `/api/support-ticket/:ticketId/read`. Also rolled back the intrusive two-column mobile voice cancel rail to a compact recording card and tightened swipe slop/thresholds so tiny finger drift does not cancel the recording. Verification: focused `chat-input.test.tsx`, `assistant-api-client.test.ts`, `@persai/web` typecheck, `@persai/web` lint, and targeted Prettier check.

- **User support tickets — base system (`apps/api`, `apps/web`, `docs`; 2026-05-22):** added `support_tickets` / `support_ticket_messages`, user create/list/detail APIs, admin `Support` page (reply / reply later / close), assistant settings **Поддержка** section, `admin_system` `support_ticket_opened` alert, and `user_support` notifications (email template `support.reply` + `user_preferred` push) when an admin replies. Migration: `20260522120000_user_support_tickets`. Verification: `@persai/api` / `@persai/web` typecheck, lint, focused support tests.

- **`admin_system` daily-report test-send (`apps/api`, `apps/web`, `docs`; 2026-05-22):** `Admin -> Notifications -> admin_system` now has a dedicated **Test daily report** action. Backend `testSendForSource("admin_system", { eventCode: "daily_report" })` builds a synthetic digest body and routes it through the first configured recipient assistant's effective `user_preferred` channel, so operators can verify the admin digest path without waiting for the scheduler. Verification: focused `manage-notification-platform.service.test.ts`, `@persai/api` / `@persai/web` typecheck, workspace lint, `format:check`. Full `pnpm run test` currently still fails in unrelated `apps/sandbox` coverage (`sandbox.service.test.ts` assertion on `usage !== null`).

- **`admin_system` audit cleanup follow-up (`apps/api`, `docs`; 2026-05-22):** closed the first external-review findings on the new admin notification slice. Billing lead-time admin pushes now keep their future `scheduledAt` instead of firing immediately, the daily digest runs safely after late restarts (deduped once per local day), `admin_system` delivery/test-send now forces effective `user_preferred` routing even on legacy persisted rows, global notification singleton control-plane actions require a platform-scoped admin role, and malformed `dailyReportTimeLocal` values are rejected. Verification: `corepack pnpm -r --if-present run lint`, `corepack pnpm run format:check`, `corepack pnpm --filter @persai/api run typecheck`, `corepack pnpm --filter @persai/web run typecheck`, `corepack pnpm --filter @persai/api exec tsx test/admin-system-notification-producer.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/billing-lifecycle-producer.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/manage-notification-platform.service.test.ts`.

- **Admin system push + daily report via `admin_system` (`apps/api`, `apps/web`, `docs`; 2026-05-22):** reactivated `admin_system` as the single admin notification source instead of inventing a separate "Admin PUSH" channel. `admin_system` now defaults to `user_preferred`, stores `recipientAssistantIds[]`, selected admin event codes, and a local-time daily report schedule in `notification_policies.config`, fans out deterministic notifications from onboarding/assistant creation, billing lifecycle, and selected admin audit events, and ships a leased daily digest scheduler that summarizes new users, payments/revenue, USD cost, runtime apply health, and unresolved dead letters. Admin → Notifications now exposes recipient assistant IDs, event checkboxes, and daily report time directly inside the `admin_system` policy editor. Verification: `corepack pnpm -r --if-present run lint`, `corepack pnpm run format:check`, `corepack pnpm --filter @persai/api run typecheck`, `corepack pnpm --filter @persai/web run typecheck`, `corepack pnpm --filter @persai/api exec tsx test/admin-system-notification-producer.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/manage-notification-platform.service.test.ts`.

- **ADR-099 — status Implemented, docs reconciled (`docs`; 2026-05-21):** marked `docs/ADR/099-provider-pricing-catalog-and-unified-model-cost-ledger.md` as **Implemented** with an updated audit summary (Block 1 + Block 2 landed; optional follow-ups listed). Updated `docs/API-BOUNDARY.md` tool-path economics text and `docs/SESSION-HANDOFF.md` closeout. No code changes.

- **Media package quota_status price labels + Admin Plans stat chips (`apps/api`, `apps/web`, `packages/contracts`, `packages/runtime-contract`, `docs`; 2026-05-21):** `packageOffers` now expose `amountMajor` and localized `priceLabel` per catalog row; `quota_status` bootstrap guidance forbids quoting raw `amountMinor`. Admin Plans collapsed stats and media package rows use read-only styling distinct from form inputs. Verification: `quota-offers.test.ts`, `read-internal-runtime-quota-status.service.test.ts`, lint, format:check, typecheck, api/web test.

- **Admin UI + Business all-time economics (`apps/api`, `apps/runtime`, `apps/web`, `packages/contracts`, `docs`; 2026-05-21):** Admin Plans/Tools/Ops layout polish; Business global ledger window switched from rolling 7d to all time with `platformPaymentRevenueAllTime` (RUB succeeded payments, optional USD); Ops ledger panel stretch layout; runtime TTS artifacts set `sourceToolCode: "tts"` for persisted billing-facts ledger append. Verification: lint, format:check, typecheck, api/runtime/web tests, build.

- **ADR-099 Block 2 — shipped on `main` (`27868c40`; 2026-05-21):** atomic commit lands Steps A–D (tool-path pricing catalog, admin economics API/UI, billing facts + ledger append, Ops layout polish). Verification: lint, format:check, typecheck, full test, build.

- **ADR-099 Block 2 Step D — Admin Tools economics UI (`apps/api`, `apps/web`, `docs`; 2026-05-21):** Admin → Tools now edits tool-path unit prices (web_search, web_fetch, browser, document_render) via `GET/PUT /api/v1/admin/tools/economics`, with default document_render tier seeds and Ops/Business ledger purpose labels for tool-path rows. Verification: `tool-path-pricing-catalog.test.ts`, `apps/web/app/admin/tools/page.test.tsx`, `@persai/api` / `@persai/web` typecheck.

- **ADR-099 Block 2 Step C — tool-path billing facts + ledger append (`apps/api`, `apps/runtime`, `apps/provider-gateway`, `packages/runtime-contract`, `docs`; 2026-05-21):** provider-gateway now emits normalized `billingFacts` for successful web_search, web_fetch, browser, and document_render calls; runtime forwards them on tool invocations and document artifacts; API appends non-blocking `model_cost_ledger_events` via `RecordToolPathLedgerFromToolInvocationsService` on ordinary web/Telegram turns and document-job delivery. Verification: `provider-web-search.service.test.ts`, `record-model-cost-ledger.service.test.ts`, `@persai/api` / `@persai/runtime` / `@persai/provider-gateway` typecheck.

- **ADR-099 Block 2 Step B — tool-path pricing catalog + ledger purposes (`apps/api`, `packages/contracts`, `packages/runtime-contract`, `docs`; 2026-05-21):** added `tool_path_pricing_catalog` on platform settings, `GET/PUT /api/v1/admin/tools/economics`, and `RecordModelCostLedgerService.recordToolPathBillingFactsEvent()` with ledger purposes `web_search`, `web_fetch`, `browser`, `document_render`. Extended `RuntimeBillingFacts` capabilities for tool-path economics; runtime/provider emission and Tools UI price fields remain Step C/D. Verification: `tool-path-pricing-catalog.test.ts`, `record-model-cost-ledger.service.test.ts`, `@persai/api` typecheck.

- **Admin → Tools Step A (`apps/api`, `apps/web`, `docs`; 2026-05-21):** regrouped Admin Tools into a two-column layout (Document Processing, Document Generation, Web & Browser, TTS, Media with Runtime link, Billing, Notifications) and hid unused `tool_memory_search` from `buildAdminToolCredentialsState`. Verification: `tool-credential-settings.test.ts`, `@persai/web` typecheck.

- **ADR-099 — Ops period economics + knowledge indexing embedding ledger (`apps/api`, `apps/web`, `packages/contracts`, `docs`; 2026-05-21):** added `periodEconomics` to Admin Ops user directory and per-user cockpit: paid subscription/package totals from succeeded payment intents in the current quota period (RUB minor units) and USD model-cost ledger spend for the same window, without margin or FX. Admin Ops UI now shows paid/cost columns in the user table and a Period economics card in cockpit detail. Knowledge indexing jobs now record non-blocking `knowledge_embedding` ledger events from embedding token usage after successful index runs. Verification: `corepack pnpm run contracts:generate`, focused API tests (`admin-ops-user-directory`, `resolve-admin-ops-cockpit`, `knowledge-indexing-job-worker`, `record-model-cost-ledger`), and `apps/web` ops page vitest.

- **ADR-099 Block 1 — ledger coverage for remaining model-priced paths (`apps/api`, `packages/contracts`, `packages/runtime-contract`, `docs`; 2026-05-21):** closed the main Block 1 ledger tails that already had replay-safe usage or billing facts. Added migration-backed persistence for media/document completion framing usage, standalone voice HTTP transcribe events, and non-blocking ledger writes for retrieval-helper reranker usage, async completion framing (`chat_helper`), standalone transcribe (`stt` via persisted facts), and Mistral OCR (`ocr_or_document_parsing`). Admin Business/Ops `coverageScope` is now `adr099_block1_model_priced_paths` with an updated honesty note; provider document render jobs without model-priced billing facts remain explicitly excluded. Verification: `corepack pnpm --filter @persai/api run typecheck`, `corepack pnpm run contracts:generate`, and focused API tests for ledger, completion delivery, manage-chat-media, and admin read-models.

- **ADR-099 Block 1 — ledger writes from persisted media/STT/TTS billing facts (`apps/api`, `apps/web`, `docs`; 2026-05-21):** extended the immutable `model_cost_ledger_events` proof to replay-safe rows priced from durable `billingFacts` instead of transient runtime responses. `RecordModelCostLedgerService.recordPersistedBillingFactsEvent()` now resolves timestamp-matched Admin Runtime catalog rows across provider catalogs and supports `time_metered`, `text_chars_metered`, `fixed_operation`, and `tiered_operation` billing modes with deterministic ids and duplicate skipping. Non-blocking append wiring now runs after persistence on assistant media jobs (`image_generation` / `image_edit` / `video_generation`), attachment STT ingest (`stt`), and delivered TTS attachments (`tts`) only. Business/Ops coverage note/scope now honestly includes this persisted media/STT/TTS set while still excluding retrieval-helper, standalone voice-transcribe, and other non-persisted paths. Verification: `corepack pnpm --filter @persai/api run typecheck`, `corepack pnpm --filter @persai/api exec tsx test/record-model-cost-ledger.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/assistant-media-job-scheduler.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/resolve-admin-business-platform.service.test.ts`, and `corepack pnpm --filter @persai/api exec tsx test/resolve-admin-ops-cockpit.service.test.ts`.

- **ADR-099 Block 1 follow-up — reviewed billing-facts corrections (`apps/api`, `docs`; 2026-05-21):** corrected three scoped issues in the media/STT/TTS billing-facts foundation without widening scope. API-side Admin Runtime catalog normalization now fully accepts and preserves `text_chars_metered` profiles, with focused backend coverage in `platform-runtime-provider-settings.test.ts`. Video attachment ingest now carries STT-derived normalized `billingFacts` through the video-audio preprocess path into attachment persistence, covered by focused preprocess and upload tests. Delivered attachment persistence now matches the documented ownership split: image/video billing facts remain only on `assistant_media_jobs`, while delivered attachment rows persist billing facts only for TTS outputs. Verification: `corepack pnpm --filter @persai/api run typecheck`, `corepack pnpm --filter @persai/api exec tsx test/platform-runtime-provider-settings.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/manage-chat-media.stage-web-thread.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/media-preprocessor.service.test.ts`, and `corepack pnpm --filter @persai/api exec tsx test/media-delivery.service.test.ts`.

- **ADR-099 Block 1 foundation — durable media/STT/TTS billing facts, standalone voice-transcribe deferred (`apps/api`, `apps/runtime`, `apps/provider-gateway`, `apps/web`, `packages/contracts`, `docs`; 2026-05-21):** landed the additive non-ledger persistence seam for provider-priced media paths. Runtime/provider-gateway contracts now carry normalized `billingFacts` for image, video, STT, and TTS; OpenAI image/video results now emit operation-metered facts, TTS emits `text_chars_metered`, and STT emits `time_metered` facts when audio duration can be probed honestly. API now persists those facts durably on the owning rows: `assistant_media_jobs.billing_facts_json` for generated image/video jobs, and `assistant_chat_message_attachments.billing_facts_json` for attachment-ingest STT plus delivered TTS attachments. Admin Runtime catalog truth now also recognizes `speech_to_text`, `text_to_speech`, and `text_chars_metered` without changing existing chat-model selector semantics. The standalone `/api/v1/media/transcribe` path was intentionally deferred because it still lacks its own dedicated replay-safe durable event row. Verification: `corepack pnpm --filter @persai/contracts run typecheck`, `corepack pnpm --filter @persai/provider-gateway run typecheck`, `corepack pnpm --filter @persai/runtime run typecheck`, `corepack pnpm --filter @persai/api run typecheck`, `corepack pnpm --filter @persai/web run typecheck`, `corepack pnpm --filter @persai/api exec tsx test/manage-chat-media.stage-web-thread.test.ts`, and `corepack pnpm --filter @persai/api exec tsx --test test/assistant-media-job-scheduler.service.test.ts`.

- **ADR-099 Session C closeout — background-task evaluator ledger, retrieval-helper deferred (`apps/api`, `apps/web`, `packages/contracts`, `docs`; 2026-05-21):** widened the additive `model_cost_ledger_events` proof to the first honest non-ordinary-chat path without changing quota semantics. `PersaiBackgroundTaskSchedulerService` now appends a non-blocking ledger write after the matching `assistant_background_task_runs` row durably stores evaluator usage, and that background-task event now prices against the durable run-start timestamp seam (`assistant_background_task_runs.started_at`) instead of a later scheduler-finished clock. `RecordModelCostLedgerService` prices that persisted token-metered snapshot as a `background_task` event using the same timestamp-matched Admin Runtime catalog snapshot, deterministic event id discipline, and immutable canonical ledger shape as the ordinary-chat proof set. The shared Business/Ops ledger coverage contract/note now honestly describes the current covered set as ordinary chat plus background-task evaluator rows. Retrieval-helper / reranker was intentionally left out because current `knowledge_retrieval_events` persistence still lacks a clean replay-safe per-helper source seam and durable user attribution for honest money rows. Verification: `corepack pnpm --filter @persai/api exec prisma generate --schema prisma/schema.prisma`, `corepack pnpm --filter @persai/api exec tsx test/record-model-cost-ledger.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/send-web-chat-turn.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/stream-web-chat-turn.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/handle-internal-telegram-turn.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/persai-background-task-scheduler.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/resolve-admin-business-platform.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/resolve-admin-ops-cockpit.service.test.ts`, `corepack pnpm --filter @persai/api run typecheck`, and `corepack pnpm --filter @persai/web run typecheck`.

- **ADR-099 Session D — first Business/Ops ledger-backed read models (`apps/api`, `apps/web`, `docs`; 2026-05-20):** added the first minimal admin economics rollout on top of the existing catalog + ordinary-chat ledger proof set. `ResolveAdminBusinessPlatformService` and `Admin > Business` now expose a compact last-7-day ledger-backed model-cost block, while `ResolveAdminOpsCockpitService` and `Admin > Ops` now expose a compact current-quota-period ledger-backed model-cost block plus top provider/model/purpose rows for the selected workspace. Both surfaces reuse one shared API-side ledger summary helper, explicitly label the current ordinary-chat-only coverage set, and leave quota semantics plus existing billing/support controls unchanged. Verification: `corepack pnpm --filter @persai/api exec tsx test/resolve-admin-business-platform.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/resolve-admin-ops-cockpit.service.test.ts`, `corepack pnpm --filter @persai/api run typecheck`, and `corepack pnpm --filter @persai/web run typecheck`.

- **ADR-099 Session C follow-up — Telegram claim completion + ledger idempotency (`apps/api`, `docs`; 2026-05-20):** fixed the two concrete ordinary-chat correctness gaps found in readonly review without widening coverage. `HandleInternalTelegramTurnService` now finalizes successful Telegram update claims through `completeTelegramUpdateProcessing()` instead of relying on stale-claim expiry, while preserving the existing failure fallback to release the claim when completion itself fails. `RecordModelCostLedgerService` now derives a deterministic per-entry event id from the ordinary-chat logical source event plus entry identity and inserts with duplicate skipping, so replay/retry of the same ordinary web or Telegram priced call does not append duplicate immutable money rows. Verification: `corepack pnpm --filter @persai/api exec prisma generate --schema prisma/schema.prisma`, `corepack pnpm --filter @persai/api exec tsx test/record-model-cost-ledger.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/send-web-chat-turn.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/stream-web-chat-turn.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/handle-internal-telegram-turn.service.test.ts`, `corepack pnpm -r --if-present run lint`, `corepack pnpm run format:check`, `corepack pnpm --filter @persai/api run typecheck`, and `corepack pnpm --filter @persai/web run typecheck`.

- **ADR-099 Session C path expansion — ordinary Telegram + router/classifier usage (`apps/api`, `docs`; 2026-05-20):** widened the additive `model_cost_ledger_events` proof without changing quota semantics. `RecordModelCostLedgerService` now reuses the Session B canonical event shape and strict timestamp-matched catalog lookup for both ordinary main-reply entries (`chat_main_reply`) and the existing router/classifier system-tool entries already carried in ordinary-chat `usageAccounting.entries` (`router`). `HandleInternalTelegramTurnService` now appends the same non-blocking ledger writes for completed ordinary Telegram turns after assistant-message persistence, so ordinary web sync, ordinary web stream, and ordinary Telegram sync all write the replay-safe money ledger from the same pricing source. Verification: `corepack pnpm --filter @persai/api exec prisma generate --schema prisma/schema.prisma`, `corepack pnpm --filter @persai/api exec tsx test/record-model-cost-ledger.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/send-web-chat-turn.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/stream-web-chat-turn.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/handle-internal-telegram-turn.service.test.ts`, and `corepack pnpm --filter @persai/api run typecheck`.

- **ADR-099 Session B follow-up — single migration path + strict timestamp match (`apps/api`, `docs`; 2026-05-20):** removed the duplicate earlier Prisma migration for `model_cost_ledger_events`, keeping `apps/api/prisma/migrations/20260520215000_adr099_session_b_ledger_foundation/migration.sql` as the single deployable Session B migration path. Tightened `findRuntimeProviderCatalogProfileForTimestamp()` so the ledger foundation no longer falls back to a non-effective provider/model catalog row when history has a gap; the Session B writer now skips that event instead of persisting a misleading price snapshot, and focused `record-model-cost-ledger.service.test.ts` coverage now asserts the no-match/no-write behavior. Verification: `corepack pnpm --filter @persai/api exec prisma generate --schema prisma/schema.prisma`, `corepack pnpm --filter @persai/api exec tsx test/record-model-cost-ledger.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/send-web-chat-turn.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/stream-web-chat-turn.service.test.ts`, and `corepack pnpm --filter @persai/api run typecheck`.

- **ADR-099 Session B ledger foundation (`apps/api`, `docs`; 2026-05-20):** added the first immutable `model_cost_ledger_events` table plus `RecordModelCostLedgerService`, establishing the canonical money-first event shape for provider/model-priced costs: attribution ids, provider/model/capability/purpose/surface/source, billing mode, raw usage JSON, integer `actualCostMicros`, currency, hashed `priceCatalogVersion`, full `priceCatalogSnapshot`, correlation ids, and `occurredAt`. The first end-to-end proof now writes ledger events for completed ordinary **web chat** reply-generation entries only (`web_chat_turn_sync` and `web_chat_turn_stream_completed`) when runtime returns concrete `usageAccounting.entries`; pricing is resolved from the archived/effective Admin Runtime catalog row at the turn timestamp, while existing quota semantics remain unchanged. Verification: `corepack pnpm --filter @persai/api run prisma:generate`, `corepack pnpm --filter @persai/api exec tsx test/record-model-cost-ledger.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/send-web-chat-turn.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/stream-web-chat-turn.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/quota-accounting.test.ts`, `corepack pnpm -r --if-present run lint`, `corepack pnpm run format:check`, `corepack pnpm --filter @persai/api run typecheck`, and `corepack pnpm --filter @persai/web run typecheck`.

- **ADR-099 Session A follow-up — single-branch pricing + archive-safe runtime editor (`apps/api`, `apps/web`, `packages/contracts`, `docs`; 2026-05-20):** tightened the runtime catalog shape so each provider/model row now persists exactly one pricing branch matching its `billingMode` instead of carrying unrelated nullable branches in `providerPriceMetadata`. `Admin > Runtime` now archives historical rows by inactivating/date-bounding them instead of deleting them from the catalog, and focused page-level coverage now exercises billing-mode/pricing switching plus archive handling in `apps/web/app/admin/runtime/page.test.tsx`. Verification: `corepack pnpm contracts:generate`, `corepack pnpm --filter @persai/contracts run typecheck`, `corepack pnpm --filter @persai/api exec tsx test/platform-runtime-provider-settings.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/materialize-assistant-published-version.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/apply-assistant-published-version.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/generate-skill-authoring-draft.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/quota-accounting.test.ts`, `corepack pnpm --filter @persai/web exec vitest run app/app/runtime-provider-settings-admin.test.ts app/admin/knowledge/page.test.tsx app/admin/plans/page.test.tsx app/app/assistant-api-client.test.ts app/admin/runtime/page.test.tsx --config vitest.config.ts`, `corepack pnpm --filter @persai/api run typecheck`, and `corepack pnpm --filter @persai/web run typecheck`.

- **ADR-099 Session A catalog foundation (`apps/api`, `apps/web`, `packages/contracts`, `docs`; 2026-05-20):** replaced the last textarea-centric Admin Runtime model-profile flow with a structured provider/model catalog editor. `apps/web/app/admin/runtime/page.tsx` now edits provider-scoped model cards with `active`, capabilities, `billingMode`, effective dates, token weights, notes, and billing-mode-specific pricing fields instead of one pipe-delimited textarea blob. On the backend, `runtime-provider-profile.ts` and `platform-runtime-provider-settings.ts` now treat those structured catalog rows as persisted truth, normalize legacy rows forward, and derive `availableModelsByProvider` from active chat-capable catalog rows so downstream model selectors keep the same semantics while inactive historical rows stay out of ordinary picks. `Admin > Plans` and `Admin > Knowledge` were tightened to read only active catalog rows from the structured catalog. Verification: `corepack pnpm contracts:generate`, `corepack pnpm --filter @persai/contracts run typecheck`, `corepack pnpm --filter @persai/api run typecheck`, `corepack pnpm --filter @persai/web run typecheck`, `corepack pnpm --filter @persai/api exec tsx test/platform-runtime-provider-settings.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/materialize-assistant-published-version.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/apply-assistant-published-version.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/generate-skill-authoring-draft.service.test.ts`, `corepack pnpm --filter @persai/api exec tsx test/quota-accounting.test.ts`, and `corepack pnpm --filter @persai/web exec vitest run app/app/runtime-provider-settings-admin.test.ts app/admin/knowledge/page.test.tsx app/admin/plans/page.test.tsx app/app/assistant-api-client.test.ts --config vitest.config.ts`.

- **ADR-099 proposed — provider pricing catalog + unified model cost ledger audit (`docs`; 2026-05-20):** completed a focused architecture/code audit for long-term unit economics and documented the target state in `docs/ADR/099-provider-pricing-catalog-and-unified-model-cost-ledger.md`. The proposed ADR keeps user-facing quota semantics unchanged (`text` in PersAI-owned internal text units; tools/media/documents in product units) while introducing a separate money-first internal cost ledger. The audit pins the main current gaps: `TrackWorkspaceQuotaUsageService` is still weight-first rather than price-first, `RuntimeUsageAccountingEntry` is token-only rather than billing-mode-aware, `providerPriceMetadata` exists but the admin runtime UI still centers on pipe-delimited weights, and `Admin > Business` / `Admin > Ops` read different proxy inputs instead of one ledger-backed cost truth. ADR-099 formalizes the two execution blocks agreed in discussion: Block 1 covers provider/model-priced paths (text, image, video, STT, and helper/router/background model calls plus the runtime/business/ops/admin surfaces needed to price and observe them), while Block 2 is reserved for later non-model tool/path economics. A follow-up update in the same ADR now also adds explicit execution rules for parent agents and subagents: one bounded slice per session, parent-controlled edits only, readonly subagent support for audit/search/verification, mandatory Block 1 -> Block 2 order, and a reusable implementation-session prompt so the work can be executed cleanly by future agent sessions.

- **Preset avatar in `Name, voice, character` + media portrait tile removal (`apps/web`, `docs`; 2026-05-20):** founder review of the pushed landing polish identified two remaining scene-level art issues. In `apps/web/app/_components/landing/workflow-surface.tsx`, the personality scene's placeholder silhouette card has been replaced with the real preset image `apps/web/public/avatar-presets/luma.png` rendered through `next/image`, so the "your assistant" scene now uses an actual PersAI avatar instead of a schematic stand-in. In the same file, the lower-left portrait tile was removed from the `Images and video` scene because it visually climbed into the assistant reply and read as accidental overlap rather than a useful output artifact. Verification: `corepack pnpm -r --if-present run lint`, `corepack pnpm run format:check`, `corepack pnpm --filter @persai/api run typecheck`, `corepack pnpm --filter @persai/web run typecheck`, and `corepack pnpm --filter @persai/web exec vitest run app/page.test.tsx` (`3/3` green).

- **Document-job live UI continuity for PPTX prep + chat-list activity (`apps/web`, `docs`; 2026-05-20):** fixed the two remaining UX gaps in the explicit PPTX-preparation flow. When the user confirms `Prepare PPTX`, the active chat now immediately marks a local document job as in-flight, triggers a history refresh, and reloads the chat list, so the in-chat "work in progress" state appears without a full page reload and the finished PPTX banner can materialize through the existing document-job polling path. The shared live-thread registry now tracks document jobs alongside streaming and media, and the sidebar row indicator also falls back to server-provided `activeDocumentJobs`, so background document renders pulse in the chat list the same way media jobs do. Focused verification: `corepack pnpm --filter @persai/web exec vitest run app/app/_components/presentation-pptx-prepare-action.test.tsx app/app/_components/sidebar.test.tsx app/app/_components/chat-area.test.tsx app/app/_components/streaming-threads.test.tsx`; `corepack pnpm --filter @persai/web run typecheck`.

- **Dark SBP visibility + auth footer parity + narrow document-label cleanup (`apps/web`, `docs`; 2026-05-20):** production review after the founder-pushed landing/mobile pass surfaced three remaining UI polish issues. First, the small `SBP` mark in the finale trust chip was too faint on dark surfaces, so `apps/web/app/_components/landing/finale-section.tsx` now applies a dark-mode invert/brightness treatment to the SVG when rendered inside the chip. Second, `sign-in` and `sign-up` still ended with a simpler auth footer instead of the calmer bordered legal-page footer, so `apps/web/app/_components/public-auth-shell.tsx` now uses the same border-top / centered-width / top-padding rhythm as legal pages, and both `apps/web/app/sign-in/[[...sign-in]]/page.tsx` and `apps/web/app/sign-up/[[...sign-in]]/page.tsx` now enable that footer in loading, main, and completion states. Third, the workflow `Documents` scene was still carrying narrow-screen-fragile slide labels; `apps/web/app/_components/landing/workflow-surface.tsx` removed the `Slide 1 / 12` captions from the `PDF`, `PPTX`, and `DOCX` cards and also removed the small `12 slides` footer from the `PPTX` card, while `apps/web/app/_components/landing/workflow-section.tsx` and `apps/web/app/page.test.tsx` were updated to match the new rendering truth. Verification: `corepack pnpm -r --if-present run lint`, `corepack pnpm run format:check`, `corepack pnpm --filter @persai/api run typecheck`, `corepack pnpm --filter @persai/web run typecheck`, and `corepack pnpm --filter @persai/web exec vitest run app/page.test.tsx` (`3/3` green).
