---
description: PersAI session continuity — handoff, changelog, and bounded slice discipline
alwaysApply: true
---

# PersAI session continuity

- Read in order before implementing: `AGENTS.md`, `docs/SESSION-HANDOFF.md`, `docs/CHANGELOG.md`, `docs/ARCHITECTURE.md`, `docs/API-BOUNDARY.md`, `docs/DATA-MODEL.md`, `docs/TEST-PLAN.md`, then any ADRs referenced by the **user's explicit task** only.
- **Closed founder program:** ADR-147 Assistant Roles and Effective Skills closed 2026-07-14 after S0–S6 implementation, parent CLEAN audits, Release C `a11c8b6b` / bot pin `05ccaed4`, 189-migration and full local/GitHub gates, Argo/exact-image/health/live DB acceptance, authenticated B2C/B2B Role behavior, Skill/scenario title indication, refreshed Settings current-Role visual, authenticated Admin Role preview, and five Role MCP tools in a newly opened post-reload Agent chat. Every Assistant, including each Assistant in a B2B multi-assistant account, has one mandatory Role; effective Skills come only from that Role while existing Skill/Scenario/Knowledge/runtime mechanics remain intact. Do not reopen ADR-147 for new scope. ADR-146 is closed after S6 live acceptance. ADR-148 is closed 2026-07-15 after warm session-pod reuse + fail-closed cleanup landed (`9e26f145` / repair `2342c2ae`) and founder live-accepted that idle TTL holds; do not reopen ADR-146 or ADR-148 for that. **ADR-149** durable Stop / turn deadlines / live activity / orphans closed 2026-07-16 (founder-accepted). **ADR-150** ephemeral session install layer closed 2026-07-16 (founder-accepted; push `314ee37a`). ADR-078, ADR-102, ADR-105–ADR-115, and the other closed numbered programs in `docs/ADR/` remain **closed archive**; do not reopen them or re-derive scope from them.
- **ADR-151** reusable Scripts core is **closed 2026-07-17** after release `f0944d31` / GitOps pin `95c7d68d`, deployed `job.files` repair `5fb61f3c`, final runtime release `43f653b4`, MCP authoring, exact warm-sandbox execution, strict model-driven output smoke, and approved-account Admin Scripts UI acceptance. Scope is platform-global Script + immutable ScriptVersion, ordered Skill↔Script links, bounded active-step `scriptRef`, synchronous warm-sandbox `script.execute`, existing `SandboxJob`, Roles-style Admin UI, and nine thin MCP tools. ADR-147 remains unchanged. No ScriptRun, nested PersAI/LLM, Tool SDK, browser executor, async/jobRef/wait/notify, Script-specific sandbox contour, or managed secrets. Do not reopen ADR-151.
- ADR-151 Admin/MCP final status: all findings from two DIRTY audits were
  corrected and the final targeted allowed-model re-audit returned CLEAN.
  Deployment and founder live acceptance passed; ADR-151 is closed.
- **ADR-156** mode-aware tool observation windows is **closed 2026-07-17**:
  global in-turn newest 3 full / next 3 compact and cross-turn newest 1 full /
  next 4 compact, older masked, errors never bare-mask. No tool-specific
  exceptions; canonical storage stays full; closed ADR-143 is not reopened.
  Dynamic/token-threshold compaction is out of scope. Independent allowed-model
  audit, exact-image deploy, projection-metric smoke, and founder live
  acceptance passed. Do not reopen ADR-156.
- **ADR-152** Browser Script SDK and Durable Job Wait/Notify is the current
  founder-approved parent-orchestrated program (opened 2026-07-17; docs-only
  opening). It adds only narrow profile-backed Script browser `snapshot`/`act`
  through the existing local bridge and universal `await` `wait`/`notify` for
  canonical media/document jobs. It adds no general-purpose SDK; Document SDK
  is NO-GO. Parent audits/commits only; implementation subagents may use only
  `gpt-5.6-terra-medium` or `claude-sonnet-5-thinking-high`. The next checkpoint
  is a docs audit, then implementation beginning with contracts, durable handle,
  and bounded `await wait`; no intermediate deploy.
- **ADR-161** provider-native prompt-cache discipline and canonical usage
  accounting is an active founder-prioritized parent-orchestrated program
  (docs-only opening 2026-07-20; long-loop refinement independently audited
  CLEAN with zero P0/P1/P2; DeepSeek D2a rolled back 2026-07-22). Founder
  2026-07-22 amendment: append **full** tool results; micro clear at 50% of
  `compactionTriggerThreshold` (keep last 5, only after user-turn completion);
  100% path stays the existing `SessionCompactionService` (A3 = no-op); delete
  compact-at-insert spine and observation overlays; **no D2a**. Active slices
  A1/A2/A4/A5. Prior spine / newest-three overlay text in older ADR sections is
  historical debt until A1/A2/A4.
  Closure still requires sequential 50-iteration benchmarks per active
  provider/model-family policy and one authenticated 40–50-tool PersAI turn
  proving growing cache reads and positive net provider-cost savings; hit
  percentage alone fails. Parent orchestrates/audits/commits; ADR-161
  implementation subagents may use **only** Grok 4.5
  (`cursor-grok-4.5-high-fast`); Terra, Sonnet, Sol, and Opus are forbidden for
  those subs. Do not proliferate artificial slices.

- Treat `docs/SESSION-HANDOFF.md` as the source of truth for the latest completed checkpoint and the user's next stated priority.
- Treat `docs/CHANGELOG.md` as the source of truth for what has actually landed.
- If ADR, handoff, changelog, and code disagree, stop and reconcile docs before code changes.
- One session = one bounded slice unless the user explicitly asks for broader work. New product/architecture waves require an explicit user priority and usually a **new ADR** before implementation.
- At session start, restate: task scope, purpose, likely affected files/modules, and what is out of scope.
- Start only from a clean git tree; if dirty, stop and report. Record baseline SHA in the handoff when the slice changes repo truth.
- Keep the architecture fully PersAI-native. Do not reintroduce OpenClaw runtime/deploy wiring, compatibility shims, filesystem-owned session state, or TODO scaffolding.
- Touch native OpenClaw only if historical analysis explicitly requires it and a PersAI-side change is insufficient.
- Before ending the session, update `docs/SESSION-HANDOFF.md`, update `docs/CHANGELOG.md` if repo truth changed, update or add an ADR only when long-term system truth changed, and state the next recommended step.
